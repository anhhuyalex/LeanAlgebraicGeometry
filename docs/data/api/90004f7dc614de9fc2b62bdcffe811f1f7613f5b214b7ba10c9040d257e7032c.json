{
  "content": "# Recommendations — next plan iter (post iter-054)\n\n## State: B1/B2 engine + tensor-flank S2 DONE. Sorry 4 in `TensorObjInverse.lean`.\nS2 `tensorObj_restrict_iso_restrict_compat` CLOSED (verified `lake build` EXIT 0). The B1-route\ntemplate is now PROVEN to work on the tensor flank. 4 sorries remain:\nS3 (L1088), S4a (L1113), S4b (L1129), `trivialisation_restrict_compat` (L1201).\n\n## CRITICAL — the two genuine remaining blockers are MISSING INFRASTRUCTURE, not proof tactics\nBoth need a fresh construction of B1-comparable depth before any prove lane can succeed. Do NOT send\na bare prove lane at S3/S4a/S4b — they will fail for lack of the lemma, not lack of effort.\n\n1. **Dual-B1 cone (gates S3 + S4a).** The dual analogue of Bridge B1 —\n   `pullbackDualMap` / internal-hom base-change cone — does NOT exist (grep-empty:\n   `pullbackDualMap`, `internalHomPullback`, `dual_restrict_iso_eq`). This is the dual twin of the\n   `pullbackTensorMap` base-change. **Action:** decide the dual-flank route (planner's deferred fork):\n   (a) build the `pullbackDualMap` cone (mirror of the tensor B1 construction — substantial but the\n   tensor side now validates the pattern), OR (b) the unproven subsingleton route. Recommend\n   `strategy-auditor` / `mathlib-analogist` (api-alignment) to confirm the cone shape before a\n   `lean-scaffolder` builds it. Then S3/S4a follow the S2 template.\n\n2. **Left-unitor pullback-coherence lemma (gates S4b — tensor flank's last square).**\n   `tensorObj_unit_iso = sheafification.map (λ_ 𝟙) ≫ counit`; S4b needs the immersion-naturality of\n   the presheaf left unitor `λ_` under sheafification + `pullbackUnitIso`/`counit` interplay. This is\n   the unit analogue of `pullbackTensorMap` base-change. **Action:** scaffold a `presheaf_leftUnitor`\n   / pullback-unit-coherence lemma (mathlib-analogist for the monoidal-functor-unit idiom), then S4b\n   follows the S2 B1-route template + the 9 reusable helpers from this iter (esp.\n   `tensorObj_functoriality_comp3`, the whnf-seam idioms).\n\n3. **`trivialisation_restrict_compat` (L1201)** is the telescope of all 5 squares — DO NOT assign\n   until S3/S4a/S4b land. Gated transitively.\n\n## MEDIUM — code hygiene (lean-auditor iter054, 6 major; route to next prover on this file)\nAll stale-narrative / dead-code, NONE a proof defect. Hand to the prover that next touches\n`TensorObjInverse.lean` (e.g. the S4b lane):\n- **Dead helper pair:** `map_tensorHom_comp2` (L910) + `tensorObj_functoriality_comp` (L917) — neither\n  is called (`tensorObj_functoriality_comp3` is used instead). Either remove, or wire the S2 proof to\n  use them. Also fix the comment at L1061 that names the wrong lemma.\n- **Stale ESCALATION block** L501–539 inside the *proved* `hKEY` subproof of\n  `sheafPullbackUnit_forget_eq` — says goal \"still open\"/\"ESCALATED\" though the proof closes right\n  after. Misleading; delete.\n- **Stale STATUS** L830–841 in `tensorObj_restrict_iso_eq_pullbackTensorMap`: iter-044 clause dead;\n  iter-051 clause falsely claims a live dep on `sheafPullbackUnit_forget_eq` (closed iter-053).\n- **Iter-number drift** L1341–1358 in `exists_tensorObj_inverse` docstring (`iter-226/225/218`, parent\n  numbering — outside this subproject's iter-025…054 range).\n- (Minor) 34 deprecated `Sheaf.val` warnings in the `sheafPullbackUnit_forget_eq` region — accumulating.\n\n## Coverage debt — 9 new `lean_aux` nodes need blueprint entries (planner authors prose)\n`archon dag-query unmatched` = 114 (was ~105). New this iter, all in `TensorObjInverse.lean`,\nall proven/axiom-clean — each needs a `\\label`/`\\lean{}`/`\\uses` block:\n- `tensorObj_functoriality_id` — `TF (𝟙)(𝟙) = 𝟙`.\n- `pullbackTensorMap_pullbackCongr` (`@[reassoc]`) — naturality of `pullbackTensorMap` along\n  `pullbackCongr` transport (proved by `subst`).\n- `tensorObjIsoOfIso_hom` — `(tensorObjIsoOfIso e e').hom = tensorObj_functoriality e.hom e'.hom`.\n- `map_tensorHom_comp2` — generic 2-fold `F.map`-tensor interchange. *(but see dead-code note above —\n  may be removed instead of blueprinted)*\n- `tensorObj_functoriality_comp` / `_comp3` — `TF` composes (2-/3-fold). *(`_comp` is dead-code)*\n- `natIso_hom_inv_id_app_assoc` — generic `α.hom.app X ≫ α.inv.app X ≫ f = f`.\n- `pullbackTensorMap_restrict_cancel` (`@[reassoc]`) — pre-cancelled `pullbackTensorMap_restrict`.\n- `restrictFunctorIsoPullback_comp_compat_leg` — the per-module `(*)` coherence (inverse B2 + `RFIP j`\n  inverse naturality) each tensor leg of S2 reduces to.\n\n## DO-NOT-RETRY\n- S3/S4a/S4b via bare prove lane with no new lemma — they are infrastructure-gated (see CRITICAL).\n- `erw [Iso.hom_inv_id_app_assoc]` / placeholder-`refine tensorObj_functoriality_comp` on\n  `pullbackTensorMap`-laden goals — whnf-bombs the kernel even at 6.4M heartbeats. Use the\n  isolated-helper + fully-applied-`exact` idiom (proven this iter).\n\n## Reusable pattern (added to PROJECT_STATUS Knowledge Base)\nThe whnf-seam idiom: on sheafification-carrier goals, `exact`/`refine` a FULLY-APPLIED generic lemma\n(explicit morphism args) rather than `erw`/placeholder-`refine`; isolate any unavoidable seam `erw`\ninto a tiny helper discharged by a generic single-`[Category C]` lemma via `exact`.\n"
}
