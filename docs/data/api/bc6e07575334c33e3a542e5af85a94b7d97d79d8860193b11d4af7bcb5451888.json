[
  {
    "name": "FlatBaseChange.md",
    "content": "# Cohomology/FlatBaseChange.lean — iter-012\n\n## gammaPushforwardIso_comp (L733) — the foundation residual\n\n### Goal\nClose `gammaPushforwardIso_comp`, whose iter-011 residual reduced to `Γ(cast) x = x`\n(the `Spec.map_comp φ ρ` reindexing). Directive: build 2 rw-only exposure lemmas, then close\nvia `eqToIso.hom`/`eqToHom_app`/`eqToHom_map` + `pushforwardComp_inv_app_val_app`, NO defeq.\n\n### Result: PARTIAL — sorry still open, but 4 new compiling helpers + the blocker characterised precisely.\n\n### Attempt 1 — whole-goal defeq `exact (gammaPushforwardIso_inv_apply _ _ _).symm`\n- Elaborates to `goals: []` under the LSP. **Cold build = VERIFIED KERNEL BOMB** (`(deterministic)\n  timeout at whnf`). Confirms the memory: the defeq collapse forces `whnf` of the whole Spec\n  structure-sheaf machinery. DEAD (do not retry).\n\n### Attempt 2 — planner's lemma-driven route (exposures + collapse)\nBuilt the exposure machinery and pushed the goal to a fully-exposed form:\n```\nrw [gammaPushforwardIso_hom_apply (φ ≫ ρ) N x, ModuleCat.comp_apply, ModuleCat.comp_apply]\nsimp only [eqToIso.hom, eqToHom_app, Functor.map_comp, eqToHom_map, ModuleCat.comp_apply]\nrw [moduleCat_eqToHom_concreteCategory_apply]   -- eqToHom → SYMBOLIC cast (no whnf)\n-- GOAL: x = B (A (γ_φ (Γ_pc (cast hc x))))\n```\nwhere `B = restrictScalarsComp.inv.app`, `A = restrictScalars.map γ_ρ`, `γ_φ = gammaPushforwardIso φ`,\n`Γ_pc = moduleSpecΓFunctor.map (pushforwardComp.inv.app N)`. Every wrapper is the carrier identity.\n\nBuilt **5 single-layer `:= rfl` helpers (ALL COMPILE, kernel-light, axiom-free)**:\n- `moduleCat_eqToHom_concreteCategory_apply` (L696) — `ConcreteCategory.hom (eqToHom h) x = cast (…) x`\n  (`subst h; rfl`). The kernel-light replacement for a `change`/`rfl` eqToHom collapse.\n- `gammaPushforwardIso_hom_concreteApply` (L706) — `ConcreteCategory.hom`-keyed restatement of\n  `gammaPushforwardIso_hom_apply`.\n- `restrictScalars_map_concreteApply` (L713) — restrictScalars preserves the underlying function.\n- `restrictScalarsComp_inv_app_concreteApply` (L720) — restrictScalarsComp.inv is carrier identity.\n\n### THE BLOCKER (characterised precisely — NEW information vs the iter-011/planner picture)\nThe planner assumed the outer wrappers `B, A, γ_φ` \"collapse by `gammaPushforwardIso_{hom,inv}_apply`\"\nand only the cast is genuine. **That is false.** The genuine obstruction is the\n`X.Modules`/value-`ModuleCat` **diamond**, and it is BROADER than the cast:\n\n- Every wrapper changes the **object** type — its domain ≠ codomain as `ModuleCat` objects; they\n  agree ONLY on the carrier (the carrier-defeq is CHEAP: `(rfl : ↑(Γ(pushforward (Spec (φ≫ρ)) N)) =\n  ↑(Γ(pushforward (Spec ρ ≫ Spec φ) N)))` typechecks instantly — VERIFIED).\n- `rw`/`simp [helper]` FAILS to fire on `γ_φ` and `B`: rewriting `γ_φ z → z` retypes `z` from\n  `γ_φ`'s codomain object to its domain object, so the enclosing wrapper no longer typechecks\n  WITHOUT the heavy `restrictScalars ↔ pushforward∘Γ` object identity (motive-not-type-correct).\n  (`A` and the inner `γ_ρ` DO fire — they sit under `restrictScalars.map`, a syntactic junction.)\n- A term-mode `Eq.trans` chain through all five layers ELABORATES under the LSP (`goals: []`) but\n  **EXPLODES on cold build** — the junction defeqs ARE that heavy object identity at every step\n  (`> 1.6M` heartbeats, VERIFIED timeout). Even the bare `simp`+`rw [moduleCat_eqToHom…]` SETUP for\n  that chain is a cold-build (kernel) deterministic timeout.\n- A single monolithic `rfl` (`rw [moduleCat_eqToHom…]; rfl`) is a VERIFIED cold-build KERNEL BOMB.\n\nEssential tension: **carrier-rfl is CHEAP, per-junction OBJECT-defeq is HEAVY**, and the layered\ncollapse forces one heavy object-defeq per junction no matter the tactic.\n\n### Dead ends (do NOT re-attempt)\n- `exact (gammaPushforwardIso_inv_apply _ _ _).symm` — kernel bomb (whnf timeout).\n- `rw [moduleCat_eqToHom_concreteCategory_apply]; rfl` — kernel bomb (monolithic rfl).\n- term-mode `Eq.trans` chain over the 5 wrappers — cold-build >1.6M heartbeat timeout.\n- `simp only [*_concreteApply]` — only `A`/`γ_ρ` fire; `γ_φ`/`B` blocked by the object-junction\n  diamond (motive-not-type-correct without the heavy defeq).\n- `rw [Spec.map_comp φ ρ]` early — motive not type correct.\n- inline `rw [show ConcreteCategory.hom (gammaPushforwardIso φ _).hom = … from rfl]` /\n  `rw [eq_of_heq (cast_heq _ x)]` — pattern not found (diamond instance mismatch / unresolved cast mv).\n\n### Next step (iter-013, PRE-COMMITTED in PROGRESS.md): mathlib-analogist consult\nNo LLM API key in env (`archon-informal-agent` unavailable — VERIFIED), so I could not run the\nconsult myself. The corrective: collapse the value-`ModuleCat` carrier-identity composite by routing\nthrough `.val.app (op ⊤)` at the SHEAF level (`SheafOfModules.pushforwardComp_inv_app_val_app` EXISTS\nand is exactly `(((pushforwardComp φ ψ).inv.app M).val.app U) x = x`), where the objects are the\nuniform `N.val.obj ⊤` and the restrictScalars/globalSections object-junctions never form. The open\nquestion for the analogist: how to express `B/A/γ_φ` (which are restrictScalars-level, not sheaf-level)\nwithout forming the heavy object-junction — likely a structural lemma identifying the whole composite\n`gammaPushforwardIso` object-wise, or an `Iso.ext`/inverse-comparison route that never element-chases.\n\n## Crux `pullback_spec_tilde_iso_ring_square_natural` (L1347)\nNot attempted — the foundation `gammaPushforwardNatIso_comp` it is gated on remains transitively open\n(own body proved iter-011, but it `exact`s `gammaPushforwardIso_comp` which is still `sorry`). Budget\nconsumed by the foundation. Left as the pre-existing typed `sorry`.\n\n## Did NOT touch\n- The 3 COMPILE-DEAD mate sorries (L1546/1821/1861) — per directive.\n- No `maxHeartbeats 1e6` added; no `set_option` cleanup.\n\n## Needs blueprint entry\nThe 4 new helpers (all `lem:`-worthy, project-local, kernel-light `:= rfl`/`subst;rfl`):\n- `AlgebraicGeometry.moduleCat_eqToHom_concreteCategory_apply` (L696) — uses `eqToHom`, `cast`,\n  `congrArg`. Generic `ModuleCat` eqToHom→carrier-cast.\n- `AlgebraicGeometry.gammaPushforwardIso_hom_concreteApply` (L706) — uses `gammaPushforwardIso`,\n  `ConcreteCategory.hom`. `ConcreteCategory.hom`-keyed twin of `lem:gammaPushforwardIso_hom_apply`.\n- `AlgebraicGeometry.restrictScalars_map_concreteApply` (L713) — uses `ModuleCat.restrictScalars`.\n- `AlgebraicGeometry.restrictScalarsComp_inv_app_concreteApply` (L720) — uses\n  `ModuleCat.restrictScalarsComp`.\n\n## Summary\n- **Sorry count: 5 → 5** (no sorries closed, no sorries added). The 5 are: `gammaPushforwardIso_comp`\n  (L733, assigned — STILL OPEN), `pullback_spec_tilde_iso_ring_square_natural` (L1347, crux), and the\n  3 dead-mate sorries (L1546/1821/1861, do-not-touch).\n- **Sorries closed:** none.\n- **Sorries still open + why:** `gammaPushforwardIso_comp` — blocked by the value-`ModuleCat` object-\n  junction diamond (see BLOCKER); all three collapse routes (rw/simp matching, term-mode Eq.trans,\n  monolithic rfl) fail with VERIFIED specific failures. Needs the iter-013 analogist consult.\n- **Adjacent sorries:** did not attempt the crux (transitively gated on this foundation).\n- File compiles cleanly (`lake build AlgebraicJacobian.Cohomology.FlatBaseChange` → BUILD OK).\n\n## Why I stopped\n`Partial progress.` Concrete CODE advances this iter (not just prose):\n1. Added 4 new helper lemmas that COMPILE and are kernel-light/axiom-free (verified by cold build) —\n   the reusable foundation any future finish needs.\n2. Pushed the residual goal from iter-011's opaque `Γ(cast) x = x` to a fully-exposed\n   `x = B(A(γ_φ(Γ_pc(cast x))))` with every layer isolated and named.\n3. VERIFIED (cold build) that all three plausible finishes — whole-goal defeq, monolithic rfl, and\n   the term-mode Eq.trans chain — fail, and characterised WHY (object-junction diamond; carrier-rfl\n   cheap, object-defeq heavy). This rules out the entire \"collapse element-wise\" family and is the\n   decisive new fact for the planner.\nThe genuine remaining blocker is a Mathlib-idiom/structural problem (collapse a value-`ModuleCat`\ncarrier-identity composite without forming heavy per-junction object defeqs). No API key is present,\nso the informal-agent consult must be dispatched by the planner (iter-013, already pre-committed).\nThis is NOT difficulty-avoidance: the element-wise route is provably exhausted; the next move is a\ndifferent (sheaf-level / structural) proof, which is a planning decision the prover lane cannot make\nunilaterally without the analogist.\n"
  },
  {
    "name": "SectionGradedRing.md",
    "content": "# Picard/SectionGradedRing.lean — fine-grained pass (iter-012)\n\nObjective: the associator bridge `tensorObjAssoc_eq_localizedAssociator` (was 1 sorry @L1844).\nPlan: Step 0 derive a well-typed common form `K`; Step 1/2 prove the two half-assemblies\n`hK_lhs`/`hK_rhs`; Step 3 assemble `= hK_lhs.trans hK_rhs.symm`.\n\n## Sentence 0 (GATING): \"the two halves meet at the common form K\" → `assocCommonForm`\n- **Result: RESOLVED.** Defined `private noncomputable def assocCommonForm (A B C) : tensorObj\n  (tensorObj A B) C ⟶ A ⊗_loc (B ⊗_loc C)` (@L1847), the well-typed Lean realisation of the\n  blueprint schematic `K = L'(α^p) ; μ⁻¹ ; (L'a ◁ μ⁻¹) ; (c_A ⊗ (c_B ⊗ c_C))` **prefixed by the\n  inverse segment-1 whiskered unit** `(L'(η_{a⊗b} ▷ c))⁻¹` (= the seg-1 iso of `tensorObjAssoc`).\n  That prefix is exactly the \"counit object-glue\" the plan flagged: it fixes the domain mismatch\n  between the schematic core's domain `L'((a⊗b)⊗c)` and the assembly domain\n  `tensorObj (tensorObj A B) C`.\n- **K typechecks** (only needed `@asIso … (isIso_sheafification_whiskerRight_unit …)` to supply the\n  seg-1 `IsIso` instance, as `tensorObjAssoc` does). The plan's gating worry (\"K may not\n  typecheck\") is **cleared** — surfacing per plan: K is sound.\n\n## Sentence 3 (assembly): `tensorObjAssoc_eq_localizedAssociator`\n- **Result: RESOLVED (own body sorry-free).** Now `:= (hK_lhs A B C).trans (hK_rhs A B C).symm`\n  (@L1968). The `.trans/.symm` typechecks (confirms K's domain/codomain match both halves) — the\n  bridge no longer has its own sorry; it is transitively gated on the two half-lemmas.\n\n## Sentence 1: `tensorObjAssoc_eq_localizedAssociator_hK_lhs : Φ^L ≫ α^loc = K`\n- **Result: PARTIAL** (sorry @L1903). Proof unfolds `assocCommonForm` + `tensorObjLocalizedIso` to\n  the **isolated localized-side goal** (compiles; goal reached).\n- **Precise state:** LHS `= μ_{(A⊗B)♭,c}.inv ≫ (c_{A⊗B} ⊗ c_C) ≫ ((μ_{a,b}.inv ≫ (c_A⊗c_B)) ▷ C) ≫\n  (α_ A B C).hom`; RHS `= K`. There is **no whiskered unit** on this side.\n- **Blocker (named):** `Localization.Monoidal.associator_hom_app` does NOT fire on `(α_ A B C).hom`\n  — verified: its LHS pattern requires the objects to be literally `(L').obj _`, but `A,B,C` are\n  arbitrary. Next step (documented in-code): push the counit isos `c_A,c_B,c_C` through `α_ A B C`\n  via `Localization.Monoidal.associator_naturality` (+ `tensorHom_id`/`id_tensorHom`) to obtain\n  `α_ (L'a)(L'b)(L'c)`, THEN `associator_hom_app` makes the μ's native and the μ-pairs cancel.\n\n## Sentence 2: `tensorObjAssoc_eq_localizedAssociator_hK_rhs : α ≫ Φ^R = K`\n- **Result: PARTIAL but substantially advanced** (sorry @L1960). The proof now executes, in order:\n  1. `rw [tensorObjAssoc]` + unfold `tensorObjLocalizedIso`/`assocCommonForm` →\n     **both sides share the identical prefix** `inv(map (η_{a⊗b}▷c)) ≫ map(α^p) ≫ …`.\n  2. **Two `congrArg (fun t => _ ≫ t)`** strip that common prefix — reduces the obligation to the\n     small tail (segments 3–5 vs `Φ^R`). ✅ works.\n  3. `slice_lhs 1 3 => erw [hcol]` collapses the braiding-conjugated whiskered unit (segments 3–5)\n     to `(A ◁ η_{b⊗c})^#` (reusing the proven seam\n     `sheafification_braiding_whiskerRight_unit_eq_whiskerLeft_unit` after the symmetric swap). ✅.\n  4. `erw [sheafification_whiskerLeft_unit_eq_mu' …]` rewrites it to a μ-conjugate, placing the\n     keystone's trailing `μ_{a,(L'(b⊗c))♭}.hom` adjacent to the `μ_{a,(L'(b⊗c))♭}.inv` from\n     `i_{A,B⊗C}` (**identical object** `B.tensorObj C`). ✅.\n- **Blocker (named, verified in isolation — corrects a prior plan assumption):** the μ-pair\n  `μ_{a,(L'(b⊗c))♭}.hom ≫ μ_{a,(L'(b⊗c))♭}.inv` still **does NOT cancel even on this small isolated\n  goal**. The keystone RHS is a grouped composite `(μ.inv ≫ L'η ≫ μ.hom)` whose inner `≫` is the\n  `Localization.Monoidal` localized-category `CategoryStruct.comp`, while the boundary `≫ μ.inv`\n  from `tensorObjLocalizedIso` is the `X.Modules`-synonym `comp`; defeq but NOT syntactic. So\n  `simp only [Category.assoc]` makes **no progress** (cannot re-associate across the boundary), and\n  `rw`/`erw [Iso.hom_inv_id_assoc]`, `slice … rw [Iso.hom_inv_id]`, `rw [← Category.assoc,\n  Iso.eq_comp_inv]` all fail to find the adjacent `μ.hom ≫ μ.inv`. Forcing it with\n  `simp only [Localization.Monoidal.μ]` (unfold to `tensorBifunctorIso`) **times out at isDefEq**.\n- **Residual after the (blocked) cancel** = the counit coherence\n  `(L'a ◁ L'η_{bc}) ≫ (c_A ⊗ c_{bc}) ≫ (A ◁ (μ_{bc}.inv ≫ (c_B⊗c_C))) = (L'a ◁ μ_{bc}.inv) ≫\n  (c_A ⊗ (c_B⊗c_C))` (`μ_natural_right` + the sheafification unit/counit triangle `L'η ≫ c = id`).\n\n## Summary\n- **Sentences:** 2/4 RESOLVED (the GATING `K` definition + the assembly own-body), 2/4 PARTIAL with\n  precise blockers (`hK_lhs`, `hK_rhs`).\n- **Sorry count (this file, my region):** the assembly's 1 sorry @L1844 → assembly now sorry-free;\n  +2 new sorries in the isolated half-lemmas (`hK_lhs` @L1903, `hK_rhs` @L1960). Net +1 sorry, but\n  the residual is now isolated and precisely characterised (and `hK_rhs` is reduced through\n  collapse + keystone all the way to the single μ-cancel + counit coherence).\n- **Cold build:** `lake build AlgebraicJacobian.Picard.SectionGradedRing` → **success, 2441 jobs**\n  (only `sorry`/style warnings; no kernel timeout — the `erw`/`slice`/`congrArg` chain is\n  kernel-light). No `maxHeartbeats 1e6` added.\n\n## Why I stopped (honest)\n- **Real progress:** GATING passed (well-typed `K` exists — clears the plan's main worry); the\n  assembly is closed term-mode via `hK_lhs.trans hK_rhs.symm`; `hK_rhs` is driven through the\n  prefix-strip + braiding-collapse + keystone to the precise μ-cancel; `hK_lhs` reduced to its\n  isolated goal. All steps verified by cold build.\n- **STOP-and-surface (plan's explicit condition):** the plan said \"if `hK_rhs` won't close → STOP\n  and surface it; iter-013 corrective is a Mathlib-idiom consult on the `Localization.Monoidal`\n  entry point.\" That condition has fired. **Key finding correcting the iter-011 hypothesis:**\n  isolating the goal into the half-assemblies does **NOT** by itself let `Iso.hom_inv_id_assoc`\n  fire — the `CategoryStruct.comp` instance mismatch (localized `Localization.Monoidal` comp vs the\n  `LocalizedMonoidal`-synonym/`X.Modules` comp) persists even on the small goal, and `Category.assoc`\n  cannot bridge it. The remaining lever is the plan's `show`/`change` instance-pin of both μ's to one\n  literal localized-`comp` form before the cancel — but a naive `simp only [Localization.Monoidal.μ]`\n  unfold times out, so the iter-013 consult should target: **what is the canonical instance-agnostic\n  way to cancel `e.hom ≫ e.inv` across the `LocalizedMonoidal` synonym/base `comp` boundary?**\n  (e.g. a `change` to `(L').obj`-level comp, or a dedicated `Localization.Monoidal` cancellation\n  idiom). Once that single cancel is available, both halves close mechanically (hK_rhs: + μ_natural_\n  right + counit triangle; hK_lhs: + associator_naturality + associator_hom_app + the same cancel).\n\n## Blueprint markers (for the review agent)\n- `lem:tensorObjAssoc_hK_lhs` / `lem:tensorObjAssoc_hK_rhs` (L1516/L1560 in the chapter) now have\n  matching Lean decls `…_hK_lhs` / `…_hK_rhs` (both PARTIAL = `sorry` present → statement-`\\leanok`\n  only, no proof-`\\leanok`; sync_leanok will set this).\n- `lem:tensorObjAssoc_eq_localizedAssociator`: the Lean own-body is sorry-free but transitively gated\n  on the two half-lemmas — sync_leanok should keep proof-`\\leanok` OFF until the halves close.\n- New def `assocCommonForm` realises the chapter's `K`; if the planner wants it blueprinted, it is\n  the well-typed `K` (with the seg-1 unit prefix the schematic omits).\n"
  },
  {
    "name": "blueprint-writer-snap-mucancel.md",
    "content": "# Blueprint Writer Report: snap-mucancel\n**Status:** COMPLETE\n\n## Changes\n- Add `def:assocCommonForm` (`...Modules.assocCommonForm`): well-typed 5-factor K = schematic core prefixed by `(L'(η_{a⊗b}▷c))⁻¹` counit object-glue (fixes domain mismatch `L'((a⊗b)⊗c)` vs `(A⊗B)⊗C`); notes K = shared codomain of both half-assemblies. `\\uses` = isIso_sheafification_whiskerRight_unit + 4 schematic-factor deps.\n- Revise `lem:tensorObjAssoc_hK_lhs` proof: deleted stale isolation note + iter-012 `% NOTE`; correct mechanism = `associator_naturality` pushes counit isos through `α` → `associator_hom_app` makes μ's native → μ-pair cancels across localized/base comp boundary (peel leading μ⁻¹ first) → triangle + `μ_natural_left` leave K. Repointed `\\uses`→`def:assocCommonForm`.\n- Revise `lem:tensorObjAssoc_hK_rhs` proof: replaced \"syntactically same / Iso.hom_inv_id_assoc\" with TWO stacked defeq mismatches (comp-instance + μ object-arg) absorbed up-to-reducible-defeq, mandatory leading-μ⁻¹ peel; residual closed by `μ_natural_right` + counit triangle. Math content (segments, braiding, keystone) intact. Repointed `\\uses`.\n- Cited `analogies/snap-localized-comp-cancel.md` as mechanism authority in both proofs.\n\n## Notes / Strategy\n- leandag: 0 unknown_uses, `def:assocCommonForm` matched to Lean + non-isolated. No `\\leanok` added by me. Project-bespoke — no external SOURCE.\n"
  },
  {
    "name": "lean-auditor-iter012.md",
    "content": "# Lean Audit Report\n\n## Slug\niter012\n\n## Iteration\n012\n\n## Scope\n- files audited: 5\n- files skipped (per directive): 0\n\n---\n\n## Per-file checklist\n\n### AlgebraicJacobian.lean\n- **outdated comments**: none\n- **suspect definitions**: none\n- **dead-end proofs**: none\n- **bad practices**: none\n- **excuse-comments**: none\n- **notes**:\n  - 5-line root import file. Only function: imports the four subfiles (RegroupHelper, FlatBaseChange,\n    FlatBaseChangeGlobal, SectionGradedRing). No declarations; nothing to audit.\n\n---\n\n### AlgebraicJacobian/Cohomology/RegroupHelper.lean\n- **outdated comments**: none\n- **suspect definitions**: none\n- **dead-end proofs**: none\n- **bad practices**: none\n- **excuse-comments**: none\n- **notes**:\n  - Single declaration: `base_change_regroup_linearEquiv` — the R'-linear equivalence\n    `(A ⊗[R] R') ⊗[A] M ≃ₗ[R'] R' ⊗[R] M`. Fully proved. `#check_axioms` would show only\n    `propext` and `Quot.sound`. No `sorry`, no `erw`, no stale comments. Clean file.\n\n---\n\n### AlgebraicJacobian/Cohomology/FlatBaseChangeGlobal.lean\n- **outdated comments**: none\n- **suspect definitions**: none\n- **dead-end proofs**: 4 flagged (all pre-existing, documented as genuine mathematical gaps)\n- **bad practices**: none\n- **excuse-comments**: none\n- **notes**:\n  - `baseChange_sheafConditionFork_tensorIso` (sorry): documented as a genuine gap requiring\n    sheaf-tensor vs sheaf-of-homs interchange at the fork level. Not this iter's work.\n  - `flatBaseChange_pushforward_isIso_of_isSeparated` (sorry): main theorem placeholder;\n    explicitly awaiting the affine-local machinery to be complete upstream. Pre-existing.\n  - `flatBaseChange_pushforward_mayerVietoris` (sorry): Mayer–Vietoris gluing step.\n    Pre-existing.\n  - `flatBaseChange_isIso_iff_gammaTensorComparison` (one sorry in the reverse direction):\n    reverse-direction gap, pre-existing.\n  - All four sorries are documented with accurate technical descriptions; none contain\n    excuse-comments. No new sorries introduced this iter.\n\n---\n\n### AlgebraicJacobian/Cohomology/FlatBaseChange.lean  ← PRIMARY FOCUS\n- **outdated comments**: 1 flagged (stale iteration-counter references in module docstring)\n- **suspect definitions**: none\n- **dead-end proofs**: 5 flagged (1 new this iter, 4 pre-existing)\n- **bad practices**: 1 flagged — CRITICAL latent bug: `set_option maxHeartbeats` stacking scopes\n  to `/-! ... -/` comment blocks instead of the theorems they precede (iter-011 latent bug,\n  **still unfixed**)\n- **excuse-comments**: none\n- **notes**:\n\n  **New declarations (iter-012 focus, L680–784):**\n\n  - L683 `gammaPushforwardIso_hom_apply := rfl` — kernel-light, valid. The claim (γ's hom\n    is the identity on the underlying section) is a genuine rfl: the carrier of\n    `restrictScalarsComp'App` isos is the identity by construction.\n  - L689 `gammaPushforwardIso_inv_apply := rfl` — same justification as above, inv direction.\n    Valid.\n  - L696–701 `moduleCat_eqToHom_concreteCategory_apply` — `subst h; rfl`. The body is correct:\n    after substituting `h : M = N`, the `eqToHom`-application reduces to the cast, which\n    collapses to `rfl`. Kernel-light. The docstring accurately explains why this replaces\n    `change`/`rfl` (the latter forces whnf of the structure-sheaf machinery and times out).\n  - L706–709 `gammaPushforwardIso_hom_concreteApply := rfl` — `ConcreteCategory.hom`-keyed\n    restatement of the `hom_apply` helper. Valid; the type difference (`Hom.hom` vs\n    `ConcreteCategory.hom`) is the sole motivation and `:= rfl` is correct.\n  - L713–716 `restrictScalars_map_concreteApply := rfl` — `restrictScalars.map` is\n    carrier-transparent. Valid single-layer rfl.\n  - L720–723 `restrictScalarsComp_inv_app_concreteApply := rfl` — `restrictScalarsComp` iso is\n    carrier-identity. Valid.\n  - All six helpers compile to `:= rfl` or `subst; rfl`; no `erw`, no `simp`, no heartbeat\n    risk. Axiom profile: clean.\n\n  - L733–784 `gammaPushforwardIso_comp` — **NEW open sorry.** The 20-line comment block\n    L744–783 accurately characterises the blocker: the `X.Modules`/value-`ModuleCat` diamond\n    makes the per-layer carrier-identity collapse non-assembl­able via `rw`/`simp` (motive error\n    after retyping), the `Eq.trans` term chain is a cold-build timeout (verified > 1.6M\n    heartbeats), and the monolithic `rfl` is a kernel bomb (forces full structure-sheaf whnf).\n    The comment names the iter-013 corrective: route through `.val.app (op ⊤)` at the sheaf\n    level where the `X.Modules`/`ModuleCat` junction never forms. This is NOT an\n    excuse-comment; it is a precisely characterized technical obstacle with a documented plan.\n    Load-bearing result: `gammaPushforwardNatIso_comp` at L816–840 calls\n    `exact gammaPushforwardIso_comp φ ρ N` and is therefore transitively sorry-backed.\n\n  **Pre-existing sorries (no change this iter):**\n\n  - L1424 `pullback_spec_tilde_iso_ring_square_natural` — pre-existing.\n  - L1671 `base_change_mate_gstar_transpose` — pre-existing; genuine crux.\n  - L1852 `affineBaseChange_pushforward_iso` — pre-existing.\n  - L1874 `flatBaseChange_pushforward_isIso` — pre-existing.\n\n  **Latent bug — `set_option maxHeartbeats` stacking (ITER-011 LATENT BUG, UNRESOLVED):**\n\n  - **L1488**: `set_option maxHeartbeats 4000000 in` at L1488 applies to the command\n    immediately following it. That command is `/-! ### Seam 2, conjugate-route foundation\n    (iter-035 pivot) ... -/` at L1489–1497 — a `/-! ... -/` section documentation block, which\n    IS a Lean 4 command. The option is entirely consumed by the comment; it does NOT propagate\n    to any subsequent declaration. The comment at L1486–1487 (\"The conjugate-unit calculus\n    chains several `erw` … which collectively exceed the default heartbeat budget\") was clearly\n    intended to budget the upcoming theorems, but it does not.\n\n  - **L1523–1526**: Four stacked `set_option maxHeartbeats ... in` directives:\n    ```\n    set_option maxHeartbeats 4000000 in   -- L1523\n    set_option maxHeartbeats 4000000 in   -- L1524\n    set_option maxHeartbeats 4000000 in   -- L1525\n    set_option maxHeartbeats 1600000 in   -- L1526\n    /-! ### Seam 3, the gstar chain ...   -- L1527–1536: absorbs all four set_options\n    ...  -/\n    ```\n    The four options are all absorbed by `/-! ### Seam 3 ... -/` (L1527–1536). The theorem\n    `base_change_mate_gstar_transpose` at L1546 runs under the **default** heartbeat budget.\n    Current impact is limited because `base_change_mate_gstar_transpose` carries an open sorry\n    (L1671) and no tactic proof is running. When the proof is written, it will silently time out\n    without the intended 4M/1.6M budget. This is a future-proof blocker, not a current error.\n\n  - **Stale iter-NNN references** (minor): The module docstring at L183–248 references\n    \"iter-234\", \"iter-236\", \"iter-241\". These are from a different project-internal iteration\n    counter (not the Archon archon-iteration numbering). The content of those references is\n    factually accurate (the cited results do exist and do what the docstring claims), so this\n    is not a correctness issue — only a potential source of reader confusion.\n\n  **`erw` audit:**\n\n  - L154: `erw` on stalkFunctor naturality — load-bearing, documented, standard usage.\n  - L1121, L1134, L1138, L1144: `erw` cluster in the tilde-dictionary section. All documented\n    with rationale (defeq-matching across module vs presheaf-of-modules instances). None fragile\n    beyond the documented reason.\n\n---\n\n### AlgebraicJacobian/Picard/SectionGradedRing.lean  ← PRIMARY FOCUS\n- **outdated comments**: 1 flagged (stale iter-NNN planning annotations)\n- **suspect definitions**: none\n- **dead-end proofs**: 7 flagged (2 new this iter, 5 pre-existing)\n- **bad practices**: none\n- **excuse-comments**: none\n- **notes**:\n\n  **New declarations (iter-012 focus, L1847–1979):**\n\n  - L1847–1881 `assocCommonForm` (private noncomputable def): The common form `K` that mediates\n    the `hK_lhs`/`hK_rhs` split. Body is a 5-factor composite:\n    `(seg-1-unit prefix)⁻¹ ≫ L'(α^p) ≫ μ⁻¹ ≫ (L'A ◁ μ⁻¹) ≫ (c_A ⊗ (c_B ⊗ c_C))`.\n    Signature and body are well-typed for the stated purpose (bridging `tensorObj (tensorObj A B) C`\n    to `A ⊗^loc (B ⊗^loc C)` in `modulesLocalizedMonoidal X`). No sorry in the def body itself.\n    Private, so no external axiom concerns.\n\n  - L1885–1903 `tensorObjAssoc_eq_localizedAssociator_hK_lhs` (private): **NEW open sorry.**\n    The comment at L1895–1902 accurately characterises the required route:\n    `associator_naturality` to push the counit isos `c_A, c_B, c_C` through `α_ A B C`, making\n    the objects literally `(L').obj _` so that `associator_hom_app` fires. The comment notes\n    (and the prior session verified) that `associator_hom_app` does NOT fire on `α_ A B C`\n    directly. The sorry is properly isolated: the lemma body runs `rw [assocCommonForm]; simp\n    only [...]` reducing to a well-characterized local goal before the sorry. Not an\n    excuse-comment.\n\n  - L1908–1960 `tensorObjAssoc_eq_localizedAssociator_hK_rhs` (private): **NEW open sorry.**\n    Proof advances through three real steps: `rw [tensorObjAssoc]`, a `simp only [...]`\n    unfolding, two `congrArg` strip-offs of the shared prefix, and then the `hcol`\n    braiding-collapse (proven via `sheafification_braiding_whiskerRight_unit_eq_whiskerLeft_unit`),\n    applied via `slice_lhs 1 3 => erw [hcol]` (load-bearing, instance-mismatch driven). The `erw`\n    at L1941 (`sheafification_whiskerLeft_unit_eq_mu'`) is also load-bearing. The sorry at L1960\n    is isolated at the μ-cancel goal; the comment at L1945–1959 documents precisely why\n    `Iso.hom_inv_id_assoc` fails (the `μ.hom ≫ μ.inv` pair straddles a `Localization.Monoidal`\n    comp boundary vs the `X.Modules` synonym comp — defeq but not syntactic). The iter-013\n    corrective (instance-pin via `show`/`change` before `Iso.hom_inv_id_assoc`) is named.\n    Not an excuse-comment.\n\n  - L1968–1979 `tensorObjAssoc_eq_localizedAssociator` (public): **own body sorry-FREE.**\n    Proof is `(tensorObjAssoc_eq_localizedAssociator_hK_lhs A B C).trans (tensorObjAssoc_eq_localizedAssociator_hK_rhs A B C).symm`.\n    The assembly via `.trans ... .symm` is logically correct given the two halves both equal\n    `assocCommonForm A B C`. Transitively sorry-backed through the two private halves; that is\n    expected and documented in the preceding docstring.\n\n  **Former proof block (L1981–2039): well-formed comment.**\n\n  The block opens with `/-` at L1981 and closes with `-/` at L2039. Content is the superseded\n  monolithic reduction recipe (tactic code for reference, not active). Verified: the first\n  active declaration after the block is at L2041 — it is NOT inside the comment. No code is\n  stranded. The `slice_rhs`/`erw` at L2009 and L2014 are documentation-only (inside the block).\n\n  **Pre-existing sorries (no change this iter):**\n\n  - L2510 `tensorPowAdd_rightUnit` (succ case) — pre-existing coherence sorry.\n  - L2532 `tensorPowAdd_braiding` (succ case) — pre-existing coherence sorry.\n  - L2547 `tensorPowAdd_assoc` (zero case) — pre-existing coherence sorry.\n  - L2550 `tensorPowAdd_assoc` (succ case) — pre-existing coherence sorry.\n  - L2610 `sectionsMul_mul_assoc` — pre-existing; the comment documents the missing whiskering\n    naturality bricks as the obstruction.\n\n  **Transitively sorry-backed instances:**\n\n  - `instGMonoid`, `instGSemiring`, `instGCommSemiring` (L2644–2689): all pass through\n    `sectionsMul_mul_assoc` (L2610, sorry). Their docstrings do not flag this transitivity\n    explicitly; minor omission, not an excuse-comment.\n\n  **`erw` and `slice` audit:**\n\n  - L887 region: `set_option maxHeartbeats 800000 in` followed by `/-- ... -/` (docstring)\n    then `private noncomputable def uTripIso`. In Lean 4, a docstring is part of the following\n    declaration, so the option correctly scopes to `uTripIso`. Not a bug (unlike the\n    `/-! ... -/` case in FlatBaseChange.lean).\n  - L1671 `erw` (β_hom_app bridge): load-bearing, documented.\n  - L1937 `slice_lhs 1 3 => erw [hcol]`: load-bearing; `hcol` is proven in-scope by the\n    `have` block above. Instance-mismatch between `MonoidalPresheaf X` and bare presheaf\n    monoidal drives the `erw`.\n  - L1941 `erw [sheafification_whiskerLeft_unit_eq_mu']`: load-bearing `◁` instance-form\n    mismatch; consistent with prior `erw` usage pattern in this file.\n\n  **Stale iter-NNN references (minor):**\n\n  - Planning comment blocks reference \"iter-063\", \"iter-053\", \"iter-066\", \"iter-078\". These\n    are advisory project-history annotations; content is factually accurate and the referenced\n    tactics/strategies are not invalidated by iter-012 work. Minor readability issue only.\n\n---\n\n## Must-fix-this-iter\n\n*None.* No excuse-comments, no weakened-wrong definitions, no unauthorized axioms, no\n`:= rfl` on a non-trivial claim were found. The open sorries in new declarations are\nexplicitly documented with precise technical blockers and iter-013 corrective plans; they do\nnot meet the \"suspect body\" criterion (which targets hidden or undocumented sorry, not\ntracked development checkpoints).\n\n---\n\n## Major\n\n- `FlatBaseChange.lean:1488` — `set_option maxHeartbeats 4000000 in` scopes to the `/-! ... -/`\n  section comment at L1489–1497, NOT to any theorem. The theorem that was intended to receive\n  the budget is not protected. Current impact: limited (affected theorem region carries open\n  sorries). Future impact: when proofs are written, silent timeout will occur without the\n  intended budget. This is a confirmed iter-011 latent bug that has not been fixed.\n\n- `FlatBaseChange.lean:1523–1526` — Four stacked `set_option maxHeartbeats ... in` directives\n  (4M / 4M / 4M / 1.6M) all consumed by `/-! ### Seam 3 ... -/` at L1527–1536. The theorem\n  `base_change_mate_gstar_transpose` at L1546 runs under the default heartbeat budget. Same\n  root cause as above; same future impact.\n\n- `FlatBaseChange.lean:784` — `gammaPushforwardIso_comp`: open sorry on a load-bearing\n  composition-coherence result. Transitively sorry-backs `gammaPushforwardNatIso_comp` (L816–840).\n  Blocker is precisely characterised and a corrective is pre-committed for iter-013.\n\n- `SectionGradedRing.lean:1903` — `tensorObjAssoc_eq_localizedAssociator_hK_lhs`: open sorry;\n  route documented (`associator_naturality` + `associator_hom_app` after object conversion).\n  Blocks the localized-side half of the associator bridge.\n\n- `SectionGradedRing.lean:1960` — `tensorObjAssoc_eq_localizedAssociator_hK_rhs`: open sorry;\n  μ-cancel blocker documented (comp-boundary defeq, not syntactic). Iter-013 corrective named.\n  Blocks the hand-built-side half of the associator bridge.\n\n---\n\n## Minor\n\n- `FlatBaseChange.lean:183–248` — Module docstring references \"iter-234\", \"iter-236\",\n  \"iter-241\" from a different project-internal iteration counter (not Archon archon-iter\n  numbering). Content is factually accurate but potentially confusing to readers unfamiliar\n  with the counter distinction.\n\n- `SectionGradedRing.lean:2644–2689` — `instGMonoid`, `instGSemiring`, `instGCommSemiring`\n  are transitively sorry-backed through `sectionsMul_mul_assoc` (L2610). Their docstrings\n  do not flag this. Low-impact omission during active development but worth noting for the\n  record.\n\n- `SectionGradedRing.lean` planning comments — References to \"iter-063\", \"iter-053\",\n  \"iter-066\", \"iter-078\" in advisory comment blocks. Factually accurate; readability noise.\n\n---\n\n## Excuse-comments (always called out separately)\n\n**None found.** No declaration in any file carries a \"TODO: replace with real def\",\n\"placeholder\", \"temporary\", \"wrong but works\", or \"will fix later\" comment. The open\nsorry in `gammaPushforwardIso_comp` and the two SNAP halves all have precise technical\ncharacterisations and named correctives; these are the opposite of excuse-comments.\n\n---\n\n## Severity summary\n\n- **must-fix-this-iter**: 0\n- **major**: 5 (2 × heartbeat stacking bug; 3 × documented open sorry on load-bearing claims)\n- **minor**: 3\n- **excuse-comments**: 0\n\nOverall verdict: The project's iter-012 additions are structurally sound — the six new `:=\nrfl` helpers are kernel-light and correct, the `assocCommonForm` def is well-typed, and the\nassembly `tensorObjAssoc_eq_localizedAssociator` is sorry-free in its own body with both\nhalves' blockers precisely documented — but the `set_option maxHeartbeats` stacking bug\nfrom iter-011 remains unfixed at two sites in `FlatBaseChange.lean` and will silently starve\nthe Seam 2/3 proofs of their heartbeat budget when those sorries are filled in.\n"
  },
  {
    "name": "lean-vs-blueprint-checker-fbc012.md",
    "content": "# Lean ↔ Blueprint Check Report\n\n## Slug\nfbc012\n\n## Iteration\n012\n\n## Files audited\n- Lean: `AlgebraicJacobian/Cohomology/FlatBaseChange.lean`\n- Blueprint: `blueprint/src/chapters/Cohomology_FlatBaseChange.tex`\n\nScope: the 4 new helper lemmas added this iter (L696–723) plus the still-open\n`gammaPushforwardIso_comp` (L733, `sorry`@784) and its blueprint counterpart\n`lem:gammaPushforwardIso_comp`. LSP diagnostics over L675–790 confirm the **only**\nsorry/error in the region is L733; the four helpers emit no errors (only cosmetic\nlong-line warnings on comment lines L750–783).\n\n## Per-declaration\n\n### 4 new helpers (no `\\lean{...}` block — see \"Unreferenced declarations\")\n\n#### `AlgebraicGeometry.moduleCat_eqToHom_concreteCategory_apply` (L696)\n- **Lean target exists**: yes\n- **Body genuine / non-fake**: yes — `by subst h; rfl`. Substantive content is the\n  `eqToHom`-as-`cast` identity; honest, compiles.\n- **Blueprint block**: none (sub-`gammaPushforwardIso_hom_apply` plumbing).\n- **notes**: kernel-light replacement for a `change`/`rfl` collapse; faithful.\n\n#### `AlgebraicGeometry.gammaPushforwardIso_hom_concreteApply` (L706)\n- **Lean target exists**: yes\n- **Body genuine / non-fake**: yes — `:= rfl`. A `ConcreteCategory.hom`-keyed\n  restatement of the already-blueprinted `lem:gammaPushforwardIso_hom_apply`. Compiles.\n- **Blueprint block**: none (mechanical restatement of `lem:gammaPushforwardIso_hom_apply`).\n\n#### `AlgebraicGeometry.restrictScalars_map_concreteApply` (L713)\n- **Lean target exists**: yes\n- **Body genuine / non-fake**: yes — `:= rfl`. \"Restriction of scalars does not change\n  the underlying function.\" Compiles.\n- **Blueprint block**: none (generic `ModuleCat` plumbing).\n\n#### `AlgebraicGeometry.restrictScalarsComp_inv_app_concreteApply` (L720)\n- **Lean target exists**: yes\n- **Body genuine / non-fake**: yes — `:= rfl`. \"`restrictScalarsComp.inv` is identity on\n  carriers.\" Compiles.\n- **Blueprint block**: none (generic `ModuleCat` plumbing).\n\n**Verdict on the 4 helpers: all faithful and non-fake.** Each is a genuine single-layer\n`rfl` / `subst;rfl`, verified to elaborate without error. None is a placeholder, none has\na suspicious body, none carries an excuse-comment. They are legitimate kernel-light glue\nsitting *below* the level the blueprint formalizes (carrier-identity restatements of\n`gammaPushforwardIso_hom_apply` + generic `ModuleCat.restrictScalars` facts), so the absence\nof `\\lean{...}` blocks for them is correct, not a coverage gap.\n\n### `\\lean{AlgebraicGeometry.gammaPushforwardIso_comp}` (chapter: `lem:gammaPushforwardIso_comp`, L794–837)\n- **Lean target exists**: yes (L733).\n- **Signature matches**: yes. The Lean statement (composite `(φ ≫ ρ)` comparison =\n  pushforwardComp/`Spec.map_comp`-cast domain reorg ≫ single-map comparisons ≫\n  `restrictScalarsComp` codomain reorg) is a faithful encoding of the blueprint statement.\n- **Proof follows sketch**: **no / N/A — open `sorry`@784**, and the blueprint sketch\n  diverges from the discovered Lean reality (see Blueprint adequacy below). This is a\n  documented genuine WIP, *not* a fake/placeholder claiming completion: the body collapses\n  the LHS via `gammaPushforwardIso_hom_apply` and bottoms out in an honest `sorry` with a\n  precise multi-line account of the blocker.\n- **Markers consistent**: statement block has `\\leanok` (correct — declaration exists);\n  proof block (L822) has **no** `\\leanok` (correct — `sorry` present). The\n  `sync_leanok` markers are right here.\n\n## Red flags\n\nNone of must-fix class.\n\n- No fake/placeholder bodies: the four helpers are real `rfl`s; the lone `sorry`\n  (`gammaPushforwardIso_comp`@784) is a documented open obligation, not a stand-in for a\n  claim the blueprint marks complete.\n- No excuse-comments masking wrong code. The long comment block L745–783 is an honest,\n  precise blocker characterization (the value-`ModuleCat`/`X.Modules` object-junction\n  diamond), not an \"it's wrong but works\" note.\n- No axioms / `Classical.choice` on substantive claims introduced this iter.\n\n## Stale docstrings (major)\n\n### `gammaPushforwardNatIso_comp` docstring, `FlatBaseChange.lean:813`\nThe docstring asserts: *\"Every constituent is identity-on-carriers (as is\n`gammaPushforwardNatIso` itself), so the proof is pointwise reflexivity.\"* This is **stale\nand mildly misleading**: the actual proof (L828–840) is **not** pointwise reflexivity — it\n`simp only [...]`-collapses the whiskering/associator pasting and then `exact\ngammaPushforwardIso_comp φ ρ N`, i.e. it *delegates* to the open, non-pointwise\n`gammaPushforwardIso_comp`. The blueprint itself was already corrected on this point —\n`lem:gammaPushforwardNatIso_comp`'s proof (chapter L917) explicitly states *\"That component\ncoherence is **not** a pointwise identity\"* — so the Lean docstring now contradicts both the\nrealized proof and the (updated) chapter. Recommend rewording the L813 docstring to \"reduces\nper-component to `gammaPushforwardIso_comp`\" rather than \"pointwise reflexivity.\"\n\n(For contrast, the \"pointwise `rfl`\" phrasing in the `gammaPushforwardNatIso` docstring at\nL795 is **accurate** — that def really is `ext x; rfl` — and the `gammaPushforwardIso_comp`\ndocstring/comment L725–783 is accurate and current. Only the L813 claim is stale.)\n\n## Unreferenced declarations (informational)\n\n- `moduleCat_eqToHom_concreteCategory_apply`, `gammaPushforwardIso_hom_concreteApply`,\n  `restrictScalars_map_concreteApply`, `restrictScalarsComp_inv_app_concreteApply` — all\n  four are unreferenced by any `\\lean{...}` block. **Acceptable as helpers**: they are\n  carrier-level `rfl` plumbing strictly below the blueprint's granularity (the math content\n  \"the comparison is identity on carriers\" is already captured by the blueprinted\n  `lem:gammaPushforwardIso_hom_apply` / `lem:gammaPushforwardIso_inv_apply`). No new blueprint\n  blocks warranted; at most a one-line `% NOTE` could acknowledge them, but this is not\n  required.\n\n## Blueprint adequacy for this file\n\n- **Coverage**: the substantive declarations of the file map to `\\lean{...}` blocks\n  (`gammaPushforwardIso_comp`, `gammaPushforwardNatIso_comp`, `gammaPushforwardIso_{hom,inv}_apply`,\n  the locality / tilde / localization lemmas, etc.). The 4 new helpers are intentionally\n  unblueprinted plumbing (acceptable). No substantive declaration is orphaned.\n- **Proof-sketch depth for `lem:gammaPushforwardIso_comp`**: **under-specified / partly stale\n  vs the discovered blocker (major).** The chapter proof (L822–837) treats the crux as\n  *\"Unwinding both sides to these underlying carriers, the asserted equality reduces to … the\n  three-fold global-sections ring coherence `globalSectionsIso_hom_comp3_specMap_appTop`.\"*\n  But the prover's realized route shows the genuine obstruction is **not** an application of\n  that ring lemma — once the LHS is collapsed by `gammaPushforwardIso_hom_apply` (a `rfl`)\n  and the `Spec.map_comp` reindexing cast is seen to be carrier-identity (`Γ(cast) x = x`),\n  the whole equation is a carrier identity, and the ring coherence is already *baked into the\n  definition* of `gammaPushforwardIso` (via `globalSectionsIso_hom_comp_specMap_appTop`). The\n  three-fold lemma `globalSectionsIso_hom_comp3_specMap_appTop` **does not appear anywhere in\n  the proof attempt.** The actual wall is the value-`ModuleCat`/`X.Modules`\n  **object-junction diamond**: carrier-`rfl` is cheap, but each wrapper changes the\n  `ModuleCat` *object* type, so per-junction object-defeq is kernel-heavy — a monolithic\n  `rfl` is a verified cold-build `whnf` kernel bomb and the term-mode `Eq.trans` chain\n  explodes (>1.6M heartbeats). The chapter's \"unwinding to carriers\" framing silently\n  understates exactly this step. A prover working *from prose alone* would have been\n  steered toward `globalSectionsIso_hom_comp3_specMap_appTop` and away from the real route\n  (sheaf-level `.val.app (op ⊤)` via `pushforwardComp_inv_app_val_app`, where the\n  restrictScalars/globalSections junctions never form). The `\\uses{… \n  globalSectionsIso_hom_comp3_specMap_appTop}` annotation is likewise suspect for the\n  realized route.\n- **Hint precision**: precise — `\\lean{...}` targets all resolve to the right declarations\n  with matching signatures.\n- **Generality**: matches need.\n- **Recommended chapter-side actions** (for a blueprint-writing subagent):\n  1. In the `lem:gammaPushforwardIso_comp` proof (L822–837), add a `% NOTE:` (or a sentence)\n     recording that the Lean obstruction is the carrier-cheap / object-defeq-heavy\n     value-`ModuleCat` junction diamond, and that the realized route goes through the\n     sheaf-level `.val.app (op ⊤)` (`pushforwardComp_inv_app_val_app`) rather than a direct\n     carrier unwinding. Re-examine whether `globalSectionsIso_hom_comp3_specMap_appTop` is\n     genuinely invoked on the realized route or whether the ring coherence is subsumed by the\n     definition of `gammaPushforwardIso`; adjust the `\\uses` accordingly.\n  2. (Lean-side, for the prover/plan, not the chapter) Reword the stale\n     `gammaPushforwardNatIso_comp` docstring at `FlatBaseChange.lean:813` away from \"pointwise\n     reflexivity\" to match the chapter's already-correct \"not a pointwise identity; reduces\n     per-component to `gammaPushforwardIso_comp`.\"\n\n## Severity summary\n\n- **must-fix-this-iter**: none. Signatures match the prose; the four helpers are genuine\n  non-fake `rfl`s; the lone `sorry` is a documented WIP (not a placeholder on a\n  blueprint-claims-complete declaration); `\\leanok` markers are consistent with sorry status.\n- **major**:\n  - Blueprint proof sketch for `lem:gammaPushforwardIso_comp` is under-specified / partly\n    stale vs the discovered blocker — it points at `globalSectionsIso_hom_comp3_specMap_appTop`\n    and treats carrier-unwinding as trivial, whereas the real obstruction is the value-\n    `ModuleCat` object-junction defeq (kernel `whnf` bomb). Recommend the chapter `% NOTE`\n    above (does not block the prover, who has already characterized the blocker in-file).\n  - Stale Lean docstring at `FlatBaseChange.lean:813` (\"the proof is pointwise reflexivity\")\n    contradicting both the realized delegating proof and the corrected chapter prose.\n- **minor**:\n  - 4 new helpers unreferenced by the blueprint — acceptable as sub-granularity plumbing.\n\nOverall verdict: the four new helpers are faithful, non-fake, kernel-light `rfl`s with no\nred flags; the one open `sorry` (`gammaPushforwardIso_comp`) is an honestly-documented WIP,\nbut the chapter's proof sketch for it (and the stale L813 \"pointwise reflexivity\" docstring)\nare now out of step with the prover's discovered value-`ModuleCat` object-junction blocker —\ntwo **major**, non-blocking, bidirectional findings to align prose with reality.\n"
  },
  {
    "name": "lean-vs-blueprint-checker-snap012.md",
    "content": "# Lean ↔ Blueprint Check Report\n\n## Slug\nsnap012\n\n## Iteration\n012\n\n## Files audited\n- Lean: `AlgebraicJacobian/Picard/SectionGradedRing.lean`\n- Blueprint: `blueprint/src/chapters/Picard_SectionGradedRing.tex`\n\nScope: the associator-bridge seams touched this iter — `assocCommonForm` (L1847),\n`tensorObjAssoc_eq_localizedAssociator_hK_lhs` (L1885), `_hK_rhs` (L1908), and the\nassembled `tensorObjAssoc_eq_localizedAssociator` (L1968). Namespace confirmed:\nall three live in `namespace AlgebraicGeometry.Scheme.Modules` (opened L69), so the\nfully-qualified `\\lean{...}` names resolve.\n\n## Per-declaration\n\n### `\\lean{...tensorObjAssoc_eq_localizedAssociator_hK_lhs}` (chapter: `lem:tensorObjAssoc_hK_lhs`, L1517)\n- **Lean target exists**: yes — `tensorObjAssoc_eq_localizedAssociator_hK_lhs` (L1885). `\\lean{}` name exact match.\n- **Signature matches**: yes. Chapter: `Φ^L ; α^loc = K` with\n  `Φ^L = i_{A⊗B,C} ; (i_{A,B} ▷ C)`. Lean LHS =\n  `(tensorObjLocalizedIso (tensorObj A B) C).hom ≫ whiskerRight (tensorObjLocalizedIso A B).hom C ≫ (associator A B C).hom`,\n  RHS = `assocCommonForm A B C`. Each schematic factor maps 1:1 (`i_{-,-}` = `tensorObjLocalizedIso`,\n  `α^loc` = `MonoidalCategoryStruct.associator` in `modulesLocalizedMonoidal`). Faithful, not a placeholder.\n- **Proof follows sketch**: partial — proof body ends in `sorry` (L1903). The Lean\n  reduction so far (rw `assocCommonForm`; `simp only` to expose the isolated goal) matches\n  the chapter's \"expand `i` via `μ⁻¹;(c⊗c)`, convert `α_ A B C` to the localized associator\" sketch.\n  Residual obligation open. **Honestly marked**: the chapter `\\leanok` sits on the *statement*\n  block, the *proof* block carries no `\\leanok` — so the blueprint does NOT claim a closed proof.\n- **notes**: `private` decl; referenced by FQ name from the chapter (see Red flags / minor).\n\n### `\\lean{...tensorObjAssoc_eq_localizedAssociator_hK_rhs}` (chapter: `lem:tensorObjAssoc_hK_rhs`, L1563)\n- **Lean target exists**: yes — `tensorObjAssoc_eq_localizedAssociator_hK_rhs` (L1908). Exact match.\n- **Signature matches**: yes. Chapter: `α ; Φ^R = K` with `Φ^R = i_{A,B⊗C} ; (A ◁ i_{B,C})`.\n  Lean LHS = `(tensorObjAssoc A B C).hom ≫ (tensorObjLocalizedIso A (tensorObj B C)).hom ≫ whiskerLeft A (tensorObjLocalizedIso B C).hom`,\n  RHS = `assocCommonForm A B C`. Faithful 1:1.\n- **Proof follows sketch**: partial — `sorry` at L1960, after substantial honest progress\n  (prefix-strip via two `congrArg`, braiding-conjugated-unit collapse `hcol`, keystone\n  `sheafification_whiskerLeft_unit_eq_mu'`). Matches the chapter sketch's segment-2/3–5 collapse +\n  keystone-as-μ-conjugate route. Statement `\\leanok`, proof block unmarked — honest.\n- **notes**: `private`. The residual is the μ-cancel the chapter discusses — see Bidirectional.\n\n### `\\lean{...tensorObjAssoc_eq_localizedAssociator}` (chapter: `lem:tensorObjAssoc_eq_localizedAssociator`, L1613)\n- **Lean target exists**: yes — `tensorObjAssoc_eq_localizedAssociator` (L1968), public.\n- **Signature matches**: yes. The commuting square `Φ^L ; α^loc = α ; Φ^R` (chapter L1644-1649)\n  is exactly the Lean statement (LHS L1969-1973, RHS L1974-1977).\n- **Proof follows sketch**: yes. Chapter: \"the equality is `hK_lhs.trans hK_rhs.symm`\" (L1668).\n  Lean body (L1978-1979) is literally `(…_hK_lhs A B C).trans (…_hK_rhs A B C).symm`. Own body sorry-free.\n- **notes**: This decl is honest — it is sorry-free in its own body; its two premises carry the open sorries.\n\n### `assocCommonForm` (L1847) — the Lean realisation of chapter `K`\n- **Lean target exists**: yes, but **no `\\lean{...}` block references it** (confirmed: `assocCommonForm`\n  appears nowhere in the chapter). `K` is described only in inline prose (chapter L1500-1511).\n- **Signature/content vs chapter `K`**: **partial — the chapter `K` is missing a factor.**\n  Chapter `K` (L1502-1505) = `L'(α^p) ; μ⁻¹_{a,b⊗c} ; (L'a ◁ μ⁻¹_{b,c}) ; (c_A ⊗ (c_B ⊗ c_C))` — **4 factors**,\n  domain `L'((a⊗b)⊗c)`. Lean `assocCommonForm` = **5 factors**: a leading inverse seg-1 whiskered\n  unit `(L'(η_{a⊗b} ▷ c))⁻¹`, then those same four. The prefix re-bases the domain to\n  `tensorObj (tensorObj A B) C` (the assembly domain shared by both half-assemblies). The chapter's\n  schematic `K` is therefore type-incorrect as a stand-alone meeting morphism — it omits exactly the\n  prefix the Lean needed. This is a **blueprint detail gap** (the directive's suspicion is confirmed).\n  The Lean realisation itself is the genuine well-typed `K`, not a weakened stand-in — the gap is on\n  the blueprint side.\n\n## Red flags\n\n### Placeholder / suspect bodies\n- None that qualify as must-fix. `_hK_lhs` (L1903) and `_hK_rhs` (L1960) bodies end in `sorry`,\n  but the blueprint marks only their **statements** `\\leanok` (proof blocks unmarked), so these are\n  honest work-in-progress, not placeholders masquerading as proven. The assembled\n  `tensorObjAssoc_eq_localizedAssociator` has a sorry-free own body.\n\n### Excuse-comments\n- None. The long Lean comments at L1895-1902 and L1943-1959 are precise engineering notes\n  documenting a real, reproduced obstruction (the μ-cancel comp-instance mismatch) and the planned\n  iter-013 fix. They explain a stuck *proof*, not a wrong *statement* — not the \"wrong def for now\"\n  pattern the rule targets.\n\n### Axioms / Classical.choice\n- None introduced.\n\n## Unreferenced declarations (informational)\n- `assocCommonForm` (L1847): substantive — it is the Lean `K`. Should gain a blueprint block\n  (see Adequacy). Flagged.\n- `tensorObjWhiskerRightIso` (L1478): genuine helper, acceptable unreferenced.\n\n## Blueprint adequacy for this file\n\n- **Coverage**: the three bridge half/assembly lemmas all have matching `\\lean{...}` blocks with\n  exact names. The one substantive gap is `assocCommonForm` (the well-typed `K`), which has no block —\n  it survives only as schematic prose that is one factor short of the actual Lean object.\n- **Proof-sketch depth**: **under-specified on the residual blocker.** The `lem:tensorObjAssoc_hK_lhs`\n  proof (chapter L1552-1556) asserts the μ⁻¹;μ cancel \"fails on the full bridge goal only as a\n  `whnf`-at-scale artifact\" and that on the small isolated goal a `show`/`change` pin is \"cheap\"\n  before `Iso.hom_inv_id_assoc` fires. The prover's iter-012 finding (Lean comment L1945-1955,\n  **verified in isolation**) contradicts this: the cancel fails *even isolated* because the keystone\n  RHS's inner `≫` is `Localization.Monoidal`'s localized-category `comp` while the boundary `μ.inv`\n  uses the `X.Modules` synonym's `comp` — defeq but not syntactic; `simp only [Category.assoc]` makes\n  no progress and `simp only [Localization.Monoidal.μ]` **times out** at `isDefEq`. So the chapter's\n  \"whnf-at-scale artifact / cheap pin\" framing is actively misleading about the one obstruction that\n  is blocking the file. `lem:tensorObjAssoc_hK_rhs`'s \"lower-fragility alternative\" (route via\n  `Localization.Monoidal.associator_hom_app` so the μ's unify natively) is the more useful hint and\n  is consistent with the prover's intended fix, but it is offered as an aside, not the main recipe.\n- **Hint precision**: `\\lean{...}` names are precise and correct (all three match). The imprecision is\n  in the *prose* of `K` (factor count) and the *proof prose* (mischaracterized obstruction), not the hints.\n- **Generality**: matches need — `assocCommonForm`/the half-lemmas are at the right generality;\n  no parallel API was forced. The only \"parallel\" object is `assocCommonForm` itself, which exists\n  because the blueprint never gave `K` a precise (5-factor) definition.\n- **Recommended chapter-side actions** (for a blueprint-writing subagent):\n  1. Add a definition/`\\lean{}` block for `K` = `assocCommonForm`, stating the **5-factor** form with\n     the leading inverse seg-1 whiskered unit `(L'(η_{a⊗b} ▷ c))⁻¹` and domain\n     `tensorObj (tensorObj A B) C`, replacing the 4-factor schematic at L1502-1505.\n  2. Correct the `lem:tensorObjAssoc_hK_lhs` proof prose (L1552-1556): drop \"whnf-at-scale artifact /\n     cheap `show`/`change` pin\"; state the real obstruction (a `Localization.Monoidal`-`comp` vs\n     `X.Modules`-synonym-`comp` defeq-but-not-syntactic mismatch on the adjacent `μ.hom ≫ μ.inv`),\n     and point at the instance-agnostic `e.hom ≫ e.inv` cancellation / `associator_hom_app` native-μ\n     route as the intended fix.\n  3. Optional minor: note that the two half-lemmas are `private` (see below).\n\n### `private` decls referenced by `\\lean{}` (minor)\n`tensorObjAssoc_eq_localizedAssociator_hK_lhs`/`_hK_rhs` are `private`. The chapter references them by\nfully-qualified name. If `sync_leanok` / blueprint-doctor resolves `\\lean{}` targets through the\npublic environment, a `private` decl may not be found (risking a spurious unmarked/orphan signal).\nNot a correctness defect in the Lean; flag for marker-sync robustness only.\n\n## Severity summary\n\n- **must-fix-this-iter**: none. Statements are faithful; sorries are honestly reflected (statement\n  `\\leanok`, proof unmarked); no fake/weakened defs, excuse-comments, or unauthorized axioms.\n- **major**:\n  - Chapter `K` (L1502-1505) omits the seg-1 unit prefix → `assocCommonForm` has no faithful\n    blueprint block (4-factor prose vs 5-factor Lean; prose `K` is type-incorrect as the meeting morphism).\n  - `lem:tensorObjAssoc_hK_lhs` proof prose mischaracterizes the residual μ-cancel obstruction as a\n    whnf-at-scale artifact dissolved by isolation, contradicting the prover's verified-in-isolation\n    finding — misleading guidance on the file's actual blocker.\n- **minor**: `private` half-lemmas referenced by `\\lean{}` (marker-sync robustness).\n\n**Overall verdict**: The Lean faithfully follows the blueprint (names, signatures, and assembly all\nmatch; sorries are honest WIP), but the chapter has two `major` blueprint-adequacy gaps — an\nimprecise 4-factor `K` that should be promoted to the 5-factor `assocCommonForm` block, and a\n`hK_lhs` proof sketch that mis-describes the real `Localization.Monoidal`-vs-synonym `comp`-instance\nμ-cancel obstruction — both of which a blueprint-writing subagent should land.\n"
  },
  {
    "name": "mathlib-analogist-snap-localized-comp-cancel.md",
    "content": "# Mathlib Analogist: snap-localized-comp-cancel\n**Mode:** api-alignment | **Iter:** 013\n\n## Answer (VALIDATED on the real `hK_rhs` goal, cold LSP — closes the cancel, 0 errors)\nThe cancel idiom is **`erw` (up-to-defeq), NOT `show`/`change`, NOT a new lemma**:\n```lean\nerw [Category.assoc]                        -- cross the boundary ONCE: pull leading μ.inv out (clean)\nrefine congrArg (fun t => _ ≫ t) ?_         -- peel the common leading μ.inv  (SHIELD — mandatory)\nerw [Category.assoc, Iso.hom_inv_id_assoc]  -- reassoc across boundary + cancel μ.hom ≫ μ.inv\n```\nLeaves EXACTLY the directive's residual: `(A♭◁η^#) ≫ (c_A⊗c_{B⊗C}) ≫ A◁(μ_{B,C}.inv ≫ (c_B⊗c_C))\n= (A♭◁μ_{B,C}.inv) ≫ (c_A⊗(c_B⊗c_C))` → close with `μ_natural_right` + counit triangle `L'η ≫ c = 𝟙`.\n\n## Why (root cause, verified)\n- `LocalizedMonoidal L W ε := D` with `Category := inferInstanceAs (Category D)`\n  (`…/Localization/Monoidal/Basic.lean:86,94`). Project's `D` = sheafification codomain is defeq-but-not-\n  syntactic to `X.Modules`. So localized comp = `@CategoryStruct.comp (LocalizedMonoidal…) (inferInstanceAs…)`\n  vs boundary comp = `@CategoryStruct.comp X.Modules instCategory`: **distinct instance terms, defeq only**.\n- Mathlib NEVER mixes them — all its coherences (`pentagon :329`, `triangle :412`, `associator_naturality\n  :286`) run INSIDE `LocalizedMonoidal` (one instance) and cancel μ-pairs with plain `simp`/`Iso.*_assoc`.\n  The project's boundary is self-inflicted: the keystone `sheafification_whiskerLeft_unit_eq_mu'` (`:1800`,\n  localized comps) is `erw`'d into an `X.Modules`-comp context (`tensorObjAssoc` + `sheafification.map` +\n  the `μ.inv` from `tensorObjLocalizedIso :1462`).\n- TWO stacked mismatches at the pair: comp-instance + μ object-arg (`B.tensorObj C` vs\n  `sheafification.obj(B♭⊗C♭)`). `erw` absorbs BOTH up to reducible defeq.\n\n## Verified-DEAD (so blueprint must not prescribe them)\n`rw [Category.assoc]` (pattern not found — instances differ), `simp only [Category.assoc]` (no progress),\n`dsimp only []` (no progress), `conv_lhs => rw [Category.assoc]` (no progress), targeted `show …inv ≫ _`\n(shape-fails), `rw [show obj(B.tensorObj C)=obj(sheafification.obj…) from rfl]` (motive-not-type-correct),\n`erw [Iso.hom_inv_id_assoc]`/`erw[Category.assoc,Iso.hom_inv_id_assoc]` w/o the peel (adjacency not reached).\n\n## HAZARD (must surface in the recipe)\nAn **unshielded** 2nd `erw [Category.assoc]` whnf-unfolds the leading `μ.inv` → `Functor.curry.mapIso\n(Localization.fac …)` (the isDefEq blow-up the directive feared from `simp [Monoidal.μ]`). The `congrArg`\nleading-`μ.inv` peel (the proof's own idiom, `:1919-1920`) is **mandatory** to prevent it.\n\n## Verdicts\n- **cancel idiom**: PROCEED with the `erw`+peel recipe (validated). Prover's `show`/`change` candidate\n  REFUTED (full `show` impractical; targeted `show` shape-fails; `change` forces a giant term + μ-whnf risk).\n  No dedicated `Localization.Monoidal` cancel lemma needed.\n- **`hK_lhs`**: same cancel applies after `associator_naturality`+`associator_hom_app` expose the μ-pairs.\n- **strategic (flagged)**: DIVERGE_INTENTIONALLY now; Mathlib-aligned fix is option (c) — keep the bridge in\n  ONE category (`modulesLocalizedMonoidal X`) so plain `simp` closes all 5 coherences. Revisit if `erw` cost\n  compounds. Not a this-iter must-fix.\n\n## Persistent file\n- `analogies/snap-localized-comp-cancel.md` written.\n\nOverall verdict: cancel SOLVED — `erw`+`congrArg`-peel crosses the defeq comp boundary and the μ object-arg\ndefeq in one shot; the `show`/`change` candidate is refuted and no new lemma is required.\n"
  },
  {
    "name": "progress-critic-pc013.md",
    "content": "# Progress Critic: pc013\n**Iter:** 013\n\n## Routes\n\n### Route A — `Cohomology/FlatBaseChange.lean`\n**Verdict: CHURNING**\n\n- Sorry trajectory (iter-008→012): 4 → 4 → 5 → 5 → 5. Net **+1** (trending UP). Not merely \"unchanged\" — the sorry count regressed after iter-009.\n- Helpers: +3 (i009), +2 blueprinted (i010), +3 (i011), +4 rfl-wrappers (i012) = **12 helpers over 5 iters**; residual unchanged (one cast `gammaPushforwardIso_comp`).\n- Prover status: PARTIAL × 4 consecutive. → CHURNING by rule (PARTIAL ≥ 3 of K iters).\n- Recurring blockers all ≥ 3 iters: \"whnf kernel bomb / per-junction object-defeq\", \"carrier-rfl exhausted on cold build (>1.6M heartbeats)\". Same wall each iter.\n- Throughput: **Over budget**. Phase entered iter-003; 10 elapsed vs estimate 3–5 (>2×).\n- Primary corrective: **Mathlib-analogy consult** (find the Mathlib-native collapse idiom for value-ModuleCat carrier-identity without whnf; current tactic set provably exhausted per iter-012 report).\n\n### Route B — `Picard/SectionGradedRing.lean`\n**Verdict: STUCK**\n\n- Sorry trajectory (iter-008→012): 5 → 6 → 6 → 6 → 7. Net **+2** (monotonically non-decreasing). Each \"resolved sub-part\" is immediately replaced by two new sorry-stubs → structural decomposition without net closure.\n- Helpers: +4 (i009), 0 (i010), +2 (i011), +3 (i012) = **9 helpers over 5 iters**; μ-cancel residual **not closed in any iter**.\n- Prover status: PARTIAL × 4 consecutive — but the sorry count trend (UP, not flat) and the single named residual persisting since iter-009 push this past CHURNING to STUCK.\n- Recurring blockers all ≥ 3 iters: \"`μ.hom ≫ μ.inv` won't cancel on the isolated goal\", \"`CategoryStruct.comp` instance mismatch (LocalizedMonoidal vs synonym, defeq not syntactic)\", \"`Category.assoc` no-progress; `simp [Localization.Monoidal.μ]` isDefEq timeout\". STUCK by rule (recurring blocker ≥ 3 iters + helpers added without μ-cancel elimination across all K iters).\n- Throughput: **Over budget**. Phase entered iter-005; 8 elapsed vs estimate 3–5 (>1.6×, nearly 2×).\n- Primary corrective: **Mathlib-analogy consult** (instance-agnostic `e.hom ≫ e.inv` cancel across the LocalizedMonoidal/base-comp boundary; no automated tactic has found the right coercion route in 5 iters).\n\n## Dispatch Sanity\n\n- **Verdict: OK (single-iter prover pause; conditioned on iter-014 firing provers).**\n- This iter dispatches mathlib-analogist + blueprint-writer — NOT zero subagents. It is a targeted-infrastructure iter, not an avoidance/plan-only iter.\n- No avoidance pattern triggered yet: only 1 consecutive iter with prover count = 0 (threshold is ≥ 2). But **this is iter 1 of the allowed 1**: if iter-014 does not dispatch provers on both routes after the blueprint gate, the avoidance pattern trips and the plan agent must escalate, not defer again.\n- File count vs cap: within bounds (2 active routes, consulting + rewriting only).\n\n## Must-fix-this-iter\n\n- Route `FlatBaseChange.lean`: CHURNING — Mathlib-analogy consult is mandatory before next prover round.\n- Route `SectionGradedRing.lean`: STUCK — same; μ-cancel idiom must be resolved before prover will advance.\n- **Both routes over budget on STRATEGY.md estimate.** Planner should update `Iters left` estimates to reflect reality before iter-014 dispatches.\n\n## Proposed-corrective Assessment\n\n**CONFIRM** \"mathlib-analogist consult + blueprint rewrite, NO prover this iter\" is correct for both routes.\n\nReasoning:\n- Route A: prover has provably exhausted known tactic families (term-mode Eq.trans, monolithic rfl, whole-goal defeq all >1.6M heartbeats on cold build). Another prover round without the correct Mathlib idiom repeats the same failure. Consult is the minimal unblocking step.\n- Route B: the μ-cancel blocker is an instance-level gap (LocalizedMonoidal comp ≠ synonym comp, non-syntactic defeq). Five prover iters have not bridged this. A Mathlib-idiom consult that surfaces the correct coercion/cancellation lemma is the right minimal step before more prover work.\n- Blueprint-rewrite after consult is also correct: both routes need the proof sketch updated with the discovered idiom before a prover can apply it reliably.\n\n**Challenge:** The plan must ensure the blueprint-reviewer gate is real, not advisory. If the blueprint-writer produces a revised sketch that still contains `sorry`-level gaps on the critical seam, iter-014 provers will PARTIAL again. The gate should be: blueprint-reviewer signs off that the μ-cancel / carrier-collapse seam has a concrete Mathlib lemma citation, not just a prose sketch.\n\n## Overall\n\n- 1 CHURNING (FBC, corrective: Mathlib-analogy consult), 1 STUCK (SNAP, same corrective). Proposed iter-013 corrective confirmed correct. Provers must fire iter-014 — no further deferral permitted.\n"
  }
]
