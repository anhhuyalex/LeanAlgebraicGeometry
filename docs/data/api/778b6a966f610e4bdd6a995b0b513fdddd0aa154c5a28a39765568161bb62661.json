{
  "content": "# Session 16 (iter-016) — review summary\n\n## Metadata\n- File: `AlgebraicJacobian/RiemannRoch/OcOfD.lean` (single lane).\n- Decl-level sorry: **11 → 6** (file). 5 closed + 1 helper added.\n- All 5 closed decls **axiom-clean** `{propext, Classical.choice, Quot.sound}` (verified via `lean_verify` on `carrierSheaf_stalk_eq`, `carrierSheaf_stalk_iso_iSup`, `exists_open_mem_carrierSubmoduleSheaf`).\n- Build green; only warning = `open scoped Classical` lint (pre-existing).\n\n## Headline — the entire carrier-stalk chain closed (binding leaf dissolved)\niter-015 isolated the S3 deep work in the binding leaf `carrierSheaf_stalk_eq` (feared 200–400 LOC, no Mathlib carrier-stalk API). iter-016 closed it AND both seams it factors through, all axiom-clean:\n\n- **B0 `finite_orderConstraintFail_affine`** — DIVERGED from blueprint. The blueprint routes via the affine `finite_order_support_affine`, but that lemma is `private` in `WeilDivisor.lean` (cross-file inaccessible). Prover pivoted to GLOBAL finiteness of the principal-divisor support `Scheme.WeilDivisor.principal g hg` (a `Finsupp`) on the proper curve — strictly stronger, avoids the bridge. Cost: un-omitted `[IsProper C.hom]`. Name + `\\lean` pin unchanged.\n- **B1 `exists_open_mem_carrierSubmoduleSheaf`** — `g=0→U=⊤`; else affine `W∋P.point`, bad set `B` finite (B0), each prime-divisor point closed (`isClosed_singleton_of_coheight_eq_one` + `krullDim_curve_le_one`), `Z=⋃_{Q∈B}{Q.point}` closed, `U:=W⊓Zᶜ`. Gotcha: `TopologicalSpace.Opens.not_mem_bot` absent → membership-rewrite `have hbot' : (W⊓Zc)=⊥ := hbot; rw[hbot']`.\n- **B `iSup_carrierSubmoduleSheaf_eq_orderAtPSubmodule`** — `le_antisymm`; `≤` via `iSup_le` pointwise at `Q=P`; `≥` via B1 + `Submodule.mem_iSup_of_mem`.\n- **Seam A `carrierSheaf_stalk_iso_iSup`** (the \"IF BUDGET REMAINS\" stretch) — germ-API bijective descent per `analogies/stalk-colimit.md`: `φ := colimit.desc` of the descent cocone (legs = `Submodule.inclusion`), cocone naturality reduced to new helper `carrierPresheaf_map_coe`; injective via `injective_iff_map_eq_zero`, surjective via `Submodule.mem_iSup_of_directed` (family directed through `U⊓V`); `ConcreteCategory.isIso_iff_bijective` + `asIso`.\n- **`carrierSheaf_stalk_eq`** (binding leaf) — `carrierSheaf_stalk_iso_iSup ≪≫ eqToIso (by rw [B])`. Two lines once A+B existed.\n- **New helper `carrierPresheaf_map_coe`** — `(((carrierPresheaf D).map g).hom x).1 = x.1` away from `⊥`. NO blueprint entry (coverage debt).\n\n## Still open (6 decls / 7 sorry-bodies)\n- `cokernel_stalk_at_iso_kbar` (G2), `cokernel_skyscraper_hom` (+`_isIso`) (G3) — now UNBLOCKED by closed `carrierSheaf_stalk_eq`, planner-deferred this iter (mult-by-`π^{n+1}` automorphism + residue=k̄).\n- `carrierSheaf_zero_iso_toModuleKSheaf` (bridge) — deep, NOT attempted. Hartshorne II.6.3A; Mathlib gap pinned: `IsDedekindDomain.HeightOneSpectrum.mem_integers_of_valuation_le_one`. Needs Dedekind-per-chart + valuation=order + gluing (~150–250 LOC, dedicated lane).\n- `sheafOf_ses_single_add` corners ×2 — gated on the bridge.\n- `sheafOf_singlePoint` (L771) — off-cone (OCofP private internals).\n\n## Review subagents (both 0 must-fix)\n- **lean-auditor/iter016** (`logs/iter-016/lean-auditor-iter016-report.md`): 0/6/3. All 6 majors = stale docstrings/headers claiming STUBBED/Tier-3-sorry for now-closed `sheafOf`/`sheafOf_zero`/`carrierSheaf_stalk_eq`. `asIso`/`colimit.desc` assembly sound + non-circular; `omit … in` scopes correct (no vacuating drops); `eqToIso (by rw)` honest. Minor: `hW : IsAffineOpen W` is a dead param in B0. No laundering, no axioms.\n- **lean-vs-blueprint-checker/ocofd** (`logs/iter-016/lean-vs-blueprint-checker-ocofd-report.md`): 0 must-fix, 1 major + 4 minor. Confirms 5 chain decls axiom-clean, every `\\lean` pin resolves. MAJOR = B0 divergence (blueprint still sketches affine route + spurious `\\uses lem:finite_order_support_affine` edges) → I added a `% NOTE:` (below); prose rewrite + `\\uses` removal routed to planner. Minors: `carrierPresheaf_map_coe` coverage debt; `carrierSheaf_stalk_eq_fractionalIdeal` chapter promises the explicit `π_P^{-n}𝒪` form (Lean delivers `≅ orderAtPSubmodule`, identification prose-only); nonzero-`g` hypothesis chapter states but Lean drops; over-elaborate injectivity sketch in seam-A. Seam-A colimit prose judged adequate.\n\n## Blueprint markers updated (manual)\n- `RiemannRoch_OcOfD.tex`, `lem:finite_orderConstraintFail_affine`: added `% NOTE:` documenting the iter-016 B0 divergence (Lean uses global `principal`-divisor finiteness, not the affine `finite_order_support_affine`; the two `\\uses` edges are now spurious; needs `[IsProper C.hom]`). Routed the prose-rewrite + `\\uses`-removal to the planner.\n- No `\\leanok` overrides (sync iter=16, +5/−0 on the 5 chain decls — correct). No `\\mathlibok` warranted.\n\n## sync_leanok attribution\n`sync_leanok-state.json` iter=16, sha 98374cb, +5/−0 on `RiemannRoch_OcOfD.tex` — exactly the 5 closed chain decls. Verified, no laundering.\n\n## blueprint-doctor\nClean — no orphan chapters, no broken refs, no new axioms.\n\n## Coverage debt (`archon dag-query unmatched`)\n139 unmatched `lean_aux` nodes; 138 are out-of-cone substrate (Cohomology/*, Genus0BaseObjects/* — never owed blueprint per ARCHON_MEMORY). The ONE new in-cone uncovered decl is `sheafOf.carrierPresheaf_map_coe` → listed in recommendations.\n\n## Recommendations (see recommendations.md)\nLead with the G2/G3 cokernel leaves (now unblocked) as the next prover lane; the Hartshorne-6.3A bridge as a dedicated mathlib-build lane; the B0 blueprint prose rewrite; the comment-only OcOfD docstring cleanup; the `carrierPresheaf_map_coe` blueprint entry.\n"
}
