{
  "content": "# Recommendations — next plan iter (iter-016)\n\n## HIGH — sync_leanok tooling defect (recurring, user-side fix)\n- `sync_leanok` has now stripped verified-axiom-clean proof-block `\\leanok` from\n  `Picard_SectionGradedRing.tex` for **2 consecutive iters** (iter-014 removed 22/added 1; iter-015\n  removed 19/added 0). Both times review `lean_verify`-restored ~19–21 genuinely closed markers. The\n  whole module builds green and each restored decl is axiom-clean, so the per-decl strip is a sync defect\n  (likely heartbeat/timeout during the per-decl `lake build`/analysis on this large slow file), NOT a real\n  regression. **Action: this is outside agent control — surfaced in TO_USER for the user to fix the sync\n  (e.g. raise the per-decl build budget for this chapter or skip re-stripping already-`\\leanok` axiom-clean\n  decls).** Until fixed, every review burns effort re-restoring; the DAG/dashboard under-reports SGR\n  between the sync and review phases.\n\n## HIGH — close ★ `tensorObjAssoc_eta_factor_sheaf` (the assoc-chain crux, gates B4/B5)\n- Mechanism is FOUND and the prefix COMPILES. Next prover: keep the current prefix; from the\n  post-`erw [Iso.hom_inv_id_assoc]` goal, apply cancel lemmas one `erw` at a time **in goal order**\n  (inspect with NON-pp.all `lean_goal`), interleaving the big `simp only [hc, …]` when a positional pass\n  makes progress, until only the α-naturality step remains; then `erw [MonoidalCategory.associator_naturality]`\n  + counit `erw [Iso.hom_inv_id_assoc]` pairs + `rfl`.\n- **DO NOT** re-use the reflexive default comp-bridge (`fun f g => rfl` keyed to the ambient/inferInstance\n  head) — it is a no-op. Use the explicit `AlgebraicGeometry.Scheme.Modules.instCategory (X := X)` bridge,\n  placed INSIDE the big `simp only`.\n- **DO NOT** use unbounded `repeat erw` / `repeat (first | erw …)` — it hits the 200000-heartbeat `whnf`\n  timeout. **DO NOT** raise `maxHeartbeats` (project rule).\n- Per the iter-015 plan's hard-stop: if a real push cannot pin the bounded ordered sequence, iter-016\n  escalates to the user (no further non-user variants). Closing ★ auto-cleans B4 and B5 to axiom-clean.\n\n## MEDIUM — land B6 `tensorPowAdd_assoc` (bankable, independent of ★)\n- Diamond-free pentagon induction; base reduced iter-014 (canonical unit-triangle coherence), succ →\n  `MonoidalCategory.pentagon`. Closable regardless of ★ — schedule it in the same lane so progress banks\n  even if ★ stalls. If the succ case hits the comp diamond, reuse the explicit-`instCategory` `hc` +\n  bounded-`erw` recipe (NOT the reflexive bridge).\n\n## MEDIUM — stale `.lean` comments (review can't edit `.lean`; next prover/refactor to fix)\n- `RelativeSpec.lean:701–709` — docstring on `functor` claims `functor`/`structureMorphism` bodies are\n  `sorry`; both are now concrete (`functor := fun 𝒜 => Over.mk (RelativeSpec.structureMorphism 𝒜)`;\n  `structureMorphism := .toBase`). File is axiom-clean throughout. Update/remove the docstring.\n- `SectionGradedRing.lean:3009–3013` — comment says the blueprint claims \"Mac Lane coherence … discharges\n  the hexagon\" for `lem:tensorPowAdd_comm` and is wrong. The blueprint proof (tex 1342–1368) has SINCE\n  been corrected (it now states Mac Lane coherence does NOT close the step + requires invertibility), so\n  the comment's reference is stale. Tighten the comment to drop the obsolete blueprint reference.\n\n## Blocked — DO NOT re-assign\n- comm chain (`tensorBraiding_self_eq_id_of_isInvertible` 2960, `tensorPowAdd_comm` 2975,\n  `sectionsMul_mul_comm` 3017): invertibility-gated FUTURE, NO consumer (`GCommSemiring` unbuilt). Signature\n  correctly `[IsInvertible L]`. Not in the active cone — do not dispatch.\n- χ leg (`QuotScheme.lean` 123/161/198/225: `hilbertPolynomial`, `QuotFunctor`, `Grassmannian`,\n  `Grassmannian.representable`): χ-semantic, DEFERRED to the cohomology engine at merge. Honest scaffolds.\n  Do not re-probe.\n\n## Reusable proof patterns discovered\n- **Comp-instance-diamond bridge (CORRECTED).** When a goal mixes `X.Modules`-native `≫` with\n  `LocalizedMonoidal`-comp factors: `have hc : @CategoryStruct.comp X.Modules\n  (AlgebraicGeometry.Scheme.Modules.instCategory (X := X)).toCategoryStruct … = @CategoryStruct.comp\n  (LocalizedMonoidal …) _ … := fun f g => rfl`, **inside** the big `simp only` (NOT a separate prior\n  `simp only [hc]`, NOT keyed to `inferInstance`). Then `Category.assoc`+`tensorObj`+`tensorHom_def`+the\n  whisker/Iso cancel lemmas fire on all non-diamond junctions; cross-diamond pairs cancel under `erw`.\n- Cross-diamond `hom≫inv` cancellation: positional `rw`/`simp [Iso.hom_inv_id_assoc]` FAIL; `erw\n  [Iso.hom_inv_id_assoc]` succeeds (one keyed defeq match). Bounded+ordered only — never `repeat`.\n\n## Subagent dispatch note\n- Both highly-recommended review subagents dispatched this iter (prover touched SGR.lean). Reports:\n  `.archon/task_results/lean-auditor-iter015.md`, `.archon/task_results/lean-vs-blueprint-checker-iter015.md`.\n"
}
