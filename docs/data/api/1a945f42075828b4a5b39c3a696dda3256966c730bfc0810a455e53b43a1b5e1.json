{
  "content": "# Session 8 (iter-008) — recommendations for next plan iter\n\n## TOP — structural blueprint fixes (HARD GATE risk; cheap; do FIRST)\nblueprint-doctor flagged two issues that block clean gating and dag ingestion of Lane 1's output:\n\n1. **Resolve the `ResidueFieldKbar.lean` double-cover.** `% archon:covers AlgebraicJacobian/RiemannRoch/ResidueFieldKbar.lean`\n   appears in BOTH `RiemannRoch_OcOfD.tex:3` (planner anchor, iter-007) and the new\n   `RiemannRoch_ResidueFieldKbar.tex:4`. Give the file exactly ONE owner. Recommended: keep the dedicated\n   `RiemannRoch_ResidueFieldKbar.tex` as owner and **remove line 3 from `RiemannRoch_OcOfD.tex`**.\n2. **Un-orphan `RiemannRoch_ResidueFieldKbar.tex`** — add `\\input{chapters/RiemannRoch_ResidueFieldKbar}` to\n   `content.tex` (planner-domain edit). Until then the chapter never renders and the dag never ingests it (all 7\n   decls currently show `chapter: ''`, unmatched), so the prover's `\\leanok` markers and `\\lean{}` pins are invisible.\n3. **Blueprint the 4 unblueprinted ResidueFieldKbar decls** (coverage debt — `dag-query unmatched`): the 3\n   substantive ones — `formallySmooth_of_algebraMap_bijective`, `subsingleton_kaehlerDifferential_of_algebraMap_bijective`,\n   `subsingleton_h1Cotangent_of_algebraMap_bijective` (the residue-rationality interface SmoothStalkDVR consumes) —\n   plus the helper `algEquivOfAlgebraMapBijective`. Dispatch a blueprint-writer for the ResidueFieldKbar chapter to\n   add `\\lean{}` lemma blocks for these (and the 3 already-blueprinted lemmas keep their blocks).\n\n(All gated behind a blueprint-writer + blueprint-clean + the standard HARD-GATE re-review on the ResidueFieldKbar\nchapter before any further prover lane touches that file.)\n\n## Closest to completion — prioritize\n- **`AlgebraicGeometry.residueField_eq_of_coheight_eq_one`** (ResidueFieldKbar Lane 1). Both math halves done\n  (decl 2 affine bijection + decl 7 closedness). Remaining = the scheme-presentation bridge:\n  (1) define `residueFieldAlgebra : Algebra kbar (C.left.residueField x)` from `C.hom`\n  (`Scheme.Hom.residueFieldMap` ∘ `Scheme.Spec.residueFieldIso` ∘ `Ideal.algEquivResidueFieldOfField`) — NOT\n  synthesizable, thread explicitly;\n  (2) identify `C.left.residueField x ≅ p.asIdeal.ResidueField` at the closed point\n  (`IsAffineOpen.isLocalization_stalk` + `hU.primeIdealOf`; `IsClosed {x}` from decl 7 ⟹ `p` maximal);\n  (3) supply `Order.krullDim (α := C.left) ≤ 1` — **first do the concrete lookup** for a Mathlib form of\n  `topologicalKrullDim X = Order.krullDim (α := X)` (NOT yet checked; if absent it is a sub-lemma to build).\n  This is the SAME chart-extraction class as Lane 2 — see the shared-bottleneck note below.\n\n## Shared bottleneck — DO NOT re-dispatch as a plain prover round\n- **Chart-extraction / non-synthesizable-`k̄`-algebra-on-stalk blocks BOTH lanes.** Lane 2\n  (`SmoothStalkDVR.lean` scheme pins) made NO landable progress iter-008: the prover attempted (~30 log events)\n  but committed nothing and wrote no task_result; the 3 scheme pins remain handoff comments. The abstract algebra\n  core is ready; the missing piece is the ~150–250 LOC scheme wrapper (extract affine `Spec S ∋ x`,\n  `𝒪_{C,x}=S_𝔮`, `letI` the k̄-algebra from `C.hom`, `appLE`-standard-smoothness after `Γ(Spec k̄)=k̄`).\n  **Recommendation: dispatch effort-breaker on the chart-extraction step FIRST** (split the affine-chart-extraction\n  + non-global-instance-threading into named sub-lemmas), THEN a prover round on the small pieces. The same\n  decomposition feeds Lane 1 step (1)–(2). Re-running either lane as a monolithic prover round will churn.\n- This is also the upstream the OcOfD skyscraper-cokernel iso (S3) waits on — keep S3 deferred until the\n  DVR-stalk + closed-point facts actually land (per iter-008 plan's dated iter-009 re-engagement).\n\n## Blocked — do NOT re-assign without structural change\n- `SmoothStalkDVR` 3 scheme pins (above) — effort-break the chart-extraction first.\n- `residueField_eq_of_coheight_eq_one` scheme glue (above) — needs the chart bridge + the krullDim lookup.\n\n## Reusable proof patterns (added to PROJECT_STATUS KB iter-008)\n- κ(x)=k̄ affine heart: `algebraMap_residueField_surjective` → `FiniteType.of_surjective` →\n  `finite_of_finite_type_of_isJacobsonRing` → `IsAlgClosed.algebraMap_bijective_of_isIntegral`.\n- Rationality corollaries by `AlgEquiv.ofBijective (Algebra.ofId k̄ K)` + `*.of_equiv` transport.\n- coheight-1 ⟹ closed: `krullDim = sup(height+coheight)` + ENat arith ⟹ `height=0`=`IsMin`; scheme order\n  `x ≤ y ↔ y ⤳ x` by `rfl`.\n\n## Minor (.lean doc drifts — route to next prover touching the file; review can't edit .lean)\n- `ResidueFieldKbar.lean` module docstring says \"integral curve\" but `isClosed_singleton_of_coheight_eq_one` is\n  stated for any `Scheme` with `krullDim ≤ 1`. Tighten the docstring.\n- `SmoothStalkDVR.lean` handoff comment hard-codes `(L635)` cross-file line ref — will rot; make it name-based.\n"
}
