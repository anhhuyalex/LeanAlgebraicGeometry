{
  "content": "# Session 20 (iter-020) — Review Summary\n\n## Metadata\n- **Stage:** prover. One opus lane on `AlgebraicJacobian/Picard/TensorObjSubstrate.lean`.\n- **Sorry count (this file): 2 → 2.** The active seed-1 sorry was *eliminated and replaced* by a\n  sharper one. Bare sorries: `exists_tensorObj_inverse` (L734, deferred terminal, untouched) +\n  `pullbackTensorMap_isIso_of_isOpenImmersion` (K1, L4172, NEW — the sole open D4′ brick).\n- **Targets attempted:** seed-1 `pullbackTensorIsoOfLocallyTrivial` (D4′ chart-chase); residual K1.\n- File GREEN (only the two expected sorry warnings). 5 new declarations landed.\n\n## Target: `pullbackTensorIsoOfLocallyTrivial` (D4′, `lem:pullback_tensor_iso_loctriv`) — PARTIAL\nThe scaffolded opaque sorry `haveI : IsIso (pullbackTensorMap f M N) := sorry` (old L4103) was\n**replaced by a complete, sorry-free chart-chase body**. Five decls added (line numbers post-edit):\n- `isIso_of_isIso_comp4_mid` (L4095, generic): middle factor of `a ≫ b ≫ c ≫ d` is iso when the\n  composite + 3 flanking factors are. ⚠ composite-hyp must come FIRST so `a,c,d` pin by unification\n  before instance args resolve (else metavariables → TC fails); final form takes the iso proofs as\n  explicit args (`h ha hc hd`), not instances.\n- `pullbackTensorMap_isIso_of_base_unit` (K2, L4108): `pullbackTensorMap f P Q` iso when `P,Q ≅ 𝒪_X`,\n  via `pullbackTensorMap_natural` (D1′) + `pullbackTensorMap_unit_isIso` (D2′) + `tensorObjIsoOfIso`.\n  Sorry-free, axiom-clean.\n- `chart_isIso` (L4184): the per-chart obligation, two `pullbackTensorMap_restrict` (D3′) splits of\n  `j' ≫ f = g ≫ W.ι`, isolate middle factor by `isIso_of_isIso_comp4_mid`, close by K1 (twice) + K2.\n- `pullbackTensorIsoOfLocallyTrivial` (L4238): cover `Y` by `{f⁻¹W y}` (common trivialising affine\n  charts via `exists_isAffineOpen_mem_and_subset` + `restrictIsoUnitOfLE`), `isIso_of_isIso_restrict`,\n  `chart_isIso` per point, `asIso`. **Body sorry-free; `.hom` defeq to `pullbackTensorMap`.**\n- (K1 below.)\n\nTactic notes captured: nat-iso inverse components `(pullbackComp _ _).inv.app T` are NOT found by\nplain `inferInstance` → use `inferInstanceAs (IsIso (((pullbackComp _ _).app T).inv))` (defeq cast);\n`IsIso (a ≫ b ≫ c ≫ d)` is not an auto instance → chain `IsIso.comp_isIso'`.\n\n**Key finding — the blueprint UNDER-COUNTED.** The blueprint proof (and the overview's claim \"the only\ngenuinely new sub-step is D3′\", ~L2303) asserts the per-chart obligation reduces *directly* to the unit\npair via `lem:pullback_tensor_iso_unit`. It does not: both D3′ base-change factorisations leave a\n`pullbackTensorMap` over an **open immersion** (the chart inclusions `j'`, `W.ι`) with arbitrary\nmodules, forcing a fifth ingredient K1 that the blueprint never names. Confirmed by tos020 (must-fix).\n\n## Target: `pullbackTensorMap_isIso_of_isOpenImmersion` (K1, L4139) — BLOCKED (reversal signal hit)\nAttempt: reduce via `isIso_pullbackTensorMap_of_isIso_sheafifyDelta` to `IsIso (a_Y.map δ)`, then to\n`IsIso δ` at presheaf level, then invoke Mathlib `instance [F.Monoidal] : IsIso (δ F X Y)` by exhibiting\n`(pullback φR).Monoidal` via `Functor.Monoidal.transport` of the strong-monoidal `pushforward β`\n(β = open-immersion structure-ring iso; H1 = `leftAdjointUniq : pushforward β ≅ pullback φR`).\n**Failed to elaborate on TWO Mathlib-absent instance diamonds:**\n1. `MonoidalCategory (PresheafOfModules X.ringCatSheaf.obj)` not globally synthesizable (keyed on the\n   syntactic `X.presheaf ⋙ forget₂` carrier, only defeq) — `transport` needs the functor-level\n   `(pushforward β).Monoidal` instance which mentions that non-synthesizable carrier.\n2. Goal `δ` uses `presheafPullbackOplaxMonoidal` (adjunction mate); `transport`'s\n   `Monoidal.toOplaxMonoidal.δ` (via `coreMonoidalTransport`) is not defeq → no `transport_δ` bridge.\n\nThis is **exactly the iter-020 pre-authorized reversal signal** (\"an instance class the generic-lemma\n`exact` device does NOT cross → STOP + report; that class → mathlib-analogist next iter\") and matches\nthe ARCHON_MEMORY top bullet (top-level monoidal-carried extraction FAILS — keep reductions IN-PROOF).\nThe math is settled (open-immersion pullback is strong monoidal, used object-level in the CLOSED\n`tensorObj_restrict_iso`); what is missing is a Mathlib-scale *functor-level* strong-monoidal pullback\nmodel. Informal agent correctly NOT consulted (obstacle is a Lean instance-carrier diamond, not a\nmissing argument). Typed sorry retained; ingredient written to\n`informal/pullbackTensorMap_isIso_of_isOpenImmersion.md`.\n\n## Reviewer audits\n- **lean-auditor (aud020):** 0 must-fix / 2 major / 2 minor. Axiom-checked: `pullbackTensorIsoOf...`\n  and K1 carry `sorryAx` (K1 direct, the iso transitive via `chart_isIso`); `isIso_of_isIso_comp4_mid`\n  + K2 axiom-clean; all 5 helpers non-vacuous + used; body NOT laundered (proper `by`-block chart-chase,\n  not bare sorry). 2 MAJOR = stale .lean comments (L46 header says `~L4001`/\"prover closes next via\n  chart-chase\" — decl at L4238, chart-chase already the body; L4014 \"SORRY STRUCTURE CHOSEN: haveI :=\n  sorry\" — body uses full `by`-block). 2 MINOR = L44 line drift, L158-159 layout omits 4 new helpers.\n  Report: `task_results/lean-auditor-aud020.md`.\n- **lean-vs-blueprint (tos020):** 1 must-fix / 1 major / 1 minor. MUST-FIX = blueprint under-counts D4′:\n  K1 missing from both `\\uses{}` and any node; \"only D3′ is new\" claim wrong. MAJOR = the pinned iso's\n  proof not closed (transitive sorry via K1) — genuine open math, not fake. MINOR = stale `% NOTE:`\n  L3666-3669. Signature + `\\lean{}` pin + statement `\\leanok` all now well-formed.\n  Report: `task_results/lean-vs-blueprint-checker-tos020.md`.\n\n## Blueprint markers updated (manual)\n- `Picard_TensorObjSubstrate.tex`, `lem:pullback_tensor_iso_loctriv`: **rewrote the stale `% NOTE:`**\n  at L3666-3669 (it claimed the `\\lean{}` pin target was absent / \"iter-271 forward reference\" — the\n  decl landed iter-020 at L4238 and the pin now resolves). New NOTE records that the proof is NOT yet\n  closed (open via K1), that K1 is omitted from the prose/`\\uses{}`, and the planner TODO to add a K1\n  node. No `\\leanok` override: statement `\\leanok` (L3662, decl formalized w/ transitive sorry) and the\n  proof block carrying NO `\\leanok` (open sorry) are both deterministically correct.\n\n## Recurring-pattern note\n7th consecutive \"this file: sorry 2→2\" iter, but this one is a genuine PIVOT (D3′ DONE iter-019;\nthis iter built the entire D4′ chart-chase, eliminating the opaque seed-1 sorry and replacing it with\nthe crisp, well-scoped K1). Not helper-churn on the same wall: 4 of 5 new decls fully closed, residual\nis a single named Mathlib-scale brick. K1 is the genuine frontier — see recommendations.\n"
}
