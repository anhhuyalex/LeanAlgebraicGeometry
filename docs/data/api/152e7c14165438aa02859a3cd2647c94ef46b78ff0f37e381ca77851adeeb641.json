{
  "content": "# Recommendations for the next plan-agent iteration (iter-050)\n\n## Universe `u → u+1` HModule transport closed iter-049\n\n**Top-supremum HModule transport bridge is now in place.** Iter-049 landed two new declarations in `Cohomology/MayerVietoris.lean` `section CoverTotality` (single combined Edit, zero corrective): an abstract sheaf-form transport `subsingleton_HModule_of_isCechAcyclicCover_top` chaining iter-048's `subsingleton_HModule'_supr_of_isCechAcyclicCover` consumer with iter-034's `HModule'_eq_HModule_linearEquiv` universe bridge, and the curve specialisation `_curve` at `F := Scheme.toModuleKSheaf C`. Sorry trajectory `9 → 9`. LOC `1024 → 1077 (+53, vs +30–50 plan estimate band)`. Kernel-only axioms `[propext, Classical.choice, Quot.sound]` on both declarations.\n\n**Substantive depth still ahead.** The `IsAffineHModuleVanishing k C (toModuleKSheaf C)` producer chain now stands at: iter-047 (foundational Čech infra; closed) → iter-048 (Čech-side acyclicity carrier + consumer; closed) → **iter-049 (top-supremum HModule transport bridge; closed this pass)** → iter-050 (Čech-vs-derived comparison theorem) → iter-051+ (Čech-side acyclicity *producer* instances for affine basic-open covers — substantive multi-iteration) → iter-052 (final `IsAffineHModuleVanishing` producer chaining iter-051+ via iter-050's comparison through iter-048/049's consumers into iter-040's class field). Phase A iterations-remaining held at ~5 (no change from iter-048).\n\n## Highest-priority track for iter-050\n\n### Track 2A.2 (recommended primary, iter-050 prover lane): Čech-vs-derived comparison theorem\n\n**Step 4.6.3 of the genus-`Module.Finite` ladder.** Construct, as a *theorem* (not a class field), the comparison\n```\nScheme.cechCohomologyToHModule'_LinearEquiv :\n  ∀ (n : ℕ), Scheme.cechCohomology C F 𝒰 n ≃ₗ[k]\n                Scheme.HModule' k F n (⨆ i, 𝒰 i)\n```\nunder appropriate hypotheses (acyclicity of the cover, sheaf condition on `F`). This is the data argument **both** iter-048's and iter-049's consumers accept as the explicit `compIso` parameter. Iter-048/049's design (compIso as explicit argument, not class field) means iter-050 can land on its own without re-touching iter-048 or iter-049.\n\n**Plan-agent probe mandate for iter-050**:\n1. Search Mathlib for `Cech.toCohomology` / `cechToDerived` / `Cech.isIsoCohomology` / similar — confirm not present at the iter-047/048/049 level of generality (over arbitrary `Sheaf J (ModuleCat k)`).\n2. Construction strategy options:\n   - **(i)** Build via the standard augmentation `F → Č(𝒰, F)` cochain, then take cohomology and identify the result with `HModule'` via the standard derived-functor / Čech-cohomology comparison (the spectral-sequence-collapses-on-acyclic-covers argument).\n   - **(ii)** Direct natural-transformation construction via Mathlib's `cechComplexFunctor` augmentation API + the iter-026 short-exact / Mayer–Vietoris machinery for the `n+1`-th step.\n   - **(iii)** Project-local axiomatisation as a class `class HasCechComparison` with field of type `cechCohomology … ≃ₗ[k] HModule' …`, deferring the proof to iter-050b. (Last resort if (i)/(ii) prove multi-iteration.)\n3. Probe the chosen body via `lean_run_code` end-to-end before proposing.\n\n**Plausible iter-050 outcomes**:\n- **Single-iteration close (~50–100 LOC)** if the augmentation-and-take-homology approach drops in cleanly via existing Mathlib `cechComplexFunctor` API.\n- **2-iteration close (~150–250 LOC)** if a project-local naturality lemma or short-exact bridge is needed.\n- **3+-iteration close** if the comparison must be built from manual derived-functor unfolding.\n\n### Track 2A.3 (queued, iter-051+): `IsCechAcyclicCover` producers for affine basic-open covers\n\nSubstantive multi-iteration: produce `IsCechAcyclicCover` instances for basic-open covers of affine schemes by way of Serre's classical computation (the alternating-sum complex of localisations is exact in positive degrees). Mathlib probe required for any pre-existing `Cech.cohomology_basic_open_cover_acyclic`-style theorem; if absent, project-local construction. Likely ~150+ LOC over 2–3 iterations.\n\n### Track 2A.4 (queued, iter-052): the final `IsAffineHModuleVanishing` producer\n\nChain Track 2A.3 with iter-050's comparison theorem and iter-049's curve consumer (`subsingleton_HModule_of_isCechAcyclicCover_top_curve`) to close the H${}^{>0}$ side of the genus-`Module.Finite` ladder.\n\n## Track 2B (alternative warm-up, iter-050 prover lane): finrank corollary of iter-046 producer\n\nA `Module.finrank`-flavoured restatement of iter-046's `instIsHModuleHomFinite_toModuleKSheaf` giving an explicit identity `Module.finrank k (HModule k (toModuleKSheaf C) 0) = Module.finrank k Γ(C, 𝒪_C)`. Plausibly single-iteration ~30–50 LOC. Remains a low-risk warm-up option if iter-050 plan-agent prefers a smaller-surface deliverable while waiting on iter-051 prep work.\n\n## Track 2C (alternative, iter-050 prover lane): sharper Mayer–Vietoris LES consumer\n\nCombine iter-029 LES + iter-035 → iter-039 transports + iter-040 + iter-041 + iter-042 + iter-043 consumers + iter-044 Stein input + iter-045 LinearEquiv + iter-046 producer instance into a four-term LES on `HModule k F (n+1)` for the curve case. Plausibly single-iteration ~30–50 LOC.\n\n## Track 2D (off-Archon side track): Mathlib upstream PRs\n\nThe five new `CategoryTheory.*` declarations from iter-046 (`Functor.const_additive`, `Functor.const_linear`, `Adjunction.left_adjoint_linear`, `Adjunction.right_adjoint_linear`, `Adjunction.homLinearEquiv`) remain pure Mathlib gap-fills. Recommendation unchanged: open Mathlib upstream PRs to merge into `Mathlib/CategoryTheory/Adjunction/Additive.lean` or sibling `Linear.lean`. Once merged, the project-local block can be deleted.\n\n## Continuing-discipline reinforcements (iter-040 → iter-049)\n\n- **`blueprint/lean_decls` clear-as-you-go discipline holds for the tenth iteration in a row** (iter-040 → iter-049). The iter-049 plan-agent appended 2 new entries (L93–L94: `subsingleton_HModule_of_isCechAcyclicCover_top` + `_curve`), bringing the total in scope for the project to 115 entries. Continue this discipline.\n- **Plan-agent pre-marking blueprint `\\leanok` markers, with review-side validation** holds for the fifth iteration in a row (iter-045 → iter-046 → iter-047 → iter-048 → iter-049). Iter-049 pre-marked 4 markers (2 statement, 2 proof). All 4 verified valid this pass via clean diagnostics + kernel-only axioms on both declarations. Reduces review-agent surface area to validation only. Continue.\n- **Verbatim probe-confirmed body landing** holds: 12 of 15 iterations (iter-035 → iter-049) zero-corrective-Edit, 1 with cosmetic corrective (iter-044), 1 with cosmetic corrective (iter-046). The pattern continues to deliver clean, atomic Edits. **Iter-049 = third zero-corrective Edit in a row** (iter-047 → iter-048 → iter-049).\n- **`archon-protected.yaml` untouched** for the tenth iteration in a row. Continue.\n- **Single combined Edit for multi-declaration packaging holds** *(iter-049 confirms scaling on a homogeneous abstract+`_curve` cohort in `MayerVietoris.lean`)*.\n- **Cross-file consumer chain landing in a single iteration** *(iter-049, new this iteration)*: iter-049's body chains iter-048 (in `StructureSheafModuleK.lean`) with iter-034 (in `MayerVietoris.lean`) — a cross-file consumer chain landing in `MayerVietoris.lean` itself. Confirms the namespace organisation supports this without import gymnastics; the iter-048 consumer is reachable because `MayerVietoris.lean` transitively imports `StructureSheafModuleK.lean`.\n- **LOC overage from multi-paragraph docstrings** *(iter-047: +54; iter-048: +66; iter-049: +53 — all vs +30–50 plan estimate band)*: docstrings copied verbatim from PROGRESS.md tend to push LOC slightly above the plan-agent's estimate band. Tolerable — docstrings are load-bearing for design rationale (e.g., the iter-049 universe `u → u+1` rationale is documented in the abstract form's docstring). Plan-agent may consider widening the LOC band for docstring-heavy iterations to `+50–80` going forward.\n\n## Targets blocked — do NOT assign in iter-050\n\n- All 8 protected sorries (5 in `Jacobian.lean`, 3 in `AbelJacobi.lean`) — gated on Phase C step 4 (FGA representability), `LineBundle` Mathlib refinement, and `noncomputable` user-decisions. **Plan-agent should NOT assign these.**\n- `PicardFunctor.representable` — intentionally deferred. **Plan-agent should NOT assign.**\n- All closed scaffold sites in `Cohomology/MayerVietoris.lean` and `Cohomology/StructureSheafModuleK.lean` (iter-006 → iter-049).\n\n## Reusable proof patterns from iter-049 (for iter-050+ planning)\n\n1. **Universe `u → u+1` HModule transport via `h ▸ this` rewrite + `.symm.toEquiv.subsingleton`** — three-step chain: extract iter-048 at `⨆ 𝒰`, rewrite via `h : ⨆ 𝒰 = ⊤` to land on `⊤`, transport along iter-034's universal `LinearEquiv` via `.symm.toEquiv.subsingleton`. Reusable for any downstream chain that needs to transport `Subsingleton`-flavoured cohomology vanishing across the `HModule'`/`HModule` universe boundary under a cover-totality hypothesis.\n2. **Named-argument syntax `(𝒰 := 𝒰)` for cover lock-in at the `_curve` call site** — load-bearing in the `_curve` companion of an abstract consumer parameterised by a cover, to keep Lean's elaborator from re-deriving the cover from a typeclass instance argument and accidentally matching the wrong one.\n3. **Cross-file consumer chain landing in a single iteration** — iter-049's body chains iter-048 (`StructureSheafModuleK.lean`) with iter-034 (`MayerVietoris.lean`) — confirms the namespace organisation supports cross-file consumer chains without import gymnastics. Reusable for iter-050+ chains that touch both files.\n4. **`Preorder.isTerminalTop _` as the `IsTerminal ⊤` argument supplier for iter-034's universal bridge** — Mathlib gadget that lifts the lattice's `⊤` to a categorical `IsTerminal ⊤`. Reusable wherever the bridge applies.\n\n## Summary\n\nIter-049 closed the **universe `u → u+1` HModule transport bridge (abstract sheaf form + curve specialisation)**, decoupled cleanly from the substantive iter-050+ Čech-vs-derived comparison theorem and from the iter-051+ Čech-side acyclicity producers via the explicit `compIso` argument design. **Iter-050 should attack Track 2A.2 (comparison theorem)** as the next genuinely substantive step, with Track 2B (finrank corollary) as the low-risk warm-up alternative.\n"
}
