{
  "content": "# Recommendations — next plan iter (iter-015)\n\n## Closest-to-completion / priority\n1. **S3 skyscraper-cokernel iso** `cokernel_carrierSheafHom_iso_skyscraper` (OcOfD L1086) — THE next\n   critical leaf. Now unblocked: the codim-1 DVR-stalk lemmas it can reference are all available\n   (`isDiscreteValuationRing_stalk_of_smooth`, the global regularity instance). Per plan iter-014 +\n   progress-critic: this carries a STUCK sub-signal (3 prior attempts, sorry 0→2→4). **DO NOT send a\n   blind 4th whole-iso `prove`.** Lead with an **effort-breaker** dispatch to split into the already-\n   blueprinted G1/G2/G3 sub-blocks (chapter L1623), THEN prove the smallest piece. The decomposition is\n   blueprint-ready (blueprint-reviewer iter-014 confirmed — no blueprint-writer needed first).\n\n2. **`sheafOf_ses_single_add` corner cases** (OcOfD L1159) — main case CLOSED this iter; the 2 corner\n   sorries (`D=0`, `[P]+D=0`) need a `carrierSheaf 0 ≅ toModuleKSheaf C` bridge (regular functions =\n   order-≥0 on integral scheme, Hartshorne II §6 pre-Prop 6.11). Queue WITH the S3 cokernel work (same\n   file, same iter). Not yet blueprinted as its own lemma — consider a blueprint-writer block for the bridge.\n\n## Stale `.lean` comment cleanup (lean-auditor/iter014 — 5 major, all non-code)\nReview cannot edit `.lean`. Hand a prover/refactor a comment-only cleanup pass on OcOfD.lean (no proof\nchange) for these now-FALSE comments contradicting iter-014's closed work:\n- Module docstring ~L68: \"each of four decls is Tier-3 sorry\" — `sheafOf`+`sheafOf_zero` now closed.\n- `sheafOf` docstring ~L606-633: \"general case still a sorry\" / \"iter-183 Lane K Tier-3 honest typed sorry\" — stale.\n- Body comment ~L661-667: \"residual sorry is cotangent fact; no Mathlib bridge\" — both false (closed iter-011/014).\n- `sheafOf_ses_single_add` comment ~L1192: \"gated on L635 finrank fact\" — gate lifted iter-014.\nLow-priority but accumulating; fold into the iter-015 OcOfD prover's preamble if it touches the file.\n\n## Blocked — do NOT re-assign as-is\n- `sheafOf_singlePoint_iso` (L745): off-cone, blocked on an OCofP-`private` declaration. Needs a structural\n  change (expose the OCofP helper) before it is provable — not a `prove` target.\n\n## Reusable patterns discovered\n- **Substrate-wiring as a global instance**: lifting a codim-1 regularity witness to a top-level\n  `instance` (rather than an inline `haveI ... ⟨…⟩`) makes downstream `haveI := inferInstance` and is\n  reusable by any smooth-curve consumer. Pattern: `⟨fun Y => <stalk-lemma> Y.point Y.coheight⟩`.\n- **Namespace placement matters for blueprint pins**: place a decl in the namespace its blueprint `\\lean{}`\n  pin names (here `AlgebraicGeometry`, NOT the enclosing `Scheme.WeilDivisor` section) or sync_leanok/dag\n  resolution breaks. The prover caught and corrected this mid-session.\n- **Import-direction check before adding an import**: CurveKrullDim→OcOfD was safe because CurveKrullDim\n  imports only SmoothStalkDVR+CoheightBridge (neither imports OcOfD/WeilDivisor) — verify the back-edge is absent.\n\n## Markers / coverage\n- 2 stale blueprint `% NOTE` markers FIXED this pass (see summary). No outstanding marker debt.\n- `unmatched` = 138, all out-of-cone substrate; both new decls covered. No RR-cone 1-to-1 debt to blueprint.\n"
}
