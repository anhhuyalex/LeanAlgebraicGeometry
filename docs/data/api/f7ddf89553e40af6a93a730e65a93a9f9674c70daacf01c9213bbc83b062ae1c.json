{
  "content": "# Recommendations — after iter-049\n\n## TOP PRIORITY — fine-grained / effort-break the shared conjugate telescope\nThe single live obstacle is now one fully-planned, ingredient-complete, mechanical bookkeeping task:\nthe `conjugateEquiv_comp` leg-by-leg telescope inside `restrictFunctorIsoPullback_comp_compat_hom`\n(`hNat`, TensorObjInverse.lean L260). It is the SHARED crux that also discharges the B1 crux\n`H1inv_app_eq_pullbackVal_restrict` (L392).\n\n- **Action:** dispatch the **effort-breaker** on `restrictFunctorIsoPullback_comp_compat_hom` (granularity\n  \"fine — one mathematical claim per lemma\"): split `hNat` into named per-leg `conjugateEquiv_comp`\n  sub-lemmas, **one per G_k boundary** (G₀..G₆). Per-leg targets are already pinned and verified present:\n  - c₂ ↦ `conjugateEquiv_restrictFunctorComp_inv j U.ι` (public root keystone, iter-048)\n  - c₅ ↦ inverse of `conjugateEquiv_pullbackComp_inv j U.ι` (Mathlib)\n  - c₃,c₄ ↦ `unit_leftAdjointUniq_hom_app` at U.ι / j\n  - c₁,c₆ ↦ `pushforwardCongr` / eqToHom\n  Telescope: c₂·c₅ cancel (`pushforwardComp.hom ≫ .inv = 𝟙`); c₁·c₆ cancel against the `j≫U.ι` reindex.\n- **Then** reuse the per-leg routine on the B1 crux (add `leftAdjointUniqUnitEta` root L1531 for the\n  SCPB unit leg).\n- **Do NOT** re-dispatch a plain whole-`hNat` prover round and do NOT reach for `mateEquiv_hcomp`/\n  `mateEquiv_vcomp` — the prover proved leg-by-leg `conjugateEquiv_comp` is the route (fixed (C,D) over\n  `X.Mod ↔ V.Mod`), overturning `b2mate045.md`. The recipe `analogies/b2mate045.md` is stale on this point.\n\n## Blueprint coverage debt (vs-checker MAJOR) — dispatch blueprint-writer this iter\nTwo extracted cruxes — the file's only uncovered decls and its two active-work sorries — have NO\n`\\lean{}` block in `Picard_TensorObjSubstrate.tex` (consolidated chapter, covers TensorObjInverse.lean):\n1. `restrictFunctorIsoPullback_comp_compat_hom` (L216, sorry L260) — B2 `.hom.app A` crux.\n2. `H1inv_app_eq_pullbackVal_restrict` (L336, sorry L392) — B1 crux.\n- **Action:** dispatch the blueprint-writer on `Picard_TensorObjSubstrate.tex` to (a) add a dedicated\n  block for each, with a proof sketch matching the ACTUAL `conjugateEquiv_comp` leg-by-leg route (7\n  intermediate adjunctions, per-leg targets above), wired into B2's / B1's `\\uses`; (b) reconcile B2's\n  proof prose `mateEquiv_vcomp` → `conjugateEquiv_comp` (I left a `% NOTE: (iter-049)` marker at the B2\n  block flagging this). Note: I am the review agent and do not author informal prose; these go to the\n  planner/writer.\n- This is not a Lean-statement blocker (signatures faithful) so it does NOT gate edits to B1/B2\n  themselves — but per the HARD GATE, do the writer round before sending a prover at the cruxes.\n\n## Stale docstrings (auditor MAJOR) — schedule a prover cleanup\n`.lean` docstrings are prover-domain (review cannot edit `.lean`). Have whichever prover next touches\nTensorObjInverse.lean fix, in passing:\n- L417–423: B1 docstring \"NEW PRIVACY BLOCKER (iter-044) / UNBLOCK = de-private\" contradicts body L461\n  \"Both privacy gates CLEARED (iter-045)\" — delete the stale blocker paragraph.\n- L288–297: B2 docstring \"PARTIAL (iter-041) / RESIDUAL\" — body is now sorry-free; relabel as\n  \"complete mod helper `restrictFunctorIsoPullback_comp_compat_hom`\".\n- Minor: L409 stale `STATUS (iter-044)`, L584 confusing \"RESIDUAL\" on a closed sub-goal.\n\n## Closest-to-completion ordering\n1. `restrictFunctorIsoPullback_comp_compat_hom` (`hNat`) — fully planned, mechanical → unblocks B2 fully + the routine.\n2. `H1inv_app_eq_pullbackVal_restrict` (B1 crux) — same routine + `leftAdjointUniqUnitEta`.\n3. S2/S3/S4a/S4b + `trivialisation_restrict_compat` — immersion-naturality, gated behind 1&2.\n\n## Do NOT retry\n- Whole-`hNat` plain prover round (mechanical bookkeeping too large for one pass — break it).\n- `congrArg (pushforward _).map _` to cross the carrier diamond (recursion-depth blowup; use\n  `rw [helper, Functor.map_comp ×N] + rfl`).\n- `mateEquiv_hcomp`/`mateEquiv_vcomp` for the telescope (proven unnecessary; leg-by-leg `conjugateEquiv_comp`).\n\n## Coverage debt for the DAG (Step 6) — for the planner to blueprint\n`archon dag-query unmatched` (107 total `lean_aux`, mostly historical private plumbing). The one NEW,\nactive-work uncovered node this iter:\n- `AlgebraicGeometry.Scheme.Modules.restrictFunctorIsoPullback_comp_compat_hom`\n  (TensorObjInverse.lean L216) — depends on `Adjunction.conjugateEquiv`, `conjugateEquiv_comp`,\n  `conjugateEquiv_restrictFunctorComp_inv`, `conjugateEquiv_pullbackComp_inv`, `unit_leftAdjointUniq_hom_app`,\n  `pushforwardComp`/`pushforwardCongr`. (Also still uncovered: `H1inv_app_eq_pullbackVal_restrict`.)\n  Both are addressed by the blueprint-writer action above.\n"
}
