{
  "content": "# Recommendations — after iter-013\n\n## Closest to completion (prioritize)\n1. **★ `tensorObjAssoc_eta_factor_sheaf`** (SGR ~L2458) — the single live crux of the whole assoc chain. Closing it auto-cleans B4 (`tensorObjAssoc_eta_factor`) AND B5 (`tensorObjAssoc_hom_sectionsMul`) to axiom-clean. Route is FULLY documented in the sorry body and NOT blocked by any missing ingredient:\n   - Unfold `tensorObjAssoc` to its 5-segment `tensorObjIso`/`α_` composite.\n   - Rewrite the 3 sheafified-presheaf maps: `L(η_P▷Q)` via `Localization.Monoidal.μ_natural_left`; `L(P'◁η_Q)` via `μ_natural_right`; `L(α^p)` via `associator_hom_app`; with `L(η_P) = ε_{LP}⁻¹` (`sheafification_map_unit_eq`, proven this iter).\n   - Cancel adjacent `μ`/`tensorObjIso` pairs (full-defeq `simp only` recipe of `tensorPowAdd_zero_right`), leaving canonical `α_` (`rfl`).\n   - **MANDATORY technique:** every `μ`-conjugation/cancellation hits the `restrictScalars(𝟙)` decoration at the `μ`-second-index → use `erw`/term-mode at each junction (the exact recipe just proven on the B4 reduction). Plain `rw`/`Category.assoc`/`Functor.map_comp` will FAIL with \"did not find pattern\". Do NOT re-attempt standalone `μ`-bridge helper lemmas (they resisted clean `rw`/`exact` for this reason and were removed) — assemble inline.\n2. **B6 `tensorPowAdd_assoc`** (SGR ~L2768) — INDEPENDENT of ★/B4; can run in the same lane or parallel. Induction on `m` (analogue of `tensorPowAdd_zero_right`), step from `MonoidalCategory.pentagon`.\n\n## Then\n3. **B7 `sectionsMul_mul_assoc`** (SGR ~L2798) — final assoc leg; assemble after ★ + B6 land (needs B5 modulo ★, B6, + a μ-naturality-slide ingredient).\n\n## Do NOT retry\n- The `.val`/presheaf-descent recipe for B4 is SUPERSEDED — do not reconstruct it; the sheaf-level ★ route is strictly cleaner and proven to reduce.\n- comm chain (`tensorBraiding_self_eq_id_of_isInvertible`, `tensorPowAdd_comm`, `sectionsMul_mul_comm`) — invertibility-gated FUTURE, no consumer (`GCommSemiring` unbuilt). NOT a prover lane.\n- χ (`QuotScheme.lean`, 4 sorries) — deferred to the cohomology leg.\n\n## Coverage debt — planner: author blueprint blocks (review cannot write prose)\nTwo NEW Lean decls in `SectionGradedRing.lean` have no `\\label`/`\\lean`/`\\uses` (invisible to leandag):\n- `AlgebraicGeometry.Scheme.Modules.sheafification_map_unit_eq` (~L2440) — proven, axiom-clean. Left-triangle `L.map η_P = ε_{LP}⁻¹`. Uses: `Adjunction.left_triangle_components`, `Iso.comp_hom_eq_id`. (Mathlib-leaf-ish; could carry `\\mathlibok`-anchored uses.)\n- `AlgebraicGeometry.Scheme.Modules.tensorObjAssoc_eta_factor_sheaf` (~L2458) — NEW typed sorry, the sheaf-level core of B4. This is the new atom `lem:tensorObjAssoc_eta_factor` now `\\uses`. Uses (once closed): `tensorObjAssoc`, `tensorObjIso`, `Localization.Monoidal.{μ_natural_left,μ_natural_right,associator_hom_app}`, `sheafification_map_unit_eq`. **Add `\\uses{lem:tensorObjAssoc_eta_factor_sheaf}` to `lem:tensorObjAssoc_eta_factor`'s proof and relocate the bridge-telescoping sketch into ★'s block.**\n\n## Stale comments flagged by lean-auditor (review cannot edit .lean — for prover/plan)\n- SGR L731–784: \"NEXT-ITER HANDLES / objRestrict-style fix untried\" block — the fix is already implemented; comment is stale.\n- SGR L2787: \"the single remaining sorry of that chain\" referring to B4 — B4 is now proved (modulo ★); stale.\n- `RelativeSpec.lean` L701–706: docstring says `functor` body \"is left as sorry\" but the body is concrete; stale.\n\n## Marker housekeeping done this iter (no action needed)\n- Removed proof-block `\\leanok` from `lem:tensorObjAssoc_eta_factor` and `lem:tensorObjAssoc_hom_sectionsMul` (both carry transitive `sorryAx` via ★). **Heads-up to next review:** the deterministic `sync_leanok` may RE-ADD these next iter (its iter-013 `added:1` was the B4 proof block, triggered by the now-sorry-free own-body). If it does, re-strip — they stay unproved until ★ closes. A durable fix would teach `sync_leanok` to use transitive `#print axioms`, but that is out of agent scope.\n"
}
