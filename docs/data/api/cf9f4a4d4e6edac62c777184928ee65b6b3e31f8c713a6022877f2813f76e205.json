{
  "content": "# Recommendations — next plan iter (post iter-037)\n\n## Closest-to-completion — PRIORITIZE\n- **`pushforward_lax_mu_comparison_lhs_tmul` (TensorObjSubstrate.lean) — Route A, the seed-1 bottleneck.**\n  Telescope steps 1–2 committed + lake-verified; BOTH missing helpers now in-file\n  (`pushforwardPushforwardAdj_counit_app_app_apply`, `restrictScalars_δ_app_tmul`). Residual = steps 3–4:\n  - (3) δ leg: `conv` into the inner `(G.map δ).app W`, `pushforward_map_app_apply` reindex, peel\n    `δ Gβ = RS.map(δ pf0) ≫ δ RS` via `Functor.OplaxMonoidal.comp` (`δ pf0 = 𝟙` `rfl`), discharge `δ RS`\n    on the pure tensor by `restrictScalars_δ_app_tmul` (instantiate-then-`erw` template).\n  - (4) counit pair: `ModuleCat … tensorHom_tmul` split, collapse each `hadj'.counit.app _` by\n    `pushforwardPushforwardAdj_counit_app_app_apply`; then open-immersion triangle cancel\n    (`adj.left/right_triangle` on opens) → `m ⊗ₜ n = RHS` (`pushforward_lax_mu_comparison_rhs_tmul`, proven).\n  - **NOTE for planner:** each step is another instantiate-then-`erw` round against carrier-diamond objects\n    on a >4800-LOC file with ~20k-token goal queries — token-heavy but mechanically de-risked. Roadmap is in-code.\n\n## Reusable proof patterns discovered (for Knowledge Base)\n- **Instantiate-then-`erw` for carrier-diamond value lemmas:** state a ∀-`have` over an ABSTRACT ring functor\n  `R` (so `R ⋙ forget₂` matches the `MonoidalCategoryStruct` instance head syntactically — dodges the\n  `PresheafOfModules X.ringCatSheaf.obj` vs `_ ⋙ forget₂` diamond), instantiate at the goal's concrete\n  objects, then `erw`. Bare `rw`/`erw`/`simp only` silently no-op on the diamond; `set` infers the heavy\n  type and `MonoidalCategoryStruct` synth fails.\n- **Helper type fixes on this seam:** counit value-lemma binder is the BIG composite object\n  `((pushforward ψ ⋙ pushforward φ).obj N).obj U`, NOT the section `N.obj U` (counit ≠ symmetric to unit);\n  δ pure-tensor RHS `m ⊗ₜ n` must be ascribed to the codomain tensor object so the `restrictScalars`\n  R-module instance resolves.\n- **LSP STALE-GREEN:** on this file `lean_diagnostic_messages` reports 0 errors while `lake build` is RED.\n  Always confirm with `lake build`.\n\n## Blueprint debt — planner action (from lean-vs-blueprint-checker substrate037, MAJOR ×2)\n1. **`restrictScalars_δ_app_tmul` — author a blueprint block (1-to-1 coverage debt).** New `private` lemma\n   ~L2137, δ-twin of `lem:restrictscalars_mu_app_tmul`. Deps to wire in `\\uses`: `Functor.Monoidal.μ_δ`,\n   `lem:restrictscalars_mu_app_tmul`. (Still in `dag-query unmatched`; total unmatched = 107, mostly\n   pre-existing aux helpers — full restoration is a separate backlog, but THIS one is new this iter.)\n2. **Expand the `lhs_tmul` blueprint sketch (`lem:pushforward_lax_mu_comparison_lhs_tmul`).** Currently says\n   \"the three legs compose to the identity\" but never previews the adjunction **triangle cancellation**\n   (the unit-leg's `counit`-restriction vs the counit-pair's `unit`-restriction) that is the actual content\n   behind the sorry, and frames the δ-leg via the μ lemmas rather than the new δ-value lemma. A blueprint-writer\n   round would de-risk steps 3–4. NOT a must-fix (Lean-mechanics, not under-spec blocking) but recommended\n   before the next Route A prover round per the HARD GATE spirit.\n\n## Lean hygiene — refactor-route (from lean-auditor iter037, MAJOR ×1 + MINOR ×2, none blocking)\n- **MAJOR:** header docstring mixes pre-Archon iter-numbers (250–261) with current Archon numbers (022–037)\n  unlabelled → future-agent misread risk. Deterministic comment fix (refactor subagent, not a prover lane).\n- **MINOR:** ~10 stale L-number cross-refs in dev comments (4–52 lines off); 2 long-line lints in the new\n  `restrictScalars_δ_app_tmul` docstring. Cosmetic; batch with the header fix.\n\n## Route B — UNBLOCK BEFORE DISPATCH (iter-038 scheduled per the binding interleave)\n- **`TensorObjInverse.lean` is RED at SOURCE, predating this iter:** parse-time syntax error `399:38`\n  (`unexpected identifier`) + unsolved goals at 338/379; `.olean` never built. This is NOT a regression from\n  the Route A root churn (a syntax error can't be induced by an imported edit). The scheduled iter-038 Route B\n  lane MUST first repair its own source before any `trivialisation_restrict_compat` work. Flag this to the\n  Route B prover explicitly so it doesn't mistake the breakage for a build-race artifact.\n\n## Do NOT retry (blocked approaches)\n- bare `rw`/`erw`/`simp only` of `tensorObj_map_tmul` on the heavy `⊗`; `set GA := …` tensor fold;\n  second `rw [pushforward_map_app_apply]` on the inner δ leg; naive counit binder `y : N.obj U`;\n  bare δ RHS `m ⊗ₜ[R] n`. (All confirmed DEAD this iter.)\n- The iter-035 abstract `{C D : Type*}` helper model for `lhs_tmul` — value is irreducibly\n  PresheafOfModules-specific (settled iter-036, re-confirmed iter-037). Do not re-propose.\n"
}
