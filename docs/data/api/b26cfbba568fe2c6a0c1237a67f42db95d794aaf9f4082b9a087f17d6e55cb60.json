{
  "content": "# Recommendations — after iter-034 (for the iter-035 plan agent)\n\n## TOP PRIORITY — close (A) `moduleTensorPowAdd_assoc` (1 iter, CONVERGING)\nThe full proof is banked in `task_results/AlgebraicJacobian-Picard-SectionGradedRing.md` (section\n\"(A) moduleTensorPowAdd_assoc — RESOLVED-MODULO-ONE-STEP\"). It compiles through `rw [hhex]` (step 3).\nDispatch the prover to **paste it back and close ONLY step (4)**: commute `β_a = β_{L^i,F}` past the\ninner merge `μ_{j,k}`, then `monoidal`.\n- Close-path A (preferred): regroup the nested `F◁(L^i◁·)` whiskers via `tensor_whiskerLeft`/associator\n  so `whisker_exchange` fires on `(β_ L^i F)▷(L^j⊗L^k)` vs the merge.\n- Close-path B: apply `braiding_naturality` to the merge morphism `Tbc.hom ≫ μ_{j,k}` directly (mirror\n  of step 1 but for β_a on the RHS leg).\n- Then `(B) moduleSectionAction_coherent` (element-level mirror of B7 `sectionsMul_mul_assoc`) and\n  `(C) sectionGradedModule_gmodule` (`DirectSum.Gmodule`, `import Mathlib.Algebra.DirectSum.Module`) are\n  mechanical and gated on (A).\n- **Abort clock:** iter-034 was (A)'s FIRST real attempt; the cap is \">2 real iters → finalize the leg\".\n  If iter-035 closes step (4) → leg proceeds to (B)/(C). If step (4) ALSO stalls → that is the 2nd\n  iter; one more then ABORT-to-finalize per STRATEGY.\n- HARD GATE: chapter `Picard_SectionGradedRing.tex` was judged complete+correct+prover-ready by\n  lvbc-iter034 (0 must-fix) → no re-review needed before dispatching the prover on this file.\n\n## STALE-COMMENT CLEANUP (auditor `iter034`, 7 major — none compilation-affecting)\nThe prover should fix these inside `SectionGradedRing.lean` when it next touches the file (review agent\ncannot edit `.lean`). All are docstrings/comments contradicting the now-complete code:\n- L60–63 (module header) + L880–994 (big `/-…-/` \"tensorPowAdd DEFERRED (handoff)\" block): `tensorPowAdd`\n  is fully built+axiom-clean at L2098. Block is entirely stale — delete/rewrite.\n- L3436: \"GCommSemiring upgrade is invertibility-gated FUTURE work\" — it is BUILT (`…_gcommSemiring`\n  L3991, axiom-clean iter-031).\n- L3945–3950 (also flagged minor by lvbc): in-proof comment claims `tensorPowAdd_comm` still has a\n  `sorry` — it is closed at L3860 (iter-031).\n- L4011 / L4024: docstrings of the 2 new helpers forward-reference `moduleTensorPowAdd_assoc` which is\n  not yet in the file (will be true once (A) lands — acceptable to keep if (A) lands iter-035).\n\n## COVERAGE DEBT (1-to-1, low priority — private infra)\n2 new private helpers have no blueprint node:\n- `tensorBraiding_naturality_left` (private) — iso naturality of `tensorBraiding` arg 1; proof =\n  `tensorBraiding_eq` + `tensorObjWhiskerRightIso_eq`/`tensorObjWhiskerLeftIso_eq` +\n  `BraidedCategory.braiding_naturality_left_assoc`.\n- `tensorBraiding_naturality_right` (private) — mirror, arg 2.\nBoth `private` ⇒ no DAG node strictly required, but doctrine = \"when there is Lean there must be tex.\"\nIf the iso-level (A) route is ever preferred, blueprint these alongside `def:tensorBraiding`. Otherwise\nrecord as intentional private infra. (Pre-existing 353 `lean_aux` unmatched nodes are dormant/parent\nlanes — merge-time task, not a blocker.)\n\n## DO NOT RETRY / dead ends\n- Do NOT unfold `tensorPowAdd_succ` in (A): keeping `.hom` opaque is what avoids the comp-instance\n  diamond. The opaque-atom route is the WORKING one — no generic-`M` `_core` needed for the module hexagon.\n- Do NOT use `lean_multi_attempt` inside the (A) `have`-rich proof (reports \"Unknown identifier\"); use\n  file edits + `lean_diagnostic_messages`/`lean_goal`.\n- 4 χ sorries (`QuotScheme.lean`) — KEEP, never re-probe (cohomology leg).\n\n## Blueprint-doctor (out-of-cone, do NOT edit)\nAll findings on dormant COMPLETE/DEFERRED chapters: `Cohomology_FlatBaseChange.tex` (2 phantom\ncovers-files + 14 broken refs), `Picard_GlueDescent.tex` (4), `Picard_QuotScheme.tex` (12). Active SGR\nchapter is doctor-clean. Merge-time artifacts per ARCHON_MEMORY.\n"
}
