{
  "content": "# Recommendations — after iter-030\n\n## TOP PRIORITY — close brick 1′ `tensorPowAdd_succ_left_braided` succ (the single live wall)\n- This is the ONLY genuine blocker; everything downstream is already wired and verified-by-hand.\n- The succ case is a B6-scale opaque-μ-cancellation, the EXACT shape iter-025 crossed for B6\n  (`tensorPowAdd_assoc`). The proven recipe is in code + summary:\n  1. Split the RHS braiding `WL_{Lᶜ}(β_{L, Lᵏ⊗L})` at the **HAND-BUILT** level via brick 2\n     `tensorBraiding_hexagon_forward L (L^k) L` (the `hβ` isolation pattern), BEFORE any canonical descent.\n  2. `rfl`-reindex `L^{k+1} = L^k ⊗ L`; distribute `WL_{Lᶜ}` via `tensorObjWhiskerLeftIso_trans`.\n  3. Fold the IH; collapse the emergent `β_{L,L}=𝟙` via PRIMARY `tensorBraiding_self_eq_id_of_isInvertible`\n     (or the new canonical `braiding_canonical_self_eq_id_of_isInvertible` if the residue is canonical).\n  4. Descend the structural residue to canonical (`_eq` bridges) and **build a generic-`M` core lemma**\n     (mirror `tensorPowAdd_assoc_succ_core`, pin `M := LocalizedMonoidal …`, `maxRecDepth 4000`) so the\n     opaque `μ_{c,k}` (`▷L`) cancels across the comp-instance diamond and `monoidal` fires.\n- ⚠ DEAD END — do NOT retry: canonical-FIRST descent (`apply Iso.ext; simp [tensorBraiding_eq, …]` then\n  `braiding_tensor_right_hom`). The braiding's 2nd factor becomes the hand-built `tensorObj`, so the Mathlib\n  `braiding_tensor_right_hom` (pattern `(β_ X (Y⊗Z)).hom`) never fires. Verified this iter.\n- If the prover stalls again on the generic-`M` core sub-step: **effort-breaker** the brick 1′ succ\n  (granularity: fine — one mathematical claim per lemma), specifically isolate the generic-`M` core as its\n  own `{M}[MonoidalCategory M]` lemma. This is the same decomposition that unblocked B6 at iter-025.\n\n## Then (mechanical cascade, no further invertibility) — DO IN ONE LANE once brick 1′ closes\n- `tensorPowAdd_comm` succ `(*)`: substitute brick 1′ for `μ_{c+1,m}`, collapse the prefix via\n  **`tensorBraiding_symm`** (delivered this iter), cancel `WR(μ_{c,m})L`, `monoidal`. Path verified by hand.\n- `sectionsMul_mul_comm`: already reduced to `tensorPowAdd_comm` (auto-clean once it closes).\n- `sectionGradedRing_gcommSemiring`: extend `sectionGradedRing_gsemiring` with graded `mul_comm` via\n  `gradedMonoid_eq_of_cast` + `sectionsMul_mul_comm` — the faithful Stacks 01CV conclusion.\n\n## Coverage debt (planner — author blueprint blocks; review agent does not write prose)\nTwo new axiom-clean private helpers are `lean_aux` nodes with NO blueprint block (lvbc minor):\n- `AlgebraicGeometry.Scheme.Modules.tensorBraiding_symm` — `tensorBraiding A B ≪≫ tensorBraiding B A =\n  Iso.refl`. Content = `cor:sheafModule_braided_symmetric` (β∘β=id) at the project tensor product.\n  In `ancestors(lem:tensorPowAdd_comm)` (consumed by the `(*)` closure). Suggest a one-line lemma block.\n- `AlgebraicGeometry.Scheme.Modules.braiding_canonical_self_eq_id_of_isInvertible` — canonical `(β_ L L).hom\n  = 𝟙` for invertible L; canonical image of `lem:braiding_eq_id_of_invertible` via `tensorBraiding_eq`.\n  Suggest a one-line block under `lem:tensorPowAdd_succ_left_braided`'s `\\uses`.\n\n## Blueprint hygiene (planner)\n- The `[[sync-leanok-overstrip]]` glitch RECURRED this iter (sync removed all 38 proof-`\\leanok` +\n  ate the `\\uses{` opener on the `sectionGradedRing_gsemiring` proof block). Both repaired this review.\n  Durable fix is sync-side — keep flagging to user. Until then, EVERY heavy-chapter-edit iter must expect\n  this; review restores via the exclusion principle + repairs eaten openers.\n- 3 blueprint-doctor bare-label findings (def:sectionMul, lem:gradedMonoid_eq_of_cast,\n  lem:directSum_gsemiring_mathlib) were the symptom of the eaten opener above — RESOLVED this review.\n\n## Do NOT re-assign / out-of-cone (deferred, no consumer)\n- 4 χ sorries in `QuotScheme.lean` (`hilbertPolynomial`, `QuotFunctor`, `Grassmannian`,\n  `Grassmannian.representable`) — cohomology leg, intentional iter-176 skeleton. Auditor must-fix×4 are\n  the auto-flagged excuse-comments on these; deferred, do not retry here.\n- `Cohomology_FlatBaseChange.tex` non-existent covers + 33 broken refs across dormant chapters — merge-back\n  artifacts; do NOT edit (per ARCHON_MEMORY); resolve at merge.\n- `RelativeSpec.lean:701–703` stale \"left as sorry\" comment vs concrete L709 body — LOW cosmetic cleanup.\n- `sectionGradedModule_gmodule` stretch — blocked on braided hexagon (mul_smul); off critical path.\n"
}
