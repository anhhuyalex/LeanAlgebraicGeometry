[
  {
    "name": "blueprint-reviewer-iter001-fastpath.md",
    "content": "# Blueprint Review: iter001-fastpath\n**Iter:** 001 (fast-path re-review — `Overview.tex` only)\n\n---\n\n## Fast-path verification: 5 must-fix items\n\n### Fix 1 — `lem:homc` `\\uses{}` includes `thm:def-c` and `def:pi-n`\n- **Status: CONFIRMED** — line 180: `\\uses{def:c-invariant, thm:def-c, def:pi-n}`. Both `thm:def-c` and `def:pi-n` are present.\n\n### Fix 2 — `lem:exact-seq` `\\uses{}` includes `lem:kernelXU`\n- **Status: CONFIRMED** — line 288: `\\uses{def:truncated-grothendieck, def:pi-n, lem:kernelXU}`. `lem:kernelXU` is present.\n\n### Fix 3 — All 14 declarations have per-declaration `% SOURCE:` with `(read from ...)` parenthetical\n- **Status: CONFIRMED** — all 14 verified:\n  - `def:variety` (line 24): `(read from references/MR4681148-birational-motivic-invariants.tex)` ✓\n  - `def:birational-map` (line 44): ✓\n  - `def:exceptional-set` (line 77): ✓\n  - `def:burnside-ring` (line 110): ✓\n  - `def:c-invariant` (line 138): ✓\n  - `lem:homc` (line 171): ✓\n  - `def:truncated-grothendieck` (line 203): ✓\n  - `def:pi-n` (line 240): ✓\n  - `lem:exact-seq` (line 280): ✓\n  - `lem:kernelXU` (line 330): ✓\n  - `def:c-tilde` (line 436): ✓\n  - `thm:def-c` (line 456): ✓\n  - `lem:kernelXY` (line 567): ✓\n  - `prop:ker-iota` (line 623): ✓\n\n### Fix 4 — All 14 declarations have `% SOURCE QUOTE:` verbatim blocks\n- **Status: CONFIRMED** — all 14 verified. All proof blocks that derive from the source also have `% SOURCE QUOTE PROOF:` before `\\begin{proof}` (lem:homc, lem:exact-seq, lem:kernelXU, thm:def-c, lem:kernelXY, prop:ker-iota). All 7 previously-missing `\\textit{Source: ...}` visible lines are now present in every block.\n\n### Fix 5 — \"Definition 2.0\" fabricated citation replaced\n- **Status: CONFIRMED** — `def:c-invariant` now reads:\n  - `% SOURCE:` pointer: `§2.1, unnumbered Def (c(φ))` (line 138)\n  - `\\textit{Source:}`: `Lin--Shinder, §2.1, unnumbered Def.` (line 157)\n  - No fabricated label remains.\n\n---\n\n## New issues introduced by writer\n\n**None found.** No new broken `\\uses{}`, no new fabricated citations, no new verbatim violations.\n\nMinor observation (not must-fix): visible equation numbers in `\\textit{Source:}` lines use published-paper numbers (\"eq.~(2.3)\", \"(2.5)\", \"(2.7)\") while the `% SOURCE:` comments use arXiv draft labels (\"K0-seq\", \"def-pi\", \"def-ct\"). The cross-check matches: these refer to the same equations under different numbering schemes. No drift between `% SOURCE:` and `\\textit{Source:}` that rises to a hard fail.\n\n---\n\n## Per-chapter\n\n### `Overview.tex`\n- **Complete**: true\n- **Correct**: true\n- **Notes**: All 14 declarations fully citation-compliant. All `\\uses{}` edges correct. All `% SOURCE QUOTE:` and `% SOURCE QUOTE PROOF:` blocks present and verbatim. `def:c-invariant` fabricated pointer fixed. Two missing dependency edges (`lem:homc`, `lem:exact-seq`) fixed.\n\n---\n\n## Severity summary\n\n- **must-fix**: none.\n- **HARD GATE passed: true**\n\nAll 5 must-fix items from `iter001-review` are resolved. `Overview.tex` is `complete: true`, `correct: true`, no must-fix findings. Prover dispatch for both Lean files covered by this chapter (`MR4681148MotivicInvariantsOfBirationalMaps.lean`, `MR4681148MotivicInvariantsOfBirationalMaps/Basic.lean`) is now unblocked.\n"
  },
  {
    "name": "blueprint-reviewer-iter001-review.md",
    "content": "# Blueprint Review: iter001-review\n**Iter:** 001\n\n---\n\n## Top-level summaries\n\n- **Deps/missing edges**: `lem:homc` — proof invokes `thm:def-c` directly but `\\uses{thm:def-c}` is absent (prover lane ordering broken). `lem:exact-seq` — proof cites `lem:kernelXU` for exactness at KVar^n but `\\uses{lem:kernelXU}` absent.\n- **Citations (hard fail — all 14 declarations)**: Every `% SOURCE:` comment uses format `% SOURCE: references/<file>.tex §...` — missing mandatory `(read from references/<file>.md)` parenthetical. No declaration has a `% SOURCE QUOTE:` verbatim block. No proof block derived from the source has `% SOURCE QUOTE PROOF:`.\n- **Citations (fabricated pointer)**: `def:c-invariant` cites \"Definition 2.0\" — source paper has no labeled \"Definition 2.0\"; the definition is an unnumbered `\\begin{Def}`. Fabricated label.\n- **Citations (missing visible source lines)**: `def:variety`, `def:birational-map`, `def:exceptional-set`, `def:burnside-ring`, `def:truncated-grothendieck`, `def:pi-n`, `def:c-tilde` — no `\\textit{Source: ...}` as first prose line.\n- **leandag**: 0 isolated nodes; 14 unmatched `\\lean{}` (all TODO placeholders — expected); no broken `\\uses{}` in DAG (2 *missing* edges found by reading proofs).\n- **blueprint-doctor**: clean — no malformed_refs, no orphan chapters, no broken refs.\n\n---\n\n## Unstarted-phase proposals\n\n*(omitted — all 5 strategy phases have adequate blueprint coverage in `Overview.tex`)*\n\n---\n\n## Per-chapter\n\n### `Overview.tex` (14 declarations, covers both Lean files)\n\n**Global notes on the chapter:**\n- `% SOURCE:` comments appear at *section level*, not per-declaration — technically accepted as coverage for declarations within the section, but format is wrong (see hard fails above).\n- No `% SOURCE QUOTE:` on any block and no `% SOURCE QUOTE PROOF:` before any proof block.\n\n---\n\n#### `def:variety`\n- **complete**: true\n- **correct**: true\n- **must-fix-this-iter**: `% SOURCE:` missing `(read from references/MR4681148-birational-motivic-invariants.tex)` parenthetical (use `.tex` not `.md` since that is the actual local file); add `% SOURCE QUOTE:` verbatim block; add `\\textit{Source:}` as first prose line.\n- **nice-to-fix**: []\n\n#### `def:birational-map`\n- **complete**: true\n- **correct**: true — statement faithful to §2.1 (\"morphism defined on a Zariski dense open subscheme, isomorphic onto its image\", groupoid structure). Blueprint uses \"open immersion\" = source's \"open embedding\" ✓.\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `\\textit{Source:}` line.\n- **nice-to-fix**: []\n\n#### `def:exceptional-set`\n- **complete**: true\n- **correct**: true — matches §2.1 definition and isomorphism X\\Ex(φ) ≅ Y\\Ex(φ⁻¹).\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `\\textit{Source:}` line.\n- **nice-to-fix**: []\n\n#### `def:burnside-ring`\n- **complete**: true\n- **correct**: true — Z[Bir_n/k] = ⊕_{[X]∈Bir_n/k} Z·[X]; graded Burnside ring Z[Bir/k] = ⊕_n Z[Bir_n/k] correct per §1.4.\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `\\textit{Source:}` line.\n- **nice-to-fix**: [Definition header calls this \"Burnside ring\" but declares only the degree-n piece Z[Bir_n/k]; minor terminological clarification useful for prover.]\n\n#### `def:c-invariant`\n- **complete**: true\n- **correct**: partial — formula matches source eq.(2.3) exactly. BUT `% SOURCE:` pointer cites \"Definition 2.0\" — source has no such label (unnumbered `\\begin{Def}`). Fabricated reference pointer.\n- **must-fix-this-iter**: Fix `% SOURCE:` pointer (remove \"Definition 2.0\", pointer should just be §2.1); `% SOURCE:` format (add parenthetical); `% SOURCE QUOTE:` verbatim block; `\\textit{Source:}` line.\n- **nice-to-fix**: []\n\n#### `lem:homc`\n- **complete**: true\n- **correct**: partial — statement correct. Proof claims \"This follows from the existence and uniqueness of c̃ in thm:def-c\" but `\\uses{def:c-invariant}` does NOT list `thm:def-c`. Missing dependency edge causes wrong dispatch order (prover would attempt lem:homc before thm:def-c is proved).\n- **must-fix-this-iter**: Add `\\uses{thm:def-c}` (and `\\uses{def:pi-n}` since the proof uses π_{n-1}).\n- **nice-to-fix**: [% SOURCE QUOTE PROOF: before proof env; the paper (lem-homc) explicitly defers its proof to Theorem thm-Defc — the blueprint proof sketch is correct but could note this deferral more explicitly.]\n\n#### `def:truncated-grothendieck`\n- **complete**: true\n- **correct**: true — generators and cut-and-paste relations match §2.3 eq.(2.2). Colimit = K₀(Var/k) correct.\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `\\textit{Source:}` line.\n- **nice-to-fix**: []\n\n#### `def:pi-n`\n- **complete**: true\n- **correct**: true — formula matches source eq.(def-pi) exactly; well-definedness argument present.\n- **must-fix-this-iter**: `% SOURCE:` format (currently only section-level `% SOURCE:` covers this; add per-declaration or confirm section-level suffices after format fix); `% SOURCE QUOTE:`; `\\textit{Source:}` line.\n- **nice-to-fix**: []\n\n#### `lem:exact-seq`\n- **complete**: true\n- **correct**: partial — sequence K^{≤n-1} → K^{≤n} → Z[Bir_n] → 0 matches source eq.(K0-seq). **Proof explicitly uses `lem:kernelXU` for exactness at K^{≤n}** but `\\uses{def:truncated-grothendieck, def:pi-n}` does NOT list `lem:kernelXU`. Missing dependency edge.\n- **must-fix-this-iter**: Add `\\uses{lem:kernelXU}`.\n- **nice-to-fix**: [% SOURCE QUOTE PROOF: before proof env.]\n\n#### `lem:kernelXU`\n- **complete**: true\n- **correct**: true — statement and inverse construction α match source Lemma lem:kernelXU and its proof verbatim. Proof is detailed enough for a Lean prover.\n- **must-fix-this-iter**: `% SOURCE:` format (no per-declaration `% SOURCE:` comment; section-level comment exists); `% SOURCE QUOTE:`; `% SOURCE QUOTE PROOF:`.\n- **nice-to-fix**: []\n\n#### `def:c-tilde`\n- **complete**: true\n- **correct**: true — formula c̃(φ) = [Y\\φ(U)]_{n-1} - [X\\U]_{n-1} matches source eq.(def-ct). Forward ref to `thm:def-c` for well-definedness is appropriate.\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `\\textit{Source:}` line.\n- **nice-to-fix**: []\n\n#### `thm:def-c`\n- **complete**: true — conditions (i), (ii), and relation c = π_{n-1}∘c̃ all present; proof covers uniqueness, well-definedness, homomorphism, and relation.\n- **correct**: true — statement matches source Theorem thm-Defc exactly; proof steps match source proof (eq:def-ct, independence of U, composition check, final relation).\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `% SOURCE QUOTE PROOF:`.\n- **nice-to-fix**: []\n\n#### `lem:kernelXY`\n- **complete**: true\n- **correct**: true — statement matches source Lemma lem:kernelXY. `\\uses{lem:kernelXU, thm:def-c, def:c-tilde}` is complete and correct.\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `% SOURCE QUOTE PROOF:`.\n- **nice-to-fix**: []\n\n#### `prop:ker-iota`\n- **complete**: true\n- **correct**: true — statement and exact sequence match source Proposition prop:ker-iota. Proof sketch correctly identifies the permutation-orbit argument.\n- **must-fix-this-iter**: `% SOURCE:` format; `% SOURCE QUOTE:`; `% SOURCE QUOTE PROOF:`.\n- **nice-to-fix**: []\n\n---\n\n## Missing declarations (Section 2 of paper)\n\nNone in scope. Out-of-scope items confirmed absent:\n- Proposition 2.4 / pro-cWF (weak factorization, smooth complete varieties) — excluded in STRATEGY.md.\n- Theorem 2.5 (vanishing for surfaces) — excluded.\n- Theorem 2.6 (Neron-Severi/Galois representation) — excluded.\n\n---\n\n## Severity summary\n\n- **must-fix (HARD GATE blocking)**:\n  - `lem:homc`: add `\\uses{thm:def-c}`, `\\uses{def:pi-n}`.\n  - `lem:exact-seq`: add `\\uses{lem:kernelXU}`.\n  - ALL 14 declarations: `% SOURCE:` format missing `(read from ...)` parenthetical — hard fail per citation discipline.\n  - ALL 14 declarations: `% SOURCE QUOTE:` verbatim block absent — hard fail.\n  - `def:c-invariant`: fabricated \"Definition 2.0\" citation pointer — must fix to remove/replace with §2.1 unnumbered def.\n- **soon (citation completeness)**:\n  - 7 definitions missing `\\textit{Source: ...}` as first prose line.\n  - All proof blocks derived from source missing `% SOURCE QUOTE PROOF:`.\n- **HARD GATE passed**: **false** — 2 missing `\\uses{}` edges + systemic citation hard fails.\n\n---\n\n## \\lean{} hint quality\n\nAll 14 hints use `...Basic.TODO.<Name>` namespace pattern. All 14 are unmatched (expected — Lean files are empty stubs). Names align with STRATEGY.md end-state targets:\n- `TruncatedGrothendieckGroup`, `FreeBiratClasses`, `cInvariant`, `cTildeInvariant`, `thm_DefC`, `prop_ker_iota` — exact STRATEGY.md names ✓.\n- `lem_kernelXU`, `lem_kernelXY`, `exactSeq`, `piMap`, `cInvariant_comp` — reasonable.\n- `Variety`, `BirationalMap`, `exceptionalSet` — reasonable; note mixed case styles (`exceptionalSet` vs `BirationalMap`) that the prover should normalize.\n\nNo quality issues block dispatch; prover will rename `TODO.` prefix away.\n\n---\n\n## Notes for Plan Agent\n\n1. A single blueprint-writer directive against `Overview.tex` can fix all must-fix items in one pass: (a) add two `\\uses{}` edges, (b) add per-declaration `% SOURCE:` with parenthetical, (c) add `% SOURCE QUOTE:` blocks, (d) fix \"Definition 2.0\" pointer, (e) add missing `\\textit{Source:}` lines to 7 definitions.\n2. The `references/MR4681148-birational-motivic-invariants.tex` file is the correct local source; writer should read it and copy verbatim quotations.\n3. After the writer returns and blueprint-doctor is green, re-dispatch me scoped to `Overview.tex` (fast path) — on a clean re-review, all 14 declarations can be cleared and all 5 prover phases unblocked in the same iter.\n4. No strategy modification needed; single-route confirmed.\n"
  },
  {
    "name": "blueprint-writer-overview-citation-fix.md",
    "content": "# Blueprint Writer Report: overview-citation-fix\n**Status:** COMPLETE\n\n## Changes\n- `lem:homc`: `\\uses{}` → `{def:c-invariant, thm:def-c, def:pi-n}` (added `thm:def-c`, `def:pi-n`).\n- `lem:exact-seq`: `\\uses{}` → `{def:truncated-grothendieck, def:pi-n, lem:kernelXU}` (added `lem:kernelXU`).\n- `def:c-invariant`: `% SOURCE:` fixed from \"Definition 2.0\" → \"§2.1, unnumbered Def (c(φ))\".\n- All 14 declarations: added per-declaration `% SOURCE:` (with parenthetical), `% SOURCE QUOTE:` (verbatim from file), `\\textit{Source:}` as first prose line.\n- Proof blocks for `lem:homc`, `lem:exact-seq`, `lem:kernelXU`, `thm:def-c`, `lem:kernelXY`, `prop:ker-iota`: added `% SOURCE QUOTE PROOF:` immediately before `\\begin{proof}`.\n\n## References consulted\n- `references/MR4681148-birational-motivic-invariants.tex` — lines 978–985 (§1.4 variety), 988–994 (§1.4(a) Burnside), 1032–1048 (§2.1 birational map), 1062–1073 (§2.1 exceptional set), 1081–1093 (§2.1 unnumbered Def c(φ)), 1111–1121 (§2.1 lem-homc), 1450–1497 (§2.3 truncated K₀, π_n, exact seq), 1511–1578 (§2.3 lem:kernelXU + proof), 1583–1650 (§2.4 thm-Defc + proof), 1681–1750 (§2.5 lem:kernelXY + prop:ker-iota + proofs).\n"
  },
  {
    "name": "strategy-critic-iter001-strategy-v2.md",
    "content": "# Strategy Critic Report\n\n## Slug\niter001-strategy-v2\n\n## Iteration\n001\n\n## Routes audited\n\n### Route: Single route — formalize Section 2 via RationalMap substrate\n\n- **Goal-alignment**: PARTIAL — the route targets Theorem 2.3 and Proposition 2.9, which are the correct final goals. However, the `cInvariant` definition (a prerequisite of `thm_DefC` and `prop_ker_iota`) requires iterating over codimension-1 points of the exceptional set and taking their residue fields as birational classes of (n-1)-dimensional varieties. This dependency chain is absent from the phase table and understated in the Mathlib gaps section; if it blocks Phase 3, the route cannot reach the goal without revisiting the plan.\n- **Mathematical soundness**: PARTIAL — two mathematical errors in the \"Mathlib gaps & new material\" section need correction before Phase 2 begins (see CHALLENGE entries below).\n- **Sunk-cost reasoning detected**: no\n- **Infrastructure-deferral detected**: no — all required constructions appear in the phase table with concrete iter estimates; none labelled \"off-critical path\" or deferred to upstream Mathlib.\n- **Phantom prerequisites**: `GroupTheory.FreeAbelianGroup.span` (listed as Phase 5 Mathlib need) — confirmed MISSING in Mathlib. No `FreeAbelianGroup.span` exists; the correct tool is `AddSubgroup.closure` (or a `Finsupp`-based reformulation).\n- **Effort honesty**: under-counted for Phase 3 and Phase 5 — see below.\n- **Parallelism under-exploited**: yes — within Phase 2, `BirationalMap` (built atop `AlgebraicGeometry.Scheme.RationalMap`) and `TruncatedGrothendieckGroup` (built atop `GroupTheory.FreeAbelianGroup`) are independent constructions requiring distinct Mathlib substrates; the strategy serializes them through a single \"Infrastructure\" phase with no prover-lane split.\n- **Verdict**: CHALLENGE\n\n## Format compliance\n\n- **Size**: 55 lines / 3028 bytes — within budget.\n- **Headings**: FAIL — heading `## Open key strategic questions` does not match the canonical `## Open strategic questions`. All other headings are present in correct order.\n- **Per-iter narrative detected**: no\n- **Accumulation detected**: no — the `## Completed` table is present with a placeholder row (nothing completed yet). Strictly, the spec says this section is optional when nothing is done; an all-placeholder row is marginal but not a material violation.\n- **Table discipline**: PASS — both tables have correct columns, one short line per cell.\n- **Format verdict**: DRIFTED\n\n## Infrastructure-deferral findings\n\nNo infrastructure-deferral patterns detected. All required constructions are on the plan with iter estimates.\n\n## Prerequisite verification\n\n- `Mathlib.GroupTheory.FreeAbelianGroup`: VERIFIED (`GroupTheory/FreeAbelianGroup.lean` present; provides `FreeAbelianGroup`, `lift`, `map`, universal property).\n- `Mathlib.AlgebraicGeometry.RationalMap` (as `Scheme.RationalMap`): VERIFIED — present as `Scheme.RationalMap X Y` (notation `X ⤏ Y`), quotient of `Scheme.PartialMap` by domain equivalence. NOTE: `RationalMap.compHom` only composes a `RationalMap` with a full morphism on the right; general `RationalMap ∘ RationalMap` composition is NOT provided by Mathlib and must be built by the project.\n- `Mathlib.AlgebraicGeometry.OpenImmersion`: VERIFIED (`AlgebraicGeometry/OpenImmersion.lean` and `AlgebraicGeometry/Morphisms/OpenImmersion.lean` present).\n- `GroupTheory.FreeAbelianGroup.span`: MISSING — no such name exists in Mathlib. Replace with `AddSubgroup.closure` or a `Finsupp`-based subgroup.\n\n## Must-fix-this-iter\n\n- Route (single route): CHALLENGE — **`TruncatedGrothendieckGroup` generator type is wrong.** The strategy describes `TruncatedGrothendieckGroup n k` as `FreeAbelianGroup (Σ m ≤ n, Variety m k)`, but by definition (Lin–Shinder §2.3 and the blueprint), generators are isomorphism classes of **finite type k-schemes** of dimension ≤ n — not `Variety` (which is irreducible, reduced, separated, finite type). Using `Variety` as generators yields a strictly smaller free abelian group and a wrong quotient. Fix the `## Mathlib gaps & new material` description before Phase 2 begins.\n\n- Route (single route): CHALLENGE — **`ExceptionalSet` definition is imprecise.** The strategy states \"exceptional set = complement of `RationalMap.domain`.\" Two problems: (1) `Scheme.RationalMap` is a quotient type with no `.domain` field — only `Scheme.PartialMap` has a `domain : X.Opens`; (2) even for a `PartialMap`, the domain is where a morphism exists, not where it is an **open immersion** (the latter is the locus where φ is an isomorphism at each point). The exceptional set requires the maximal open on which the representative PartialMap is an open immersion, which is a finer condition than the plain domain. Fix the `## Open key strategic questions` entry for `ExceptionalSet` design accordingly.\n\n- Route (single route): CHALLENGE — **Phase 3 Mathlib needs are understated.** The `cInvariant` definition sums `[k(x)]` over codimension-1 points `x ∈ Ex(φ^{-1})` and `[k(y)]` over codimension-1 points of `Ex(φ)`. This requires: (a) Krull codimension of a point in a scheme (Mathlib has `Order.krullDim` for topological spaces but the AG-side codimension API is thin; no `AlgebraicGeometry.codim` was found); (b) formation of the residue field `k(x)` (not the function field of `X`, but the residue field `LocalRing.residueField (X.stalk x)`); (c) identifying this residue field as an (n−1)-dimensional variety/birational class. None of these appear in the Phase 3 \"Key Mathlib needs\" column; listing only `AlgebraicGeometry.OpenImmersion` leaves the hardest gap (codimension iteration + residue-field-to-birational-class bridge) unnamed. Add these to Phase 3 before Phase 2 concludes.\n\n- Route (single route): CHALLENGE — **Phantom prerequisite in Phase 5.** `GroupTheory.FreeAbelianGroup.span` is listed as a key Mathlib need but does not exist. Replace with `AddSubgroup.closure` (available in `Mathlib.Algebra.Group.Subgroup.Basic`) or a `Finsupp`-based subgroup spanning API.\n\n- Route (single route): CHALLENGE — **Parallelism under-exploited in Phase 2.** `BirationalMap` (RationalMap substrate) and `TruncatedGrothendieckGroup` (FreeAbelianGroup substrate) are independent constructions. Serializing them through one phase artificially doubles the elapsed iter count for Phase 2. Split Phase 2 into two parallel prover lanes or add a sub-phase note.\n\n- Format: DRIFTED — heading `## Open key strategic questions` must be renamed to `## Open strategic questions` to match the canonical skeleton.\n\n## Overall verdict\n\nThe strategy is correctly scoped and goal-aligned at the high level. The single-route approach through `Scheme.RationalMap` and `FreeAbelianGroup` is mathematically sound in outline and avoids resolution of singularities — a reasonable simplification for Section 2 of Lin–Shinder. No infrastructure-deferral patterns are present and no sunk-cost reasoning was detected. However, five CHALLENGE findings must be resolved before Phase 2 implementation begins: the `TruncatedGrothendieckGroup` generator type is mathematically wrong (uses `Variety` where the definition needs finite-type schemes); the exceptional set description conflates `RationalMap` (no domain field) with `PartialMap` and conflates morphism domain with isomorphism locus; Phase 3's \"Key Mathlib needs\" omit the codimension-1 iteration and residue-field-to-birational-class bridge that `cInvariant` requires; `GroupTheory.FreeAbelianGroup.span` is a phantom prerequisite (use `AddSubgroup.closure`); and Phase 2 parallelism is unexploited. The format has a single drift: `## Open key strategic questions` must be `## Open strategic questions`.\n"
  },
  {
    "name": "strategy-critic-iter001-strategy-v3.md",
    "content": "# Strategy Critic Report\n\n## Slug\niter001-strategy-v3\n\n## Iteration\n001\n\n## Routes audited\n\n### Route: Single route — formalize Section 2 of Lin–Shinder directly\n\n- **Goal-alignment**: PASS — all 14 blueprint declarations (Variety, BirationalMap, exceptionalSet, FreeBiratClasses, cInvariant, cInvariant_comp, TruncatedGrothendieckGroup, piMap, exactSeq, lem_kernelXU, cTildeInvariant, thm_DefC, lem_kernelXY, prop_ker_iota) map onto the five phases. No goal-statement gap detected.\n- **Mathematical soundness**: PARTIAL — the route is mathematically correct but Phase 3 interweaves two definitions of very different difficulty: `cTildeInvariant` (requires only open-immersion arithmetic on KVar{n-1}, no codimension API) and `cInvariant` (requires a codimension-1 iteration, residue-field identification, and finiteness argument). Treating them as one phase misrepresents the blocking structure. Additionally, `piMap`'s well-definedness requires an irreducible-component distribution argument over an open/closed decomposition that is non-trivial in Lean.\n- **Sunk-cost reasoning detected**: no\n- **Infrastructure-deferral detected**: yes — the residue-field-to-birational-class bridge (\"Plan: use `AlgebraicGeometry.IsIntegral` + dimension assumption to extract the function field\") is listed as a \"Phase 3 gap\" with no plan for the non-trivial step: showing that `X.residueField x` at a codimension-1 point x has transcendence degree n−1 over k, i.e., that `Spec(κ(x))` carries a variety of dimension n−1 in the `FreeBiratClasses (n−1) k` sense. This step is required by the `cInvariant` definition (which is needed for `thm_DefC` and thence for the entire chain up to `prop_ker_iota`). No route in the strategy builds it with a concrete timeline or LOC estimate.\n- **Phantom prerequisites**:\n  - `LocalRing.residueField`: MISSING by that name. The correct API is `AlgebraicGeometry.Scheme.residueField` (defined in `Mathlib/AlgebraicGeometry/ResidueField.lean` as `def residueField (x : X) : CommRingCat := CommRingCat.of (IsLocalRing.ResidueField (X.presheaf.stalk x))`). The concept exists; the name in the strategy is wrong.\n  - `irreducibleComponents` for `piMap`: not listed in Phase 2b or Phase 3 Mathlib needs. `piMap([X]) = Σ_i [X_i]` requires enumerating the n-dimensional irreducible components of X as a `Finset`, which needs `TopologicalSpace.irreducibleComponents` (exists: `Topology/Irreducible.lean`) + `AlgebraicGeometry.Noetherian.finite_irreducibleComponents_of_isNoetherian` (exists: `AlgebraicGeometry/Noetherian.lean`). The well-definedness check on cut-and-paste relations also requires a dimension argument about component boundaries.\n  - `Z[Var^n/k]` (isomorphism class group): `lem_kernelXU` in the blueprint states `β: KVar{n-1} ⊕ Z[Var^n/k] → KVar{n}` where `Z[Var^n/k]` is the FREE ABELIAN GROUP on ISOMORPHISM classes of irreducible n-dimensional varieties — strictly finer than `FreeBiratClasses n k` (which uses birational equivalence). This group is needed to STATE `lem_kernelXU` and `lem_kernelXY` in Lean. The strategy never names it; it is an undeclared required definition.\n- **Effort honesty**: under-counted for Phase 3 — at 3–4 iters / 150–280 LOC, Phase 3 must (a) build the codimension wrapper via `Ideal.primeHeight` + stalk-prime identification, (b) prove finiteness of codimension-1 points in Ex(φ) via `finite_irreducibleComponents_of_isNoetherian`, (c) build the residue-field-to-birational-class bridge (transcendence degree = n−1), and (d) prove `piMap` well-definedness via irreducible-component distribution. Each sub-task is 30–80 LOC. Together these exceed the stated range; 5–7 iters / 250–450 LOC is more realistic.\n- **Parallelism under-exploited**: no — Phase 2a / 2b parallelism is correctly identified. Phases 3–5 are genuinely sequential.\n- **Verdict**: CHALLENGE\n\n## Format compliance\n\n- **Size**: 65 lines / 4096 bytes — within budget (250 lines / 12 KB).\n- **Headings**: PASS — `## Goal`, `## Phases & estimations`, `## Completed`, `## Routes`, `## Open strategic questions`, `## Mathlib gaps & new material` in correct order.\n- **Per-iter narrative detected**: no\n- **Accumulation detected**: no — `## Completed` has a single placeholder row (all dashes), correct for a fresh project.\n- **Table discipline**: PASS — both tables have the required column sets, one short line per cell.\n- **Format verdict**: COMPLIANT\n\n## Infrastructure-deferral findings\n\n### Deferred: residue-field-to-birational-class bridge\n\n- **Required by goal**: yes — `cInvariant` is defined as `Σ_{codim x=1} [κ(x)]` (blueprint def:c-invariant); `thm_DefC` proves `c = π_{n-1} ∘ c̃` using this; `prop_ker_iota` (Proposition 2.9) depends on `thm_DefC`. The bridge is on the critical path.\n- **Current plan for building it**: \"use `AlgebraicGeometry.IsIntegral` + dimension assumption to extract the function field\" — in Open strategic questions only; not mentioned in Phase 3 risks or Mathlib gaps.\n- **Timeline**: vague (\"Phase 3 gap\") — no iter estimate or sub-task decomposition.\n- **Verdict**: CHALLENGE — the bridge requires proving that `X.residueField x` at a height-1 stalk point is the function field of an (n−1)-dimensional variety; this uses `Scheme.functionField` + a dimension theorem for stalks at codimension-1 points (that `dim 𝒪_{X,x} = codim_X(x)` for a catenary variety over k), which is non-trivial in Lean and requires its own sub-phase.\n\n## Alternative routes (suggested)\n\n### Alternative: c̃-first with c derived\n\n- **What it looks like**: Define `cTildeInvariant(φ) := [Y \\ φ(U)]_{n-1} − [X \\ U]_{n-1}` (no codimension needed) first. Define `cInvariant := piMap ∘ cTildeInvariant` as a derived definition. Prove Theorem 2.3 (`thm_DefC`) using only open-immersion arithmetic. The codimension formula for `cInvariant` becomes a derived theorem, not a definition. This allows Phases 4 and 5 to proceed without waiting for the codimension wrapper.\n- **Why it might be cheaper or sounder**: the codimension formula for c is proved as a consequence of c̃'s construction (blueprint proof of thm_DefC §\"Relation c = π_{n-1} ∘ c̃\"), not needed for the definition. Separating the easy part (c̃ definition + thm_DefC) from the hard part (codimension formula verification) unblocks ~40% of the remaining blueprint declarations ahead of the codimension gap.\n- **What the current strategy may have rejected**: the blueprint's Section 2.1 defines c before c̃, following the paper's presentation order. Departing from that order requires restructuring the blueprint dependency graph (specifically, making def:c-invariant depend on thm_DefC instead of the reverse).\n- **Severity of the omission**: major — the current strategy silently serializes c̃ behind c, causing the codimension gap to block all downstream phases. Explicitly addressing this ordering decision in the strategy (either adopt c̃-first or explain why the paper's order is preferred) would clarify the blocking structure.\n\n## Prerequisite verification\n\n- `AlgebraicGeometry.Scheme.PartialMap`: VERIFIED (in `Mathlib/AlgebraicGeometry/RationalMap.lean` — `structure PartialMap (X Y : Scheme.{u}) where`)\n- `AlgebraicGeometry.OpenImmersion`: VERIFIED (in `Mathlib/AlgebraicGeometry/OpenImmersion.lean`)\n- `RationalMap.compHom` (right-morphism composition only): VERIFIED — confirmed that composition of two `RationalMap`s is absent; `compHom` only takes `X.PartialMap Y` + full morphism `Y ⟶ Z`.\n- `GroupTheory.FreeAbelianGroup`: VERIFIED (in `Mathlib/GroupTheory/FreeAbelianGroup.lean`)\n- `AddSubgroup.closure`: VERIFIED (in `Mathlib/Algebra/Group/Subgroup/Lattice.lean`)\n- `LocalRing.residueField`: MISSING by that name — correct name is `AlgebraicGeometry.Scheme.residueField` (in `Mathlib/AlgebraicGeometry/ResidueField.lean`)\n- `AlgebraicGeometry.Scheme.codim`: MISSING — confirmed absent from all `AlgebraicGeometry/` files in Mathlib v4.30.0\n- `Order.krullDim`: VERIFIED (in `Mathlib/Order/KrullDimension.lean`)\n- `Ideal.primeHeight` / `Ideal.height`: VERIFIED (in `Mathlib/RingTheory/Ideal/Height.lean`)\n- `AlgebraicGeometry.IsIntegral`: VERIFIED (in `Mathlib/AlgebraicGeometry/Properties.lean`)\n- `AlgebraicGeometry.Scheme.functionField`: VERIFIED (in `Mathlib/AlgebraicGeometry/FunctionField.lean`)\n- `TopologicalSpace.irreducibleComponents`: VERIFIED (in `Mathlib/Topology/Irreducible.lean`)\n- `AlgebraicGeometry.Noetherian.finite_irreducibleComponents_of_isNoetherian`: VERIFIED (in `Mathlib/AlgebraicGeometry/Noetherian.lean`)\n\n## Must-fix-this-iter\n\n- Route Single: CHALLENGE — Phase 3 codimension plan is incomplete. The strategy says \"use `Ideal.height` of the prime ideal directly\" but does not address (a) the bridge from a scheme point to its stalk's maximal ideal height, (b) the finiteness argument for codimension-1 points of Ex(φ) (requires Noetherianness), or (c) the residue-field dimension step (showing transcendence degree = n−1). Planner must either (i) add these as an explicit sub-phase with LOC/iter estimates in Phase 3 or (ii) adopt the c̃-first alternative to defer the codimension dependency.\n- Route Single: infrastructure-deferral CHALLENGE — residue-field-to-birational-class bridge required by `cInvariant` (→ `thm_DefC` → `prop_ker_iota`), no concrete timeline. Planner must add a concrete sub-plan or split Phase 3 into 3a (c̃ + thm_DefC, no codimension) and 3b (c formula + bridge).\n- Phantom prerequisite `LocalRing.residueField`: rename to `AlgebraicGeometry.Scheme.residueField` everywhere in STRATEGY.md and (when written) in Lean files.\n- Missing Mathlib needs: add `TopologicalSpace.irreducibleComponents` + `AlgebraicGeometry.Noetherian` to Phase 3 key Mathlib needs (for `piMap` well-definedness); add `Z[Var^n/k]` (free abelian group on isomorphism classes of irreducible dim-n varieties) to Phase 2b new material (needed to state `lem_kernelXU` / `lem_kernelXY` in Lean).\n\n## Overall verdict\n\nThe strategy is structurally sound after its prior revision: generator types are correct, `PartialMap` is used for the exceptional set, `AddSubgroup.closure` replaces the phantom `GroupTheory.FreeAbelianGroup.span`, Phase 2 is parallelized, and the format is fully compliant. Three live issues remain. First, the strategy defers the residue-field-to-birational-class bridge, which is required for the stated goal (`cInvariant` → `thm_DefC` → `prop_ker_iota`); the current placeholder \"use IsIntegral + dimension assumption\" is not a plan — the gap requires proving `dim 𝒪_{X,x} = codim_X(x)` (a dimension theorem for catenary varieties) and identifying `κ(x)` with a `FreeBiratClasses (n−1) k` element, neither of which has a Mathlib lemma. Second, two Mathlib needs are absent from the strategy: `irreducibleComponents` (required for `piMap` well-definedness) and `Z[Var^n/k]` as a separate group from `FreeBiratClasses` (required to state `lem_kernelXU`). Third, `LocalRing.residueField` is the wrong Mathlib name (correct: `AlgebraicGeometry.Scheme.residueField`). None of these require a route change, but Phase 3 needs to be split or its estimate expanded, and the two missing definitions must be named.\n"
  },
  {
    "name": "strategy-critic-iter001-strategy.md",
    "content": "# Strategy Critic Report\n\n## Slug\niter001-strategy\n\n## Iteration\n001\n\n## Routes audited\n\n### Route: Single route — formalize Section 2 via direct construction\n\n- **Goal-alignment**: PARTIAL — the five target declarations are all covered by the five phases, but there is a subtle gap: `thm_DefC` requires that open immersions are a special case of the `BirationalMap` type (condition (i) of the theorem: `c̃(U↪X) = [X∖U]_{n-1}`). The strategy never specifies how open immersions embed into the `BirationalMap` structure it plans to build. Without that, condition (i) cannot even be stated, let alone proved.\n- **Mathematical soundness**: PASS — the overall approach (quotient free abelian group for `K₀(Var^{≤n}/k)`, direct construction of `c̃` via the open-domain formula, induction on composition for homomorphism) is mathematically coherent. The paper avoids resolution of singularities, and no step in the route requires techniques the strategy doesn't claim.\n- **Sunk-cost reasoning detected**: no\n- **Infrastructure-deferral detected**: yes — (1) `BirationalMap` type with composition and exceptional sets (see § Infrastructure-deferral findings below); (2) birational equivalence quotient for `FreeBiratClasses` (see § Infrastructure-deferral findings below).\n- **Phantom prerequisites**: `Mathlib.RingTheory.FreeAbelianGroup` — MISSING. The actual module is `Mathlib.GroupTheory.FreeAbelianGroup` (confirmed by .olean search on the installed mathlib4 at rev `c5ea003`). No file exists at the claimed `RingTheory` path.\n- **Effort honesty**: under-counted — the phases table has no LOC column at all, making honest assessment impossible. Qualitatively: Phase 2 (\"2–3 iters\") must build four definitions entirely from scratch, including a custom `BirationalMap` type with no Mathlib substrate and a quotient-type construction for birational equivalence classes — a well-known formalization bottleneck. \"2–3 iters\" for all four is optimistic. Phase 4 (\"Purely algebraic … clean induction\") understates the well-definedness subtlety of `c̃` (independence of the choice of open domain `U` in the formula `[Y∖φ(U)]_{n-1} − [X∖U]_{n-1}`), which requires detailed cut-and-paste bookkeeping inside `K₀(Var^{≤n-1}/k)`.\n- **Parallelism under-exploited**: no — the strategy correctly identifies that `TruncatedGrothendieckGroup` and `FreeBiratClasses` can be parallelized within Phase 2. (The `## Parallel Independence` section is a format violation and should be absorbed into the phases table, but the content is correct.)\n- **Verdict**: CHALLENGE\n\n## Format compliance\n\n- **Size**: 70 lines / 4115 bytes — within budget.\n- **Headings**: FAIL — the following violations are present:\n  - `## End State` must be `## Goal`.\n  - `## Phases & Estimations` must be `## Phases & estimations` (canonical casing; also wrong section name — it should describe estimations, not just \"Estimations\").\n  - `## Mathlib Gaps & Known Hazards` must be `## Mathlib gaps & new material`.\n  - `## Parallel Independence` is not in the canonical skeleton; its content belongs as notes in the phases table.\n  - `## Scope Boundary` is not in the canonical skeleton; out-of-scope items belong under `## Open strategic questions` or are omitted (they are derivable from the goal statement).\n  - `## Open strategic questions` is entirely absent (required section).\n- **Per-iter narrative detected**: no.\n- **Accumulation detected**: no — no completed phases yet, which is correct for iter 001.\n- **Table discipline**: FAIL — the `## Phases & estimations` table has columns `Phase | Goal | Iters left | Notes`. The canonical schema requires `Phase | Status | Iters left | LOC | Key Mathlib needs | Risks`. Missing columns: `Status`, `LOC`, `Key Mathlib needs`, `Risks`. The `Notes` cell blurs Key Mathlib needs and Risks into a single prose field.  No LOC range is provided for any phase.\n- **Format verdict**: NON-COMPLIANT\n\n## Infrastructure-deferral findings\n\n### Deferred: BirationalMap type (composition + exceptional sets)\n\n- **Required by goal**: yes — `BirationalMap` is the domain of definition of `cInvariant`, `cTildeInvariant`, and `thm_DefC`. Without a formalized composition, condition (ii) of `thm_DefC` (`c̃(ψ∘φ) = c̃(φ) + c̃(ψ)`) cannot be stated. Without exceptional sets, the definition of `cInvariant` cannot be written.\n- **Current plan for building it**: \"a fully bundled `BirationalMap` type … may need to be introduced\" — listed in `## Mathlib Gaps & Known Hazards` as a \"known hazard\"; no design, no file assignment, no decomposition into sub-steps.\n- **Timeline**: absent — Phase 2 is marked \"2–3 iters\" with no sub-phases, no concrete design (e.g., whether to represent birational maps as spans of open immersions, as invertible `RationalMap`s, or as an entirely new structure), and no acknowledgment that this sub-problem may take all of Phase 2 on its own.\n- **Verdict**: CHALLENGE — `BirationalMap` is on the critical path for every phase from 2 onwards. The strategy must either (a) add a concrete design for the type in Phase 2 with an LOC estimate for that sub-phase alone, or (b) explicitly choose the `RationalMap`-based alternative route described below.\n\n### Deferred: Birational equivalence quotient (for FreeBiratClasses)\n\n- **Required by goal**: yes — `FreeBiratClasses` (`ℤ[Bir_n/k]`) is required for `cInvariant`, and `cInvariant` is a named goal declaration.\n- **Current plan for building it**: \"quotienting schemes by birational equivalence requires care\" — listed in `## Mathlib Gaps & Known Hazards`. No plan is given for constructing the `Setoid` on `Scheme.{u}` restricted to dimension-n varieties, nor for defining the quotient type in a universe-safe way.\n- **Timeline**: absent — no concrete sub-phase, no approach, no iter estimate.\n- **Verdict**: CHALLENGE — this is a standard but non-trivial Lean 4 quotient-type construction (requires `Setoid`, `Quotient`, and a proof that birational equivalence is an equivalence relation on irreducible separated schemes of dimension n). It must appear as a concrete sub-phase of Phase 2 with its own design note.\n\n## Alternative routes (suggested)\n\n### Alternative: Build BirationalMap on top of Mathlib's RationalMap\n\n- **What it looks like**: Mathlib already has `AlgebraicGeometry.Scheme.RationalMap` (`X ⤏ Y`), defined as an equivalence class of `PartialMap`s. A birational map could be defined as a `RationalMap f : X ⤏ Y` together with a `RationalMap g : Y ⤏ X` satisfying `g ∘ f = id` and `f ∘ g = id` at the level of rational maps. This reuses the domain-of-definition and equivalence infrastructure already in Mathlib. The exceptional set `Ex(φ)` would be the complement of `f.domain` (the maximal domain of definition, which Mathlib provides for integral separated schemes via `RationalMap.toPartialMap`).\n- **Why it might be cheaper or sounder**: Avoids reimplementing partial-map equivalence from scratch; reuses `AlgebraicGeometry.Scheme.RationalMap.domain`, `PartialMap`, and the function-field API. The domain-restriction and composition lemmas already exist in the `RationalMap` API.\n- **What the current strategy may have rejected**: The strategy lists `Mathlib.AlgebraicGeometry.Morphisms.Basic` as a dependency and mentions `IsOpenImmersion`, but never explicitly considers the `RationalMap` file that ships in the same Mathlib version. The rejection may be implicit (perhaps the `RationalMap` type doesn't satisfy the groupoid composition law easily), but no reasoning is given.\n- **Severity of the omission**: major — the decision between \"define BirationalMap from scratch\" and \"build on RationalMap\" affects Phase 2 scope by potentially 30–50% of LOC and should be an explicit strategic choice, not an undiscovered option.\n\n## Prerequisite verification\n\n- `Mathlib.RingTheory.FreeAbelianGroup`: MISSING — no `.olean` at this path; confirmed by filesystem search on installed Mathlib rev `c5ea003`. Actual location: `Mathlib.GroupTheory.FreeAbelianGroup`.\n- `Mathlib.Algebra.Group.Defs`: VERIFIED — standard Mathlib4 file, present.\n- `Mathlib.AlgebraicGeometry.Scheme`: VERIFIED — `.olean` present.\n- `Mathlib.AlgebraicGeometry.Morphisms.Basic`: VERIFIED — `.olean` present.\n- `Mathlib.AlgebraicGeometry.RationalMap` (unmentioned but relevant): VERIFIED — `.olean` and `.lean` present; defines `Scheme.RationalMap` and `Scheme.PartialMap`.\n\n## Must-fix-this-iter\n\n- Route \"Single route\": CHALLENGE — goal-alignment gap: how open immersions embed into the planned `BirationalMap` type must be made explicit before Phase 3 can state `thm_DefC` condition (i).\n- Route \"Single route\": infrastructure-deferral CHALLENGE — `BirationalMap` (with composition and exceptional sets) is required by the goal from Phase 2 onwards; no concrete design or timeline exists. Planner must add a sub-phase design to Phase 2 or adopt the RationalMap-based alternative explicitly.\n- Route \"Single route\": infrastructure-deferral CHALLENGE — `FreeBiratClasses` birational equivalence quotient is required by `cInvariant` (a named goal declaration); no concrete plan exists. Planner must add a sub-phase in Phase 2 covering the Setoid + Quotient construction.\n- Phantom prerequisite `Mathlib.RingTheory.FreeAbelianGroup`: MISSING — correct to `Mathlib.GroupTheory.FreeAbelianGroup` in STRATEGY.md.\n- Alternative \"Build BirationalMap on top of Mathlib's RationalMap\": major omission — must be acknowledged and explicitly accepted or rejected in STRATEGY.md.\n- Format: NON-COMPLIANT — restructure STRATEGY.md this iter: (1) rename `## End State` → `## Goal`, `## Phases & Estimations` → `## Phases & estimations`, `## Mathlib Gaps & Known Hazards` → `## Mathlib gaps & new material`; (2) remove `## Parallel Independence` and `## Scope Boundary` (absorb contents into phases table notes and goal section respectively); (3) add `## Open strategic questions`; (4) fix phases table columns to `Phase | Status | Iters left | LOC | Key Mathlib needs | Risks` with LOC ranges for each active phase.\n\n## Overall verdict\n\nThe single-route strategy is mathematically coherent but has two active infrastructure-deferral problems that block every phase past the blueprint: the `BirationalMap` type (with composition and exceptional sets) is listed only as a \"known hazard\" with no concrete design or timeline, and the birational equivalence quotient for `FreeBiratClasses` is similarly unaddressed — yet both are required by the stated goal. The strategy defers X (`BirationalMap` construction), which is required for the stated goal, and defers Y (birational equivalence quotient), which is required for the stated goal. Additionally, `Mathlib.RingTheory.FreeAbelianGroup` is a phantom import (correct path: `Mathlib.GroupTheory.FreeAbelianGroup`), and STRATEGY.md is NON-COMPLIANT with the canonical skeleton (five heading violations, two extra sections, one missing required section, and a phases table missing the Status, LOC, Key Mathlib needs, and Risks columns). All items in `## Must-fix-this-iter` must be resolved before any prover work begins.\n"
  }
]
