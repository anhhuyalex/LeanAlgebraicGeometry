{
  "content": "# Recommendations — for iter-037 plan\n\n## Closest-to-completion — PRIORITIZE\n**`lhs_tmul` (Route A, TensorObjSubstrate.lean:4379) — wall #1 cracked, now an unblocked telescope.**\nThis is no longer a \"stuck\" target: the 4-iter blocker is gone and the residual is a long-but-mechanical\n4-step outer δ/counit telescope, each step named in-code (L4356–4378). Continue Route A — do NOT pivot.\nSuggested next prover lane (SOLO, root must stay green):\n1. **Add the NEW counit helper first** — `pushforwardPushforwardAdj_counit_app_app_apply` (counit\n   `rfl`-dual of the existing unit lemma in `PresheafInternalHom.lean`; counit acts sectionwise by the\n   bijective `f.appIso` structure-ring map). It is step (4) but is the only genuinely new declaration —\n   landing it first de-risks the lane. **Blueprint debt:** add a `\\label`/`\\lean{}`/`\\uses` block for\n   it (dual of `lem:pushforwardpushforwardadj_unit_app_app_apply`) BEFORE the prover lane — the\n   lean-vs-blueprint-checker flagged its absence as the sole (minor) coverage gap.\n2. Then steps (1)–(3): `tensorObj_map_tmul` as a fvar-keyed `have`+`erw` (SAME recipe as the wall-#1\n   crack — head `…Monoidal.tensorObj` ≠ `⊗`); `Functor.map_comp`+`pushforward_map_app_apply` reindex;\n   δ-collapse via the `restrictScalars_μ_app`/`forget₂_restrictScalars_μ_hom_tmul` family. Close with\n   `pushforward_lax_mu_comparison_rhs_tmul` (PROVEN).\n\n## Reusable proof pattern discovered (durable)\n**fvar-keyed-`have` + `erw` for blocked `rfl` value-lemmas.** When an `rfl` value-lemma won't fire\n(`rw`/`simp`/`erw`/explicit-args all dead) because of a *let-fvar shadow + heavy tensor object*: state a\n∀-`have` whose LHS is keyed on the goal's OWN fvar (write NO `⊗` → dodges the carrier-diamond synthInstance\nwall), prove `fun … => lemma _…_ args` (defeq through the fvar), discharge with **`erw`** (plain `rw`\nfails — needs the coercion/instance defeq). Already earmarked for steps (1) and (4) of the telescope.\n\n## Route B — run SOLO when a green window allows\n`trivialisation_restrict_compat` (TensorObjInverse.lean:244) untouched 4+ iters. Residual = 5\nper-constituent restrict-naturality squares (S2–S4c, effort-broken iter-033, blueprint 6312–6535).\nProve **S2 `tensorObj_restrict_iso_restrict_compat` FIRST as the template**. MUST be a SOLO lane (its\nfile imports the churning root; co-dispatch with a Route A root-edit = the documented iter-029 build\nrace). Do NOT co-dispatch with a Route A lane that edits TensorObjSubstrate.\n\n## Do NOT retry (dead ends, confirmed iter-036)\n- Whole-morphism `(★)` route for `lhs_tmul` (`Adjunction.map_μ_comp_counit_app_tensor`) — CIRCULAR\n  (consumes `lhs_tmul` itself via the `hmon` IsMonoidal chain).\n- Abstract `{C D : Type*}` helper for the unit value — impossible (PresheafOfModules-specific).\n- bare `rw`/`simp only`/explicit-`Z:=…⊗…` `have` for the unit lemma — all confirmed dead (the `erw`\n  fvar-keyed recipe is the only thing that fires).\n\n## Deterministic cleanup (refactor route in plan phase — NOT a prover lane)\nlean-auditor iter036 flagged 2 MAJOR stale file-header entries in `TensorObjSubstrate.lean` (both on\nsorry-free decls → `objectivesNoop` drops them from prover dispatch; apply via `refactor`):\n- **L162:** header marks `pullbackTensorIsoOfLocallyTrivial` as `(sorry, seed-1 active iter-020)` but it\n  is fully proved at L4847–4872 — wrong status.\n- **L161:** header lists `exists_tensorObj_inverse` as \"this file\" but it moved to `TensorObjInverse.lean`\n  in iter-023.\nPlus 3 minor lean-auditor items — see `task_results/lean-auditor-iter036.md`.\n\n## Coverage debt (1-to-1, pre-existing — for the planner to blueprint)\n`archon dag-query unmatched` = 106 `lean_aux` nodes with no blueprint entry (UNCHANGED from iter-033 —\nno new helpers this iter). Long-standing debt, not iter-036-introduced; flagged here for continuity. The\nplanner should chip at these (author `\\label`/`\\lean`/`\\uses` blocks) as bandwidth allows — they are\ninvisible to the dependency graph until blueprinted.\n"
}
