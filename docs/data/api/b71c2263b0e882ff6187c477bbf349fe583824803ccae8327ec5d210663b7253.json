{
  "content": "# Recommendations — next plan iter (post iter-018)\n\n## HIGH — blueprint adequacy gate BEFORE the next S3-bridge prove lane\n`lem:carrierSheaf_zero_sections_eq_structureSheaf` (RiemannRoch_OcOfD.tex L2204–2263) proves only\nthe **affine** `U = Spec A` case, but the Lean `carrierSheaf_zero_sections_eq_structureSheaf` is stated\nand consumed (`NatIso.ofComponents`) for **all opens**. The prove lane that closes S3 needs the\ngeneral-`U` → affine-cover **locality/gluing** step, which the chapter does not sketch.\n→ Dispatch **blueprint-writer** on RiemannRoch_OcOfD.tex to: (a) add the general-`U` reduction\n(carrier order-conditions are local; `Γ(U,𝒪_C)` = ∩ over an affine cover; cite `carrierSet_mono`);\n(b) state the component iso explicitly as the **identity-on-`K(C)`** map (the naturality square depends\non this). This is the HARD GATE for the S3 lane — do NOT send a prover at L1800/L1819 until the chapter\ncovers all-`U`. (lean-vs-blueprint-checker/ocofd, major.)\n\n## HIGH — next prove target: the S3 inverse (Hartogs / Dedekind-per-chart)\nClosest deep gap to closure. Recipe (from task_result + checker):\n1. Build `IsDedekindDomain Γ(C.left, U)` for an affine open: project `SmoothRegular` (smooth ⟹ regular)\n   ⟹ integrally-closed Noetherian dim-1 ⟹ Dedekind. NO single-call Mathlib lemma — likely Mathlib-build.\n   The P¹ PID route (`WeilDivisor` L1811, `HomogeneousLocalization.Away`) does NOT generalise.\n2. Discharge the inverse via `IsDedekindDomain.HeightOneSpectrum.mem_integers_of_valuation_le_one`\n   (already `\\mathlibok`) over `functionField_isFractionRing_of_isAffineOpen`.\n3. Naturality square (L1819) then closes mechanically (identity on `K(C)` both sides).\n→ Consider an **effort-breaker** on `carrierSheaf_zero_sections_eq_structureSheaf` once the blueprint\ncovers all-`U`, splitting the Dedekind-instance build from the `mem_integers` application.\n\n## MEDIUM — G2 residue cluster (independent of the bridge)\n`cokernel_stalk_at_iso_kbar` (L1509) — the DVR residue quotient `(π^{-(n+1)}𝒪)/(π^{-n}𝒪) ≅ k̄`.\niter-017 reduced it to this single sorry; untouched iter-018. Separate cluster from the bridge — can be\ndispatched in parallel (different decl, same file ⇒ sequential prover round). iter-017 review recommended\neffort-breaking into (a) stalk-map naturality + (b) DVR residue before proving.\n\n## MEDIUM — coverage debt (restore tex; planner authors prose, not the review agent)\n- `sheafOf.carrierSet_zero_eq` (OcOfD.lean:1730) — no `\\lean{}`. Depends on `carrierSet`; realises the\n  first sentence of `lem:carrierSheaf_zero_sections_eq_structureSheaf`.\n- `sheafOf.structureSection_mem_carrierSet_zero` (OcOfD.lean:1748) — no `\\lean{}`. Closed reusable forward\n  inclusion `Γ(U,𝒪_C) → carrier₀` (germ/ordFrac). Worth its OWN pinned blueprint block (checker rec).\n\n## LOW — module-header + docstring cleanup (comment-only; route to planner/writer)\nlean-auditor/iter018 flagged 7 stale comments (no Lean impact, but actively misleading):\n- L63–71 module-header \"Status\" under-reports active sorries (says 2, actually 6) and falsely classifies\n  `sheafOf`/`sheafOf_zero` as \"Tier-3 honest typed sorry\" — both are now sorry-free (closed iter-011/014/016).\n- Stale iter numbers `iter-181/183/184/185/186` (pre-3-digit-reset) in docstrings at L606, L625, L660, L692, L1868.\n- L1629 \"SOLE remaining deep ingredient\" — now one of several (alongside the S3 bridge).\nThe plan agent owns blueprint/docstring prose; provers own `.lean` bodies. Flag for a cleanup pass.\n\n## Do NOT re-attempt\n- The whole cokernel iso blind (sorry 0→2→4 regression, iter pre-015). It is decomposed; only the two\n  leaf clusters (S3 bridge gap + G2 residue) remain.\n- The P¹ PID route for the Dedekind-per-chart instance — does not generalise to arbitrary smooth curves.\n"
}
