{
  "content": "# Recommendations — after iter-025\n\n## State: B6 closed, plateau broken. Pivot to B7.\n\n### 1. PRIORITY — B7 `sectionsMul_mul_assoc` (L3264, the sole in-cone open SNAP sorry)\nNow genuinely unblocked: ingredients (1)=B5 `tensorObjAssoc_hom_sectionsMul` and (3)=B6\n`tensorPowAdd_assoc` are BOTH proved axiom-clean. Only ingredient (2) remains:\n- **Build the `μ`-slide naturality helper** — naturality of (whiskered) `sectionsMul` under a\n  general comparison `Γ(μ)` (vs a single `η` in B2/B3 `sectionsMul_whiskerRight_unit` /\n  `sectionsMul_whiskerLeft_unit`). This slides `Γ(μ_{na,nb})` / `Γ(μ_{nb,nc})` past the outer\n  `sectionsMul`. It is the analogue of B2/B3 with `Γ(μ)` in place of `η`.\n- **Then assemble B7** mirroring `sectionsMul_mul_one`: combine (1)+(2)+(3) modulo the\n  `sectionsCast (add_assoc …)` reindexing (analogue of `hμ_n0` in `sectionsMul_mul_one`).\n- Dispatch: effort-breaker to name the (2) `μ`-slide helper as a blueprint block (if not already\n  pinned), then a prover lane. The B6 generic-`M`+`exact` idiom is now proven in-file precedent for\n  any residual coherence.\n\n### 2. Cleanup (prover lane, mechanical) — stale comments the auditor flagged (4 major)\nReview cannot edit `.lean`. Have the next prover/cleanup pass remove/correct:\n- `SectionGradedRing.lean` succ-branch **L3181–3207** (\"REMAINING BLOCKER (iter-024 …)\", \"NEXT (the\n  path, fully scoped) …\") — now describe a solved problem; B6 is closed. Replace with a one-line\n  \"closed iter-025 via `tensorPowAdd_assoc_succ_core`\" (the close-comment at L3229–3234 already\n  states this; the L3181–3207 block is the stale duplicate).\n- `sectionsMul_mul_assoc` body **L3278, L3285–3286, L3288** — call B4/B6 \"the single remaining\n  sorry\"/\"typed sorry\"; B4 and B6 are both proved. Update to: (1)=B5 and (3)=B6 done; only (2)\n  `μ`-slide remains.\n- Minor: **L2534–2535** \"at the top of this file\" cross-ref for the L1194 `maxHeartbeats` option is\n  inaccurate (it's mid-file) — reword.\n\n### 3. Blueprint hygiene (planner / blueprint-writer)\n- **`lem:sectionMul_coherent` bundling** (lvbc minor): the chapter's bundled `\\lean{}` block ties the\n  2 closed unit laws to the 2 open assoc/comm laws, so sync can't credit the finished halves. Consider\n  splitting the `\\lean{}` block so the unit laws + (once B7 lands) assoc can be `\\leanok`'d\n  independently of the FALSE-for-general-`L` comm law. Low priority.\n- **Coverage debt**: new `private lemma tensorPowAdd_assoc_succ_core` has no blueprint entry\n  (helper-exempt per lvbc, but the \"Lean ⇒ tex\" doctrine wants an entry). Optionally add a thin\n  pinned block under `Picard_SectionGradedRing.tex`.\n\n### 4. DO NOT re-assign (gated / no consumer)\n- `sectionsMul_mul_comm` (L3336), `tensorPowAdd_comm` (L3315), `tensorBraiding_self_eq_id_of_isInvertible`\n  (L3301): invertibility-gated FUTURE, no consumer until `GCommSemiring` assembly exists.\n  `sectionsMul_mul_comm` is FALSE for general `L`. The honest typed sorries are correct; leave them.\n- 4 χ sorries in `QuotScheme.lean` (L126/165/201/228): χ-semantic, need the cohomology leg this i=0\n  leg lacks. Deferred (unchanged). Do NOT build an H⁰ `Φ_s` under the χ label.\n\n## Reusable pattern discovered (add to Knowledge Base)\n**generic-`M` core + pinned `exact` crosses the `LocalizedMonoidal`/`X.Modules` comp-instance\ndiamond.** When a canonical monoidal-coherence goal stalls because `≫` mixes the native `X.Modules`\ncomp and the `LocalizedMonoidal` comp (`rw`/`simp`/`erw`/`hc`-bridge all no-match; `erw` 200k-whnf\ntimeout): (a) abstract the entire canonical hom-equation to a `private lemma … {M}[Category M]\n[MonoidalCategory M]` over a free monoidal `M` — develop it there (ONE instance ⇒ NO diamond ⇒\n`lean_run_code` iterates fast), closing with explicit fold/cancellation + ONE\n`associator_inv_naturality_left` + `whisker_exchange` THEN `monoidal` (`monoidal` alone stalls on\nnon-structural atoms / opaque iso pairs); (b) wire via `exact core (M := LocalizedMonoidal …) …`\nwith `set_option maxRecDepth 4000 in` and the goal-only isos pinned explicit so heads align —\n`exact`'s `isDefEq` bridges the rfl-defeq diamond. Same idiom as `tensorObjAssoc_eta_factor_sheaf`\n(B4) and ★. **No staging into sub-lemmas needed** — the monolithic generic-`M` core closes in one\nchain once built in the diamond-free generic setting.\n"
}
