[
  {
    "name": "AlgebraicJacobian_Picard_SectionGradedRing.lean.md",
    "content": "# AlgebraicJacobian/Picard/SectionGradedRing.lean\n\nIter 006 (prover, mode `prove`). SNAP-S0 graded-section residue.\n\n## Summary\n\n- **Sorry count: 9 → 3.** Cold build `lake build AlgebraicJacobian.Picard.SectionGradedRing`\n  succeeds (exit 0, 2439 jobs). `#print axioms` on the closed chain =\n  `[propext, Classical.choice, Quot.sound]` (NO `sorryAx`).\n- **Closed sorry-free (6):**\n  - `sectionsCast` (`def:sectionsCast`)\n  - `sectionsCast_refl` (`lem:sectionsCast_refl`)\n  - `gradedMonoid_eq_of_cast` (`lem:gradedMonoid_eq_of_cast`)\n  - `instGMulNatSectionDeg` — the `GradedMonoid.GMul (sectionDeg L)` instance (`def:sectionGradedGMul`)\n  - `instGOneNatSectionDeg` — the `GradedMonoid.GOne (sectionDeg L)` instance (`def:sectionGradedGOne`)\n  - `sectionsMul_one_mul` — left unitality, the first of the four `lem:sectionMul_coherent` laws.\n    Verified axiom-clean.\n- **Still open (3 sorries), all reduced + structured (NOT bare):**\n  - `tensorPowAdd_zero_right` — extracted standalone lemma\n    `tensorPowAdd L n 0 = tensorObjRightUnitor (tensorPow L n)`, proved by `induction n`. **Base case\n    `n=0` CLOSED** (`unitors_equal` descended through `sheafification`, reflexive `eqToIso` dropped).\n    Only the `succ` inductive step is `sorry` (the triangle coherence). `sectionsMul_mul_one`'s own\n    body is now **sorry-free** and closes the moment this step lands.\n  - `sectionsMul_mul_assoc` — reduced via `simp only [gMul_mul_apply]`; gap = associativity\n    constraint of `tensorPowAdd`.\n  - `sectionsMul_mul_comm` — reduced via `simp only [gMul_mul_apply]`; gap = commutativity\n    constraint of `tensorPowAdd`.\n- Attempted adjacent sorries beyond the assigned set: yes — pushed `mul_one` to a single\n  coherence gap and reduced `mul_assoc`/`mul_comm`.\n\n## Per-declaration log\n\n### sectionsCast (RESOLVED)\n- **Approach:** image under the evaluation functor `toPresheafOfModules X ⋙\n  PresheafOfModules.evaluation X.ringCatSheaf.obj (op ⊤)` of `eqToIso (congrArg (tensorPow L) h)`,\n  then `.toLinearEquiv`. Typechecks directly.\n\n### sectionsCast_refl (RESOLVED)\n- `by ext x; rfl` — the `mapIso` of `eqToIso rfl` collapses definitionally to the identity.\n\n### gradedMonoid_eq_of_cast (RESOLVED)\n- `obtain ⟨i,x⟩, ⟨j,y⟩; obtain rfl := h; simp [sectionsCast_refl] at h2; subst h2; rfl`.\n\n### instGMulNatSectionDeg (RESOLVED)\n- `mul a b = ((tensorPowAdd L i j).hom.val.app (op ⊤)).hom (sectionsMul _ _ (a ⊗ₜ b))`.\n- **Key:** the `⊗ₜ` ring annotation must be the *CommRing* form\n  `(X.sheaf.obj ⋙ forget₂ CommRingCat RingCat).obj (op ⊤)`, NOT `↥(X.ringCatSheaf.obj.obj (op ⊤))`\n  (the `CommSemiring`/`CommRing` instance for `TensorProduct` is keyed to the composition form).\n\n### instGOneNatSectionDeg (RESOLVED)\n- `one := (1 : ↥(X.ringCatSheaf.obj.obj (op ⊤)))`; `(unit R).obj X = ModuleCat.of _ (R.obj X)` so\n  the degree-0 carrier is the ring itself, defeq-accepted.\n\n### sectionsMul_one_mul (RESOLVED, axiom-clean)\n- **Route (decisive):** the lax-monoidal unit law via the sheafification-adjunction.\n  1. `gMul_mul_apply`, `gOne_one_eq` expose the explicit `Γ(μ_{0,n}) ∘ sectionsMul` term.\n  2. `tensorPowAdd L 0 n` is the base case `= tensorObjUnitIso (tensorPow L n) ≪≫ eqToIso(...)` (rfl);\n     the inner `eqToIso` pairs with the outer `sectionsCast (zero_add n)` and the two cancel\n     (`sectionsCast_sectionsCast`) — handled by `change` to the nested-cast form.\n  3. The core `tensorObjUnitIso_hom_sectionsMul` finishes.\n- **Key insight / dead-ends:**\n  - The adjunction right adjoint is `SheafOfModules.forget ⋙ restrictScalars (𝟙)`, NOT just\n    `forget`/`.val`. `rw`/`simp` with `unit_naturality_assoc` / `right_triangle_components` FAIL on\n    the `𝟙_` vs `(toP).obj (unitModule X)` and `.val` vs `(forget⋙restrictScalars).map`\n    syntactic gaps — but `erw` (defeq matching) succeeds for these *morphism-level* structural\n    rewrites. (This is a structural rewrite, distinct from the value-`ModuleCat` element diamond\n    that the memory warns `erw` fails on.)\n  - The `ModuleCat` left-unitor on `1 ⊗ₜ a` is defeq to `1 • a`: closed by\n    `change (1 : ...) • a = a; rw [one_smul]`.\n  - `Iso.trans_assoc` / `Category.assoc` `rw` mysteriously fail to match here (same family of\n    syntactic issues); the element-level `sectionsCast_sectionsCast` route sidesteps them.\n\n### sectionsMul_mul_one (body sorry-free; depends on `tensorPowAdd_zero_right` succ step)\n- Full body in place and **sorry-free**: `gMul_mul_apply, gOne_one_eq` →\n  `tensorPowAdd_zero_right L n` → `tensorObjRightUnitor_hom_sectionsMul` → `sectionsCast_self`.\n- Closes automatically once `tensorPowAdd_zero_right`'s `succ` step lands.\n\n### tensorPowAdd_zero_right (PARTIAL — base done, succ step `sorry`)\n- `tensorPowAdd L n 0 = tensorObjRightUnitor (tensorPow L n)`, by `induction n`.\n- **base `n=0` CLOSED:** `change` to the base-case unfolding, drop the reflexive\n  `eqToIso (congrArg (tensorPow L) (Nat.zero_add 0).symm)` via\n  `Subsingleton.elim _ rfl; eqToIso_refl; Iso.trans_refl`, then `change` to the\n  `mapIso (λ_) ≪≫ counit = mapIso (ρ_) ≪≫ counit` form, `congr 2; apply Iso.ext;\n  exact MonoidalCategory.unitors_equal`.\n- **succ `n=k+1` `sorry`:** goal\n  `tensorPowAdd L (k+1) 0 = tensorObjRightUnitor (tensorObj (tensorPow L k) L)`, with IH\n  `tensorPowAdd L k 0 = tensorObjRightUnitor (tensorPow L k)`. After unfolding the `succ` branch of\n  `tensorPowAdd` and rewriting by `ih`, the goal is the monoidal **triangle identity**\n  `assoc (L^k,L,unit) ≪≫ whiskerLeft (L^k) (braiding L unit) ≪≫ (assoc (L^k,unit,L)).symm ≪≫\n  whiskerRight (rightUnitor L^k) L ≪≫ eqToIso = rightUnitor (L^k ⊗ L)`. **Genuine hard residue:**\n  the participating isos (`tensorObjAssoc`, `tensorBraiding`, `tensorObjRightUnitor`, the whisker\n  isos) are bespoke sheaf-level isos defined via `sheafification`, NOT a registered\n  `MonoidalCategory` instance, so Mathlib's `coherence` tactic does not apply; it must be proved by\n  unfolding each iso's definition and descending to the presheaf-level triangle coherence through\n  `sheafification` functoriality (the same `erw`-through-naturality skeleton, but threaded through\n  `tensorObjAssoc`'s multi-segment definition).\n\n### sectionsMul_mul_assoc (PARTIAL — reduced)\n- `simp only [gMul_mul_apply]` unfolds both nested mults. Gap = the **associativity constraint** of\n  `tensorPowAdd` (`lem:sheafTensorPow_add`) + naturality of `sectionsMul`; an associativity\n  induction mirroring `tensorPowAdd`'s recursion (analogue of `hμn0`).\n\n### sectionsMul_mul_comm (PARTIAL — reduced)\n- `simp only [gMul_mul_apply]`. Gap = the **commutativity constraint** of `tensorPowAdd` (μ_{na,nb}\n  vs μ_{nb,na} after `tensorBraiding`) + symmetry of `sectionsMul`; descends from the presheaf\n  hexagon/symmetry through sheafification.\n\n## Reusable idiom established (for the remaining 3)\nThe proven `tensorObjUnitIso_hom_sectionsMul` / `tensorObjRightUnitor_hom_sectionsMul` are the\n**template** for the coherence-through-sheafification proofs:\n`e1` (rfl, in `(forget⋙restrictScalars (𝟙)).map ((sheafification (𝟙)).map _ ≫ counit)` form) →\n`Functor.map_comp` → `erw [adj.unit_naturality_assoc, adj.right_triangle_components, comp_id]` →\nevaluate at `op ⊤` on the element via `congrArg (fun φ => (φ.app (op ⊤)).hom z)` + `key.trans`.\nThe associator/braiding versions need the same skeleton plus `tensorObjAssoc`/`tensorBraiding`\nnaturality, inside an induction on the relevant index.\n\n## Needs blueprint entry\nThe following NEW project-local declarations were added (no blueprint block yet). Reviewer/planner:\nadd `\\label`+`\\lean`+`\\uses` so the 1-to-1 Lean↔blueprint correspondence holds. All are private\nhelper lemmas in `AlgebraicGeometry.Scheme.Modules`.\n- `gMul_mul_apply` — defeq unfolding of `GradedMonoid.GMul.mul` (rfl). Uses: `tensorPowAdd`,\n  `sectionsMul`.\n- `gOne_one_eq` — defeq unfolding of `GradedMonoid.GOne.one` (rfl).\n- `sectionsCast_apply` — defeq unfolding of `sectionsCast` on an element (rfl).\n- `sectionsCast_sectionsCast` — two transports along inverse equalities cancel. Uses:\n  `sectionsCast_refl`.\n- `sectionsCast_self` — transport along a reflexive index equality is the identity. Uses:\n  `sectionsCast_refl`.\n- `tensorObjUnitIso_hom_sectionsMul` — core left-unit identity at the top open (the math of\n  `sectionsMul_one_mul`). Uses: `sheafificationAdjunction.unit_naturality_assoc`,\n  `right_triangle_components`, `ModuleCat` left unitor, `tensorObjUnitIso`, `sectionsMul`.\n- `tensorObjRightUnitor_hom_sectionsMul` — core right-unit identity (mirror). Uses the same +\n  `tensorObjRightUnitor`.\n\n## Blueprint marker readiness (for sync/review)\nReady for `\\leanok` (sorry-free, axiom-clean): `def:sectionsCast`, `lem:sectionsCast_refl`,\n`lem:gradedMonoid_eq_of_cast`, `def:sectionGradedGMul`, `def:sectionGradedGOne`, and the left-unit\nleg of `lem:sectionMul_coherent` (`sectionsMul_one_mul`). The other three legs of\n`lem:sectionMul_coherent` (`sectionsMul_mul_one/_mul_assoc/_mul_comm`) remain `sorry` (coherence\ninductions) — leave unmarked.\n\n## Why I stopped\n`Real progress`: closed 6 of 9 sorries (`sectionsCast`, `sectionsCast_refl`,\n`gradedMonoid_eq_of_cast`, `instGMulNatSectionDeg`, `instGOneNatSectionDeg`, `sectionsMul_one_mul`),\nall axiom-clean (`#print axioms` = `[propext, Classical.choice, Quot.sound]`), cold-build verified\n(2439 jobs, exit 0); added 7 compiling helper lemmas; gave `sectionsMul_mul_one` a fully sorry-free\nbody (gated only on `tensorPowAdd_zero_right`); **closed the base case** of\n`tensorPowAdd_zero_right`; reduced `mul_assoc`/`mul_comm` to their named coherence constraints.\n\nThe 3 remaining sorries (`tensorPowAdd_zero_right` succ step, `sectionsMul_mul_assoc`,\n`sectionsMul_mul_comm`) are the genuine SNAP-S0 residue: the unit/associativity/commutativity\n**coherence constraints of the inductively-defined comparison family `tensorPowAdd`** — each an\ninduction whose inductive step is the triangle/pentagon/hexagon coherence threaded through the\nbespoke sheaf-level isos `tensorObjAssoc`/`tensorBraiding` (defined via `sheafification`, so\nMathlib's `coherence` tactic does not apply). The one-step `erw`-through-(adjunction\nnaturality+triangle) idiom — established and reused three times in this file\n(`tensorObjUnitIso_hom_sectionsMul`, `tensorObjRightUnitor_hom_sectionsMul`, and the base case of\n`tensorPowAdd_zero_right`) — is the proven foundation these inductive steps build on. Not attempted\nto completion this session due to size, not a mathematical obstacle. Next concrete step: the `succ`\nbranch of `tensorPowAdd_zero_right` (unfold the `succ` arm of `tensorPowAdd`, `rw [ih]`, then prove\nthe sheaf-level triangle identity by descending each segment through `sheafification`); closing it\nimmediately closes `sectionsMul_mul_one`.\n"
  },
  {
    "name": "blueprint-clean-sgr.md",
    "content": "# Blueprint Clean Report — iter-007 — sgr\n\n**Target:** `blueprint/src/chapters/Picard_SectionGradedRing.tex`\n**Scope:** Monoidal-localization-transport subsection (lines 358–506)\n**Status:** DONE — 2 citation blocks replaced, 1 prose trim applied.\n\n---\n\n## Changes made\n\n### 1. `lem:monoidalLocalization_mathlib` — citation replacement + prose trim\n\n**Citation (`% Mathlib pointers:` → `% SOURCE:` / `% SOURCE QUOTE:`).**\nThe 7-line `% Mathlib pointers:` comment (internal Lean line-number annotations) was replaced by a standard 14-line `% SOURCE:` / `% SOURCE QUOTE:` block containing verbatim declarations extracted from:\n- `Mathlib/CategoryTheory/Localization/Monoidal/Basic.lean`: `class MorphismProperty.IsMonoidal`, `def LocalizedMonoidal`, and `noncomputable def μ`\n- `Mathlib/CategoryTheory/Localization/Monoidal/Braided.lean`: cited in the `% SOURCE:` line\n\n**Prose trim.**  \nRemoved the parenthetical `(the two fields of the class, assembled by the builder \\(\\mathrm{IsMonoidal.mk'}\\))` — this was Lean-implementation narrative (a constructor name) that does not belong in mathematical prose. The surrounding sentence now reads cleanly: \"…satisfying $\\mathrm{MorphismProperty.IsMonoidal}$: $W$ is multiplicative and stable under left- and right-whiskering by arbitrary objects.\"\n\n### 2. `lem:moduleSheafification_isLocalization_mathlib` — citation replacement\n\n**Citation (`% Mathlib pointers:` → `% SOURCE:` / `% SOURCE QUOTE:`).**\nThe 4-line `% Mathlib pointers:` comment was replaced by a 6-line `% SOURCE:` / `% SOURCE QUOTE:` block containing the verbatim `instance` declaration from:\n- `Mathlib/Algebra/Category/ModuleCat/Sheaf/Localization.lean` (lines 48–50)\n\n---\n\n## Blocks left unchanged\n\n- **Subsection intro prose** (lines 361–370): mathematically precise, no leakage.\n- **`def:sheafModule_W_isMonoidal`** (definition + proof): no Lean leakage, no verbosity issues; the remark \"stated there for the unit, but established for an arbitrary morphism inverted by sheafification\" is mathematical commentary about the scope of the argument, not implementation narrative.\n- **`def:sheafModule_monoidalStructure`** (definition + proof): clean. The final sentence \"The three coherence identities required downstream for the section-graded-ring assembly hence hold by inheritance rather than by separate induction.\" is forward-linking mathematical motivation, not project history.\n- All pre-iter-007 blocks: untouched per constraint.\n- All `\\leanok` / `\\mathlibok` markers: untouched per constraint.\n\n---\n\n## Source files consulted (verbatim quotes extracted from)\n\n| Mathlib path | Key declarations quoted |\n|---|---|\n| `Mathlib/CategoryTheory/Localization/Monoidal/Basic.lean` | `MorphismProperty.IsMonoidal` (L44–46), `def LocalizedMonoidal` (L86–88), `def μ` (L184–185) |\n| `Mathlib/Algebra/Category/ModuleCat/Sheaf/Localization.lean` | `instance IsLocalization` (L48–50) |\n| `Mathlib/CategoryTheory/Localization/Monoidal/Braided.lean` | cited only (braided/symmetric transport) |\n\n---\n\n## No spawned subagents\n\nNo missing references were identified that required spawning a `reference-retriever`.\n"
  },
  {
    "name": "blueprint-writer-monoidal-localization.md",
    "content": "# Blueprint Writer Report: monoidal-localization\n**Status:** COMPLETE\n\n## Changes\n- New `\\subsection{Monoidal structure by localization transport}` in `sec:sgr_tensor_powers`, inserted after `lem:isIso_sheafification_whiskerRight_unit` proof, before `cor:sheafTensorObjAssoc`.\n- Add `lem:monoidalLocalization_mathlib` (`\\mathlibok`, `\\lean{CategoryTheory.Localization.LocalizedMonoidal}`): transport thm, pentagon+triangle, braided/symmetric→hexagon, strong-monoidal `μ`.\n- Add `lem:moduleSheafification_isLocalization_mathlib` (`\\mathlibok`, `\\lean{PresheafOfModules.sheafification}`): sheafification is localization for `W'=J.W.inverseImage(toPresheaf R₀)`.\n- Add `def:sheafModule_W_isMonoidal` (`\\lean{...W_isMonoidal}`) + 1-line proof: `W'.IsMonoidal` via whisker brick + braiding conjugation.\n- Add `def:sheafModule_monoidalStructure` (`\\lean{...monoidalCategory}`) + proof: instantiate transport → symmetric monoidal `X.Modules`, coherences inherited.\n- `leandag`: no unknown_uses, no conflicts, none isolated.\n- No Mathlib source on disk → no fabricated `% SOURCE QUOTE`; file:line pointers kept as plain `%` comments per anchor convention.\n\n## Notes / Strategy\n- `lem:moduleSheafification_isLocalization_mathlib` shares `\\lean{PresheafOfModules.sheafification}` with existing `lem:presheafModule_sheafification_mathlib`; planner should point it at the real localization instance (`…/Sheaf/Localization.lean:48`). Lean names `W_isMonoidal`/`monoidalCategory` are natural-form placeholders for planner reconciliation.\n"
  },
  {
    "name": "lean-auditor-iter006.md",
    "content": "# Lean Audit Report\n\n## Slug\niter006\n\n## Iteration\n006\n\n## Scope\n- files audited: 1\n- files skipped (per directive): 0\n\n---\n\n## Per-file checklist\n\n### AlgebraicJacobian/Picard/SectionGradedRing.lean\n- **outdated comments**: none (in focus range 1840–2110)\n- **suspect definitions**: 1 flagged\n- **dead-end proofs**: 4 flagged (3 explicit sorries + 1 transitive contamination)\n- **bad practices**: 2 flagged (erw, double-change pattern)\n- **excuse-comments**: 1 flagged\n- **notes**:\n  - **Line 2031** (`tensorPowAdd_zero_right`, succ case): `sorry`. The succ step requires the triangle identity relating `tensorObjAssoc`, `tensorBraiding`, and `tensorObjRightUnitor` at the sheaf level, descended from the presheaf-level monoidal coherence through sheafification. Confirmed **genuinely open** — no silent discharge via axiom, `Classical.choice`, or tautological `rfl`.\n  - **Line 2091** (`sectionsMul_mul_assoc`): `sorry`. After `simp only [gMul_mul_apply]` the remaining goal is the associativity constraint of the `tensorPowAdd` comparison family (a pentagon-equation analog). Confirmed **genuinely open**.\n  - **Line 2107** (`sectionsMul_mul_comm`): `sorry`. After `simp only [gMul_mul_apply]` the remaining goal is the commutativity constraint (hexagon/braiding analog). Confirmed **genuinely open**.\n  - **Lines 2053–2072** (`sectionsMul_mul_one`): appears closed — no `sorry` in its body. However, it calls `tensorPowAdd_zero_right L n` (line 2063), and the `succ` case of `tensorPowAdd_zero_right` is `sorry`. For `n ≥ 1` the theorem is not actually proved. This is a **silent transitive sorry-contamination**: a downstream reader would treat `sectionsMul_mul_one` as a closed theorem for all `n`, which it is not.\n  - **Lines 2036–2047** (`sectionsMul_one_mul`): genuinely closed. Calls `sectionsCast_sectionsCast` (closed, line 1902) and `tensorObjUnitIso_hom_sectionsMul` (closed, line 1918). The `change` on line 2041 unfolds `tensorPowAdd L 0 n` by its base-case definitional body (`tensorObjUnitIso ≪≫ eqToIso ...`); the tactic is sound given the match-on-zero definition of `tensorPowAdd`.\n  - **Lines 1868–1878** (`GradedMonoid.GMul`, `GradedMonoid.GOne` instances): Anonymous instances on an `abbrev`-type family (`sectionDeg L`). `GradedMonoid.GOne.one := (1 : ↥(X.ringCatSheaf.obj.obj (Opposite.op ⊤)))` relies on definitional equality `tensorPow L 0 = unitModule X` and `(unitModule X).val.obj U = X.ringCatSheaf.obj.obj U`. Confirmed correct: the private lemma `gOne_one_eq := rfl` (line 1890) witnesses the defeq. The docstring phrase \"via the canonical `Γ𝒪`-module isomorphism\" is slightly imprecise — no explicit iso is applied; the isomorphism is absorbed by the defeq — but not wrong.\n  - **Line 1881–1899** (`gMul_mul_apply`, `gOne_one_eq`, `sectionsCast_apply`): All `:= rfl`. These are legitimate unfold handles — each RHS is syntactically the body of the corresponding instance/definition. Not vacuous; they serve as rewrite lemmas that bypass definitional unfolding in tactic proofs.\n  - **Lines 1942–1944 and 1988–1990**: `erw` for three adjunction-naturality rewrites (`unit_naturality_assoc`, `right_triangle_components`, `Category.comp_id`). These are real Mathlib `Adjunction` fields. The `erw` is used to defeat universe-level unification failures that `rw` cannot resolve; standard and accepted in Lean 4 categorical proofs. No evidence of masking a genuine gap.\n  - **Lines 1933–1940 and 1979–1986**: `have e1 : ... := rfl` in both `tensorObjUnitIso_hom_sectionsMul` and `tensorObjRightUnitor_hom_sectionsMul`. Each asserts that `(tensorObjUnitIso G).hom.val` / `(tensorObjRightUnitor G).hom.val` unfolds as a specific functor-map composition. Correct by definitional unfolding of `tensorObjUnitIso`/`tensorObjRightUnitor` (defined as `sheafification.mapIso ... ≪≫ sheafificationCounitIso`). The `rfl` body is legitimate.\n  - **Lines 2014–2027** (`tensorPowAdd_zero_right`, zero case): Fully proved. The proof:\n      1. Uses `Subsingleton.elim` to reduce the `eqToIso` argument to `rfl`, collapsing `eqToIso_refl` and `Iso.trans_refl`.\n      2. Uses two `change` tactics to unfold `tensorObjUnitIso` and `tensorObjRightUnitor` to their `sheafification.mapIso ≪≫ sheafificationCounitIso` forms.\n      3. Applies `congr 2; apply Iso.ext; exact MonoidalCategory.unitors_equal`.\n      Each step is honest. No gap masked.\n  - **Lines 1807–1830** (planning comment): Contains the phrase \"those instance defs are **LEFT UNSCAFFOLDED here**\" referring to `sectionGradedRing_gcommSemiring` / `sectionGradedModule_gmodule`. This is an in-file admission that downstream instances are not yet defined. Classified as an excuse-comment at **major** severity (no existing declaration is excused, but the file explicitly records a known gap in code that will be built on).\n  - **`tensorObjAssoc` (line 1610, outside strict focus range)**: Used directly by `tensorPowAdd` succ case and implicitly in the `sorry`-carrying `tensorPowAdd_zero_right` succ comment. Its construction goes via a double-braiding detour: `segment-3` applies `braiding_{A, B⊗C}` then a right-whiskered sheafification unit then `braiding_{B⊗C, A}`. This gives the correct *type* `(A⊗B)⊗C ≅ A⊗(B⊗C)` but produces a non-standard composite that may differ from the canonical Mac Lane associator by an endomorphism. In a symmetric monoidal category the hexagon equations do constrain the associator, but the coherence of this specific construction (especially the pentagon equation) is not separately verified in the file. This upstream potential non-canonicity could be a root cause of why `tensorPowAdd_zero_right` succ and `sectionsMul_mul_assoc` are still open.\n\n---\n\n## Must-fix-this-iter\n\n- `SectionGradedRing.lean:2031` — `tensorPowAdd_zero_right` succ case, `sorry`. Confirmed genuinely open (triangle identity for sheaf `tensorObjAssoc` / braiding / right-unitor). Why must-fix: `sectionsMul_mul_one` depends on this; it blocks the right-unitality coherence law for the graded ring.\n- `SectionGradedRing.lean:2091` — `sectionsMul_mul_assoc`, `sorry`. Confirmed genuinely open (associativity of `tensorPowAdd` comparison family). Why must-fix: load-bearing coherence theorem for the graded monoid assembly; suspends `GMonoid`/`GSemiring` instance work.\n- `SectionGradedRing.lean:2107` — `sectionsMul_mul_comm`, `sorry`. Confirmed genuinely open (commutativity of `tensorPowAdd`). Why must-fix: load-bearing coherence theorem; required for `GCommSemiring`.\n- `SectionGradedRing.lean:2053–2072` — `sectionsMul_mul_one` appears closed but is transitively sorry-contaminated via `tensorPowAdd_zero_right` (succ case sorry). For `n ≥ 1` this theorem is not proved. Why must-fix: a public theorem of a graded ring being presented as proved for all `n` when it holds only for `n = 0` is a substantive correctness gap that downstream assembly will silently propagate.\n\n## Major\n\n- `SectionGradedRing.lean:1809` — Planning comment \"those instance defs are LEFT UNSCAFFOLDED here\" documents missing `sectionGradedRing_gcommSemiring` / `sectionGradedModule_gmodule` instances. A building block admitting its own incompleteness. No existing declaration is wrong, but the comment is an in-file promise of absent code.\n- `SectionGradedRing.lean:1610` (`tensorObjAssoc`, outside focus range) — Double-braiding construction of the sheaf associator. Gives a valid iso of the right type but may differ from the canonical Mac Lane associator, potentially breaking the pentagon equation. This is the upstream definition on which `tensorPowAdd` succ, `tensorPowAdd_zero_right` succ, and `sectionsMul_mul_assoc` all depend; if it is non-canonical the open sorries may never close in the current form.\n\n## Minor\n\n- `SectionGradedRing.lean:1942–1944` and `1988–1990` — `erw` for adjunction naturality rewrites. Acceptable in practice but `erw` is a code smell; a future Mathlib update could silently break these if the adjunction API changes.\n- `SectionGradedRing.lean:2082–2090` and `2100–2106` — Long inline planning comments inside `sectionsMul_mul_assoc` / `sectionsMul_mul_comm` describing what the sorry needs. Useful for prover context but these are better kept in the blueprint chapter than in the Lean source; they bloat the file and can become stale.\n- `SectionGradedRing.lean:1876` — `GradedMonoid.GOne` docstring says \"via the canonical `Γ𝒪`-module isomorphism\" but no iso is explicitly applied; the element is cast by definitional equality. Technically correct (confirmed by `gOne_one_eq := rfl`) but the docstring is imprecise.\n- `SectionGradedRing.lean:1868,1876` — `GradedMonoid.GMul` and `GOne` instances are anonymous. Naming them (e.g., `instance sectionDeg_gMul`, `sectionDeg_gOne`) would make diagnostic output and `#check` easier. Not a correctness issue.\n\n## Excuse-comments (always called out separately)\n\n- `SectionGradedRing.lean:1809`: \"those instance defs are **LEFT UNSCAFFOLDED here**\" (inside the `Planner strategy` block, referring to `sectionGradedRing_gcommSemiring` / `sectionGradedModule_gmodule`). Severity: **major**. This is a planning-phase admission of absent code embedded in the production file. Not attached to a specific wrong declaration, but documents a known completeness gap.\n\n---\n\n## Severity summary\n\n- **must-fix-this-iter**: 4 — lines 2031, 2091, 2107 (confirmed genuine open sorries), and `sectionsMul_mul_one` (lines 2053–2072, silent transitive sorry-contamination). These block downstream graded-ring assembly.\n- **major**: 2 — planning comment at line 1809 (missing downstream instances); `tensorObjAssoc` double-braiding non-canonicity (line 1610, upstream of focus range).\n- **minor**: 4 — `erw` style (×2), long inline planning comments (×2), docstring imprecision, anonymous instances.\n- **excuse-comments**: 1 (major, counted above).\n\n**Overall verdict**: The three explicitly open sorries are confirmed genuinely open with substantive mathematical content remaining; none are silently discharged. A fourth must-fix finding is the transitive sorry-contamination of `sectionsMul_mul_one`, which presents as a closed public theorem for all `n` but is only proved for `n = 0`. The closed proofs in the focus range (`sectionsMul_one_mul`, the two unit-iso helper lemmas, all `rfl`-body helpers) are honest.\n"
  },
  {
    "name": "lean-vs-blueprint-checker-sgr-iter006.md",
    "content": "# Lean ↔ Blueprint Check Report\n\n## Slug\nsgr-iter006\n\n## Iteration\n006\n\n## Files audited\n- Lean: `AlgebraicJacobian/Picard/SectionGradedRing.lean`\n- Blueprint: `blueprint/src/chapters/Picard_SectionGradedRing.tex`\n\nScope: the section-graded-ring residue worked this iter — `def:sectionsCast`,\n`lem:sectionsCast_refl`, `lem:gradedMonoid_eq_of_cast`, `def:sectionGradedGMul`,\n`def:sectionGradedGOne`, the 4-lemma bundle `lem:sectionMul_coherent`, plus the\n7 new private helpers and the gating private lemma `tensorPowAdd_zero_right`.\n(`def:sectionDeg` is checked too, as the carrier the focus blocks depend on.)\n\nAll `\\lean{...}` names below were resolved/verified via `lean_verify` against the\nfile; axiom strings are quoted verbatim.\n\n## Per-declaration\n\n### `\\lean{AlgebraicGeometry.Scheme.Modules.sectionDeg}` (def:sectionDeg)\n- **Lean target exists**: yes (`sectionDeg`, line 1835).\n- **Signature matches**: yes. Blueprint: degree-`m` component `= Γ(X, L^{⊗m}) = L^{⊗m}(X)`.\n  Lean: `abbrev sectionDeg (L : X.Modules) (m : ℕ) : Type u := ↥((tensorPow L m).val.obj (op ⊤))`.\n  The `op ⊤` value of the underlying presheaf = global sections on the top open. ✓\n- **Proof follows sketch**: N/A (definition).\n- **notes**: blueprint says the carrier \"inherits its AddCommGroup / Γ(X,𝒪_X)-module structure\";\n  Lean gets it from the `ModuleCat` object via the coercion. Matches.\n\n### `\\lean{AlgebraicGeometry.Scheme.Modules.sectionsCast}` (def:sectionsCast)\n- **Lean target exists**: yes (`sectionsCast`, line 1841). Verified axiom-clean\n  `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).\n- **Signature matches**: yes. Lean returns a `≃ₗ[Γ(X,𝒪_X)]` from `sectionDeg L i` to\n  `sectionDeg L j` given `h : i = j`, built as `Γ(X,-)` (= `toPresheafOfModules ⋙ evaluation (op ⊤)`)\n  applied via `mapIso` to `eqToIso (congrArg (tensorPow L) h)`, then `.toLinearEquiv`. This is exactly\n  the blueprint's \"apply `Γ(X,-)` to the canonical iso `L^{⊗i} ≅ L^{⊗j}` induced by `h` under the\n  tensor-power functor.\" ✓\n- **Proof follows sketch**: N/A (definition); construction matches prose.\n- **notes**: Generality difference (in Lean's favour) — blueprint says \"Let `L` be an *invertible*\n  sheaf\"; the Lean requires only `L : X.Modules` (no invertibility). The Lean is strictly more\n  general, which is correct and harmless. Minor.\n\n### `\\lean{AlgebraicGeometry.Scheme.Modules.sectionsCast_refl}` (lem:sectionsCast_refl)\n- **Lean target exists**: yes (line 1848, `@[simp]`).\n- **Signature matches**: yes — `sectionsCast L (rfl : i = i) = LinearEquiv.refl _ (sectionDeg L i)`.\n  Blueprint: transport along reflexive `i = i` equals the identity automorphism. ✓\n- **Proof follows sketch**: yes. Blueprint: canonical iso of a reflexive equality is the identity and\n  `Γ` preserves identities. Lean: `ext x; rfl` (the `mapIso` of `eqToIso rfl` collapses definitionally).\n  Same mathematical content. ✓\n\n### `\\lean{AlgebraicGeometry.Scheme.Modules.gradedMonoid_eq_of_cast}` (lem:gradedMonoid_eq_of_cast)\n- **Lean target exists**: yes (line 1856).\n- **Signature matches**: yes — `{a b : GradedMonoid (sectionDeg L)} (h : a.1 = b.1)\n  (h2 : sectionsCast L h a.2 = b.2) : a = b`. `GradedMonoid (sectionDeg L)` is the dependent sum\n  `⨿_n Γ(X, L^{⊗n})` of the blueprint. ✓\n- **Proof follows sketch**: yes. Blueprint: substitute `j = i` via `h`, reduce transport to identity\n  (`sectionsCast_refl`), conclude `x = y`, pairs coincide. Lean: `obtain rfl := h;\n  simp [sectionsCast_refl] at h2; subst h2; rfl`. ✓\n\n### `\\lean{AlgebraicGeometry.Scheme.Modules.instGMulNatSectionDeg}` (def:sectionGradedGMul)\n- **Lean target exists**: yes. The `\\lean{}` hint points at the auto-generated name of the anonymous\n  `instance (L : X.Modules) : GradedMonoid.GMul (sectionDeg L)` (line 1868). `lean_verify` on\n  `AlgebraicGeometry.Scheme.Modules.instGMulNatSectionDeg` resolves and is axiom-clean\n  `[propext, Classical.choice, Quot.sound]`. The `\\lean{}` hint is therefore **correct** (the\n  auto-name matches). ✓\n- **Signature matches**: yes. Blueprint: `(sectionDeg L)_i × (sectionDeg L)_j → (sectionDeg L)_{i+j}`,\n  a `GradedMonoid.GMul`, given by `sectionsMul` followed by `Γ(μ_{i,j})`. Lean `mul a b :=\n  ((tensorPowAdd L i j).hom.val.app (op ⊤)).hom ((sectionsMul (tensorPow L i) (tensorPow L j)).hom\n  (a ⊗ₜ b))` — `μ = tensorPowAdd`, exactly the displayed composite. ✓\n- **Proof follows sketch**: N/A (instance/data).\n\n### `\\lean{AlgebraicGeometry.Scheme.Modules.instGOneNatSectionDeg}` (def:sectionGradedGOne)\n- **Lean target exists**: yes (auto-name of anonymous `instance ... GradedMonoid.GOne (sectionDeg L)`,\n  line 1876). `lean_verify` resolves; axiom-clean `[propext, Classical.choice, Quot.sound]`. ✓\n- **Signature matches**: yes. Blueprint: `1 ∈ (sectionDeg L)_0 = Γ(X, L^{⊗0}) = Γ(X,𝒪_X)`. Lean\n  `one := (1 : ↥(X.ringCatSheaf.obj.obj (op ⊤)))`; `(unit R)` carrier at the top open is the ring\n  itself, so degree-0 carrier is defeq to `Γ(X,𝒪_X)` and `1` lands there. ✓\n- **Proof follows sketch**: N/A (instance/data).\n\n### `\\lean{... sectionsMul_one_mul, sectionsMul_mul_one, sectionsMul_mul_assoc, sectionsMul_mul_comm}` (lem:sectionMul_coherent)\nThe chapter bundles all four legs under one block. Statement-by-statement:\n\n- **`sectionsMul_one_mul`** (left unitality, line 2036) — exists; **axiom-clean**\n  `[propext, Classical.choice, Quot.sound]` (no `sorryAx`). Signature\n  `sectionsCast L (zero_add n) (GMul.mul GOne.one a) = a` matches blueprint\n  `sectionsCast_{0+n=n}(1·a) = a` exactly (incl. the index-cast subscript convention). Proof rides the\n  sheafification unit `η` through the presheaf left-unitor — faithful to the blueprint sketch. ✓ DONE.\n- **`sectionsMul_mul_one`** (right unitality, line 2053) — exists; signature\n  `sectionsCast L (add_zero n) (GMul.mul a GOne.one) = a` matches blueprint `sectionsCast_{n+0=n}(a·1)=a`.\n  **Carries `sorryAx`** (verified): its own body is structured/sorry-free but it depends on the private\n  lemma `tensorPowAdd_zero_right`, whose `succ` step is `sorry` (line 2031). Open, per directive.\n- **`sectionsMul_mul_assoc`** (line 2077) — exists; signature\n  `sectionsCast L (add_assoc na nb nc) ((a·b)·c) = a·(b·c)` matches blueprint. **Carries `sorryAx`**\n  (body `sorry` at line 2091, after a `simp only [gMul_mul_apply]` reduction). Open, per directive.\n- **`sectionsMul_mul_comm`** (line 2096) — exists; signature\n  `sectionsCast L (add_comm na nb) (a·b) = b·a` matches blueprint. **Carries `sorryAx`** (body `sorry`\n  at line 2107). Open, per directive.\n- **notes**: The blueprint `lem:sectionMul_coherent` block has **no `\\leanok`** on either its statement\n  or its proof, so the three open `sorry`s are consistent with an honestly-unformalized block — not a\n  disguised/placeholder claim. All four statement signatures (including the `sectionsCast` index-cast\n  transport convention and the precise subscripts `zero_add/add_zero/add_assoc/add_comm`) match the\n  prose verbatim. The proof sketch (chapter L772–L791) reduces all four to presheaf-level strict-monoidal\n  identities + `η`-naturality, which is the route the Lean follows.\n\n## Red flags\n\n### Placeholder / suspect bodies\nNone that are deceptive. The three `sorry`s are **expected, directive-acknowledged, and honestly\ntracked**:\n- `tensorPowAdd_zero_right` at line 2031: `succ` branch is `sorry` (the triangle-coherence step); base\n  case is closed. This is the single gap that also keeps `sectionsMul_mul_one` un-closed.\n- `sectionsMul_mul_assoc` at line 2091: `sorry` (associativity constraint of `tensorPowAdd`).\n- `sectionsMul_mul_comm` at line 2107: `sorry` (commutativity constraint of `tensorPowAdd`).\n\nThese are **not** must-fix-this-iter: (a) the directive pre-declares mul_one/assoc/comm as still-sorry;\n(b) the governing blueprint block `lem:sectionMul_coherent` carries no `\\leanok`, so no completion is\nfalsely claimed; (c) the sorries are reduced/structured (not bare), each with an in-code comment naming\nthe precise residual coherence obligation. No fake tautology, no `:= True`, no weakened-wrong definition.\n\n### Excuse-comments\nNone. The comments on the open sorries describe the genuine remaining mathematical obligation\n(triangle / pentagon / hexagon coherence of `tensorPowAdd` through `sheafification`); they are accurate\nwork-notes, not excuses for wrong code.\n\n### Axioms / Classical.choice on non-trivial claims\nNone beyond the standard `[propext, Classical.choice, Quot.sound]` on the closed declarations. No\n`axiom` declarations in the focus region.\n\n## Unreferenced declarations (informational)\n\nPer directive, the 7 new private helpers added this iter have **no blueprint block** (Lean→blueprint\ncoverage gaps). All are `private` and live in `AlgebraicGeometry.Scheme.Modules`:\n\n1. `gMul_mul_apply` (line 1881) — `rfl` unfolding of `GradedMonoid.GMul.mul`. Helper-only; fine unreferenced.\n2. `gOne_one_eq` (line 1890) — `rfl` unfolding of `GradedMonoid.GOne.one`. Helper-only.\n3. `sectionsCast_apply` (line 1896) — `rfl` unfolding of `sectionsCast` on an element. Helper-only.\n4. `sectionsCast_sectionsCast` (line 1902) — two transports along inverse equalities cancel. Helper-only.\n5. `sectionsCast_self` (line 1909) — transport along a reflexive index equality is the identity. Helper-only.\n6. `tensorObjUnitIso_hom_sectionsMul` (line 1918) — **substantive**: the core left-unit identity at the\n   top open (the actual math of `sectionsMul_one_mul`).\n7. `tensorObjRightUnitor_hom_sectionsMul` (line 1966) — **substantive**: the mirror right-unit identity\n   (the math behind `sectionsMul_mul_one`).\n\nAdditional (not in the prover's 7-helper list, but in the same focus region):\n- `tensorPowAdd_zero_right` (line 2010) — `private`, **no blueprint block**, and **contains the `succ`\n  `sorry`**. It is effectively the right-unit coherence of the `tensorPowAdd` family\n  (`μ_{n,0} = ` right unitor). Worth a blueprint sub-block since it is a named, reusable coherence\n  obligation gating `sectionsMul_mul_one`.\n\nItems 1–5 are trivial defeq/bookkeeping helpers; leaving them unreferenced is acceptable. Items 6–7 and\n`tensorPowAdd_zero_right` carry real content but are sub-steps of `lem:sectionMul_coherent`\n/ `lem:sheafTensorPow_add`, which are already in the chapter — so a `% NOTE:` mention rather than full\nblocks is sufficient.\n\n## Blueprint adequacy for this file\n\n- **Coverage** (focus region): 7/7 directive-named blueprint blocks have a correctly-named, correctly-\n  typed `\\lean{...}` target in the file (`sectionDeg`, `sectionsCast`, `sectionsCast_refl`,\n  `gradedMonoid_eq_of_cast`, `instGMulNatSectionDeg`, `instGOneNatSectionDeg`, and the 4-lemma\n  `sectionMul_coherent` bundle). Unreferenced declarations in-region: 5 trivial helpers (acceptable) +\n  2 substantive helpers + 1 substantive gating lemma with a sorry (`tensorPowAdd_zero_right`) — all\n  flagged above.\n- **Proof-sketch depth**: adequate. The chapter preamble (L570–L577) explains the index-cast transport\n  convention precisely, and the four-leg statement (L742–L765) pins each identity with the exact\n  `sectionsCast` subscript the Lean uses. The `lem:sectionMul_coherent` proof sketch (L772–L791) gives\n  the correct high-level route (reduce to strict-monoidal presheaf identities + `η`-naturality).\n  Mildly under-specified on the *induction structure* the Lean actually needs: the sketch does not\n  preview that right-unitality requires a separate induction on `n` (`tensorPowAdd_zero_right`) nor the\n  cast-cancellation lemmas (`sectionsCast_sectionsCast`/`_self`). Not a blocker — the math content is\n  present; the induction is an implementation detail.\n- **Hint precision**: precise. Every `\\lean{...}` in the focus region resolves to an existing\n  declaration of the matching type, **including the two auto-generated instance names**\n  (`instGMulNatSectionDeg`, `instGOneNatSectionDeg`), which `lean_verify` confirms exist and are\n  axiom-clean. No loose or wrong hints found.\n- **Generality**: matches need (with `sectionsCast` strictly more general than the \"invertible `L`\"\n  prose — in the project's favour, no parallel API was forced).\n- **Recommended chapter-side actions** (all minor / non-blocking):\n  - Add `% NOTE:` references (or small blocks) for `tensorObjUnitIso_hom_sectionsMul`,\n    `tensorObjRightUnitor_hom_sectionsMul`, and `tensorPowAdd_zero_right`, since they carry real\n    content and `tensorPowAdd_zero_right` is the named gate for the right-unit law.\n  - Optionally note in the `lem:sectionMul_coherent` proof sketch that right-unitality is proved by a\n    dedicated induction on `n` (the `μ_{n,0}` = right-unitor coherence).\n\n## Severity summary\n\n- **must-fix-this-iter**: none. The three `sorry`s are directive-acknowledged open work on a blueprint\n  block that carries no `\\leanok`; statements are faithful; no fake/placeholder/weakened-wrong bodies;\n  no unauthorized axioms; the `\\lean{}` hints (incl. auto-named instances) are all correct.\n- **major**: none.\n- **minor**:\n  - 7 new private helpers + `tensorPowAdd_zero_right` have no blueprint reference (coverage gap; 5 are\n    trivial helpers, 3 are substantive sub-steps deserving a `% NOTE:`).\n  - `def:sectionsCast` prose says \"invertible sheaf\" while Lean is general over `L : X.Modules` (prose\n    slightly narrower than the formalization; harmless).\n  - `lem:sectionMul_coherent` proof sketch omits the right-unit induction / cast-cancellation detail.\n\nOverall verdict: the Lean faithfully formalizes every directive-named blueprint block — signatures,\nthe index-cast transport convention, and the `\\lean{}` hints (including the auto-generated instance\nnames) all match — with no fake or misleading statements; the only findings are the directive-expected\nopen sorries (mul_one/assoc/comm, gated on `tensorPowAdd` coherence) and minor blueprint-coverage gaps\nfor the new private helpers.\n"
  },
  {
    "name": "mathlib-analogist-tensorobjassoc.md",
    "content": "# Mathlib Analogist: tensorobjassoc\n**Mode:** api-alignment | **Iter:** 007\n\n## Verdicts\n- **`tensorObjAssoc` (double-braiding)**: NEEDS_MATHLIB_GAP_FILL→ALIGN_WITH_MATHLIB (Must-fix). Divergent-with-cost. The hand-rolled associator is non-canonical (auditor right: `β^# ≫ (η▷)^# ≫ β^#` need not equal Mac Lane — unit sits between two braidings on *different* objects, no collapse). Mathlib transports the WHOLE structure; don't fix the iso, replace the approach.\n- **Monoidal transport (b)**: AVAILABLE. `MorphismProperty.IsMonoidal` + `LocalizedMonoidal` give `MonoidalCategory`+associator+pentagon+triangle (`Localization/Monoidal/Basic.lean:44,86,165,172`); `Localization/Monoidal/Braided.lean` gives `SymmetricCategory`+hexagon; strong-monoidal `μ` L184 matches project's objectwise `tensorObj`. Sheaf precedent: `Sheaf.monoidalCategory` `Sites/Monoidal.lean:165`.\n- **Module sheafification IS the localization functor**: `(sheafification α).IsLocalization (J.W.inverseImage (toPresheaf R₀))` `ModuleCat/Sheaf/Localization.lean:48`. Project's `sheafification` = this at `α=𝟙`. `MonoidalPresheaf X` is symmetric: `Presheaf/Monoidal.lean:144`.\n- **whiskerLeft crux (i)**: ALIGN_WITH_MATHLIB. `whiskerLeft` is a real field (`GrothendieckTopology.W.whiskerLeft` `Sites/Monoidal.lean:132`); Mathlib derives whiskerRight FROM it by braiding `:147`. Project already has the *general* whiskerRight (`ztensor_whisker_localIso` L1416, arbitrary `f`, not just units); get whiskerLeft by braiding-conjugating it (symmetric presheaves). Braiding-free associator, yes — but build the full instance, not a one-off iso.\n\n## The gap (small, well-scoped)\nOnly missing input: instance `(J.W.inverseImage (toPresheaf X.ringCatSheaf.obj)).IsMonoidal`, via `IsMonoidal.mk'`:\n- whiskerRight field ← `ztensor_whisker_localIso` (DONE; bridge `W' f ⟺ (toPresheaf).map f ∈ J.W` via `isIso_sheafification_map_iff`).\n- whiskerLeft field ← braiding-conjugate it (`PresheafOfModules.symmetricCategory`).\n- multiplicativity ← free (inverseImage).\nThen `LocalizedMonoidal sheafification W' ε` (ε picks unit = `unitModule X`) ⇒ all coherence inherited.\n\n## NOT free (don't try these)\n- `(J.W (A:=AddCommGrpCat)).IsMonoidal` (`Sites/Monoidal.lean:149`) does NOT transfer — that's the abelian `⊗_ℤ`, not the relative module tensor.\n- `inverseImage`-along-monoidal-functor instance (`Localization/Monoidal/Basic.lean:71`) does NOT fire — `toPresheaf` (modules→Ab) is oplax, not strong monoidal.\n\n## Payoff\nThe 3 SNAP sorries (`tensorPowAdd_zero_right` succ, `sectionsMul_mul_assoc`, `sectionsMul_mul_comm`) become INHERITED from the `MonoidalCategory`/`SymmetricCategory` instance instead of hand inductions over a possibly-unprovable non-canonical associator. Resolves the auditor flag by construction.\n\n## Persistent file\n- `analogies/tensorobjassoc.md` written.\n\nOverall verdict: ALIGN_WITH_MATHLIB — the transport machinery + localization fact exist; one small `W'.IsMonoidal` instance (whiskerRight already done) replaces the double-braiding associator and all planned coherence inductions.\n"
  }
]
