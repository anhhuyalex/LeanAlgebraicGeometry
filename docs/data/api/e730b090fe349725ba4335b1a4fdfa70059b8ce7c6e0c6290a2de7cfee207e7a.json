{
  "content": "# Recommendations — after iter-008 (for the iter-009 plan agent)\n\n## Closest-to-completion — prioritize (SectionGradedRing.lean, SNAP)\nThe iter-008 rewire de-risked all 3 remaining coherence sorries to **mechanical** residuals. The\ncritical path is now ONE small infrastructure step, then a cascade:\n\n1. **Build two whiskering bridges** (template already exists = `tensorBraiding_eq`, the worked\n   instance-diamond finisher):\n   - `tensorObjWhiskerLeftIso_eq : tensorObjWhiskerLeftIso F e = (tensorObjIso …).symm ≪≫ (F ◁ e) ≪≫ tensorObjIso …`\n   - `tensorObjWhiskerRightIso_eq` (the `▷` analogue).\n   Proof = same μ-naturality + `Localization.Monoidal.μ_natural_left/right` simp lemmas, finished with\n   term-mode `Category.assoc` + `erw` (per the instance-diamond pattern).\n2. **`tensorPowAdd_zero_right` succ (L2234)** — after the bridges, close the residual via\n   `MonoidalCategory.triangle` + `unitors_equal`, `tensorObjIso` bridges cancelling in adjacent pairs.\n   **This transitively un-sorrys `sectionsMul_mul_one`** (currently `sorryAx`-contaminated through it,\n   per the auditor — no direct sorry warning, so don't be fooled by the build output).\n3. **`sectionsMul_mul_assoc` (L2294) / `sectionsMul_mul_comm` (L2310)** — DO NOT re-assign bare (same\n   `simp only [gMul_mul_apply]; sorry` for 3 iters). First prove the **iso-level `tensorPowAdd`\n   coherence** (canonical `MonoidalCategory.pentagon` for assoc, `hexagon_forward` for comm, using the\n   now-canonical `tensorObjAssoc`/`tensorBraiding_eq`), THEN push through `Γ(X,-) = .val.app (op ⊤)` +\n   η-naturality (template = the already-green `tensorObjUnitIso_hom_sectionsMul`).\n\n## MEDIUM — verify (lean-auditor `iter008`, major finding)\n- Non-standard attributes `@[instance_reducible]` (L1629 on `monoidalCategory`) and\n  `@[implicit_reducible]` (L1639/L1651 on `braidedCategory`/`symmetricCategory`) from iter-007 have\n  **opaque semantics** and appear in no other project file. Compiler accepts them (likely Mathlib-\n  defined or no-ops; instance exposure is correctly handled by `attribute [local instance]`\n  regardless). Confirm they resolve to something real in the import chain; if not defined, replace with\n  standard `@[reducible]`/`@[instance]`. Not blocking — flag for a prover/refactor pass.\n\n## Coverage debt — blueprint these (review agent does not author prose)\n`archon dag-query unmatched` lists them as `lean_aux`, chapter `\"\"` (invisible to the DAG):\n- `AlgebraicGeometry.Scheme.Modules.tensorObjRightUnitor_eq` (L1679, public) — bridge: hand-built right\n  unitor = canonical `ρ_` via `tensorObjIso`. Lean uses: `Localization.Monoidal.rightUnitor_hom_app`,\n  `MonoidalCategory.rightUnitor_naturality`, `tensorHom_def`.\n- `AlgebraicGeometry.Scheme.Modules.tensorObjUnitIso_eq` (L1708, private) — bridge: left unitor =\n  canonical `λ_`. Uses: `leftUnitor_hom_app`, `leftUnitor_naturality`, `tensorHom_def'`.\n- `AlgebraicGeometry.Scheme.Modules.tensorBraiding_eq` (L1732, private) — bridge: braiding = canonical\n  `β_`. Uses: `Braided.β_hom_app`, `BraidedCategory.braiding_naturality`, `tensorHom_comp_tensorHom`.\n- `AlgebraicGeometry.Scheme.Modules.tensorObjRightUnitor` (the public **def** itself, per lvbc) — never\n  got a `\\lean{}` block.\n(`cor:sheafTensorObjAssoc` prose is already current — canonical-α_-transport; no rewrite needed. lvbc\nconfirmed.)\n\n## Lean-side stale comments (NOT review-fixable; flag for the owning prover)\n- `tensorObjAssoc` docstring (L1829–1836) + planner comment (L1781–1828) still describe the abandoned\n  whiskered-unit / double-braiding route and cite a wrong `.tex` line. The prover that next touches the\n  file should update them to the canonical-transport story (the def body is correct).\n\n## Blocked — do NOT re-probe\n- χ leg: `QuotScheme.lean` 4 sorries (`hilbertPolynomial`, `quot_functor`, Grassmannian re-exports) —\n  χ-semantic, need the cohomology engine this i=0 leg lacks. Unchanged. Deferred to the cohomology leg.\n\n## Blueprint-doctor (structural, deferred chapters only — resolve at merge, NOT here)\nAll findings are out-of-cone (byte-identical-to-parent extraction artifacts):\n- `Cohomology_FlatBaseChange.tex` covers 2 non-existent files + 14 broken `\\ref`.\n- `Picard_QuotScheme.tex` 13 broken `\\cref`; `Picard_GlueDescent.tex` 4 broken `\\cref`/`\\uses`.\nNone touch the active SNAP cone. Do not edit (divergence-from-parent discipline).\n\n## Progress note\nRoute SNAP is **CONVERGING** (iter-006 planner verdict; net −6 over the 9→3 window; iter-007/008 built\nthe inherited monoidal structure + bridges that make the final 3 mechanical). The 3→3 count this iter is\nNOT a stall — it is the infrastructure iter that removed all the mathematical risk. Expect closure of\n`tensorPowAdd_zero_right` + `sectionsMul_mul_one` next iter once the 2 whiskering bridges land.\n"
}
