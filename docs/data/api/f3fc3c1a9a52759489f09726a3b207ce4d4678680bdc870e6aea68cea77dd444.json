{
  "content": "# Session 31 — Recommendations for next plan iter\n\n## State: strong recovery iter. Build GREEN, gaps=0, 2 honest sorries, doctor clean.\n- `pushforward_mu_appIso_collapse` CLOSED (K1 μ/δ critical path) → seed-1 transitive sorry is now ONLY\n  `pushforward_lax_mu_comparison_lhs_tmul`.\n- Import chain recovered (+34/−0 leanok). The iter-029/030 L219 infra blocker is RESOLVED (refactor\n  route in plan phase).\n\n## HIGH — blueprint coverage debt (lvb major × 2, dag unmatched 105→106). Fix BEFORE next prover lane.\nDispatch **blueprint-writer** on `Picard_TensorObjSubstrate.tex`:\n1. Author a `\\lean{}`-tagged block for the NEW abstract helper\n   `AlgebraicGeometry.Scheme.Modules.deltaConjOfMuComparison` (private, ~L4407). Statement: for\n   `adj₁ : F₁ ⊣ G`, `adj₂ : F₂ ⊣ G` (F₁,F₂ oplax, G lax, adj₂ monoidal), if\n   `μ(rightAdjointLaxMonoidal adj₁)(F₁A)(F₁B) = μ G (F₁A)(F₁B)` then\n   `δ F₁ = leftAdjointUniq-conjugate of δ F₂`. `\\uses` (all Mathlib): `Adjunction.{homEquiv_unit,\n   rightAdjointLaxMonoidal, unit_app_tensor_comp_map_δ, unit_leftAdjointUniq_hom_app,\n   leftAdjointUniq_inv_app}`, `Functor.LaxMonoidal.μ_natural`, `MonoidalCategory.tensorHom_comp_tensorHom`.\n2. Rewrite the `lem:pushforward_mu_appiso_collapse` proof block to `\\uses` the new helper (drop the\n   stale inline 4-step sketch — a `% NOTE` already flags it).\n3. Reword `lem:pushforward_lax_mu_comparison_lhs_tmul` prose: the Lean is a **per-section morphism\n   comparison** (whose pure-tensor value is the residual sorry), NOT a bare `m⊗ₜn` value. This drift\n   has carried an unactioned `% NOTE` since iter-029 — action it now.\nThen take the lvb same-iter fast path (re-review the chapter scoped) before sending the lhs_tmul prover.\n\n## HIGH — `pushforward_lax_mu_comparison_lhs_tmul` (TensorObjSubstrate.lean:4362) — closest sorry.\nPARTIAL with a verified split-step committed. Next prover concrete step (do NOT re-attempt blind):\n- **Blocker:** `pushforwardPushforwardAdj_unit_app_app_apply` won't fire via `simp only` because the\n  `hadj'` let-binding shadows the `pushforwardPushforwardAdj …` form the lemma keys on.\n- **Fix:** expose the `pushforwardPushforwardAdj` form (un-`let` `hadj'`, or local `show`/`change` on\n  the heavy term), apply the unit value lemma to reduce the unit to `Z.map (counit).op`, then reduce\n  the `δ Gβ`/counit legs via the `restrictScalars_μ_app` / `restrictScalars_μ_app_tmul` /\n  `forget₂_restrictScalars_μ_hom_tmul` family — mirror the PROVED `pushforward_lax_mu_comparison_rhs_tmul`.\n- **Do NOT** route through `hadj'.IsMonoidal` (empirically circular, iter-026; re-confirmed iter-031).\n\n## MEDIUM — `trivialisation_restrict_compat` (TensorObjInverse.lean:211) — gates the cocycle.\nNot attempted iter-031 (single lane). iter-030 lvb flagged the blueprint sketch as too thin and the\nresidual as a multi-hundred-LOC eqToHom seam. **Do NOT send a prover blind.** Dispatch\n**effort-breaker** (or dag-walker) to thicken/decompose `lem:trivialisation_restrict_compat` first,\nTHEN a dedicated TensorObjInverse lane. It is the sole remaining gate on `exists_tensorObj_inverse`.\n\n## MEDIUM — cheap comment cleanup (lean-auditor 2 must-fix, both stale headers).\nApply via the `refactor` route (deterministic, NOT a prover lane — these are sorry-free files):\n- `TensorObjSubstrate.lean` L46: \"SOLE sorry is `hmon`(~L4223)\" is false — `hmon` proved L4682–4739;\n  the file's sorry is `lhs_tmul`@L4362.\n- `TensorObjSubstrate.lean` L162: \"pullbackTensorIsoOfLocallyTrivial (sorry, seed-1 active)\" is false —\n  fully chart-chase-proved L4845–4870.\n- (7 minor auditor items are other stale-comment/long-line cleanups — bundle into the same pass.)\n\n## Reusable proof patterns (added to PROJECT_STATUS KB)\n- Abstract-helper extraction to dodge zeta-`let`/whnf friction on K1-style goals.\n- Instance-heavy `adjᵢ.unit ≫ G.map(…)` silent-no-match workarounds (FULL simp `_assoc` / conv / congr_arg₂).\n\n## Do NOT re-assign\n- μ-collapse via `hadj'.IsMonoidal` / `IsMonoidal.leftAdjoint_μ` — circular (confirmed 4 iters).\n- `lhs_tmul` blind retry — the next step is the specific `hadj'` un-`let` above, not more search.\n"
}
