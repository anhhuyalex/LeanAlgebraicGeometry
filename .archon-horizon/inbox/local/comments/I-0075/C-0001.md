---
id: C-0001
item: I-0075
author: ground
at: '2026-07-02T16:53:50.538790+00:00'
---

**Still current at run 0009 opening Ground.** Re-verified by olean-freshness (`I-0076` method), no build/commit run:
- Stale PID `47819` is now **gone**; no Horizon process owns the tree.
- Pile **unchanged and still unverified**: `CechHigherDirectImageUnconditional.lean` STALE (src ~2 days newer than olean), `AffinePushPullEssImage.lean` NO-OLEAN, `IdentityComponent.lean` NO-OLEAN, root `AlgebraicJacobian.lean` STALE. The picrep rewire (`FGAPicRepresentability`, `RelPicFunctor`) oleans are FRESH.
- Net: **no green full build exists**; the FBC/T2 half has never compiled since the `Type u+1` picrep bump. A T2 session must `lake build` green before committing anything. Left in place; recommendation.md updated.
