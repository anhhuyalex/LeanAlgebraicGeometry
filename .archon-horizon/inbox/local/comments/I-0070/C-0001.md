---
id: C-0001
item: I-0070
author: horizon
at: '2026-07-03T02:26:36.274616+00:00'
---

**Done this session: the FGA `picSharp` rewire landed via route (b) — no étale topology needed.**

- `picSharp C` is now REALLY `PicSharp.relPresheaf C ⋙ forget` (the honest $H_T$-coset relative Picard functor), universe-bumped to `Type (u+1)`. New class `Scheme.HasRationalPoint`; the single genuine FGA sorry `instHasPicScheme` is conditional on it, so its statement is TRUE by Kleiman §2 Thm 2.5 + §4 th:main.
- Two former sorries are now PROVED: `instPicSharpRepresentable` (`Exists.choose_spec`) and `groupSchemeStructure` — the latter upgraded to a full `CommGrpObj` via `CommGrpObj.ofRepresentableBy` (Yoneda transport of the tensor-product group law). FGA sorries **7 → 3**; all new decls axiom-clean.
- Bonus truth-audit fix: the global `instHasSmoothProperQuotient ⟨sorry⟩` asserted every presheaf representable — FALSE (Hironaka-type quotients; the Lean statement also omits Kleiman's quasi-projectivity, inexpressible in Mathlib v4.31). Deleted; the class survives as a use-site hypothesis.
- Blueprint repin (`I-0062`) done in the same coordinated step: `def:rel_pic_sharp`/`lem:rel_pic_sharp_functorial` → `relPresheaf`/`relFunctorial`, new `def:abs_pic_functor`, FGA chapter restated for pointed curves, closure plan rewritten to 3 sorries. DAG rebuilds clean (0 dangling).
- Builds: `lake build` of FGA + IdentityComponent + Pic0AbelianVariety GREEN (8586 jobs). Commits `cd5475e100` (Lean), `0be013a7ff` (blueprint).

**What this unblocks:** `representable C` now extracts a genuine bijection $(T \to \mathrm{Pic}_{C/k}) \simeq \mathrm{Pic}(C\times_k T)/\pi_T^*\mathrm{Pic}(T)$ under the class hypotheses — the attachment point `tangentSpaceIso` (AJC.pic0av) needs, with $T=\operatorname{Spec} k[\epsilon]$. The next big block is the Kleiman Thm 5.11 cocycle/truncated-exponential computation.
