---
id: C-0009
item: I-0016
author: horizon
at: '2026-07-02T16:57:22.591988+00:00'
---

**Verification status hand-off (T2 session 2, ~23:59).**

- A full `lake build` from another session (`.claude-leheng`, started 15:42, cwd = AJC) is rebuilding the `CechSectionIdentification*` chain; it started AFTER all my edits, so it covers the new `AffinePushPullEssImage.lean` + edited `CechHigherDirectImageUnconditional.lean`. **LegMid1 olean landed 22:11** (5h40m compile — the box runs ~5-10x slower than the documented 70 min under load ~65).
- I killed my own duplicate LegMid1 build to avoid the same-olean race (third racing-build incident this run; my first rebuild attempt was SIGTERM-killed at 3h39m by an external kill — plausibly a concurrent session's cleanup).
- **If the leheng build errors in `AffinePushPullEssImage.lean` or `CechHigherDirectImageUnconditional.lean`, those are MY edits** — the new file compiled standalone (`lake env lean`, 0 errors) with a temp qcoh stub; the final form (real `isQuasicoherent_pullback_opens` call) and the Unconditional edits (member node proof, per_sigma proof, 4 signatures specialized to `V₀ : X.Opens`) are kernel-unverified. Expected-fragile spots: coercion of `𝒜.le_U j` (`.base` vs hom-coercion) in the member node, and the `simp [Scheme.Opens.opensRange_ι]` closing `hRange`.
- Axiom check still to do once green: `#print axioms` on `openImmersion_beckChevalley`, `twisted_cech_nerve_per_sigma` (expect propext/Classical.choice/Quot.sound only).
