---
id: C-0010
item: I-0016
author: horizon
at: '2026-07-02T19:05:07.250201+00:00'
---

**T2 CLOSED & VERIFIED (session end).**

- `lake build AlgebraicJacobian.Cohomology.CechHigherDirectImageUnconditional` — **Build completed successfully (8584 jobs)**.
- `#print axioms`: `openImmersion_beckChevalley`, `twisted_cech_nerve_per_sigma`, `openImmersion_pushPull_essImage_member`, `restrict_pullback_pushforward_essImage`, `pullback_pushforward_affineOpen_essImage`, `pushforwardRestrictOpensIso` — all `[propext, Classical.choice, Quot.sound]` (no `sorryAx`). **`IsIso bareBC` and the per-σ X-level Beck–Chevalley are closed.**
- Remaining Unconditional sorries: 5 (the pre-existing leaves — `pullback_preservesFiniteLimits`, per-σ RHS tilde leaf + heart residual, 2 cosimplicial naturalities).
- **Note for your full-tree run** (the 15:42 `lake build`, still compiling `CechToHigherDirectImage` as of my session end): its end-of-run will report a FAILURE for `CechHigherDirectImageUnconditional` from its earlier attempt — that failure is **already fixed** (3 small errors: dangling doc-comment → `/-!`, restored `[IsOpenImmersion p']`, explicit `isoOfRangeEq` instances); the module has since been built green by my targeted run. Just re-run `lake build` (everything replays) to get the clean full-tree confirmation.
- I did NOT start a full root build myself to avoid racing your in-flight `CechToHigherDirectImage` job (three racing-build incidents this run already).
