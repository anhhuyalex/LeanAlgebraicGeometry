---
id: C-0002
item: I-0016
author: ground
at: '2026-07-02T02:12:48.772826+00:00'
---

**Still unresolved after T1 Ground review (run 0004, session 0009-ground) — full AJC build deferred again; T2 still live.**

- T2 (`AJC.fbc`, PID 3121714, fable-5) is **still building** at review time: `lake build ...CechHigherDirectImageUnconditional` waiting on the ~70-min `CechSectionIdentificationLegMid1.lean` dependency (lean ~110 min CPU). I did **not** run a competing full `lake build AlgebraicJacobian` — concurrent builds in the same project dir risk olean corruption (see memory `I-0002`).
- **Confirmed hazard realized:** the `r1 T1` commit `7401a19` swept T2's in-progress `Cohomology/CechHigherDirectImageUnconditional.lean` (**44 sorries, mid-refactor**) and `Cohomology_CechHigherDirectImage.tex` into the AJC history under a *T1* commit. Not math-lost — T2 will supersede it on its own checkpoint — but **AJC HEAD does not currently build** (that Cech file is mid-refactor).
- **Orphan build observed:** a second `lean` process (PID 3228996, ppid=1, leftover) is compiling the *same* `CechSectionIdentificationLegMid1.olean` as T2's live build → olean-corruption risk. Did not kill it (would risk the live T2 session).

**Next (unchanged):** after T2 checkpoints, run one confirming `lake build AlgebraicJacobian` from a clean single-session state; that seals both the T1 root import (`TangentSpaceDualNumbers`) and T2's Cech cone. Keep this item open until then.
