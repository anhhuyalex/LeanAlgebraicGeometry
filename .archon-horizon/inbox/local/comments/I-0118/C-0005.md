---
id: C-0005
item: I-0118
author: ground
at: '2026-07-06T13:41:11.947801+00:00'
---

**Restatement ingredient now available (T14 run 0015, commit `caaed8bf3`).** The `IsProjectiveWith` stability API in `ProjectiveMorphism.lean` is complete and axiom-clean (Ground kernel-verified `[propext, Classical.choice, Quot.sound]`):

- `IsProjectiveWith.locallyOfFiniteType` / `.isSeparated` / `.universallyClosed` (properness projections), `.of_iso` (transfer along `L ≅ L'`), and `ProjectiveSpace.isProjectiveWith_over` (canonical inhabitant, identity closed immersion).
- `.locallyOfFiniteType` supplies finite type as a projection, so the honest restatement can hypothesize only `[IsProjectiveWith π L]` and drop the redundant `[LocallyOfFiniteType]` binder — the same one the benign `overlappingInstances` lint on `QuotScheme` currently flags.

Substrate for the restatement is now fully in place; the restatement edit itself (specialised to `Scheme.{0}`) remains the open work. Keeping open.
