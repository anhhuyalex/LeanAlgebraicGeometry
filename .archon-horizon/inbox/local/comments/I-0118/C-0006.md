---
id: C-0006
item: I-0118
author: human
at: '2026-07-07T02:27:46.315348+00:00'
---

**Honest restatement landed.** `AlgebraicGeometry.Scheme.QuotScheme` now states the actual [Nitsure] §5 theorem: for locally noetherian $S$, $\pi : X \to S$ **projective** carrying a **relatively very ample** line bundle $L$ (hypothesis `hproj : π.IsProjectiveWith L` — the encoding (a) settled in C-0002), $E$ **coherent** (`hE : E.IsFinitePresentation`) and $\Phi \in \mathbb{Q}[\lambda]$, the functor $\mathrm{Quot}^{\Phi,L}_{E/X/S}$ is representable by an $S$-scheme.

- The false-or-open pin (`[IsProper π] [LocallyOfFiniteType π]` over arbitrary quasi-coherent $L$, arbitrary $E$) is gone. Finite type is derived from projectivity via `IsProjectiveWith.locallyOfFiniteType` (a `haveI` in the statement), which also clears the benign `overlappingInstances` lint the old double binder triggered.
- Specialised to `Scheme.{0}` as planned: `IsProjectiveWith` rests on the Serre twist, hence on the universe-monomorphic descent engine.
- Placement: `ProjectiveMorphism.lean` imports `QuotFunctorDef.lean`, so the restated theorem moved to the new leaf `Picard/QuotRepresentability.lean` (same split as `Grassmannian.representable` → `GrassmannianRepresentability.lean`); `QuotFunctorDef.lean` §5 keeps a pointer comment recording the I-0118 history.
- The proof remains the single tracked `sorry` of the Quot endgame — it awaits Serre finiteness (`sectionGradedModule_fg`, I-0109) plus the Castelnuovo–Mumford boundedness lane.

Blueprint side (for the sync agent): `thm:quot_representable` should flip `\notready` → statement-`\leanok` (proof still open), with the LEAN/BLUEPRINT MISMATCH comment replaced by a faithful-encoding note.

Keeping open: the restatement half of this item is done; the "then prove" half is the Quot endgame.
