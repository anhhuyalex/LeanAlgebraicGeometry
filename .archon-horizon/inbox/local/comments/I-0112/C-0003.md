---
id: C-0003
item: I-0112
author: horizon
at: '2026-07-05T12:14:35.011094+00:00'
---

**Port LANDED + verified (run 0013 T9, session 0017).** The rational-map-extension port is fully in AJC and byte-identical to the subproject: all 8 `Albanese/*.lean` files present + wired in the root; `lake build` green (8573 jobs). Real sorries now: **CodimOne 1** (`indeterminacy_pure_codim_one_into_grpScheme`, Milne 3.3), **Thm32 0** in-file, **AlbaneseUP 7** (incl. `bundle`, out of scope). Support files `PolePurity`/`SmoothPrimeRegularity`/`StandardSmoothDimension` kernel-verified **axiom-clean**. The port was already captured in the run-0013 baseline `3029a02d`, so a labelled `horizon commit` on it is a no-op.

**Soundness bottleneck.** `extend_to_av` and the sorry-free assembly `albanese_universal_property` both carry `sorryAx` transitively via the ONE CodimOne sorry (substeps 1/2/4b are independent greenfield gaps — not one session). The UP-assembly leg is separately blocked on the **missing Sym^g scheme substrate** (Mathlib v4.31 has no scheme-level symmetric power / geometric quotient / Noether finiteness of invariants).

**This session's landings** (`e9e950951a`): added `Scheme.RationalMap.stalkPullback` (axiom-clean function-field germ pullback `O_{Y,f(η_X)}→K(X)`; dominant `K(Y)→K(X)` refinement documented but closes no existing sorry); repaired Thm32 + CodimOne blueprint pin-drift (3 stale CodimOneFree nodes → 1 honest node; re-pinned `thm:codim_one_extension` off the deleted false `extend_of_codimOneFree_of_smooth` and deleted its false extension claim). Full endgame map in memory `t9-albanese-endgame-unblock-map`.
