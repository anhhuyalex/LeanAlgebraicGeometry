---
id: C-0002
item: I-0112
author: horizon
at: '2026-07-05T08:23:59.544977+00:00'
---

**Port DONE and kernel-verified this run (0013, session 0014-horizon-T9).** The rational-map-extension leg is merged into AJC.

- **State now (AJC `AlgebraicJacobian/Albanese/`):** `Thm32RationalMapExtension` sorry-free (Milne I.3.2 `extend_to_av` file-closed); `CodimOneExtension` 3→1 sorry; `AuslanderBuchsbaum` sorry-free; the three support files `PolePurity`/`SmoothPrimeRegularity`/`StandardSmoothDimension` present and sorry-free; root `AlgebraicJacobian.lean` imports all three. `AlbaneseUP` unchanged (7 substrate/representability-gated sorries, out of T9 scope).
- **Verification:** `lake build AlgebraicJacobian` green (8652 jobs, exit 0). Kernel axioms: `extend_to_av` = {propext, **sorryAx**, Classical.choice, Quot.sound} — the lone sorryAx is the Milne 3.3 substep; `isRegularLocalRing_..._perfectField` (SmoothPrimeRegularity main result) axiom-clean {propext, Classical.choice, Quot.sound}.
- **The whole extension leg is now one gap:** `indeterminacy_pure_codim_one_into_grpScheme` (CodimOneExtension.lean:1662, Milne §I.3 Lemma 3.3). Substep 4a (pole-divisor purity) is closed by `PolePurity`. The real blocker for the rest is the **`Scheme.RationalMap`→function-field pullback bridge** (K(Y)→K(X) ring map, absent from Mathlib v4.31) needed for substeps 1–3 (difference map Φ, slice argument, Φ*𝒪_{G,e}) plus substep 4b (diagonal codim-1 / Krull bound). That same pullback bridge also blocks `thm:weil_divisor_obstruction` — building it is the highest-leverage next sub-build on this leg.
- **Transitive kernel cone caveat:** CodimOne still depends on WeilDivisor's two pre-existing sorries (WeilDivisor.lean:772, :1103, the I-0106 RiemannRoch substrate), so `extend_to_av`'s cone includes those too.
- **Blueprint honest:** `thm:rational_map_to_av_extends` and `lem:milne_codim1_indeterminacy` carry statement-\leanok only; both proof blocks correctly ungreen. No false pins.
- **Concurrency note:** a parallel run landed the identical port into the shared ledger (commit 9bd00f58, `workspace[0010 r2] ground: integrate 0093-ground`, ~17:17) while this session built and verified it; on-disk == ledger HEAD, no residual uncommitted AJC Lean.
