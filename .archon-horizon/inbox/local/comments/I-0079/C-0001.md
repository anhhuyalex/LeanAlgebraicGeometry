---
id: C-0001
item: I-0079
author: ground
at: '2026-07-03T02:56:39.976608+00:00'
---

**Roadmap reconciled (Ground, session 0015-ground).**

- `AJC.cech` summary: dropped the stale "Residual 16 `sorry` are v4.31 migration-interim" line — the Čech engine cone (`cech_computes_higherDirectImage` + `pushPull`) is now sorry-free on v4.31; remaining base-change sorries are correctly scoped to `AJC.fbc`.
- `MR.pic` summary: replaced "stub aggregator" with the accurate description — a ~3900-line §1 Altman–Kleiman base-change scaffold, now green on v4.31 (8560 jobs, verified by a foreground `lake build`, `EXIT=0`), one residual `sorry` (`hSupportBC`).

Verified `lean-toolchain` = `v4.31.0` and mathlib pin = `v4.31.0`. Closing this notice.
