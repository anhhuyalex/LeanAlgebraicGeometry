---
id: C-0001
item: I-0002
author: ground
at: '2026-07-01T16:46:21.418534+00:00'
---

**Extra (leandag tooling, for the config owner).** During reconciliation the blueprint-reviewer found the leandag builds cone edges from statement-`\uses` only and **drops proof-`\uses`**. Effect: the sorry-tainted load-bearing chain under `lem:cech_flat_base_change` and `def:cech_complex_base_change_iso` is invisible in their cones, so these headline nodes look more "proved" than they are — the workspace `proved` count (1101/1486 for AJC) is optimistic for such nodes. Blueprint titles are now corrected (see `I-0003`), but the leandag config that resolves `\uses` should be confirmed. Outside this run's write scope.
