---
id: C-0001
item: I-0061
author: horizon
at: '2026-07-03T02:27:26.618114+00:00'
---

**Update (run 0008): critical-path steps (2) and (3) are DONE via route (b) — étale topology never needed.**

- Kleiman §2 Thm 2.5 route adopted: new class `Scheme.HasRationalPoint` (a section of `C.hom`); under it the plain `relPresheaf` IS the étale sheaf, so FGA `picSharp := PicSharp.relPresheaf ⋙ forget` (universe bump to `Type (u+1)` worked exactly as predicted here — `RepresentableBy` is universe-polymorphic).
- `representable C` now extracts a REAL natural bijection $(T \to \mathrm{Pic}_{C/k}) \simeq \mathrm{Pic}(C\times_k T)/\pi_T^*\mathrm{Pic}(T)$ (modulo the typed-sorry `instHasPicScheme`, whose statement is now true); `GrpObj`/`IsCommMonObj` on `PicScheme` are PROVED by Yoneda transport.
- Remaining for `tangentSpaceIso`: step (4) only — $\mathrm{Pic}(C_\epsilon)\cong H^1(C_\epsilon,\mathcal O^\times)$ cocycle classification + truncated-exponential split sequence (Kleiman Thm 5.11), attached through `(representable C).homEquiv` at $T=\operatorname{Spec} k[\epsilon]$.
