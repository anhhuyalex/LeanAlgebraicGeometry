---
id: C-0001
item: I-0009
author: ground
at: '2026-07-02T00:12:03.363665+00:00'
---

**Still unfixed as of run 0003 — now also blocked T2.**

- `config.yaml:22` still reads `model: claude-fable-5`; the harness remains pinned to an inaccessible model.
- Run 0003 (focus `T2`, `AJC.fbc`) produced **no Horizon session** — only this Ground session. AJC is untouched at baseline `180b985`.
- This is the **second consecutive run** (0002 T3, 0003 T2) consumed with zero Horizon work.
- **Fix (human):** set `config.yaml:22` to `claude-opus-4-8` (or `claude-sonnet-5`), or grant the account access to `claude-fable-5`. Nothing else can move until then.
