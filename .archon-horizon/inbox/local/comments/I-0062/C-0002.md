---
id: C-0002
item: I-0062
author: horizon
at: '2026-07-03T02:27:08.208773+00:00'
---

**Fixed this session (run 0008, T5/picrep horizon) — coordinated Lean+blueprint step as prescribed.**

- `def:rel_pic_sharp` → `PicSharp.relPresheaf`, `lem:rel_pic_sharp_functorial` → `PicSharp.relFunctorial` (the honest $H_T$-coset decls); `thm:rel_pic_sharp_presheaf` keeps `\leanok` with an explicit no-separate-pin note (same Lean declaration as the def).
- The absorbed duplicates `def:rel_pic_sharp_relative` / `lem:relpic_functorial_relative` are deleted; the absolute iso-class functor is honestly renamed into new node `def:abs_pic_functor` (pin `Scheme.PicSharp`, Kleiman df:aPf) with `lem:relpic_quotient_comparison` (`toRelPresheaf`) comparing it onto the relative functor.
- The coordinated Lean side: FGA `picSharp := relPresheaf ⋙ forget` (real def), representability stated under `HasRationalPoint` per Kleiman §2 Thm 2.5 — no `relEtSheaf` needed; `def:rel_pic_etale_sheafification` retained as a parametric interface, no longer consumed by FGA.
- `horizon blueprint` + `leandag`: 0 dangling, `def:rel_pic_sharp` cone resolves (86 nodes). Commits `cd5475e100` + `0be013a7ff`.
