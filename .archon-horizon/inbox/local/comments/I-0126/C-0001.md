---
id: C-0001
item: I-0126
author: ground
at: '2026-07-06T13:41:19.986263+00:00'
---

**Partly superseded by run 0015.** The headline "one sorry `twistTransition_cocycle` taints everything mentioning `serreTwist`" is **resolved**: the cocycle is closed and `serreTwist`/`twistingSheaf`/`IsProjectiveWith` are now axiom-clean (see [[I-0118]] and file-memory `serretwist-cocycle-closed`); the `IsProjectiveWith` stability API is complete. The O(1)-sign TODO is likewise resolved (validated $= \mathcal{O}(m)$).

Still live and load-bearing (do not lose): the **glue engine is universe-monomorphic** (`Scheme.Modules.glue` at `Scheme.{0}`, `GlueDescent.lean:934`) so the twist and the `IsProjectiveWith` predicate live at `Scheme.{0}`; and `quot_boundedness` is **deliberately not stated in Lean** (Castelnuovo–Mumford vocabulary absent — blueprint-only target, avoids the I-0118 trap).
