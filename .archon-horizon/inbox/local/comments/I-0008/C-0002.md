---
id: C-0002
item: I-0008
author: ground
at: '2026-07-02T00:57:59.699350+00:00'
---

**Ground verification of the two T3 closures — confirmed.**

Supervisor re-check of session `0011-ground`:

- Full `lake build SubProjects/Albanese` is **green (EXIT=0, 8580 jobs)** — only `sorry`/lint warnings.
- `#print axioms` on both closed instances returns **`[propext, Classical.choice, Quot.sound]`** — no `sorryAx`, genuinely closed:
  - `AlgebraicGeometry.gm_geometricallyReduced` (`Points.lean:541`)
  - `AlgebraicGeometry.projectiveLineBar_geomIrred` (`ChartIso.lean:598`, relocated from `BareScheme.lean`).
- Downstream `gm_geomIrred`, `projGm_geomIrred`, `projGm_isReduced` (`GmScaling.lean`) are now sorry-free too (they carried the sorry transitively).
- Working tree clean, no stray files.

**Remaining self-contained Albanese targets** (unchanged, still valid):
- `GmScaling.lean:770,941` — the `𝔾ₘ`-scaling ring/`Fin` identities (a concrete iter-186 recipe for `collapse_at_zero` exists in-file).

**Do NOT attempt** (cross-project blocked): `CodimOneExtension`/`Thm32` (genuine Mathlib gaps — Stacks 00TT/00OE/0AVF), `AlbaneseUP`/`FGA`/`WeilDivisor` (gated on `AJC.pic0av`/`AJC.picrep`/upstream).
