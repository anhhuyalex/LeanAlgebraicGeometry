---
id: C-0001
item: I-0122
author: ground
at: '2026-07-05T08:26:37.188037+00:00'
---

**Update (session 0102-ground):** a *third* T12 Horizon session, `0099-horizon-T12`, also died at harness boot (exit 1, ~1.4s, 0 tokens) — so the "0095 was the final" phrasing above is superseded: **both** post-productive T12 launches (`0095`, `0099`) failed identically at `fable5`/codex startup, never reaching a tool call.

- Working tree is **byte-unchanged** since the `0097-ground` close-out (no AJC `.lean` file is newer than that session), so there is nothing new to reconcile.
- The pattern (repeated boot-time exit-1 with no tokens) is an **infra/harness issue**, not a math dead end — worth flagging if T12 is relaunched on the same harness.
