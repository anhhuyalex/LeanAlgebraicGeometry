# Orientation — T13 (retire the stale genus-0 / Route-C split)

- Useful context: the genus split is *already mostly retired* — the north-star `references/challenge.lean` (`genus`, `Jacobian`, `exists_unique_ofCurve_comp`) is genus-uniform, `Jacobian C := (jacobianWitness C).J` has no genus-0 `dite` (iter-073), and `Genus0BaseObjects.lean` / `AbelianVarietyRigidity.lean` are already gone from the AJC tree. Memory `I-0106` (+ my comment) maps the residue and the one dependency trap.

- Naming caveat: `Route B`/`Route C` inside `Cohomology/*.lean` and `RigidityLemma.lean` are *proof-technique* labels for the sorry-free cohomology-free Čech engine, not the strategic genus split; the strategic Route-C is the RR→ℙ¹ base case. Relevant genus-0 residue: AJC+Albanese `RiemannRoch/WeilDivisor.lean` (dual-purpose — `PrimeDivisor`/`order` feed the live `Albanese/CodimOneExtension.lean`; the `degree`/RR→ℙ¹ block with 5 `sorry` + `-- Route C PAUSE` is the dead weight) and the Albanese-only `Genus0BaseObjects/` tree.

- Relevant files: the two `WeilDivisor.lean` copies have diverged (AJC imports `Genus`/`CoheightBridge`; Albanese imports `Genus0BaseObjects`); the whole `SubProjects/Albanese/AlgebraicJacobian/Genus0BaseObjects/` tree (~13 `sorry`, ~100 blueprint nodes under `def:genus0_base_objects`) survives only there, imported solely by Albanese's `WeilDivisor`. Carving that `WeilDivisor` block orphans the tree — the main Albanese payoff. `roadmap.md` narrative lines ~70/72/77/120 still assert stale genus-0 status (e.g. `Genus0BaseObjects` "sorry-free in-tree"); the AJC structured blueprint JSON is already genus-0-clean, the Albanese blueprint JSON is not.

- Verification note: `CodimOneExtension` (live `ALB.codim1`) imports `WeilDivisor`, so a wholesale delete breaks the build — a surgical carve plus a fresh `lake build` of **both** AJC and Albanese is the honest check. Stale `blueprint/web/chap-Genus0BaseObjects_*.html` + `.lake` artifacts in the AJC tree are regenerated cache, not source.
