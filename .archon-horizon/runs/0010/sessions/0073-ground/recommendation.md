# Orientation

- Useful context: the FlatteningStratification sub-cone of `AJC.picrep` is closed — `AlgebraicGeometry.flatLocusStratification_universal` (Nitsure §4, `n=0`) is sorry-free and axiom-clean in `AlgebraicJacobian/Picard/FlatteningStratificationUniversal.lean` (~1100 LOC); `GenericFlatnessGeometric.lean` is now sorry-free; substrate in `EntryIdealStratum.lean`. Reusable API: `pointRank_pullback`, `coherentSheafFlat_id_pullback`, `Module.FinitePresentation.exists_matrixPresentation`, `rankAtStalk_sections_eq_pointRank`.
- Relevant files for the remaining `AJC.picrep` frontier: the 3 `canonicalBaseChangeMap_app_app_isIso_*` leaves in `Picard/QuotScheme.lean` (their last non-flatness prerequisite is in place; they now gate only on `AJC.fbc`/Stacks 02KH flatness algebra), and the 5 headline Quot/Grassmannian reps decls (`QuotFunctor`, `QuotScheme`, `Grassmannian`, `Grassmannian.representable`, `hilbertPolynomial`) awaiting a Route-C coherent-$\chi$ / Riemann–Roch substrate. Memory `t12-rank-stratum-landed` and `I-0086` carry the triage.
- Blueprint context: `blueprint/src/chapters/Picard_FlatteningStratification.tex` §`sec:flatstrat_representability` (22 `\leanok` nodes, 1-to-1 with the Lean); AJC DAG reports 0 dangling.
- Environment notes: load was idle (~1.3) this session; `lake env lean` on the FlatteningStratification import chain elaborates and axiom-checks in seconds when oleans are warm. `#print axioms flatLocusStratification_universal` = `[propext, Classical.choice, Quot.sound]`.
