Done. Inbox is now at target.

## Summary

**Reality vs. premise:** The "103 open" figure was the *total across all statuses* — 61 items were already archived and 28 closed. Only **14 were actually open**, and **all 8 issues + 7 hints were already closed/archived** (0 open of each). So the entire actionable surface was the 12 open memory items (info was already at target with 2).

**Archived (2, both memory — tooling/ops, not math dead-ends):**
- `I-0051` — leandag "statement-level `\uses` only" convention. Genuine nuance but tooling trivia; the leandag SKILL.md already covers isolated-node semantics.
- `I-0084` — run-0009 ops snapshot (build-cost load-dependency, `horizon commit` empty-error, zombie sessions). Load-dependent/run-specific; the durable commit gotcha already lives in auto-memory.

**Not touched:**
- `I-0102`, `I-0103` — this round's info items (Ground handles them).
- All 8 issues / 7 hints — already closed or archived by prior rounds.

**Final open state: 12 (10 memory + 2 info)** — memory now at the ≤10 target, info within its 3-4 target.

**Preserved memory (all live math dead-ends / reusable proving recipes on the explicit preserve list):**
`I-0061` tangentSpaceIso/pic0av · `I-0065` Milne codim1 · `I-0074` FGA picSharp rewire caveats · `I-0075` FBC/T2 pullback-qcoh recipe · `I-0076` `pullback_preservesFiniteLimits` route analysis · `I-0082` A.3 Pic0 cone state (the *newer* cone-state note the supersession example says to keep) · `I-0083` FBC 3-sorries (newer, supersedes "4 leaf sorries") · `I-0088` ModuleCat-morphism v4.31 recipe · `I-0089` QuotScheme/picrep triage · `I-0092` dévissage v4.31 recipes.

**One I was mildly unsure about:** `I-0051` (leandag) — it *does* add nuance beyond the skill doc (isolated ≠ always a bug). I archived it because it's tooling trivia rather than proving context, and memory items are injected into every agent's context, so the budget is better spent on the 10 expensive-to-rediscover math dead-ends. Easy for Ground to restore if the leandag nuance is judged worth a standing slot.

No issue/hint/doc/layout problems surfaced that warrant filing — the only hygiene need here was the memory-count trim, now done.
