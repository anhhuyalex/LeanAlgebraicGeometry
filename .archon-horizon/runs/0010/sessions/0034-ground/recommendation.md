# Orientation — T12 / `AJC.picrep` after run 0010 r4

- Useful context: `AlgebraicGeometry.genericFlatness` (Nitsure §4) is proved and kernel-green in the new `MainProjects/Algebraic-Jacobian-Challenge/AlgebraicJacobian/Picard/GenericFlatnessGeometric.lean` (theorem ~L448); it carries `sorryAx` only through the re-opened algebraic core `GenericFreeness.genericallyFree_quotient_prime` (`FlatteningStratification.lean:978`, `sorry` at L1001). New axiom-clean substrate: `Module.Flat.of_isLocalizedModule_algebra`, and the Stacks 01PC finite-sections cluster in `QuotScheme.lean` (`module_finite_of_tilde_genSections` ~L5441, `..._quasicoherentData` ~L5493, `exists_affine_finite_sections_nhds` ~L5548, namespace `AlgebraicGeometry.Scheme.Modules`).

- Relevant next brick: `flatLocusReduction` (Noetherian induction on the base, consuming `genericFlatness`) — `flatteningStratification` already derives from it; the geometric-glue home is `GenericFlatnessGeometric.lean` or a successor. The `genericallyFree_quotient_prime` repair map (three localized-module instance walls; r3 text in ledger `6c30537`) is in issue `I-0093`; the sub-lemma layer beneath it is axiom-clean and survives.

- Environment/build notes: `FlatteningStratification.lean` stays Mathlib-only-imports on purpose — adding project imports perturbs its heavy dévissage proofs' instance environment (that is why the glue lives in the companion file). Load-sensitive builds; check `uptime` before budgeting. Sorry inventory: FlatteningStratification 4, QuotScheme 8, `GenericFlatnessGeometric.lean` 0. Deep dévissage recipes in memory `[[flattening-stratification-repaired-devissage]]` / `I-0092`; genericFlatness recipes in `[[generic-flatness-geometric-closed-r4]]`.

- Blueprint/consistency note: the 9 new nodes in `Picard_FlatteningStratification.tex` + `Picard_QuotScheme.tex` are reconciled (honest `\leanok` gating, dependency-correct `\uses`, faithful pins). One pre-existing math-purity smell noted for a later blueprint sweep: `Picard_QuotScheme.tex` ~L5176 (the "Iter-195" subsection, outside this diff) uses raw Lean identifiers `\texttt{unit\_conjugateEquiv}` / `\texttt{conjugateEquiv\_pullbackComp\_inv}` in prose.
