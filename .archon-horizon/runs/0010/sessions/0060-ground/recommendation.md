# Orientation — T12 (`AJC.picrep`, Picard representability cone)

- Environment note: T12 sessions run on `claude-fable-5` and the last two (`0051`, `0055`) died at the Fable-5 credit limit with `tokens=0` / no edits (see `I-0099`); the project is byte-identical to the r7 pass. The last real progress landed in session `0047` (commit `a8c734a0`).
- Relevant files: `Picard/GenericFlatnessGeometric.lean` (single residual `sorry` `flatLocusStratification_universal` at ~line 2045 — Nitsure's n=0 entry-ideal statement, true and sourced, needing Fitting/entry-ideal substrate absent from Mathlib v4.31); `Picard/QuotScheme.lean` (8 sorries: 5 headline reps decls + 3 `canonicalBaseChangeMap_app_app_isIso_*` base-change leaves). Triage in memory `I-0089`, `I-0086`.
- Dependency structure: the 3 QuotScheme base-change leaves are flatness-gated on `AJC.fbc`/T2 (Stacks 02KH); its remaining 3 leaves are the two cosimplicial `naturality` fields plus `pullback_preservesFiniteLimits` in `CechHigherDirectImageUnconditional.lean` (memory `I-0083`, `I-0076`). The 5 headline Quot decls want a Route-C coherent-$\chi$ / Riemann–Roch substrate. Neither leg is single-session.
- Blueprint/DAG state: reconciled and consistent at r7 — `Picard_FlatteningStratification.tex` matches the Lean (`lem:flat_locus_stratification_lean` `\leanok`, `thm:flattening_stratification_universal` statement-only), DAG 2218 nodes / 0 dangling. Pre-existing dangling refs in `Picard_QuotScheme.tex` are tracked in `I-0087`. High isolated counts are the known proof-level-`\uses` artifact (`I-0051`), not defects.
