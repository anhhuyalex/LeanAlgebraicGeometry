You are the blueprint agent for the Archon Horizon workspace at /home/Axel/LeanAlgebraicGeometry-Horizon, project Algebraic-Jacobian-Challenge (working directory: /home/Axel/LeanAlgebraicGeometry-Horizon/MainProjects/Algebraic-Jacobian-Challenge). Read the workspace skill file .claude/skills/blueprint-conventions/SKILL.md (at the workspace root /home/Axel/LeanAlgebraicGeometry-Horizon/.claude/skills/blueprint-conventions/SKILL.md) and the leandag skill (.claude/skills/leandag/SKILL.md) before editing.

Task: the Lean file `AlgebraicJacobian/Picard/FlatteningStratificationUniversal.lean` (new this session, sorry-free, axiom-clean, build green) proves the universal property of the flat-locus stratification (Nitsure §4, case n = 0). The blueprint chapter `blueprint/src/chapters/Picard_FlatteningStratification.tex` must be brought into 1-to-1 correspondence:

1. The existing node `thm:flattening_stratification_universal` (around line 2219): its THEOREM environment already has `\leanok`; its PROOF environment does not. The Lean proof is now complete — rewrite the proof environment to a complete mathematical proof matching the Lean argument (rank strata over chart loci for e ≤ N a rank bound; support/disjointness/covering from the rank-locus description; flatness of strata pullbacks; for the universal property, T decomposes into OPEN rank pieces by local constancy of the rank of a flat finitely presented module, each piece factors uniquely through its stratum via the closed-immersion lift and vanishing of pulled-back relation matrices, assembled through the disjoint-open-cover colimit cofan; uniqueness because a competing factorization lands piecewise in the same summand — the summand ranges have distinct point ranks — where immersions are monomorphisms), add `\leanok` to the proof, and set the proof's `\uses` to the labels of the new nodes you create below (plus existing ones it genuinely uses, e.g. the rank-stratum section nodes of `sec:flatstrat_rank_stratum`).

2. Add a new section (or subsection structure) between `sec:flatstrat_rank_stratum` and the universal theorem, containing one node per NEW substantive Lean declaration, each with: pure-math statement (LaTeX, no Lean-speak), COMPLETE proof (not sketched), `\lean{<fully qualified name>}`, `\leanok` (all are proved and axiom-clean — I verified with #print axioms), statement-level `\uses` where the mathematical dependency is in the statement, and `\source{nitsure-hilbert-quot}` where the content is from Nitsure §4 (the source is registered; L1849–L1885 of references/nitsure-hilbert-quot-src/nitsure-hilbert-quot.tex is the relevant range — read it to cite honestly; do NOT cite it for pure-Mathlib-glue lemmas).

The new declarations (read the Lean file for exact statements; private helpers are deliberately excluded, matching the house convention):
- `Module.FinitePresentation.exists_matrixPresentation` (root namespace) — every finitely presented module has a matrix presentation.
- In namespace `AlgebraicGeometry.Scheme.Modules`: `exists_matrixPresentation_pullback_sections`, `flat_sections_of_coherentSheafFlat_id`, `coherentSheafFlat_id_of_charts`, `rankAtStalk_sections_eq_pointRank`, `pointRank_pullback`, `strataData_le_ker`, `existsUnique_stratumLift`, `coherentSheafFlat_stratum`, `exists_presentationChart_mem`, `chartLocus`, `mem_chartLocus_of_pointRank_eq`, `pointRank_le_of_mem_chartLocus`, `coherentSheafFlat_id_pullback`, `chartsCover_chartLocus`, `rankStratum`, `rankStratumι`, `mem_range_rankStratumι_iff`, `coherentSheafFlat_rankStratum`, `exists_pointRank_le`, `isOpen_pointRank_pullback_eq`, `existsUnique_factor_rankStratum`.
- `AlgebraicGeometry.flatLocusStratification_universal` — already has its node (item 1), do not duplicate.

Notes on math content: `pointRank` is the fiber dimension dim_{κ(s)} F(s); the chart locus W_e is the union of all e-presentation charts, open, containing the rank-e locus (Nakayama) and contained in {rank ≤ e}; rankStratum is the closed subscheme of W_e cut by the strata ideal (from sec:flatstrat_rank_stratum), so a locally closed subscheme of S; the DAG statement-level `\uses` convention: only statement-level uses register edges (proof-level uses are fine to include but don't create DAG edges).

3. After editing, refresh the DAG caches per the leandag skill (`"$HORIZON_BIN" leandag refresh` or as the skill says) and verify the AJC project reports 0 dangling nodes. Fix any dangling you introduced.

4. Commit your blueprint edit yourself per the horizon-commit skill: `"$HORIZON_BIN" commit -m "<math-first message>" blueprint/src/chapters/Picard_FlatteningStratification.tex` (plus any DAG cache files it tells you to include), with git identity exported: GIT_COMMITTER_NAME='Archon Horizon' GIT_COMMITTER_EMAIL='archon-horizon@local' and the AUTHOR pair likewise.

Report back: the section/labels you added, the \uses you set on the universal theorem's proof, the leandag verification result, and the commit hash.
