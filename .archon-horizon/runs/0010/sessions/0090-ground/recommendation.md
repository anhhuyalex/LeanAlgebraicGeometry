# Orientation — run 0010 T12 (`AJC.picrep`)

- Useful context: `Grassmannian.representable` (`AlgebraicJacobian/Picard/GrassmannianRepresentability.lean`, 661 LOC) is now a real proof modulo exactly two typed-sorry leaves — `Scheme.representable_of_openCover` (Zariski descent of representability, EGA 0_I 4.5.4; line 445) and `Grassmannian.isZariskiSheaf` (line 634); building blocks `representable_of_iso_free`/`representable_restrict` are axiom-clean `[propext, Classical.choice, Quot.sound]`, verified this reconcile. Representative `lake build` green (8592 jobs).
- Relevant leaves: `representable_of_openCover` route is scheme gluing via `Scheme.GlueData` + cocycle-from-uniqueness + `OpenCover.glueMorphisms`; `isZariskiSheaf` route is module gluing along a cover via the `GlueDescent.lean` `Scheme.Modules.glue` engine, quotient comparison isos unique so the cocycle is free. Both docstrings carry the intended route; memory `[[grassmannian-representability-descent]]` has the landed recipes (`forgetAdjStar`, coherence-normalization, eager-instance gotcha). The descent leaf also serves Quot and Pic representability.
- Consistency note: `QuotScheme` (`QuotFunctorDef.lean`, 5 sorries) is a statement trap — pinned on `[IsProper π]` only, not the `[Nitsure]` projective/very-ample hypotheses (`I-0118`); Mathlib v4.31 lacks the ampleness vocabulary (`I-0109`), so a faithful restatement precedes any proof attempt. Blueprint node `thm:grassmannian_representable` carries statement-`\leanok` (proof unmarked, honest); `sec:quot_base_change` nodes carry statement-`\leanok` only.
- Relevant files/refs: `blueprint/src/chapters/Picard_QuotScheme.tex` (new nodes `lem:grassmannian_representable_free`, `def:zariski_sheaf_over`, `thm:representability_zariski_descent`, `lem:grassmannian_zariski_sheaf`); source `references/nitsure-hilbert-quot-src/nitsure-hilbert-quot.tex` L798-L930.
