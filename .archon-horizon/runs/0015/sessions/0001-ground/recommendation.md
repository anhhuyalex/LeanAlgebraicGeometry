# Orientation — run 0015, T14 (AJC.picrep, projectivity foundation)

- Useful context: the two open T14 leaves are `twistTransition_cocycle` (`Picard/SerreTwist.lean:302` — C2 triple-overlap cocycle for the Serre-twist transitions, a rank-1 analogue of `bundleTransition_cocycle_transport` in `Picard/GrassmannianQuot.lean`) and `sectionGradedModule_fg` (`Picard/SerreFiniteness.lean:62` — Serre finiteness of the section graded module, [Hartshorne] II.5.17 ff. / [Nitsure] §1). Both are honest typed `sorry`s; nothing else in the four T14 files is open.

- Relevant memory: `I-0126` holds the durable T14 constraints — glue engine is universe-monomorphic at `Scheme.GlueData.{0}` (so `serreTwist` and the `IsProjectiveWith` predicate live at `Scheme.{0}`), the O(1) sign convention (`i→j` transition multiplies by `(Xᵢ/Xⱼ)^m`), and `quot_boundedness` deliberately left as a blueprint-only target (not stated in Lean to avoid the `I-0118` over-strong-statement trap). `I-0109`/`I-0113`/`I-0120` cover the Hilbert-polynomial / Quot / qcqs-section-localization substrate that `sectionGradedModule_fg` ultimately feeds.

- Blueprint + references: `sec:projective_vocabulary` (`blueprint/src/chapters/Picard_QuotScheme.tex:5045`, 12 nodes, `\source{nitsure-hilbert-quot}`); the `sectionGradedModule_fg` node is at `:332`, `twistTransition_cocycle` at `:5180`. The Nitsure source is in `references/` (slug `nitsure-hilbert-quot`, tex/md/pdf) — §1 for coherent finiteness, §5 for the Quot endgame. `IsProjectiveWith` (`Picard/ProjectiveMorphism.lean`) is the very-ample encoding chosen in `I-0118` to keep the Quot statement provable.

- Env: full `lake build` is incrementally green (8666 jobs, exit 0; the only `error:`-tagged lines are `linter.style.header` info noise). Project-wide `sorry` count is 30 (all documented leaves across cones). The workspace ledger is current, but the per-project journal `Algebraic-Jacobian-Challenge.git` still points at r5 — history-only lag, no data risk. Prior blocker (`I-0127`): run-0014 T14 follow-ups aborted on Fable 5 credit exhaustion, not mathematics.
