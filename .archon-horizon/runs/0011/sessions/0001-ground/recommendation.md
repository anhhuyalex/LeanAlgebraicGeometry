# Recommendation — run 0011 opening Ground (focus T13, retire the genus-0 / Route-C split)

Orientation for the first Horizon agent of this run. Grounded in what I read; you self-scope.

- **Most of the split is already gone; the remaining work is residue, not a lane.** `AlgebraicJacobian/Genus0BaseObjects.lean`, `AbelianVarietyRigidity.lean`, and the `genusZeroWitness`/`positiveGenusWitness` dite are already deleted (iter-073 refactor); `Jacobian C := (jacobianWitness C).J` is uniform in the genus (`picardJacobianWitness`, `Jacobian.lean:170-186`). What survives is stale prose in `Jacobian.lean`, `AbelJacobi.lean`, `RigidityLemma.lean:787`, and generated leftovers — `blueprint/web/chap-Genus0BaseObjects_Cross01Substrate.html` and `.lake/**/Genus0BaseObjects.*` (regenerated on build).

- **`RiemannRoch/WeilDivisor.lean` is dual-purpose — its `Scheme.PrimeDivisor` / `Scheme.RationalMap.order` codim-1 substrate feeds `Albanese/CodimOneExtension.lean` (`ALB.codim1`, live north-star path; imports it at line 7, uses order valuation at 1823 and the coheight-1 convention at 177), while the `degree` / `principal_degree_zero` / `positivePart` / RR→ℙ¹ portion (5 sorries, "Route C PAUSE") is the obsolete genus-0 target `genusZero_curve_iso_P1`.** A wholesale delete breaks the codim-1 extension; a surgical carve of the RR/degree portion (and the file header that frames the whole file around `genusZero_curve_iso_P1`) is the dependency-safe shape. See memory `I-0105`.

- **Relevant blueprint files (in your write scope, `MainProjects/Algebraic-Jacobian-Challenge/blueprint/src/chapters/`): `AbelianVarietyRigidity.tex` is LIVE substrate** — all 12 `\lean` pointers resolve to the sorry-free `RigidityLemma.lean` rigidity-lemma + Milne §I.1–I.3 corollaries (`rigidity_lemma`, `hom_additive_decomp_of_rigidity`, `av_regularMap_isHom_of_zero`), used by the uniform Albanese construction; only its "feed the genus-0 base case (Route C)" prose is stale. `RiemannRoch_WeilDivisor.tex` mirrors the dual-purpose Lean file. `Picard_IdentityComponent.tex` / `Albanese_AlbaneseUP.tex` mention genus-0 only in the "genus 0 is not special" prose.

- **Roadmap has no genus-0 / Route-C milestone to retire** (checked the full `horizon roadmap` index); the split lives only in code comments and blueprint prose, so the "update the roadmap" part of T13 is likely a no-op beyond confirming. AJC currently reports 2 dangling blueprint nodes and builds green (8646 jobs, kernel-verified last run per `I-0104`); a `horizon leandag` refresh after the carve will show whether the RR-genus-0 node removals clear or add danglers.
