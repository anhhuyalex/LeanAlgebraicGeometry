# Orientation — run 0013, session 0024-ground

- Useful context (Albanese leg, `ALB.codim1` / `AJC.albanese` active): the function-field pullback bridge landed and is Ground-verified axiom-clean. Relevant file `MainProjects/Algebraic-Jacobian-Challenge/AlgebraicJacobian/Albanese/RationalMapFunctionField.lean` — `Scheme.RationalMap.{stalkPullback, fromFunctionField_base_eq_genericPoint, functionFieldPullback}`, blueprint-pinned `def:functionFieldPullback` + `lem:fromFunctionField_base_eq_genericPoint` (`Albanese_CodimOneExtension.tex`). The sole open extension sorry is `indeterminacy_pure_codim_one_into_grpScheme` (Milne 3.3, `CodimOneExtension.lean:1721`). Memory `t9-albanese-endgame-unblock-map` and inbox `I-0065` carry the honest Milne 3.1/3.2/3.3 chain.

- Useful context (Milne 3.3 next frontier): substep 1, the difference map `Φ = m∘(f×f) : X×X ⇢ G`, needs `RationalMap` primitives mathlib lacks — `Birational/RationalMap.lean` exposes only `compHom`. The greenfield bricks are rational-map products, precomposition with an open dominant morphism (needs "preimage of a dense open under an open map is dense"), and a germ-definedness criterion; `functionFieldPullback` covers only the substep-3 dominant/generic-point piece. A germ-at-a-point criterion, not this generic-point field hom, is what `Φ` pulls back at `Φ(x,x)=e`.

- Blueprint reconciliation this round: `thm:codim_one_extension` (Milne 3.1) proof was rewritten to the valuative-only coheight-0/coheight-1 case split matching axiom-clean `indeterminacy_codimGe2_of_smooth_of_complete`; the phantom `\uses{cor:regular_cohen_macaulay}` edge and the 0AVF "Step 2" prose were removed, and `rmk:codim1_role_of_ab` retargeted (AB enters the Milne 3.3 regularity substeps, not Milne 3.1). The leandag cache predates this edit; a `horizon leandag` refresh will drop the stale `cor:regular_cohen_macaulay → thm:codim_one_extension` edge.

- Useful context (Picard representability `AJC.picrep`): the `Grassmannian.isZariskiSheaf` leaf has in-progress work — `GrassmannianRepresentability.lean` and a new `GrassmannianZariskiSheaf.lean` (`grassmannian_isZariskiSheafOver`, ~157 lines, sorry-free on disk) were modified ~12:52 UTC by a concurrent run and are as-yet unbuilt (oleans missing), so unverified; a fresh session there could collide. `isZariskiSheaf` (`GrassmannianRepresentability.lean:582`) now delegates to `grassmannian_isZariskiSheafOver`. `QuotScheme` remains defective-as-pinned (`I-0118`: needs projective + relatively very ample + coherent, not merely proper).

- Useful context (other frontiers, unchanged): Flat base change `AJC.fbc` — 3 leaf sorries in `Cohomology/CechHigherDirectImageUnconditional.lean` (`I-0083`, `I-0076`). No Lean was rebuilt this session (edits were blueprint `.tex` + memory only; the two touched Albanese theorems were kernel-verified via `lean_verify`, not re-`lake build`).
