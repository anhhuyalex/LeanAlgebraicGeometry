# Orientation — Run 0013, T9 (Jacobian⇄Albanese endgame glue)

- Useful context: T9's Albanese universal property lives in `MainProjects/Algebraic-Jacobian-Challenge/AlgebraicJacobian/Albanese/AlbaneseUP.lean` (skeleton, 7 real sorries: `bundle` at ~183, and the `descentThroughBirationalSigma`/`albanese_universal_property` assembly). `bundle := sorry` is representability-gated (out of T9 scope per the task objective); the assembly is `thm:albanese_universal_property` ← `lem:descent_through_birational_sigma` ← `thm:rational_map_to_av_extends` (Thm32) in the honest blueprint DAG.

- Relevant files (subproject is ahead — see memory `I-0112`): `SubProjects/Albanese/AlgebraicJacobian/Albanese/Thm32RationalMapExtension.lean` is **sorry-free** vs 2 sorries in the AJC copy; the subproject `CodimOneExtension.lean` is down to **1** sorry (Milne 3.3 substep 4b, `indeterminacy_pure_codim_one_into_grpScheme` ~1721) vs 3 in AJC, backed by three new sorry-free files `PolePurity.lean` / `SmoothPrimeRegularity.lean` / `StandardSmoothDimension.lean`. Porting these into AJC closes the extra AJC sorries; the sole deep remainder is the 3.3 substep-4b diagonal-intersection bound.

- Consistency notes: the AJC blueprint DAG faithfully tracks Lean via `has_sorry` (`thm:albanese_universal_property` proved=False, `lem:descent_through_birational_sigma` has_sorry=True) — no leanok drift found on this leg. `RiemannRoch/WeilDivisor.lean`'s `PrimeDivisor`/`RationalMap.order` substrate is a live dependency of the codim-1 extension (memory `I-0106`); the RR/degree/ℙ¹ block is the only genus-0 residue. Ported subproject files were cheat-scanned clean (no `admit`/`native_decide`/`sorryAx`); kernel-verify on merge since a full AJC `lake build` was not run this session.

- Related dead-ends/recipes already in memory: `I-0065` (the honest Milne 3.1/3.3 chain and the FALSE `codimOneFree ⇒ extension` statement — never re-add), `I-0061` (tangentSpaceIso critical path, for the neighbouring T10). Representability inputs feeding `bundle` stay in the `AJC.picrep` cone (task T12, running).
