id: T5
project: Algebraic-Jacobian-Challenge
objective: 'Advance the Picard-representability (picrep) spine WITHOUT depending on
  flat base change (FBC/02KH), so this runs fully in parallel with the live FBC run
  (T2) in AJC.


  STEP 0 (gate — do this first, report before proceeding): Verify that the `Picard/IdentityComponent.lean`
  import cone is genuinely FBC/cohomology-independent. Trace its transitive imports
  in MainProjects/Algebraic-Jacobian-Challenge. If the cone pulls in the Čech/cohomology
  base-change engine (Cohomology/Cech*, the 02KH flat-base-change modules), STOP and
  report — do not extract; instead reduce scope to only the FBC-free declarations.
  If clean, continue.


  STEP 1 (extraction — own build dir, MUST NOT build the AJC tree): Create a NEW standalone
  Lean subproject (e.g. SubProjects/Picard-IdentityComponent) with its own lakefile.toml
  (require mathlib rev v4.31.0, packagesDir ../../.lake-packages), lean-toolchain
  leanprover/lean4:v4.31.0, and its own .lake — mirroring the Albanese/GR-Quot-Closure
  extraction pattern. Copy in `Picard/IdentityComponent.lean` and the minimal import
  cone needed to build it. Register it with `horizon project add` (enable vcs git_dir).
  The point of a separate subproject is build isolation: it must NOT touch or rebuild
  the AJC .lake that T2 is using.


  STEP 2 (prove): Close the 9 structural sorries in IdentityComponent — Pic⁰ as the
  open-and-closed connected-component subgroup functor and the abelian-group-structure
  (Yoneda) transport — the parts that do NOT need the cohomology / flat-base-change
  engine. Leave any sorry that genuinely depends on FBC or full FGA representability
  documented and untouched, with a note on the blocker.


  DELIVERABLE: the new subproject builds GREEN (lake build EXIT=0) with the FBC-free
  IdentityComponent sorries closed and axiom-clean; blueprint/DAG updated; it is positioned
  to merge back into AJC sorry-free like Albanese/GR. Reads from the AJC tree as source.'
title: Extract & prove Pic⁰ IdentityComponent (FBC-independent picrep spine)
explanation: ''
projects:
- Algebraic-Jacobian-Challenge
priority: high
status: failed
write_set:
  files:
  - AlgebraicJacobian/Picard/IdentityComponent*.lean
  projects:
  - Algebraic-Jacobian-Challenge
  declarations: []
  blueprint_nodes: []
  workspace: false
scope:
  projects:
  - Algebraic-Jacobian-Challenge
  files:
  - AlgebraicJacobian/Picard/IdentityComponent*.lean
roadmap_refs: []
inbox_refs: []
artifact_refs: []
created_at: '2026-07-02T04:28:32.035980+00:00'
updated_at: '2026-07-03T03:21:14.773238+00:00'
metadata:
  author: ground
