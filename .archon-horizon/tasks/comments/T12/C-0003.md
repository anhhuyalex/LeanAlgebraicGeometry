---
id: C-0003
item: T12
author: local
at: '2026-07-06T03:34:52.290732+00:00'
---

**Ultracode session (Claude Fable 5, 4 parallel provers): the Quot/Div base-change layer of the Picard cone is closed.** Full `lake build` green (8662 jobs); all closures kernel-verified.

- **Proper support is stable under base change — PROVED axiom-clean** (`[propext, Classical.choice, Quot.sound]`): for finitely presented $\mathcal F$ with support proper over $S$, annihilators pull back (smul-endomorphism transport along pullback ⊣ pushforward + localization vanishing on affines, Stacks 056H/01PC), giving a closed immersion of the support of $g'^*\mathcal F$ into the base-changed support (Stacks 01W4). New file `QuotSupportBaseChange.lean`, zero sorries; `lem:proper_support_base_change` proof \leanok.
- **The fibrewise Hilbert function is invariant under base change** — `hilbertFunction_quotBaseMap` closed by pasting the fibre squares ($({X_{T'}})_{t'} = (X_T)_t \times_{\kappa(t)} \kappa(t')$, proved axiom-clean) and transporting $\dim_\kappa\Gamma$ across the comparison isos, modulo two named leaves: `lem:pullback_moduleTensorPow` (Stacks 01CD) and `lem:gamma_fiber_baseChange_field` (Stacks 02KH, $i=0$ over a field). **The Quot functor's base-change action `pullbackAlong` is now fully well-defined** (fp, flatness, proper support all proved).
- **Lane-F flat base change (Stacks 02KH $H^0$)**: the three broad sorries in `QuotScheme.lean` are closed; the Beck–Chevalley elementwise compatibility of the canonical mate (KEY-BC) is **proved outright axiom-clean**; one precise leaf remains (the 02KE finite-cover-equalizer heart, 2 bricks already proved).
- **$\mathrm{Div}_{C/k}$ is a REAL functor** (Kleiman §3): families $\mathcal O_{X_T} \twoheadrightarrow \mathcal O_D$ flat with invertible kernel ideal, base change through the Quot engine (new `DivFunctorDef.lean`, one typed leaf `lem:relative_divisor_base_change`). `instHasDivFunctor` proved.
- **FGA assembly 4 → 2 sorries**: `instPicSchemeLocallyOfFiniteType` proved by strengthening the `HasPicScheme` existential with local finiteness (truth-preserving, Kleiman th:main(1)). Remaining: `instHasPicScheme` (the FGA theorem itself) and `instHasAbelMap` (natural next step: $D \mapsto [(\ker q)^{-1}]$, whose naturality is exactly the new divisor leaf).
- **I-0118** (Quot headline statement trap): restatement design posted on the item — project-side $\mathbb P(n;S)$ via Mathlib `Proj` + $\mathcal O(1)$ glued by the GlueDescent engine (fallback: SectionGradedRing + `Proj.fromOfGlobalSections`). Statement left open by design.

Remaining T12 leaf inventory: 2 Hilbert leaves, 1 FBC heart, 1 divisor leaf, QuotScheme restatement+proof (gated on the ampleness vocabulary, same block as I-0109), then Abel map → th:repDiv → Altman–Kleiman descent → `instHasPicScheme`. Commits: 336b55b532, f4acf06e75, 3e7cb7c1e9.
