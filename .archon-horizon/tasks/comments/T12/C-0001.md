---
id: C-0001
item: T12
author: horizon
at: '2026-07-03T08:59:16.562537+00:00'
---

Run 0010 Horizon (session 0003): **two sorries closed in the T12 cone**, both kernel-verified (`lake build` green, 8642 jobs).

- **`flatteningStratification.ofCurve` closed** (`FlatteningStratification.lean`, 7→6 sorries): direct instantiation of the main theorem at `pr_T = pullback.snd C.hom T.hom` (properness by base change, instance-synthesized). Reduces leaf count; still rests on the open main theorem (not axiom-clean until that closes).
- **`tildeIso_of_isQuasicoherent_isAffineOpen` closed, axiom-clean** (`QuotScheme.lean`, 12→11 sorries) — the Stacks 01I8 Step-1 pin *with* its Σ-pair unit identity. The Ground recommendation rated this ">1 session"; it landed in one because the file's own union-merge tail already had the sorry-free substrate (`isIso_fromTildeΓ_of_isQuasicoherent`, `isQuasicoherent_pullback_fromSpec`). Proof: base map bijective via `Adjunction.unit_leftAdjointUniq_hom_app` + `restrictAdjunction_unit_app_app` + `restrictFunctorIsoPullback` components; iso = `(tilde.map b ≫ fromTildeΓ)⁻¹`; identity via `toOpen` naturality + counit-at-`⊤`.
- **Structural**: the Lane F block was relocated below the union-merge banner in `QuotScheme.lean` (verified order-safe) so Step 1 could consume the substrate. Blueprint node `lem:tildeIso_of_isQuasicoherent_isAffineOpen` got its complete proof + `\leanok`.
- Consumer effect: `_sectionLinearEquiv` Stage 1 now feeds off a real Σ-pair witness; its Stages 2–6 (N1–N4 `baseMap` coherence helpers) are the next unbundled targets.
- Filed `I-0087` (blueprint gap: gap1/gap2 machinery has no nodes, dangling crefs), memories `I-0088`/`I-0089` (ModuleCat-hom construction recipes; updated T12 triage).
