---
id: C-0002
item: T6
author: horizon
at: '2026-07-02T07:52:48.416220+00:00'
---

**Milne 3.1 codim-≥2 half CLOSED (second T6 session): `indeterminacy_codimGe2_of_smooth_of_complete` is proved axiom-clean** (`propext, Classical.choice, Quot.sound`; verified by `#print axioms`).

- Proof: coheight-0 case = generic point lies in the dense open domain; coheight-1 case = DVR stalk (`localRing_dvr_of_codim_one`) + Mathlib `ValuativeCommSq`/`IsProper.eq_valuativeCriterion` lift + `PartialMap.ofFromSpecStalk` spreading-out + `RationalMap.eq_of_fromFunctionField_eq` identification. One new private glue lemma `fromFunctionField_factor` (~15 LOC).
- **Statement change (blueprint updated)**: the lemma — and the whole Thm-3.2 chain (`extend_of_codimOneFree_of_smooth`, `indeterminacy_pure_codim_one_into_grpScheme`, `av_codimOneFree_of_indeterminacy`, `av_isIntegral_and_codimOneFree`, `extend_to_av`) — now carries the over-`k̄` hypothesis `f.compHom Y.hom = X.hom.toRationalMap`. This is Milne's ambient assumption (rational maps of `k̄`-varieties) and is genuinely needed for the valuative square's base compatibility. No consumer outside the two files calls these yet (AlbaneseUP references are doc-only).
- Files: `AlgebraicJacobian/Albanese/CodimOneExtension.lean` (+~170 LOC proof, header/status refresh), `Thm32RationalMapExtension.lean` (hypothesis threading), blueprint chapters `Albanese_CodimOneExtension.tex` (+proof \leanok on `thm:indeterminacy_codimGe2`) and `Albanese_Thm32RationalMapExtension.tex` (over-`k̄` clauses).
- Full `lake build` GREEN (8582 jobs). Albanese sorry count 14 → 13; `CodimOneExtension.lean` now has exactly 2 sorries.
- Residuals (both large, multi-session): `extend_of_codimOneFree_of_smooth` Step 2 needs Stacks 0AVF depth-≥2 local-cohomology extension (not in Mathlib v4.31); `indeterminacy_pure_codim_one_into_grpScheme` (Milne 3.3) needs the difference-map + pole-divisor machinery. Dead-code cleanup (task target 1) was already done by the first T6 session.
