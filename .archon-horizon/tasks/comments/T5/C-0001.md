---
id: C-0001
item: T5
author: horizon
at: '2026-07-02T04:56:25.198544+00:00'
---

**STEP 0 (gate) PASSED** — transitive import cone of `Picard/IdentityComponent.lean` re-traced this session: exactly 9 AJC modules (`IdentityComponent`, `FGAPicRepresentability`, `Genus`, `Cohomology/SheafCompose`, `Cohomology/StructureSheafAb`, `Cohomology/StructureSheafModuleK{,.Carriers,.Presheaf,.SheafProperty}`) + Mathlib. **No `Cohomology/Cech*`, no 02KH flat-base-change modules** — FBC-free, safe to run in parallel with T2.

**STEP 1 (extraction) landed**: `SubProjects/Picard-IdentityComponent` created on the Albanese template —

- own `lakefile.toml` (mathlib `v4.31.0`, `packagesDir ../../.lake-packages`), `lean-toolchain v4.31.0`, own `.lake`; copied `lake-manifest.json` so mathlib oleans are reused (**zero AJC-tree rebuild**; mathlib fully cached, only the 10 project modules compile).
- blueprint: 6 chapters (`Picard_IdentityComponent`, `Picard_FGAPicRepresentability`, `Genus`, 3× `Cohomology_*`) with dangling cross-chapter `\cref`s demoted to `\texttt` (Albanese precedent); 0 dangling refs after demotion.
- registered via `horizon project add` + vcs `git_dir` enabled in `config.yaml`; pruned `archon-protected.yaml` (keeps `AlgebraicGeometry.genus`).

`lake build` running now; STEP 2 note: per run-0005 Ground verification all 9 IdentityComponent sorries are **blocked** (037Q/04KU + FGA taint), so they stay as documented placeholders per the Ground recommendation.
