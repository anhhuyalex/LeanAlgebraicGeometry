---
id: C-0002
item: T5
author: horizon
at: '2026-07-02T04:59:10.894866+00:00'
---

**STEP 1 deliverable LANDED — `SubProjects/Picard-IdentityComponent` builds GREEN** (`lake build` EXIT=0, 8568 jobs; mathlib fully cache-hit, only the 10 project modules compiled; AJC `.lake` untouched — T2 unimpeded).

- All 9 copied `.lean` files verified **byte-identical to AJC** (clean merge-back baseline). Root module + README + pruned `archon-protected.yaml` added. ~2.5k LOC cone.
- Sorry ledger as documented: **9** in `IdentityComponent.lean` (all blocked per run-0005 Ground verification: 037Q/04KU + FGA taint), **7** typed-instance `⟨sorry⟩` sites in `FGAPicRepresentability.lean`.
- DAG: `horizon blueprint` refreshed — 94 nodes / 134 edges / 0 dangling. 3 isolated nodes (`def:Scheme_IsCechAcyclicCover` + 2 thms) are extraction artifacts: declared in the included `Cohomology_StructureSheafModuleK.tex`, consumed only by the pruned `Cohomology_MayerVietoris.tex`. Left in place to keep files identical to AJC.

**Marginal `Pic0Scheme` tidy: gate NEGATIVE, skipped by design.** `LocallyOfFiniteType (PicScheme C).hom` does not resolve: `HasPicScheme` packages only the `RepresentableBy` witness (no finiteness data), and no instance exists anywhere in the cone. Making `Pic0Scheme := GroupScheme.IdentityComponent (PicScheme C)` would require a NEW `PicSchemeLocallyOfFiniteType` typed-sorry instance class (Kleiman §4: Pic is LFT as disjoint union of quasi-projectives) — net sorry count unchanged, so deferred per the Ground recommendation. That class is the natural first move if the tidy is wanted later.

**STEP 2 not attempted** per Ground recommendation (all 9 sorries verified blocked, not proof-search debt; do not chase 037Q/04KU or duplicate `AJC.pic0av`).

Roadmap note for `AJC.picrep`: the IdentityComponent leg is now iterable in isolation, in parallel with `AJC.fbc` (T2). Session: run 0005 / 0003-horizon-T5, 1 build run, new project registered with vcs enabled.
