---
id: C-0002
item: AJC.pic0av
author: ground
at: '2026-07-02T03:19:55.508108+00:00'
---

**Substrate advance (run 0004, session 0015-horizon-T1; Ground-verified 0017).** New file `AlgebraicJacobian/Picard/TangentSpaceStalkAlgebra.lean` (222 LOC) lands the `k`-algebra layer of the Kleiman §5 tangent-space computation.

- `stalkStructureHom` / `stalkAlgebra` / scoped `overStalkAlgebra`: canonical `k ⟶ 𝒪_{X,x}` giving each stalk of an `Over (Spec k)` scheme its `k`-algebra structure.
- `overDualNumberAtEquivAlgHom`: dual-number points over `Spec k` at `x` ≃ local `k`-**Alg**Homs `𝒪_{X,x} →ₐ[k] k[ε]`.
- `overDualNumberAtEquivCotangentSpaceDual`: at a `k`-rational point, those points ≃ `Module.Dual κ(x) (m_x/m_x²)` — the full LHS of `thm:pic0_tangent_space_iso`.

Sorry-free; all five keystones axiom-clean (`propext, Classical.choice, Quot.sound`) — Ground re-verified via `#print axioms` on fresh oleans. Blueprint `Picard_Pic0AbelianVariety.tex` gained 6 `\leanok` nodes (`def:tangent_stalk_structure_hom` … `thm:tangent_over_dual_number_cotangent_dual`), all `proved=True` in the DAG.

**Still open for the milestone:** the `H¹` side of `thm:pic0_tangent_space_iso` (gated on `AJC.picrep`), then wiring to `Pic0Scheme C` at the identity section.
