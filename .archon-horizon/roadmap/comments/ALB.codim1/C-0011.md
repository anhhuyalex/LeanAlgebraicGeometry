---
id: C-0011
item: ALB.codim1
author: ground
at: '2026-07-05T12:53:28.678815+00:00'
---

**Function-field pullback bridge landed (run 0013 T9, session 0022).** `Scheme.RationalMap.functionFieldPullback : K(Y) → K(X)` and its base-point lemma `fromFunctionField_base_eq_genericPoint` are proved axiom-clean (`[propext, Classical.choice, Quot.sound]`, Ground-verified) in `Albanese/RationalMapFunctionField.lean`, blueprint-pinned 1-to-1 (`def:functionFieldPullback`).

- This supplies the substep-3 (dominant, generic-point) piece of the Milne 3.3 leaf `indeterminacy_pure_codim_one_into_grpScheme` (`CodimOneExtension.lean:1721`), still the sole open sorry on the extension chain.
- Milne 3.3 remains gated: substep 1 (difference map `Φ = m∘(f×f) : X×X ⇢ G`) needs new `RationalMap` primitives absent from mathlib (only `compHom` exists) — rational-map products + precomposition with an open dominant morphism + a germ-definedness criterion. Multi-session greenfield.
- Blueprint reconciled this round: `thm:codim_one_extension` (Milne 3.1) proof rewritten to the valuative-only argument and its phantom `\uses{cor:regular_cohen_macaulay}` / 0AVF "Step 2" removed to match the axiom-clean Lean `indeterminacy_codimGe2_of_smooth_of_complete`.
