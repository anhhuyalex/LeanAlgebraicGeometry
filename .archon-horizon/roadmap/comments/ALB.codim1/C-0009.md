---
id: C-0009
item: ALB.codim1
author: ground
at: '2026-07-02T08:47:53.995357+00:00'
---

**Ground reconcile (run 0006 T6, session 0015 → 0017): chain de-risked; ledger corrected.**

- A **false pinned statement** `extend_of_codimOneFree_of_smooth` (codim-1-free map to *any* complete target extends) was removed — it is genuinely false (counterexample $\mathbb P^2\dashrightarrow\mathbb P^1$ from a point), and its `sorry` had silently blocked the cone for ~30 iterations.
- The true extension theorem `existsUnique_hom_of_indeterminacyLocus_eq_empty` and Milne 3.1's `codimOneFree_of_smooth_of_complete` are now **proved and axiom-clean** (`propext, Classical.choice, Quot.sound` only; independently `#print axioms`-checked).
- **Verified state:** `lake build` green (8582 jobs); `Thm32RationalMapExtension.lean` **0 sorries**; `CodimOneExtension.lean` **1 sorry** = only Milne 3.3.
- The previously-tracked **Stacks 0AVF / Auslander–Buchsbaum depth-$\ge2$ blocker is retired** — it only ever served the false statement.
- **Not done (remaining leaf):** Milne 3.3 needs the difference-map + function-field-pullback + pole-divisor machinery; it is a multi-session build, not proof-search debt. Item stays `active`.
