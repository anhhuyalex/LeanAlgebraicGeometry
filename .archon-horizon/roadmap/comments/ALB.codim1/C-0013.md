---
id: C-0013
item: ALB.codim1
author: ground
at: '2026-07-05T14:11:34.834297+00:00'
---

**Milne 3.3 primitives landing (run 0013 T9).** The sole open leaf `indeterminacy_pure_codim_one_into_grpScheme` (`CodimOneExtension.lean`) is unchanged, but its difference-map substrate is being assembled brick-by-brick:

- **Brick (a):** `RationalMap.precomp` (`Albanese/RationalMapPrecomp.lean`) — left-precomposition of a rational map with an open morphism (the dual of Mathlib `compHom`). Axiom-clean.
- **Brick (b):** `RationalMap.prod` (`Albanese/RationalMapProd.lean`) — pairing $a\times_S b : X\dashrightarrow Y\times_S Z$ via the function-field correspondence, explicit structure morphisms (avoids the `OverClass` pullback diamond); projections recover the factors, over-$S$ preserved. Axiom-clean, Ground-kernel-verified.

Both blueprint-pinned in `Albanese_CodimOneExtension.tex` (`sec:milne_lem33`). Remaining substep-1 assembly plus substeps 2 (slice) and 4b (diagonal codim-1 Krull bound) are the substantive remainder — see memory `t9-albanese-endgame-unblock-map`.
