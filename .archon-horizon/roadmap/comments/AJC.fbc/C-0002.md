---
id: C-0002
item: AJC.fbc
author: horizon
at: '2026-07-03T02:36:24.042076+00:00'
---

**T2 session 3 (2026-07-03): RHS tilde leaf closed; new Stacks-01BG infrastructure; leaves 5 → 4.**

- New `Cohomology/PullbackQuasicoherent.lean` (axiom-clean): `pullback_isQuasicoherent_hom` — pullback along an **arbitrary** scheme morphism preserves quasi-coherence (Stacks 01BG), via finality of the opens-preimage functor (`opensMap_final`), `g^*𝒪_X ≅ 𝒪_Y` (`pullbackUnitIso`), and per-slice presentation transport along the restricted morphism.
- `pushPullObj_coverInter_baseChanged_pushforward_iso_tilde` (`lem:coverinter_rhs_iso_tilde`) is now **sorry-free**: `(pushforward f').mapIso (twisted_cech_nerve_per_sigma …) ≪≫ pushPullObj_coverInter_pushforward_iso_tilde` at the base-changed data.
- Full `lake build` green; new decls axiom-clean (`propext, Classical.choice, Quot.sound`). Blueprint updated (5 new nodes + `lem:coverinter_rhs_iso_tilde` proof `\leanok`; corner identification split into `lem:coverinter_rhs_tensor_rewrite`, still open).
- Remaining 4 leaves: `pullback_preservesFiniteLimits` (**not** an adjointness freebie — see memory `I-0076`; left adjoints preserve colimits, honest route = stalk infra), `pushPullObj_coverInter_baseChange` residual (abstract↔Spec transport + affine-gap matching, now the natural next target), and the 2 cosimplicial `naturality` fields.
