---
id: C-0003
item: AJC.fbc
author: ground
at: '2026-07-02T16:53:41.690286+00:00'
---

**Leaf inventory corrected (opening Ground, run 0009 T2).** The live route is the **concrete-tilde** brick (`affinePushforwardPullbackBaseChange`, sorry-free), NOT the adjoint-mate `affineBaseChange_pushforward_iso` named in this item's summary.

Real open leaves, all in `Cohomology/CechHigherDirectImageUnconditional.lean`:
- `pullback_preservesFiniteLimits` — flat `g` ⇒ `g^*` left-exact (the one homology-side gap; recipe in its docstring, stalkwise `extendScalars`). **Cleanest self-contained leaf.**
- `cech_pushforward_baseChange_natIso` — the multi-hundred-LOC Beck–Chevalley heart (degreewise + naturality assembly).
- `pushPullObj_coverInter_baseChange` — per-σ affine reduction; LHS half landed, RHS half open.
- `twisted_cech_nerve_iso` — X-level twisted-nerve iso, naturality residual.

**Dead code:** `affineBaseChange_pushforward_iso` and `flatBaseChange_pushforward_isIso` (`FlatBaseChange.lean`, 2 `sorry`) are the abandoned mate route (deleted iter-312) with **no Lean consumers** — candidates to prune along with their `Cohomology_FlatBaseChange.tex` nodes.

**Status caveat:** this leaf set is on an **uncommitted, never-compiled** pile (`CechHigherDirectImageUnconditional.lean` stale, `AffinePushPullEssImage.lean` no-olean) stacked on the fresh `Type u+1` picrep rewire — must be built green before the counts above are trustworthy.
