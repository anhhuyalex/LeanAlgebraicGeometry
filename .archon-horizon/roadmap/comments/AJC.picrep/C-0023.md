---
id: C-0023
item: AJC.picrep
author: ground
at: '2026-07-05T03:17:51.136165+00:00'
---

**Generic-flatness substrate CLOSED (Nitsure §4 / Stacks 051R + 05F7), Ground-verified.** The FlatteningStratification cone of this item is now sorry-free and axiom-clean.

- `GenericFreeness.genericFlatnessAlgebraic` (algebraic generic freeness: for a noetherian domain `A`, finite-type `A`-algebra `B` and finite `B`-module `M`, some `M_f` is `A_f`-free) is **proved** — dévissage over `B` via `IsNoetherianRing.induction_on_isQuotientEquivQuotientPrime` plus the Noether-normalisation domain core `genericallyFree_quotient_prime`. The transient r3 re-open (three localized-module instance walls) is closed.
- `GenericFlatnessGeometric.genericFlatness` (geometric, Nitsure §4) and `flatLocusStratification_universal` (`n=0` flattening stratification) are sorry-free.
- **Independent kernel check** (Ground): full `lake build` green (8646 jobs, exit 0); `#print axioms` on `genericFlatnessAlgebraic`, `genericallyFree_quotient_prime`, `flatLocusStratification_universal` each = `[propext, Classical.choice, Quot.sound]`.

**Frontier of `AJC.picrep`** stays: the 3 `canonicalBaseChangeMap_app_app_isIso_*` leaves in `QuotScheme.lean` (gated on `AJC.fbc` / Stacks 02KH flatness algebra) and the 5 headline Quot/Grassmannian representability decls (`QuotFunctor`, `QuotScheme`, `Grassmannian`, `Grassmannian.representable`, `hilbertPolynomial`) awaiting a Route-C coherent-$\chi$ / Riemann–Roch substrate.
