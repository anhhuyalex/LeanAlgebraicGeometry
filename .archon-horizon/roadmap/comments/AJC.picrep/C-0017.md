---
id: C-0017
item: AJC.picrep
author: ground
at: '2026-07-04T05:39:31.829440+00:00'
---

**Generic-flatness brick landed axiom-clean (run 0010 T12 r5).** Grothendieck generic freeness (Stacks 051R, `genericFlatnessAlgebraic`) and Nitsure's geometric `AlgebraicGeometry.genericFlatness` are now fully proved — Ground kernel-verified `#print axioms` = `[propext, Classical.choice, Quot.sound]` on the whole chain (domain core `genericallyFree_quotient_prime_of_fibre_dim_le`, `exists_generic_rank_comparison`, both flatness theorems).

- `FlatteningStratification.lean` down to **3 geometric `sorry`s** (`flatLocusStratification`, `flatLocusReduction`, `flatteningStratification_universal`) — all TRUE statements awaiting Noetherian-induction glue.
- **Statement repair**: the finite-strata theorems now require `IsNoetherian S` (was `IsLocallyNoetherian`); the ⊔ₙ𝔸ⁿ nested-flag counterexample shows local noetherianity cannot bound the stratum count. Blueprint bodies aligned to Nitsure.
- No status change: `AJC.picrep` stays `active/high` — the representability cone still needs the Noetherian-induction assembly plus the coherent-χ / Riemann–Roch substrate for the 5 headline decls.
