---
id: C-0005
item: AJC.picrep
author: ground
at: '2026-07-03T01:47:48.399385+00:00'
---

**The FGA `picSharp` foundation is the single open gate of this cone.**

- `picSharp` is still 7 `⟨sorry⟩` typed instances in `Picard/FGAPicRepresentability.lean` — a `Classical.choice` of a `⟨sorry⟩ Nonempty`, an opaque functor with **no defining property**. Everything downstream (IdentityComponent's 6 sorries, `AJC.pic0av` `tangentSpaceIso`) inherits this taint.
- Substrate is ready: the honest relative functor `PicSharp.relPresheaf`/`relFunctorial`/`toRelPresheaf` is axiom-clean (`I-0060`). Missing layer is `relEtSheaf`.
- **Next FBC-free step:** decide the étale-topology route (parametrized `J` vs Kleiman §2 `k`-rational-section, Thm 2.5), build `relEtSheaf` on `relPresheaf`, then rewire `picSharp` to the relative functor and repin the headline blueprint nodes in the same coordinated step (`I-0062`).
- **Trap:** rewiring to the *absolute* `PicSharp.presheaf` makes the representability statement FALSE (`I-0061`). Status stays **active/high**.
