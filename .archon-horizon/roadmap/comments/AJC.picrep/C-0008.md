---
id: C-0008
item: AJC.picrep
author: ground
at: '2026-07-03T07:49:25.496343+00:00'
---

**Opening-Ground triage (run 0010, T12): the picrep cone has no high-value single-session win — it is gated on deep Mathlib-absent classical AG AND on the still-open T2/FBC engine.** Tree confirmed green (`lake build`, 8642 jobs).

Real in-scope sorry inventory (23): `FGAPicRepresentability` 4, `QuotScheme` 12, `FlatteningStratification` 7; `GrassmannianQuot` and `RelPicFunctor` are sorry-free.

- **The 4 FGAPic stubs are gated on the A.2.b Quot/Hilbert engine** (closure order B→C→A, per `Picard_FGAPicRepresentability.tex` §sorry-closure-order). `instHasPicScheme` (Sorry A) is the full FGA existence theorem — the blueprint itself states it "is NOT expected to close in a single session."
- **`instHasDivFunctor` / `instHasAbelMap` are DELIBERATE flags**, not proof debt: the asserted `Nonempty` is trivially true (const functor / zero nat-trans), retained only to mark `Div_{C/k}` / the Abel map as *awaiting their real definition*. Closing them with vacuous witnesses is false progress — leave them.
- **`QuotScheme` + `FlatteningStratification` headline decls need machinery Mathlib v4.31 lacks**: relative $\mathbb P^n_S$, coherent-sheaf $\chi$ / Snapper polynomiality (`hilbertPolynomial`), Castelnuovo–Mumford boundedness, Fitting-ideal rank strata + generic freeness (`genericFlatness`, `flatteningStratification`), and Altman–Kleiman flat-proper equivalence-relation descent. Each is a multi-session build.
- **The Quot base-change chain (`QuotScheme` L596/620/1065/1227/1278/1328) additionally bottoms out in the unfinished T2 engine.** `cech_flatBaseChange` (Stacks 02KH) is the intended $\chi$-openness input, but `AJC.fbc` still carries **3 leaf sorries** (`CechHigherDirectImageUnconditional.lean` L196/1646/1712). So picrep cannot draw its cohomological inputs from existing infrastructure yet.

**Only genuinely-correct single-session leaf:** `flatteningStratification.ofCurve` (`FlatteningStratification.lean` L530) = `flatteningStratification (pullback.snd C.hom T.hom) F` (properness auto from Mathlib). Correct delegation, wires the Route-A consumer — but rests on the still-sorry L445, so it reduces leaf count, not axiom-debt. **Stretch (real content, likely >1 session):** `tildeIso_of_isQuasicoherent_isAffineOpen` (L676, Stacks 01I8); substrate `qcoh_iso_tilde_sections` (`QcohTildeSections.lean`, sorry-free) exists, residual is the Σ-pair section-vs-tensor identity.

Status left **active** (guidance, not a launch). Recommend the human weigh finishing T2/`AJC.fbc` before a full T12 push — see the paired `info` item.
