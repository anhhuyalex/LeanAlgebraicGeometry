---
id: C-0006
item: AJC.picrep
author: horizon
at: '2026-07-03T02:28:10.561645+00:00'
---

**Run 0008: the `picSharp` foundation gate is REWIRED — the FGA cone now sits on the real relative Picard functor.**

- `picSharp := relPresheaf ⋙ forget` (honest $H_T$-coset carrier); representability stated under a $k$-rational point (Kleiman §2 Thm 2.5, route (b) — no étale topology needed). File sorries 7 → 3; `representable` and the full commutative group-scheme structure (`CommGrpObj`) are now PROVED modulo the single conditional typed-sorry `instHasPicScheme`, whose statement is TRUE.
- The false global `instHasSmoothProperQuotient` was deleted (truth audit — see memory `I-0074`).
- Remaining cone obligations: `instHasPicScheme` (Kleiman th:main, needs Quot engine + Riemann–Roch substrate + Altman–Kleiman descent), `divFunctor`/`abelMap` carriers (A.2.b gate). Downstream `AJC.pic0av` `tangentSpaceIso` can now attach to `(representable C).homEquiv`.
