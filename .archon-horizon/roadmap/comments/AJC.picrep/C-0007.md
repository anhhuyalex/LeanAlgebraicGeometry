---
id: C-0007
item: AJC.picrep
author: ground
at: '2026-07-03T02:44:53.901744+00:00'
---

**Ground reconcile (T5, run 0008): FGA `picSharp` rewire verified sound — no status change.**

Independent review (work-reviewer) + Ground source audit both PASS:

- `picSharp C := PicSharp.relPresheaf C ⋙ forget` is the REAL relative Picard functor (`Pic(C×T)/π_T^*Pic(T)` coset carrier), not the absolute iso-class functor — the documented FALSE-axiom trap was avoided.
- `instPicSharpRepresentable` (via `choose_spec`) and `groupSchemeStructure : CommGrpObj (PicScheme C)` (via `CommGrpObj.ofRepresentableBy`) are genuine proofs; the false global `instHasSmoothProperQuotient` was deleted.
- FGA `sorry`s **7 → 4**, all four remaining statements TRUE: `instHasPicScheme` (the one genuine FGA gap, conditional on `[HasRationalPoint C]`), `PicSchemeLocallyOfFiniteType`, and the pre-existing Quot-blocked `HasDivFunctor`/`HasAbelMap`.
- Blueprint repin consistent (`def:rel_pic_sharp`→`relPresheaf`, `lem:rel_pic_sharp_functorial`→`relFunctorial`, new `def:abs_pic_functor`/`def:has_rational_point`/`def:pic_scheme_lft`); DAG 0 dangling.

**Stays active:** the genuine FGA `sorry` (`instHasPicScheme`) remains and is conditional on a rational point (see `I-0077`). Build not re-run by Ground (concurrent Cech build); rests on Horizon green targeted build + static verification.
