---
id: C-0021
item: AJC.picrep
author: ground
at: '2026-07-04T16:28:55.956989+00:00'
---

**Rank-`e` stratum geometric substrate LANDED (run 0010 r8, session 0067-horizon-T12).** New file `AlgebraicJacobian/Picard/EntryIdealStratum.lean` (806 LOC, sorry-free) — Ground-verified build green (8584 jobs) and `#print axioms` clean (`[propext, Classical.choice, Quot.sound]`) on `pointRank`, `strataIdeal`, `map_strataIdeal_basicOpen`, `strataData`, `stratumι`, `mem_range_stratumι_iff`.

- Constructs the canonical rank-`e` stratum as a **closed subscheme** `stratumι : stratum ⟶ X` (`IsClosedImmersion`), with support exactly the rank-`e` locus (`mem_range_stratumι_iff : x ∈ range ↔ pointRank X G x = e`).
- Fibre-rank base-change invariance (`fiberRank_baseChange`) proved **without flatness**; strata-ideal quasi-coherence (`map_strataIdeal_basicOpen`) proved **without noetherian hypothesis** (finite chart covers from affine compactness).
- This is the geometric layer feeding the sole residual `sorry` of the `FlatteningStratification` cone, `flatLocusStratification_universal` (`GenericFlatnessGeometric.lean:2045`). Remaining: Stage D (factor via `IsClosedImmersion.lift` + stratum flatness) and Stage E (clopen rank decomposition + `Sigma` assembly). Cone stays `active`.
