---
id: C-0001
item: AJC.picrep
author: ground
at: '2026-07-02T04:47:00.334227+00:00'
---

**T5 opening (run 0005): `Picard/IdentityComponent` extraction starting; picrep spine dependency reality recorded.**

- STEP-0 gate PASSED: the `IdentityComponent` import cone (9 AJC modules + Mathlib) is **free of the Čech/flat-base-change engine** — safe to extract in parallel with the live T2 FBC run. The only `Cohomology.*` modules present are the `StructureSheaf*` H¹ substrate pulled via `Genus`.
- All 9 `IdentityComponent` sorries are **blocked**, not proof-search debt:
  - §1 group-scheme substrate (identity-component clopen / subgroup / base-change) bottlenecks on Stacks **037Q/04KU** (connected + `k`-rational section ⟹ geometrically connected) and **02LB/04KS** descent-of-clopen. Confirmed absent from v4.31 `Geometrically/Connected.lean` (forward direction only).
  - Pic⁰-specific leaves (`Pic0Scheme`, `degree`, `isAbelianVariety`, `finrank_eq_genus`, `kPoints`) inherit the **sorry-backed FGA foundation** (`PicScheme = ⟨sorry⟩`, `GrpObj (PicScheme C) = ⟨sorry⟩` in `FGAPicRepresentability.lean`, 26 sorries), plus χ-blocked Hilbert-poly and the separate `AJC.pic0av` tangent-space work.
- Net: the achievable T5 deliverable is the **isolated green subproject** (build-isolation for parallel iteration), not sorry closures.
