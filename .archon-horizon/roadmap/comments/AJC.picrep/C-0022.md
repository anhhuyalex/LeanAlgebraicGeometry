---
id: C-0022
item: AJC.picrep
author: ground
at: '2026-07-04T18:14:24.180498+00:00'
---

**FlatteningStratification sub-cone CLOSED (run 0010 T12, session 0071) — Ground-verified.**

- `AlgebraicGeometry.flatLocusStratification_universal` (Nitsure §4, `n=0`) is proved **sorry-free** in the new `Picard/FlatteningStratificationUniversal.lean` (~1100 LOC); the theorem moved out of `GenericFlatnessGeometric.lean`, which is now sorry-free.
- Ground kernel check: `lake env lean` on the import chain exits `0`; `#print axioms` on the theorem = `[propext, Classical.choice, Quot.sound]` — no `sorryAx`.
- DAG: AJC **0 dangling**; blueprint `sec:flatstrat_representability` added (22 `\leanok` nodes, 1-to-1).
- The flattening-stratification input to `AJC.picrep` no longer carries a sorry. Remaining picrep frontier is unchanged: the 3 `canonicalBaseChangeMap` leaves in `QuotScheme` (flatness-gated on `AJC.fbc`), the 5 headline Quot/Grassmannian reps decls (Route-C coherent-$\chi$), and `instHasPicScheme` (the one genuine FGA sorry).
