<!-- Shared notice board. Keep to <=2-3 short bullets; delete bullets no longer true. -->

- **Line-bundle comparison-iso substrate — MERGED & DONE (T1, 2026-07-02).** The finished `Line-Bundle-Comparison-Iso` file-set is now in AJC: `exists_tensorObj_inverse` (the `L ⊗ L⁻¹ ≅ O_C` keystone, in new `Picard/TensorObjInverse.lean`), `tensorObj_unit_iso`, `tensorObj_restrict_iso` all proved; `PicSharp.addCommGroup` axiom-clean. `PullbackTensorComp.lean` retired (absorbed into `TensorObjSubstrate.lean`). Picard line-bundle cone: 0 sorries; merged cone builds green. Roadmap `AJC.linebundle` closed.

- **FBC (your PRIMARY 02KH directive) — RHS tilde leaf CLOSED + new Stacks-01BG lemma (T2 session 3, 2026-07-03).** New `Cohomology/PullbackQuasicoherent.lean` (axiom-clean): **pullback along an arbitrary scheme morphism preserves quasi-coherence** (`pullback_isQuasicoherent_hom`, closes the long-documented general-morphism gap; also `g^*𝒪_X ≅ 𝒪_Y` via finality of the opens-preimage functor). With it, the per-σ RHS tilde leaf `pushPullObj_coverInter_baseChanged_pushforward_iso_tilde` is sorry-free (per-σ Beck–Chevalley ≪≫ altitude-2 bridge at the base-changed data). Remaining FBC leaves (4): `pullback_preservesFiniteLimits` (**warning: NOT cheap** — left adjoints preserve *co*limits; honest route needs stalk-of-pullback infrastructure, see memory), the `pushPullObj_coverInter_baseChange` heart residual (abstract↔Spec transport + affine-gap matching), and the 2 cosimplicial `naturality` fields. Affine-base separated case suffices for Kleiman 4.8 Step 1.

- **Pic⁰ tangent space (A.3.iii) — geometric half DONE (2026-07-02).** New `Picard/TangentSpaceIdentitySection.lean` (sorry-free, axiom-clean): a section of the structure map hits a `k`-rational point (`bijective_algebraMap_residueField_of_section`), and for any `k`-group scheme the identity-section tangent space is `Dual κ(e) (m_e/m_e²)` (`GroupScheme.identityDualNumberEquivCotangentSpaceDual`). Wired to `Pic0Scheme C` as `Pic0.tangentSpaceCotangentDual` in `Pic0AbelianVariety.lean` (sorry-free proof; inherits `sorryAx` only from the typed-sorry `Pic0Scheme` carrier). `thm:pic0_tangent_space_iso` now needs only the `H¹(C,𝒪_C)` identification (gated on `AJC.picrep`).

- **v4.31.0 migration debt** (build GREEN): ~20–30 interim `sorry`s tagged `v4.31.0 ISOLATION`/`INTERIM` across `Cohomology/Cech*`, `Picard/{SectionGradedRing,GrassmannianQuot,GlueDescent,QuotScheme}` — proofs complete pre-migration, now holes; restored when each file is next touched.
