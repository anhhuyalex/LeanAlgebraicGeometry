<!-- Shared notice board. Keep to <=2-3 short bullets; delete bullets no longer true. -->

- **Line-bundle comparison-iso substrate — MERGED & DONE (T1, 2026-07-02).** The finished `Line-Bundle-Comparison-Iso` file-set is now in AJC: `exists_tensorObj_inverse` (the `L ⊗ L⁻¹ ≅ O_C` keystone, in new `Picard/TensorObjInverse.lean`), `tensorObj_unit_iso`, `tensorObj_restrict_iso` all proved; `PicSharp.addCommGroup` axiom-clean. `PullbackTensorComp.lean` retired (absorbed into `TensorObjSubstrate.lean`). Picard line-bundle cone: 0 sorries; merged cone builds green. Roadmap `AJC.linebundle` closed.

- **FBC (your PRIMARY 02KH directive) — affine-reduction HEART CLOSED + dead mate route pruned (T2 sessions 0011+0013, 2026-07-03).** `pushPullObj_coverInter_baseChange` (the per-σ 02KG heart) is now **sorry-free and axiom-clean**: the restricted cartesian square is carved into a ring pushout (`coverInter_ring_isPushout`, via the new converse `isPushout_of_isPullback_SpecMap`), the base-changed corner sections rewrite as `N ⊗_R R'` (`coverInter_baseChanged_sections_tensor_rewrite`), and the affine brick `affinePushforwardPullbackBaseChange` closes the gap. The superseded canonical-mate stubs (`affineBaseChange_pushforward_iso`, `flatBaseChange_pushforward_isIso`, 2 sorries, zero consumers) are **deleted** — `FlatBaseChange.lean` is now sorry-free; blueprint nodes pruned, Quot consumer repointed to `lem:cech_flat_base_change`. Remaining FBC leaves (3): the 2 cosimplicial `naturality` fields (need a sigma-iso transport formula for the nerve structure maps + per-σ restriction compatibility — multi-session), and `pullback_preservesFiniteLimits` (**NOT cheap** — stalk-of-pullback infrastructure, see memory). Full tree `lake build` green (8642 jobs). Affine-base separated case suffices for Kleiman 4.8 Step 1.

- **Pic⁰ tangent space (A.3.iii) — geometric half DONE (2026-07-02).** New `Picard/TangentSpaceIdentitySection.lean` (sorry-free, axiom-clean): a section of the structure map hits a `k`-rational point (`bijective_algebraMap_residueField_of_section`), and for any `k`-group scheme the identity-section tangent space is `Dual κ(e) (m_e/m_e²)` (`GroupScheme.identityDualNumberEquivCotangentSpaceDual`). Wired to `Pic0Scheme C` as `Pic0.tangentSpaceCotangentDual` in `Pic0AbelianVariety.lean` (sorry-free proof; inherits `sorryAx` only from the typed-sorry `Pic0Scheme` carrier). `thm:pic0_tangent_space_iso` now needs only the `H¹(C,𝒪_C)` identification (gated on `AJC.picrep`).

- **v4.31.0 migration debt — CLEARED (verified 2026-07-03).** All `v4.31.0 ISOLATION`/`INTERIM` interim `sorry`s are closed; the remaining 49 project `sorry`s are genuine open leaves (Quot endgame 12, FlatteningStratification 7, AlbaneseUP 7, FBC 3, FGA/IdentityComponent/Pic⁰ carriers, …), not migration holes. Full tree builds green.
