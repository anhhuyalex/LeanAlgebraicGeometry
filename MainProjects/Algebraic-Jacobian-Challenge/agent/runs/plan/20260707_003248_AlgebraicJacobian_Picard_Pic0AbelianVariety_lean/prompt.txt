## Lean Goal State

File: AlgebraicJacobian/Picard/Pic0AbelianVariety.lean
Lines: 361
Theorem: identitySection_residueFieldIso

```lean
Line 361: theorem identitySection_residueFieldIso {k : Type u} [Field k]
  (No goals at this position — proof may be complete.)
```

---

## Blueprint Plan

### Target: thm:pic0_tangent_space_iso
File: blueprint/src/chapters/Picard_Pic0AbelianVariety.tex (lines 46-78)

```latex
\begin{theorem}\leanok
[Tangent space at the identity: \(\tu T_0 \Pic^0_{C/k} \cong H^1(C, \mathcal{O}_C)\)]
  \label{thm:pic0_tangent_space_iso}
  \lean{AlgebraicGeometry.Scheme.Pic0.tangentSpaceIso}
  \uses{def:pic_zero_subscheme,
        thm:identity_component_open_subgroup,
        def:genus}

  % SOURCE: [Kleiman], ``The Picard scheme'', \S 5, Thm.~thm:tgtsp;
  % arXiv:math/0504020, p.~41 (read from
  % references/kleiman-picard-src/kleiman-picard.tex, L3265-L3270).
  % SOURCE QUOTE:
  % "\begin{thm}\label{thm:tgtsp}
  %  Assume \(S\) is the spectrum of a field \(k\).  Assume \(\IPic_{X/k}\) exists
  % and represents \(\Pic_{(X/k)\et}\).  Let \(\tu T_0\IPic_{X/k}\) denote the
  % tangent space at \(0\).  Then
  %    \(\)\tu T_0\IPic_{X/k}=\tu H^1(\mc O_{\!X}).\(\)
  %  \end{thm}"
  \textit{Source: [Kleiman], ``The Picard scheme'', \S 5,
  Thm.~5.11; cf.\ [Hartshorne], ``Algebraic Geometry'', GTM 52,
  IV.1.1 (genus def.\ \(g(C) = \dim_k H^1(C, \mathcal{O}_C)\)).}
  Let \(C/k\) be a smooth proper geometrically integral curve of genus
  \(g = g(C)\) over a field \(k\), and let \(\Pic^0_{C/k}\)
  (\cref{def:pic_zero_subscheme}) be the identity component of the
  Picard scheme of \(C/k\). There is a canonical isomorphism of
  \(k\)-vector spaces
  \[
    \tu T_0 \Pic^0_{C/k} \;\cong\; H^1(C, \mathcal{O}_C).
  \]
  In particular, the \(k\)-dimension of \(\tu T_0 \Pic^0_{C/k}\) equals
  the genus \(g(C) = \dim_k H^1(C, \mathcal{O}_C)\)
  (\cref{def:genus}).
\end{theorem}
```

---

## Your Task

Explain what this goal is in natural language.

Write the equation of the goal in KaTeX with proper formatting.

You are a student at the Putnam Competition. You are asked to prove or
disprove the following statement. Write a mathematical proof or disproof that would
receive top marks.

Create a plan, but do not edit any files.

Note that the goal may not follow from the hypotheses as stated — check carefully
that the goal is provable. If it is not, explain in detail what hypotheses are missing.

Do not run any commands, attempt `lake build`, or call `lake exe cache get`.
Simply reason through the mathematical statement at this step of the proof.

Be rigorous and truth-seeking in your work. You will be rewarded for accuracy.

---

Search the current file, repository, and the `.lake/packages` directory for relevant definitions
and theorems. Create a ranked list of the most relevant results found in the current
file or in the directory.

Consult the provided reference documentation in docs/ if it is available.

Use `WebSearch` and `WebFetch` to research the mathematics behind this goal from a
variety of independent sources — textbooks, lecture notes, arXiv papers, nLab,
Wikipedia, MathOverflow/Math.SE, and Mathlib documentation. Do not rely on a single
result: cross-check any non-trivial claim, definition, or proof strategy against at
least two independent sources where feasible, and note it explicitly if sources
disagree. Prefer primary or authoritative references (textbooks, papers, nLab) over
forum answers when they conflict.

Be scholarly: every fact, definition, or proof idea drawn from the web must be cited
inline next to the point it supports, e.g. "(Source: <title>, <url>)", so the claim
can be independently verified. Do not assert something you found online without
attributing it.

Be rigorous and truth-seeking in your work. You will be rewarded for accuracy.