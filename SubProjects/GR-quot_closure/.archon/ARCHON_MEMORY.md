<!-- ARCHON_MEMORY.md — condensed project knowledge for all agents.
     Written by the plan agent and archon discuss. Read by all agents.

     HARD LIMITS: max 10 bullets · ~600 chars total.
     Prune before adding. Only keep what would surprise an agent reading
     the code fresh. Do NOT duplicate things obvious from the codebase.
-->

- `X.Modules`/value-ModuleCat diamond: positional `rw`/`simp`/`erw`, `comp_apply`, `hom_comp` all fail → term-mode + `change`-to-nested-application.
- SNAP (ACTIVE): carrier `AddCommGrpCat`; `P⊗Q`=`MonoidalCategory.tensorObj (C:=MonoidalPresheaf X) P Q`. Stalkwise DEAD. Inherited `MonoidalCategory`/`SymmetricCategory X.Modules` via Mathlib monoidal localization + bridge `tensorObjIso: F⊗G≅tensorObj F G`. KEY: state monoidal decls over `MonoidalPresheaf X` (not `X.PresheafOfModules`); `sheafification` non-reducible ⇒ explicit `IsLocalization` bridge needed. `tensorObjAssoc` on canonical `α_`; bridge lemmas (`tensorObj{RightUnitor,UnitIso,Braiding,WhiskerLeft/RightIso}_eq`) = hand-built iso conjugated by `tensorObjIso`, via `Localization.Monoidal.*_hom_app`+naturality. REUSABLE RECIPES: (a) bridge `hom≫inv` cancellation across the `LocalizedMonoidal`/`X.Modules` diamond goes through a FULL-DEFEQ `simp only` (NOT rw/erw) with `Iso.hom_inv_id_assoc`/`whiskerLeft_hom_inv_assoc`/`hom_inv_whiskerRight_assoc`/`Iso.cancel_iso_inv_left`. (b) THE comp-instance diamond (`CategoryStruct.comp` rfl-defeq but syntactically distinct at μ-junctions; rw/simp[Category.assoc] no-match, `repeat erw`=200k timeout): one-line comp-bridge `have hc:∀{P Q R}(f g),f≫g=@CategoryStruct.comp (LocalizedMonoidal …) _ … f g:=fun f g=>rfl; simp only[hc]` keyed to the EXPLICIT native `Scheme.Modules.instCategory` head normalizes ≫ UP ⇒ `rw[Category.assoc]` fires; then in-synonym `μ_natural_*`/`associator_naturality`/`Iso.inv_hom_id_assoc`. Push UP, never down (strips API); NO wrapper-iso. (c) generic-coherence PLACEMENT: pin the lemma's `M:=LocalizedMonoidal …` (all isos explicit) so heads match — else `exact` lets `M` default to native head ⇒ no isDefEq short-circuit ⇒ >4M-heartbeat 1.2M-char traversal; add `maxRecDepth 4000` (stack depth, NOT maxHeartbeats). See analogies/{comp-instance-diamond,coherence-placement}.md. KEY: element-level `⊗ₜ[forget₂-ring]` stmts DON'T elaborate (CommRing behind forget₂ ⇒ eager synth fails) → state at MORPHISM level (`.app(op⊤)`), drive `⊗ₜ` via enclosing `.hom`; element form = corollary. RE-ANCHOR (iter-011): section ring `⊕ₘΓ(L^⊗m)` = FREE TENSOR ALGEBRA ⇒ `sectionsMul_mul_comm` FALSE ∀ general L (counterexample L=𝒪²); NON-comm `GSemiring` ∀L, comm = invertible-only `GCommSemiring` upgrade (Stacks §17.25/01CR), gated on `braiding_eq_id_of_invertible` (`Module.Invertible.tensorProductComm_eq_refl`, LOCAL-to-global — NOT at ⊤). comm = FUTURE (no consumer; unbuilt). assoc TRUE ∀L. iter-017: ★ `tensorObjAssoc_eta_factor_sheaf` CLOSED (recipe c) ⇒ B4/B5 auto-clean; B6 base CLOSED. LIVE = B6 succ `tensorPowAdd_assoc` = BRAIDED coherence (def threads `tensorBraiding`, NOT pure pentagon; both bracketings = SAME permutation ⇒ `hexagon_forward`+`pentagon`+`whisker_exchange`, NO β=id — that's what separates assoc-∀L from comm) + B7 (gated on B6). (d) iter-020 ROUTE-B (lean_run_code-VERIFIED, analogies/whisker-synonym.md): whisker-iso functoriality (`tensorObjWhiskerRightIso (e≪≫f) G=…`,+`_refl`,+Left) — DON'T re-derive on `MonoidalPresheaf X` synonym (DEAD END, re-opens diamond); ROUTE THROUGH the proven `_eq` bridges: `rw[..._eq×n]; apply Iso.ext; simp` ⇒ uniformly `X.Modules`-comp, `comp_whiskerRight` fires, NO `hc`. In B6-succ apply iso-level functoriality BEFORE `Iso.ext` (keep `tensorPowAdd k _` folded) so `rw[ih]` fires — the current canonical-unfold-first `simp` is WHY ih won't fire. NOTHING protected (`archon-protected.yaml` empty). SNAP self-contained ⇒ cannot disturb `represents`.
- χ-BLOCKED: `def:hilbert_polynomial` (`Scheme.hilbertPolynomial`) is χ-semantic (`Φ(m)=χ=Σᵢ(-1)ⁱ dim Hⁱ`, verified in QuotScheme.lean), needs higher cohomology this i=0 leg lacks. Keep it + `def:quot_functor` as sorry; fill from cohomology leg. NEVER build an H⁰ `Φ_s` under the χ label. `Scheme.Grassmannian`/`Grassmannian.representable` are χ-COUPLED AS ENCODED (re-export `QuotFunctor`→`hilbertPolynomial`); directive's "closable here" is aspirational — a χ-free re-encode would be a divergent shared body → merge conflict. Keep sorry; do NOT re-probe.
- Build with `lake build <module>` (LSP hides kernel timeouts); never add `maxHeartbeats 1e6`. No LLM API key in env.
- Merge-back: never add `\leanok` by hand. SNAP intentionally DIVERGES from sibling iter-007 (Mathlib-aligned monoidal localization vs hand-rolled; sibling stuck 9 sorries same wall) — merge is reconciliation, not dedup. Blueprint-doctor broken `\ref`/`\uses` + leandag-unmatched are EXTRACTION artifacts (outside the 3-seed cone); do NOT edit — resolve at merge.
- DELIVERED (iter-001): goal seed `Grassmannian.represents` sorry-free + axiom-clean (disjoint from SNAP/χ). Open sorries (iter-018): 5 SNAP (`SectionGradedRing.lean`) = B6 succ `tensorPowAdd_assoc` + B7 `sectionsMul_mul_assoc` (live ∀L assoc) + 3 comm-gated FUTURE (no consumer) + 4 χ (`QuotScheme.lean`, DEFERRED→cohomology leg) + out-of-cone debt.
