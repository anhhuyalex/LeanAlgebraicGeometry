<!-- ARCHON_MEMORY.md — condensed project knowledge for all agents.
     Written by the plan agent and archon discuss. Read by all agents.

     HARD LIMITS: max 10 bullets · ~600 chars total.
     Prune before adding. Only keep what would surprise an agent reading
     the code fresh. Do NOT duplicate things obvious from the codebase.
-->

- `X.Modules`/value-ModuleCat diamond: positional `rw`/`simp`/`erw`, `comp_apply`, `hom_comp` all fail → term-mode + `change`-to-nested-application.
- SNAP (ACTIVE): carrier `AddCommGrpCat`; `P⊗Q`=`MonoidalCategory.tensorObj (C:=MonoidalPresheaf X) P Q`. Stalkwise DEAD. Inherited `MonoidalCategory`/`SymmetricCategory X.Modules` via Mathlib monoidal localization + bridge `tensorObjIso: F⊗G≅tensorObj F G`. KEY: state monoidal decls over `MonoidalPresheaf X` (not `X.PresheafOfModules`); `sheafification` non-reducible ⇒ explicit `IsLocalization` bridge needed. `tensorObjAssoc` on canonical `α_`; bridge lemmas (`tensorObj{RightUnitor,UnitIso,Braiding,WhiskerLeft/RightIso}_eq`) = hand-built iso conjugated by `tensorObjIso`, via `Localization.Monoidal.*_hom_app`+naturality. REUSABLE RECIPES: (a) bridge `hom≫inv` cancellation across the `LocalizedMonoidal`/`X.Modules` diamond goes through a FULL-DEFEQ `simp only` (NOT rw/erw) with `Iso.hom_inv_id_assoc`/`whiskerLeft_hom_inv_assoc`/`hom_inv_whiskerRight_assoc`/`Iso.cancel_iso_inv_left`. (b) THE comp-instance diamond (`CategoryStruct.comp` rfl-defeq but syntactically distinct at μ-junctions; rw/simp[Category.assoc] no-match, `repeat erw`=200k timeout): one-line comp-bridge `have hc:∀{P Q R}(f g),f≫g=@CategoryStruct.comp (LocalizedMonoidal …) _ … f g:=fun f g=>rfl; simp only[hc]` keyed to the EXPLICIT native `Scheme.Modules.instCategory` head normalizes ≫ UP ⇒ `rw[Category.assoc]` fires; then in-synonym `μ_natural_*`/`associator_naturality`/`Iso.inv_hom_id_assoc`. Push UP, never down (strips API); NO wrapper-iso. (c) generic-coherence PLACEMENT: pin the lemma's `M:=LocalizedMonoidal …` (all isos explicit) so heads match — else `exact` lets `M` default to native head ⇒ no isDefEq short-circuit ⇒ >4M-heartbeat 1.2M-char traversal; add `maxRecDepth 4000` (stack depth, NOT maxHeartbeats). See analogies/{comp-instance-diamond,coherence-placement}.md. KEY: element-level `⊗ₜ[forget₂-ring]` stmts DON'T elaborate (CommRing behind forget₂ ⇒ eager synth fails) → state at MORPHISM level (`.app(op⊤)`), drive `⊗ₜ` via enclosing `.hom`; element form = corollary. RE-ANCHOR (iter-011): section ring `⊕ₘΓ(L^⊗m)` = FREE TENSOR ALGEBRA ⇒ `sectionsMul_mul_comm` FALSE ∀ general L (counterexample L=𝒪²); NON-comm `GSemiring` ∀L, comm = invertible-only `GCommSemiring` upgrade (Stacks §17.25/01CR), gated on `braiding_eq_id_of_invertible` (`Module.Invertible.tensorProductComm_eq_refl`, LOCAL-to-global — NOT at ⊤). comm = FUTURE (no consumer; unbuilt). assoc TRUE ∀L. iter-017: ★ `tensorObjAssoc_eta_factor_sheaf` CLOSED (recipe c) ⇒ B4/B5 auto-clean; B6 base CLOSED. iter-020 ROUTE-B (lean_run_code-VERIFIED, analogies/whisker-synonym.md): route hand-built whisker-isos through the proven `_eq` bridges (`rw[..._eq]; apply Iso.ext; simp`) to reach canonical `X.Modules`-comp — DON'T re-derive on the `MonoidalPresheaf X` synonym (DEAD END). **★ iter-023 ROOT-CAUSE REFACTOR (mathlib-analogist `tensorpowadd-orient` lean_run_code-VERIFIED, analogies/tensorpowadd-orient.md): iters 017→022 were STUCK 6 iters on a BRAIDED residual in `tensorPowAdd_assoc` that was a DEFINITIONAL ARTIFACT — `tensorPowAdd` recursed on the 1st index against right-growing `tensorPow`/`Nat.add`, forcing a `tensorBraiding` to migrate the new `L`. FIX: recurse on the 2nd index `m'` (canonical `pow_add` orientation): base=`tensorObjRightUnitor` (m+0=m rfl), succ=`α⁻¹≪≫(tensorPowAdd L m c ▷ L)` (m+(c+1)=(m+c)+1 rfl) ⇒ NO braiding, NO eqToIso ⇒ `tensorPowAdd_assoc` = pure pentagon (induct m''), closed by the `monoidal` tactic (`Mathlib.Tactic.CategoryTheory.Monoidal.Basic`) after route-(b) bridges + one `associator_inv_naturality_left`. NO braided-coherence tactic exists in Mathlib ⇒ refactor NECESSARY. Effects: `tensorPowAdd_zero_right`=rfl (now base); NEW `tensorPowAdd_zero_left` (induct, triangle) for the LEFT unit `sectionsMul_one_mul`; the whole 018–022 σ-naturality/diamond tower (`tensorObjWhiskerRightIso_tensorBraiding_natural`, `…_succ_{reindex,interchange,coherence}`) RETIRED. Keep `tensorBraiding`/`_eq` for the comm chain. Standing directive #1 OVERRODE the iter-022 escalate-to-USER gate.** NOTHING protected. SNAP self-contained ⇒ cannot disturb `represents`.
- χ-BLOCKED: `def:hilbert_polynomial` (`Scheme.hilbertPolynomial`) is χ-semantic (`Φ(m)=χ=Σᵢ(-1)ⁱ dim Hⁱ`, verified in QuotScheme.lean), needs higher cohomology this i=0 leg lacks. Keep it + `def:quot_functor` as sorry; fill from cohomology leg. NEVER build an H⁰ `Φ_s` under the χ label. `Scheme.Grassmannian`/`Grassmannian.representable` are χ-COUPLED AS ENCODED (re-export `QuotFunctor`→`hilbertPolynomial`); directive's "closable here" is aspirational — a χ-free re-encode would be a divergent shared body → merge conflict. Keep sorry; do NOT re-probe.
- Build with `lake build <module>` (LSP hides kernel timeouts); never add `maxHeartbeats 1e6`. No LLM API key in env.
- Merge-back: never add `\leanok` by hand. SNAP intentionally DIVERGES from sibling iter-007 (Mathlib-aligned monoidal localization vs hand-rolled; sibling stuck 9 sorries same wall) — merge is reconciliation, not dedup. Blueprint-doctor broken `\ref`/`\uses` + leandag-unmatched are EXTRACTION artifacts (outside the 3-seed cone); do NOT edit — resolve at merge.
- DELIVERED (iter-001): goal seed `Grassmannian.represents` sorry-free + axiom-clean (disjoint from SNAP/χ). **Entire SNAP assoc chain B1–B7 CLOSED iter-026, axiom-clean ∀L** (B6 `tensorPowAdd_assoc` iter-025 via generic-`M` core `tensorPowAdd_assoc_succ_core` + pinned `exact` across the comp-instance diamond, recipe c; B7 `sectionsMul_mul_assoc` iter-026 closes at ELEMENT level where the diamond is ABSENT — `congrArg` of the B6 pentagon, recipes a/b/c NOT needed). **iter-027 DELIVERED phase (i): ∀L section graded SEMIRING `sectionGradedRing_gsemiring` (Stacks 01CV) axiom-clean** (+`gmonoid`/`semiring_nonempty`/`moduleTensorPowAdd`); GMonoid fields = done coherence clauses via `gradedMonoid_eq_of_cast`, bilinearity needs `erw` (not rw/simp), `DirectSum.GSemiring` needs `import Mathlib.Algebra.DirectSum.Ring`, `⨁`=biproduct here→write `DirectSum ℕ`. **phase (ii) GCommSemiring DONE iter-031, axiom-clean, file SORRY-FREE.** `IsInvertible` = `Prop` carrying a trivializing BASIS as data (@L142). Chain (all axiom-clean): PRIMARY `tensorBraiding_self_eq_id_of_isInvertible` (`β_{L,L}=𝟙`, basis-local descent via `TopCat.Sheaf.hom_ext` on underlying `TopCat.Sheaf Ab` — NOT `Scheme.Modules.hom_ext`=⊤-TRAP) → brick 2 `tensorBraiding_hexagon_forward` → `tensorBraiding_symm` (β∘β=refl, descends `SymmetricCategory.symmetry` thru `MonoidalPresheaf X`) → brick 1′ `tensorPowAdd_succ_left_braided` (order-REVERSING, generic-`M` `_core` mirror of `tensorPowAdd_assoc_succ_core`) → `tensorPowAdd_comm` → `sectionsMul_mul_comm` (FALSE ∀ general L) → `sectionGradedRing_gcommSemiring`. **SNAP-S1 STRETCH (iter-032 active): ℕ-graded ∀L graded MODULE `M(X,L,F)=⊕Γ(F⊗L^{⊗m})` — analog of 01CV (literal 01CV = Z-graded INVERTIBLE-L w/ L^∨; we build n≥0 ∀L).** `mul_smul` = a NEW braided hexagon `β_{L^i,F}` (DISTINCT objects; does NOT collapse under invertibility, unlike `β_{L,L}=𝟙`). DECOMPOSED iter-032: (A) `moduleTensorPowAdd_assoc` (NEW iso-level module hexagon, direct B6 `tensorPowAdd_assoc` analogue; β-split HAND-BUILT-first). **iter-035 (A) `moduleTensorPowAdd_assoc` + (B-compat) `moduleSectionAction_mul_smul` CLOSED axiom-clean (abort condition RESOLVED — (A) closed in 1 real-attempt iter).** ★ ROUTE (validated, SUPERSEDES `_core`): keep `tensorPowAdd`/`moduleTensorPowAdd` `.hom` OPAQUE (don't unfold succ) ⇒ NO comp-instance diamond ⇒ plain `rw` throughout, NO generic-`M` `_core` (cheaper than B6/comm). (A) step (4) needed TWO `whisker_exchange_assoc` transpositions (T1+T2), not one — `monoidal` does coherence but NOT atom-interchange of two non-structural atoms on disjoint factors. The 2 private helpers `tensorBraiding_naturality_{left,right}` are DEAD CODE (never called — (A) uses canonical `BraidedCategory.braiding_naturality_left`) → DELETE. (B) `mul_smul` needs `vadd_eq_add` (GSMul lands in `i+ᵥj`) + `maxRecDepth 4000 in` BEFORE docstring. STILL OPEN: (B-unit) `moduleSectionAction_one_smul` + (C) `sectionGradedModule_gmodule`, gated on building `moduleTensorPowAdd_zero_left` — BLOCKER `unitModule X`≠`𝟙_` syntactically (defeq) breaks `braiding_tensorUnit_left`/`monoidal` unit-collapse; workaround: state unit sub-square over `𝟙_`, prove by `monoidal`, transport by `change`/`convert` (NOT `rw [show unitModule X=𝟙_…]`=motive-not-type-correct); single braiding via `have hβ:=braiding_tensorUnit_left F`. Full (B-unit)/(C) recipe banked in `task_results/AlgebraicJacobian-Picard-SectionGradedRing.md`. (B) section-level reuses EXISTING `tensorBraiding_hom_sectionsMul` distinct-obj slide + `tensorObjAssoc_hom_sectionsMul` + whiskers; (C) `DirectSum.Gmodule` via module bridge `moduleGradedMonoid_eq_of_cast` (imports `Mathlib.Algebra.DirectSum.Module`/`…Module.GradedModule` added). DEAD ENDS: order-preserving `tensorPowAdd_succ_left` (brick 1, 0 call-sites); `sectionMul_coherent` "third-factor" reduction (TYPE-INCORRECT — that lemma quantifies over powers of L). Deferred: 4 χ (`QuotScheme.lean`→cohomology leg).
