# Resolved
<!-- One line per resolved sub-lane. -->

- `Foundations.lean` node-4 (d1) + stalk-instance enablers (iter-020): 4 axiom-clean decls, ~1666→1783 lines, 0 sorry. `instModuleStalkOfTilde`/`instIsLocalizedModuleStalkOfTilde` (the iter-019 blocker fix: Mathlib tilde-stalk `Module`/`IsLocalizedModule` insts inferInstance-FAIL when the ring is `CommRingCat.of A` — re-key via `inferInstanceAs` on `moduleStructurePresheaf`/`StructureSheaf.toStalkₗ`; defeq, no diamond); `extendScalars_unit_isLocalizedModule` (M→M_f-at-powers-of-f via `IsLocalization.tensorProduct_isLocalizedModule` + `TensorProduct.congr` defeq bridge — direct `exact`/`convert` FAILS); `tildePullbackComparison_stalk_source_isLocalizedModule`=node (d1) (feed (c) with g=extendScalars unit, h=tilde-stalk germ; restricted R-module via `Module.compHom _ φ` for rfl tower, re-type g/h to plain carriers, state through `restrictScalars` FUNCTOR, `convert _ using 1`). (d3) correctly NOT added (would be circular). lean-vs-blueprint `foundations-iter20` faithful; lean-auditor `iter20` 0 must-fix. Coverage debt (3 helpers) blueprinted iter-021.

- `Foundations.lean` node-4 bottom (c) + cleanup (iter-019): 1 axiom-clean theorem `Scheme.Modules.tildePullbackComparison_stalk_localizationTransitivity` (`lem:tilde_pullback_stalk_localizationTransitivity`, line 1590) + un-`private` of `pushforward_smul_coherence`. **T-FREE signature** (prover OVERRODE the planner's T-parametrised sketch — the T-version needs `Module T N`/`Algebra R T`/full tower the downstream (d1) caller cannot supply; T-free needs only `Module R N`+`IsScalarTower R Rf N`). Proof = direct 3-field `IsLocalizedModule` ctor (mem_iff via `Ideal.mem_comap`; powers_compl via `IsLocalization.map_units`+`Ideal.eq_top_of_isUnit_mem`; map_units via `Module.End.isUnit_iff` NOT `Module.End_isUnit_iff`; surj/exists_of_eq clear denominators with `IsLocalization.surj`+`IsLocalizedModule.{surj,exists_of_eq}`). HAZARD: ascribe `(↑s:R)`/`(↑t:Rf)` on EVERY coe-smul (submonoid-coe smul ambiguous with ring-coe smul). lean-vs-blueprint `foundations-iter19` PASS; lean-auditor `iter19` Foundations clean.

- `Foundations.lean` tilde-pullback chain nodes 1–3 + linchpin (iter-018): 4 axiom-clean decls, ~1473→1574 lines, 0 sorry. node 1 `basicOpenIsoSpecAway_hom_comp_specMap` (`found:basicOpenIso_hom_specMap`, `j_f=e_f.hom≫q_f` via `IsOpenImmersion.isoOfRangeEq_hom_fac`); private linchpin `pushforward_smul_coherence` (`found:pushforward_smul_coherence`, blueprinted iter-019; scalar coherence `erw[restrictScalars.smul_def×3]→congr 1→ΓSpecIso_inv_naturality`; `IsScalarTower`/`rfl` FAIL); node 2 `tilde_pullback_specMap_globalSectionsMap`=η_M (`found:tilde_pullback_globalSections`; unit of `pullbackPushforwardAdjunction`→`moduleSpecΓFunctor.map`→`tilde.isoTop`→semilinear via `semilinearMapAddEquiv`→transpose via `extendRestrictScalarsAdj`; `M_f=extendScalars(algebraMap).obj M=R_f⊗_R M`); node 3 `tildePullbackComparison`=α_M (`found:tilde_pullback_comparison`, tilde–Γ-adjunction transpose of η_M). lean-vs-blueprint `foundations-iter18` PASS 0 must-fix; lean-auditor `iter18` 0 must-fix.

- `Foundations.lean` Core-2 chain **L3** (iter-017): 1 axiom-clean theorem `Scheme.Modules.kernel_iso_tilde_of_free_ses_affine` (`lem:kernel_iso_tilde_affine`, ~lines 1407–1473), 0 sorry, ~1388→1473 lines. Free SES of `(Spec R).Modules` is `tilde` of an R-module SES, `𝒦≅tilde K`. **SPLITTING SIDESTEP**: iter-016 wall (`PreservesFiniteLimits (tilde.functor R)` absent) was real but UNNEEDED — module SES splits (`splittingOfProjective`), additive `tilde` carries it (`Splitting.map`+`.fIsKernel`)⇒`tilde K=ker(tilde φ)`; `KernelFork.isLimitOfIsLimitOfIff`+`conePointUniqueUpToIso`. φ from `tilde.fullyFaithfulFunctor.preimage`, epi via `reflectsEpimorphisms_of_faithful`+`epi_of_epi_map`. Universe pin `SheafOfModules.free (R:=(Spec R).ringCatSheaf) m`. No new helpers (all proof-local). lean-vs-blueprint `foundations-r17` FAITHFUL 0 must-fix; lean-auditor `iter17` clean.

- `Foundations.lean` Core-2 chain bottom L1+L2 (iter-016): 3 axiom-clean decls, ~1260→1388 lines, 0 sorry. **L2** `kernel_module_free_on_basicOpen_cover` (`lem:module_kernel_free_basicOpen_cover`): module SES `0→K→R^m→R^n→0` ⇒ K f.p.projective (`splittingOfProjective`+`Projective.of_split`+`finitePresentation_of_projective`) ⇒ `freeLocus=univ` ⇒ free on a finite basic-open cover (`isCompact_univ.elim_finite_subcover`). **L1** `isLocallyFree_of_isLocallyFree_cover` (`lem:isLocallyFree_local`): loc-freeness local on base, via `Scheme.Hom.isoImage` transport + helper `freeIso_of_restrictFunctor_hom_freeIso`. Helper blueprinted iter-017 (`found:free_iso_of_restrict_iso`). DEAD-END: split eqn `sp.r∘S.f=id` must NOT go through `simpa` (`f_r` is `@[simp]`→collapses to True); use explicit `rw`. lean-vs-blueprint `foundations-r16` PASS 0 must-fix; lean-auditor `iter16` Foundations clean.
- `Basic.lean` FIRST genuine paper object (iter-015): `MR4213770.kernelBundle {X} (L : X.Modules) : X.Modules := Scheme.Modules.evalKernel L` (`def:kemeny_kernel_bundle`), +import Foundations, axiom-clean, 0 sorry. lean-vs-blueprint `basic-r15` PASS. The 3 `opaque` stubs left untouched.
- `Foundations.lean` kernel-bundle loc-free chain cores (iter-015): 5 axiom-clean decls — Core 1 `trivialBundle_isLocallyFree` (`found:trivial_bundle_locally_free`; via `restrictFunctorObjFreeIso` = restrictFunctor≅pullback whose site functor `Opens.map` is FINAL ⇒ `pullbackObjFreeIso` applies — the iter-014 "dead end" was a MISDIAGNOSIS) + step 4 `evalShortComplex_shortExact_of_globallyGenerated` (`found:eval_shortexact…`; hypothesis `Epi (evalMap F)`) + 3 helpers (`restrictFunctorObjFreeIso`, `isLocallyFree_of_iso`, `evalShortComplex`). Coverage debt for the 3 helpers blueprinted iter-016 (`found:restrict_free_iso`/`locally_free_of_iso`/`eval_short_complex`). ~1152→~1260 lines.

- `Foundations.lean` eval-map substrate steps 1–3 (iter-014): 3 non-private axiom-clean decls, 1110→1152 lines, 0 sorry. `Scheme.Modules.trivialBundle` (= `SheafOfModules.free I`, index-type formulation, no `choose`); `evalMap` (= `freeHomEquiv.symm id` indexed by `F.sections`, NOT derived Γ); `evalKernel` (= `Limits.kernel evalMap` in abelian `X.Modules`). `M_F` exists ⇒ `kernelBundle:=evalKernel L` formalizable. Step 4 `evalKernel_isLocallyFree` + the trivialBundle loc-free sub-claim NOT added (clean STOPs: no Mathlib anchor / restrictFunctor `Type u` universe wall) → effort-broken iter-015. lean-vs-blueprint r14 FAITHFUL (0 must-fix), lean-auditor r14 clean (Foundations).
- `Foundations.lean` openwise + sheafified power functors, steps (5)–(6) (iter-013): 16 decls (10 non-private + 6 private), 787→1110 lines, 0 sorry, axiom-clean. Naturality `ModuleCat.{ext,sym}.baseChangeMap_{id,comp}` (restrictScalars{Id',Comp'} coherence form); categorical wrappers `…baseChangeMapCat` (+ generator lemmas); openwise `PresheafOfModules.{exteriorPower,symPower}` via `mk` (private `{ext,sym}PowMap{,_id,_comp}`); sheafified `Scheme.Modules.{exteriorPower,symPower}`. **P0a Foundations cohomology+power substrate arc (iters 006–013) CLOSED.** lean-vs-blueprint r13 FAITHFUL, lean-auditor r13 clean (Foundations). Coverage debt (10 helper nodes) blueprinted iter-014.

- `Foundations.lean` base-change power maps steps (1)–(4) (iter-012): 12 axiom-clean decls, 535→787 lines, 0 sorry. `Scheme.ringCatSheaf_commRing` (per-open CommRing from RingCat-valued sheaf, `@[reducible] def` consumed via `letI`); `ModuleCat.exteriorPower.{baseChange,baseChangeAlt,baseChange_apply_ιMulti,baseChangeMap,baseChangeMap_apply_ιMulti}`; `SymmetricPower.{baseChange,baseChange_tprod}`+priv `baseChangeAux{,_apply_tprod,_respects}`; `ModuleCat.symmetricPower.{baseChangeMap,baseChangeMap_tprod}`. Semilinear-restriction obstruction discharged at LinearAlgebra+categorical level. Techniques in `memory/lean-basechange-techniques.md`. lean-vs-blueprint PASS, lean-auditor clean (Foundations). Step (5)/(6) openwise+sheafify remain (need `baseChangeMap_id`/`_comp` naturality first).

- `Foundations.lean` symmetric-power functor (iter-010): Layer 1 `SymmetricPower.{mapAux(priv),mapAux_respects(priv),map,map_tprod,map_id,map_comp}` + Layer 2 `ModuleCat.symmetricPower{,.map,.functor}` (= `found:modulecat_sym_power`). 9 decls, 0 sorry, `{propext,Classical.choice,Quot.sound}`. UNIVERSE TRAP confirmed: index by `ULift.{u}(Fin n)`. iter-011 dag pass blueprinted the 8 helpers + cleared all coverage debt (0 lean_aux/isolated/broken-uses). Also iter-010: 2 secondary cleanups (dropped dead `[J.HasSheafCompose …]` HeavyBridge var; fixed stale "Pending that bridge" docstring).

- `Foundations.lean` HEAVY `R^if_*` bridge (iter-009, prover ran after 2 failed launches): 7 axiom-clean decls — `instAB5OfSizePresheafOfModules`, `isSeparating_ofObj_leftAdjoint` (private), `instAB5OfSizeSheafOfModules`, `instHasSeparatorSheafOfModules`, `instIsGrothendieckAbelianSheafOfModules` (NEW general instance), `instIsGrothendieckAbelianModules`, `instHasInjectiveResolutionsModules`. `X.Modules` now UNCONDITIONALLY Grothendieck abelian + has injective resolutions; `higherDirectImage` self-discharges its hypothesis. 0 sorry, `{propext,Classical.choice,Quot.sound}`. NOTE: blueprint `found:sheafmod_separator` proof was FALSE (global-free route); Lean correctly used sheafified `freeYoneda` — blueprint rewrite is iter-010 must-fix.
- `Foundations.lean` LIGHT scalar-`H^i` bridge (iter-006): `toAbSheaf` → `IsGrothendieckAbelian (TopCat.Sheaf Ab X)` → enough-injectives → injective-resolutions; `Scheme.HModule` redefined UNCONDITIONAL (dropped `[HasInjectiveResolutions X.Modules]`). 6 decls + 1 redef, axiom-clean, 0 sorry. Headline scalar cohomology unblocked.
- `Foundations.lean` interface (iter-005): 8 axiom-clean decls (module-iso locality, global sections, locally-free, R^if_* skeleton, HModule skeleton, base change).
