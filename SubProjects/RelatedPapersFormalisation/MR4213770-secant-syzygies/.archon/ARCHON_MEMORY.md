<!-- ARCHON_MEMORY.md — condensed project knowledge for all agents.
     Written by the plan agent and archon discuss. Read by all agents.

     HARD LIMITS: max 10 bullets · ~600 chars total.
     Prune before adding. Only keep what would surprise an agent reading
     the code fresh. Do NOT duplicate things obvious from the codebase.
-->

- Layout: `Foundations.lean`←`Kemeny_PeerDependencies.tex` (cohom/bundle iface); `Basic.lean`←`Kemeny_UniversalSecantBundles.tex` (whole paper). Foundations gates separately.
- Mathlib has the thin substrate (`Scheme.Modules` abelian, pushforward, rightDerived, ShortExact, stalk-locality) but NOT: inj-resolution bridge for `X.Modules`, nor ∧^i/Sym^i for (pre)sheaves of modules — all project-built.
- Bridge TWO-TIER, BOTH LANDED (LIGHT iter-006, HEAVY iter-009): `X.Modules` now UNCONDITIONALLY Grothendieck-abelian + has inj resolutions; `HModule`/`higherDirectImage` self-discharge. HEAVY separator = sheafified `PresheafOfModules.freeYoneda` (local sections); global `SheafOfModules.free` is a DEAD END (sees only GLOBAL sections, Γ not conservative). AB5: presheaf via `Ab`, sheaf via `sheafificationAdjunction (𝟙 R.obj)`.
- Sym^i: L1 `SymmetricPower.map`(+map_id/comp via `PiTensorProduct.map`) & L2 `ModuleCat.symmetricPower.functor` LANDED leanok (iter-010/011); presheaf/oxmod `symPower` + ∧^i still open. UNIVERSE TRAP: index by `ULift (Fin i)` not `Fin i` (else R:Type 0). See `analogies/symmetric-power-functor.md`. Ref TeX invalid-UTF-8 → `LC_ALL=C grep`.
- ∧^i/Sym^i openwise: `X.ringCatSheaf` is `RingCat`-valued so `CommRing (.obj U)` won't synth (use `Scheme.ringCatSheaf_commRing`, a `@[reducible] def` via `letI`); restriction maps SEMILINEAR → base-change `…baseChangeMap` over a ring hom (steps 1–4 LANDED iter-012). Openwise `PresheafOfModules.{exteriorPower,symPower}` + sheafified `Scheme.Modules.{exteriorPower,symPower}` LANDED iter-013 (`mk`, no smart ctor; `restrictScalars{Id',Comp'}` coherence → maxHeartbeats bumps). DIAMOND TRAP: `Module A N`/`IsScalarTower` do NOT synth from `[Algebra A B][Module B N]` — explicit binders or `restrictScalars`. NAME-CLASH: use `_root_.exteriorPower.map`. Pin universes `.{u}` on bridge instances. **P0a substrate arc (iters 006–013) CLOSED.**
- P0b ACTIVE: eval-map substrate `Scheme.Modules.{trivialBundle,evalMap,evalKernel}` (iter-014, axiom-clean): `evalMap`=counit `SheafOfModules.freeHomEquiv.symm id` indexed by `F.sections` (NOT derived Γ); `M_F=Limits.kernel evalMap`. `Basic.lean kernelBundle:=evalKernel L` LANDED iter-015 (FIRST paper object; `import …Foundations`). `trivialBundle_isLocallyFree` LANDED iter-015 via `restrictFunctor f ≅ pullback f` whose site functor `Opens.map f.base` is the RIGHT adjoint (FINAL) ⇒ `SheafOfModules.pullbackObjFreeIso` applies — the prior "restrictFunctor route is DEAD" memory was a MISDIAGNOSIS (a failed `PreservesColimitsOfShape` synth ≠ dead route). "Globally generated" has NO predicate → modelled as `Epi (evalMap F)` (altitude fixed iter-016).
- Core 2 `isLocallyFree_kernel_of_shortExact` (Hartshorne III Ex 6.5) = AFFINE-REDUCTION chain: L1 local-on-base + L2 module SES `0→K→Rᵐ→Rⁿ→0`⇒free on basic-open cover (iter-016) + **L3** affine bridge `𝒦≅tilde K` (iter-017) ALL LANDED. L3 = SPLITTING SIDESTEP: `tilde.functor R` LEFT-adjoint (preserves cokernels NOT kernels); `PreservesFiniteLimits (tilde)` ABSENT + UNNEEDED — SES splits (Rⁿ free⇒proj `splittingOfProjective`), additive `tilde` carries it (`Splitting.map`+`.fIsKernel`)⇒`tilde K=ker(tilde φ)`; `conePointUniqueUpToIso`. φ surj via `reflectsEpimorphisms_of_faithful`+`epi_of_epi_map`. **L4 gated on `found:tilde_restrict_basicOpen` → tilde-pullback chain. Nodes 1–3 LANDED iter-018: `basicOpenIso_hom_specMap`, η_M `tilde_pullback_globalSections` (from `pullbackPushforwardAdjunction` UNIT, NOT a near-circular global-sections iso; via `tilde.isoTop`+`semilinearMapAddEquiv`+`extendRestrictScalarsAdj`, `M_f=extendScalars.obj M=R_f⊗M`; needs linchpin `pushforward_smul_coherence` = scalar coherence `erw[restrictScalars.smul_def×3]→congr 1→ΓSpecIso_inv_naturality`, `rfl`/`IsScalarTower` FAIL), α_M `tilde_pullback_comparison` (tilde–Γ-adjn transpose). node 4 `comparison_stalk` (α_M stalkwise-iso) WALLED iter-018 (effort 1081), EFFORT-BROKE iter-019 → 7-lemma chain. READY bottom (gap-independent): (c) `…_localizationTransitivity` [PURE ALGEBRA: `IsLocalization.isLocalization_isLocalization_atPrime_isLocalization`+`IsLocalizedModule.linearEquiv`], (d3) `…_comparison_commutes`, (d1) `…_source_isLocalizedModule`. De-risked (d): both stalks `IsLocalizedModule 𝔮ᶜ M`, conclude IsIso by `linearEquiv` uniqueness — NEVER unfold α_M stalk. (c) `…_localizationTransitivity` LANDED 019 (T-FREE: 3-field `IsLocalizedModule`, `Module.compHom _ φ`+`IsScalarTower.of_algebraMap_smul` for rfl tower); (d1) `…_source_isLocalizedModule` + stalk-instance shims (`instModuleStalkOfTilde`/`instIsLocalizedModuleStalkOfTilde`=`CommRingCat.of`-keyed `inferInstanceAs` of Mathlib tilde-stalk insts) + `extendScalars_unit_isLocalizedModule` LANDED 020. **(a1) IS NOT A MATHLIB GAP (iter-021, both anchors loogle-verified):** `TopCat.Presheaf.stalkFunctor_map_unit_toSheafify_isIso` (general value cat, use at `AddCommGrpCat`) gives sheafification-stalk-iso directly; `PresheafOfModules.toPresheaf_map_sheafificationAdjunction_unit_app` rewrites unit→`toSheafify` by `rfl`. (a1)/(a2) all at **Ab** level via `Scheme.Modules.toPresheaf`/`def:peer_module_iso_locality` — NO Module↑R synth (iter-019/020 fears don't apply here). iter-021 dispatched (a1)→(a2-bridge `presheafPullback_toPresheaf_iso`, CRUX=which `TopCat.Presheaf.pullback` instance)→(a2)→(a)→(d2). ONLY (d3) `comparison_commutes` (α_M germ-naturality + `toPresheaf`↔`moduleSpecΓFunctor` germ bridge) still deferred. `Scheme.Modules.pullback`=sheafification of presheaf pullback (`SheafOfModules.pullbackIso`); `tilde.toStalk` carries `IsLocalizedModule primeCompl` (seam b).** Anchors verified v4.30.0: `AlgebraicGeometry.{tilde,basicOpenIsoSpecAway,tilde.adjunction,tilde.toStalk}`, `Scheme.Modules.{pullback,pullbackPushforwardAdjunction}`, `TopCat.Presheaf.{stalkPullbackIso,stalkFunctor_map_unit_toSheafify_isIso}`, `PresheafOfModules.toPresheaf_map_sheafificationAdjunction_unit_app`. **FABRICATION HAZARD: analogist HALLUCINATED `SheafOfModules.{IsLocallyFree,stalk}`/`PresheafOfModules.stalk`/`Tilde.stalkIso`/`ModuleCat.tilde` — ALL ABSENT; `TopCat.Presheaf.sheafifyStalkIso` is `Type`-only étale-space (NOT site sheafification, don't use for modules). KEEP bespoke `Scheme.Modules.IsLocallyFree`. ALWAYS grep-verify analogist claims.**
- no-op filter (drops zero-sorry-file objectives) keys on the LITERAL verb "scaffold" — iter-008 led with "Build"+"(mathlib-build)"+`[prover-mode]` tag and was STILL dropped (`failed_all_noop`). For a mathlib-build lane on an existing zero-sorry file, lead the line: ``**`File.lean`** — **Scaffold** …``.
