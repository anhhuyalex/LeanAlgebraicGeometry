# Strategy Critic Report

## Slug
iter-004-strategy

## Iteration
004

## Routes audited

### Route: Greenberg-Vatsal style comparison route

- **Goal-alignment**: PASS — the route, if fully executed, produces all eight target declarations named in `## Goal`. The parallel-lanes structure (algebraic/analytic independent once Selmer is in place; p-converse and BSD independent once `thm:thmA` is proved) is correctly stated and consistent with the paper's dependency graph.
- **Mathematical soundness**: PASS — the residual decomposition E[p]^{ss} ≅ F_p(φ)⊕F_p(ψ), Rubin-Hida μ=0 input, Howard-style Kolyvagin divisibility, and upgrade to the full main conjecture are the standard approach for Eisenstein-prime Iwasawa theory. The argument is the paper's own and is mathematically well-grounded.
- **Sunk-cost reasoning detected**: no
- **Infrastructure-deferral detected**: yes — three constructions; see Infrastructure-deferral findings below.
- **Phantom prerequisites**:
  - `KolyvaginSystem` (or any abstract Kolyvagin-system structure): MISSING — Loogle returns no results; no Mathlib theory exists.
  - `charIdeal` / characteristic ideal for torsion Λ-modules: MISSING — Loogle returns no results; already used in `lem:local-char-wneqp` (Local_Iwasawa_Selmer.tex:59) and `MAINalgside` (Algebraic_Iwasawa_Comparison.tex:94) without a blueprint definition.
  - `HeegnerPoint`: MISSING — Loogle returns no results.
  - `IwasawaAlgebra`: MISSING from Mathlib; the project correctly intends to define its own (listed in Mathlib gaps), but this is confirmed absent in case of any Mathlib assumption.
- **Effort honesty**: under-counted — The "Heegner-point Kolyvagin system construction" phase (Iters left: 2, LOC: ~160–260) conflates two distinct tasks: (a) formalizing the abstract Kolyvagin-system definition and its norm-relation axioms, and (b) constructing the Heegner-point system and invoking Cornut-Vatsal nonvanishing. Task (a) alone is a substantial Lean project with no Mathlib base; 160–260 LOC for the combined phase is inconsistent with the scope. Comparable foundational phases (Rubin's theory) take 500+ LOC. The estimate should be reviewed.
- **Parallelism under-exploited**: no — the strategy explicitly names the two parallel downstream lanes (p-converse, BSD) and the two parallel mid-route lanes (algebraic, analytic).
- **Verdict**: CHALLENGE — three infrastructure-deferral findings require explicit plans or postulate decisions in STRATEGY.md before this iter ends (see Must-fix-this-iter).

## Format compliance

- **Size**: 39 lines / 5 269 bytes — within budget.
- **Headings**: PASS — `## Goal`, `## Phases & estimations`, `## Routes`, `## Open strategic questions`, `## Mathlib gaps & new material` appear in canonical order; `## Completed` is correctly absent since no phase is done.
- **Per-iter narrative detected**: no
- **Accumulation detected**: no
- **Table discipline**: PASS — `## Phases & estimations` has all six required columns; `Status` values are inline tags (`ACTIVE`); `LOC` values are ranges.
- **Format verdict**: COMPLIANT

## Infrastructure-deferral findings

### Deferred: Characteristic ideal theory for torsion Λ-modules

- **Required by goal**: yes — `lem:local-char-wneqp` (already in the blueprint, Local_Iwasawa_Selmer.tex:59) states "characteristic ideal generated by P_w(θ)"; `MAINalgside` (Algebraic_Iwasawa_Comparison.tex:94) gives an explicit λ-formula that requires characteristic ideals to be meaningful. Both declarations are in `## Goal`.
- **Current plan for building it**: none — the construction is not listed as a blueprint declaration anywhere; the algebraic comparison chapter uses characteristic ideals as a black box. The Mathlib gaps section names "Characteristic ideals, Iwasawa invariants, and the finite-length bookkeeping for torsion modules" but gives no project-side construction or postulate plan.
- **Timeline**: absent — no `def:char-ideal` blueprint label, no phase row dedicated to building this infrastructure.
- **Verdict**: CHALLENGE — the algebraic comparison phase (Iters left: 1–2) depends on characteristic ideals being defined before it can be formalized. The planner must either (a) add a `def:char-ideal` blueprint declaration in the Local Iwasawa or Algebraic chapter and assign an iter estimate, or (b) explicitly record that characteristic ideals will be treated as a `sorry`-backed postulate (with the formalization debt this incurs noted in STRATEGY.md).

### Deferred: Kolyvagin systems formalism

- **Required by goal**: yes — `thm:howard` and `thm:howard-HP` (both in `## Goal`) are stated as outputs of the Howard/Kolyvagin phases, and both require a formal notion of Kolyvagin system (definition, norm-relation axioms, rank bound). Without a `def:kolyvaginSystem` blueprint declaration feeding those theorems, the phase has no foundation to build on.
- **Current plan for building it**: none explicitly — the "Heegner-point Kolyvagin system construction" phase row lists "Kolyvagin systems, norm relations" as Key Mathlib needs, notes blueprint labels `thm:howard-HPKS`, `thm:howard-HP`, but provides no `def:kolyvaginSystem` declaration and no description of how the abstract Kolyvagin-system structure will be established.
- **Timeline**: vague — Iters left: 2, but the estimate covers both the abstract definition work and the Heegner-point construction, which are distinct sub-tasks of very different difficulty. The abstract formalism alone is likely 3–5× the stated LOC.
- **Verdict**: CHALLENGE — the planner must decompose the "Heegner-point Kolyvagin system construction" phase into at least two blueprint-declared sub-steps: (1) `def:kolyvaginSystem` with norm-relation axioms; (2) the Heegner-point construction feeding into `thm:howard-HPKS`. Each needs a separate iter estimate. The current single-row conflation masks a major formalization gap.

### Deferred: BDP/Katz p-adic L-function objects

- **Required by goal**: yes — `cor:Kriz` (in `## Goal`) asserts a congruence between the BDP and Katz p-adic L-functions; this congruence is meaningless without both L-functions being formalized as concrete Lean objects (or at minimum as well-typed postulates).
- **Current plan for building it**: none — the analytic comparison phase row lists "abstract p-adic L-function interfaces" as a Key Mathlib need, the risk cell notes the statements are "source-heavy", but no blueprint declarations for the BDP or Katz L-functions appear anywhere in the current chapters.
- **Timeline**: vague — Iters left: 1–2 for the analytic comparison phase, but the phase chapter is being written this iter and no declarations have been committed yet.
- **Verdict**: CHALLENGE — the planner must confirm (in STRATEGY.md or plan.md) whether the BDP and Katz L-functions will be constructed as formal Lean definitions or treated as `sorry`-backed postulates with explicit source citations. If constructed, the LOC estimate for the analytic phase needs to reflect the cost of building two distinct p-adic L-function objects from scratch.

## Prerequisite verification

- `KolyvaginSystem`: MISSING — Loogle query returns no results.
- `IwasawaAlgebra`: MISSING from Mathlib — project-built; Mathlib gaps section correctly records this.
- `charIdeal` (or `Module.charIdeal`): MISSING — Loogle returns no results; used in blueprint without a prior definition.
- `HeegnerPoint`: MISSING — Loogle returns no results.

## Must-fix-this-iter

- Route Greenberg-Vatsal: infrastructure-deferral CHALLENGE — **characteristic ideal theory** is required by the stated goal (`lem:local-char-wneqp`, `MAINalgside`), already present in the blueprint as a black box, with no `def:char-ideal` declaration and no project-side construction plan. Planner must add a blueprint definition or record a postulate decision with explicit formalization debt.
- Route Greenberg-Vatsal: infrastructure-deferral CHALLENGE — **Kolyvagin systems formalism** is required by the stated goal (`thm:howard`, `thm:howard-HP`), entirely absent from the blueprint, and the Heegner-point phase conflates the abstract definition work with the point construction in a single under-estimated row. Planner must split the phase into `def:kolyvaginSystem` (abstract formalism) and the Heegner-point construction, with separate iter estimates.
- Route Greenberg-Vatsal: infrastructure-deferral CHALLENGE — **BDP/Katz p-adic L-function objects** are required by the stated goal (`cor:Kriz`), absent from all blueprint chapters, and the analytic phase provides no plan for their construction. Planner must commit to a concrete approach (construction vs. postulate) in STRATEGY.md.
- Route Greenberg-Vatsal: CHALLENGE — effort estimate for "Heegner-point Kolyvagin system construction" (LOC ~160–260, Iters left: 2) is inconsistent with the scope of building Kolyvagin-system theory from scratch in Lean 4 with no Mathlib base. Revise estimate after splitting the phase (see above).

## Overall verdict

The single Greenberg-Vatsal comparison route is mathematically sound and goal-aligned; the prior critiques on Howard/Kolyvagin decomposition and parallel downstream lanes are addressed. However, the strategy defers three constructions that are required for the stated goal: characteristic ideal theory (used in existing blueprint declarations without a definition), the Kolyvagin systems formalism (required for `thm:howard`/`thm:howard-HP`/`thm:thmA`, absent from the blueprint with no construction plan), and the BDP/Katz p-adic L-function objects (required for `cor:Kriz`, absent from all chapters). None of these three items have a concrete project-side construction plan or an explicit decision to treat them as `sorry`-backed postulates. Until the planner either provides construction timelines or records postulate decisions for all three, the effort estimates for the algebraic, Howard/Kolyvagin, and analytic phases are unreliable.
