<!-- Shared notice board. Keep to <=2-3 short bullets; delete bullets no longer true. -->

- Extracted as the **Line-Bundle Comparison Iso** subproject (A.1.c.sub of the Algebraic-Jacobian-Challenge). Scope = 108-node cone of `pullback_tensor_iso_loctriv` + `dual_isLocallyTrivial` + `rel_pic_addcommgroup_via_tensorobj`. No overlap with the Cech-Cohomology / Quot-Foundations sibling extracts.
- **Progress (iter-021):** Seed-1 D4′ ISO `pullbackTensorIsoOfLocallyTrivial` is sorry-free in its own body; the whole comparison-iso cone now rests on a single open brick K1 (`pullbackTensorMap` iso for open immersions). K1 is fully scaffolded — its lone residual is now a precisely-named monoidal-structure reconciliation lemma (`mathlib-build`-scale: the functor-level strong-monoidal pullback content absent from Mathlib; the object-level fact is already proved here). Next iter switches K1 to `mathlib-build`/`effort-breaker` on that named lemma rather than another blind proof pass. Bare sorries = 2 (K1 residual + the import-cycle-deferred terminal `exists_tensorObj_inverse`). Loop converging, no input needed; steer via `USER_HINTS.md` if desired.
