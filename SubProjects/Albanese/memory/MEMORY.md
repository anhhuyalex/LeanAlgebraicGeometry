# Memory index

- [iter-224 internalHomEval confirmed + tooling gotchas](ts224-internalhomeval-confirmed-and-tooling.md) — internalHomEval already closed/axiom-clean (PROGRESS objective was stale); MCP uses `file_path` not `relative_path`; first LSP diagnostic can be stale → verify via `lake env lean` + grep
- [Strategy cleaner hint](strategy-cleaner-hint.md) — "make the strategy file cleaner" = real skeleton-conformance pass, not a token trim
- [TS associator flatness gap](ts-assoc-flatness-gap.md) — **iter-224: `internalHomEval` naturality CLOSED axiom-clean (sorry 81→80, sub-step 3 RETIRED). The iter-222/223 `whnf` BOMB (and the entire ts224dual ALIGN escalation) was STALE — a Mathlib bump removed it; plain `erw [ModuleCat.hom_comp,…]` + the existing six-step `evalLin`/`naturality_apply`/`hdt` reduction just compiles (NO `with_reducible`, NO `unit`-reshape). LESSON: re-test the plain tactic each new iter before trusting a multi-iter-old "bomb" diagnosis. `tensorUnit_map`/`unit_map_apply` are UNKNOWN constants — RHS unit-map step is `rfl`, exposed by one `change`. 3 file sorries left, all FORBIDDEN this iter; next frontier sub-step 4 `lem:internal_hom_isSheaf`.** // iter-221: sub-step 3 PARTIAL — `PresheafOfModules.dual` DONE (PRIMARY target 1, `def:presheaf_dual` ready for `\leanok`) + per-object eval map `internalHomEvalApp` + helpers `evalLin`/`evalLin_add`/`evalLin_smul`/`termRingMap_terminal` (6 decls axiom-clean, sorry 3→3). `internalHomEval` FULL morphism NOT added — naturality blocked on `Over.map` coherence (reduction CONFIRMED via `tensor_ext` → `naturality_apply φ (Over.homMk f.unop).op s`). THREE reusable diamond-bridge tricks: (1) over-ring CAST via `evalLin`'s type ascription (`φ.app term` is over the over-category ring, defeq-not-syntactic to `R₀.obj X`); (2) keep eval value at NATURAL over-ring type in `change` proofs (never cast); (3) `show ModuleCat.of (R₀.obj X) _ ⟶ ModuleCat.of (R₀.obj X) (R₀.obj X) from ofHom ...` for the `ofHom` boundary. NEVER land eval in `(𝟙_).obj X` (reduces to bare RingCat carrier, loses CommRingCat module). // iter-220: sub-step 2 DONE — `PresheafOfModules.InternalHom.internalHom` ASSEMBLED axiom-clean (12 decls); reusable tricks: `hom_app_heq`+`subst` cracks `Over.map` pseudofunctor coherence (`map_id`/`comp` not rfl), `ofPresheaf` forces single-universe base `Opens X` + explicit `@ofPresheaf` instance for CommRingCat/RingCat diamond. Blueprint pin `def:presheaf_internal_hom` needs repoint to the `InternalHom.` namespace.** // iter-219: fork RESOLVED — COMMIT to the sheaf internal-hom build (mathlib-build). analogist ts219dual = NEEDS_MATHLIB_GAP_FILL ×3 (same object); the dual is CONTRAVARIANT so NOT a tensorObj-style sheafify, and is a ~6–12 iter / ~300–500 LOC build ≥ abandoned d.2. Decision-1 (presheaf slice/end `ℋom(M,N)(U)=M|_U⟶N|_U` → eval → sheaf cond) blueprinted in `sec:tensorobj_dual_infra` (sourced from `references/stacks-modules.tex`), gate-cleared; first sub-step = `PresheafOfModules.internalHom`. strategy-critic SOUND, progress-critic STUCK. Substrate route-A-specific (mooted if USER lifts RR pause → divisor `Pic⁰`). Recipe `analogies/ts219dual.md`. FORBIDDEN: prove/pin `exists_tensorObj_inverse` (iter-214 d.1 anti-pattern); assoc re-route+whiskering deletion deferred (need SheafOfModules morphism descent). // iter-218: PRIMARY hit INCOMPLETE gate (dual absent, blocked at step 1, no helper-sorry). // iter-217 prover **CLOSED `tensorObj_restrict_iso`** (axiom-clean; project 81→80, first elim in 7 iters). H1 BUILT (presheaf `pushforwardPushforwardAdj`/`pushforwardNatTrans`/`pushforwardCongr`, de-sheafified) + closed via `leftAdjointUniq`; H2 BUILT (`restrictScalarsMonoidalOfBijective`+`isIso_of_isIso_app`) + closed via `μIso`-over-`⋙forget₂`-form (GOTCHA: avoid local `MonoidalCategory` instances on `ringCatSheaf.obj` form — kernel-rejected diamond; and INLINE the open-immersion adjunction in `H₁`/`H₂` discharge — `have`/`set` makes `adj.unit` opaque). 5 new decls = upstream-PR candidates. Whiskering apparatus STILL VESTIGIAL (`assoc_iso` re-route + deletion DEFERRED). Group built BY HAND — see [[commring-pic-is-skeleton-route]]. Recipe `analogies/ts217.md`
- [TS internalHom value built](ts219-internalhom-value-built.md) — iter-219 BUILT the slice internal-hom VALUE module (`homModule`/`internalHomObjModule`, 11 decls axiom-clean) → value-level NEEDS_MATHLIB_GAP_FILL pessimism REFUTED; remaining = restriction maps + presheaf assembly. Recurring gotchas: CommRingCat/RingCat carrier duality (use `erw`), `map_add` ambiguous (use `_root_.map_add`), `ModuleCat.of` of hom-module fragile. See [[ts-assoc-flatness-gap]]
- [TS module stalk exists](ts-module-stalk-exists.md) — Mathlib HAS a PresheafOfModules stalk (`ModuleCat/Stalk.lean`); PROGRESS.md "no module stalk infra" is WRONG. d.1 R_x-linear stalk map BUILT iter-214 (`stalkLinearMap` &c); remaining = d.1-bridge + d.2 (varying-ring stalk-⊗), specialise lemma to `Opens X`
- [CommRing.Pic is Skeleton route](commring-pic-is-skeleton-route.md) — Mathlib `CommRing.Pic = Skeleton(Module.Invertible R)` IS the monoidal-category/Skeleton route; do NOT cite it as "group without a monoidal category". Project's by-hand group is needed BECAUSE Skeleton needs a coherent MonoidalCategory on X.Modules (unavailable for varying O_X)
- [iter-225 blueprint audit findings](ts225-blueprint-audit.md) — iter-225 whole-blueprint audit: HARD GATE CLEARS for sub-step 4 (`lem:internal_hom_isSheaf` → `AlgebraicGeometry.Scheme.Modules.dual`). Key finding: the sheaf-condition proof is adequate but elides one sentence about sheafification universal property for the descended evaluation. Minor gap, formalizable. `lem:dual_isLocallyTrivial` and `rem:dual_discharges_inverse` coherent with sub-step 4. Dual pin inconsistency between AbelianVarietyRigidity.tex (old `rationalMap_to_av_extends`) and Thm32RationalMapExtension.tex (canonical `Scheme.RationalMap.extend_to_av`) — soon-fix. 6 partial chapters all in held/paused routes.
