- **Stale standing hints overridden (FYI).** The carried-over ROUTE C PAUSE / "Pic_{C/k} representability (A.2.c)" / "Route A" hints are parent-Jacobian-project leftovers — this extracted subproject *is* the former Route-C work and has no Route A. Obeying ROUTE C PAUSE literally would freeze the whole subproject, so per the AUTONOMOUS directive the loop is proceeding with the genus-0 Riemann–Roch goal. To re-scope, edit `USER_HINTS.md` (e.g. remove/replace those three hints).
- **Riemann–Roch chapters** carry literal `REF` citation placeholders in prose; non-blocking but want human Hartshorne II.6 / IV.1 citations filled in.
