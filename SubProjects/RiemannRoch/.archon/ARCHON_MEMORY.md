<!-- ARCHON_MEMORY.md — condensed project knowledge for all agents.
     HARD LIMITS: max 10 bullets · ~600 chars total. Prune before adding. -->

- Standing hints ROUTE C PAUSE / "Pic_{C/k} repr (A.2.c)" / "Route A" are stale parent-Jacobian carryovers; this subproject IS the RR/Route-C work — they do NOT apply.
- Headline #1 χ-chain DOES route through skyscraper H¹-vanishing (iter-001 "independent of vanishing" was formally false); closeable by base-case factoring (direct i=1, drops `injective_flasque`/`j_!`/S5). `_hH1` stays a hypothesis.
- S1 DVR-stalk = **Route C (conormal iso)**, committed. Pure-algebra core + scheme chart/residue bridge CLOSED axiom-clean (SmoothRegular iter-006, abstract ring bridge iter-007, chart sub-lemmas + `residueField_eq_of_coheight_eq_one` iter-010 via `residueFieldIsoBase`). Recipe `analogies/chart-extraction.md`: chart entry = CLASS FIELD `exists_isStandardSmoothOfRelativeDimension` (NOT `HasRingHomProperty.appLE`); `algebraize`; use `SmoothOfRelativeDimension` (NOT deprecated `IsSmoothOfRelativeDimension`). Route R (Stacks 00TT) DROPPED — do NOT re-open.
- Cotangent pin `finrank_cotangentSpace_stalk_eq_one_of_smooth` CLOSED iter-011 (diamond AVOIDED via `algebraMap_residueField_surjective_of_isLocalization_atPrime`). S1b LAST LEAF = `krullDim C.left ≤ 1` (`CurveKrullDim.lean`): reduction + G1 + T closed iter-012. Crux `chart_ringKrullDim_le_one` via **EMBEDDING-DIMENSION** route iter-013 (trdeg route ABANDONED — `dim=trdeg`/`trdeg=rankΩ` Mathlib-ABSENT): `Ideal.sup_primeHeight_of_maximal_eq_ringKrullDim` → per maximal 𝔪 residue=k̄ by Nullstellensatz (f.t./alg-closed, NO global dim) → REUSE conormal bridge `finrank_cotangentSpace_localization_eq_one_of_isStandardSmooth_dim_one` → `ringKrullDim_le_spanFinrank_maximalIdeal` + `spanFinrank_maximalIdeal_eq_finrank_cotangentSpace`. Needs `[IsAlgClosed kbar]` on C. Normality route REJECTED (smooth⟹integrally-closed absent). OcOfD L632 wiring (iter-014) consumes `krullDim_curve_le_one` via `isDiscreteValuationRing_stalk_of_finrank_cotangentSpace_eq_one` (scheme DVR/locallyFree pins REDUNDANT).
- Headline #2 S4 narrow: degree-1 morphism⇒iso (Hartshorne IV.1.3.5); full `ofFunctionFieldEmbedding`/I.6.12 DROPPED. "finite birational ⇒ iso" is ABSENT in Mathlib (only normalization scaffold `instIsIsoToNormalizationOfIsIntegralHom`) → S4 must BUILD it. `Ideal.sum_ramification_inertia`+`isIso_of_stalkFunctor_map_iso` VERIFIED present.
- S2 base `FiniteDimensional k̄ (H¹ 𝒪_C)` NOT free from g=0 (`finrank=0 ⇏ FiniteDimensional`; headline gives only `_hH1:finrank=0`; NO Mathlib H¹ scheme-cohomology finiteness). Route (b) DECIDED **Option A**: `H¹(𝒪_C) ≅ čechH1=coker(𝒪(U)⊕𝒪(V)→𝒪(U∩V))` then `FiniteDimensional k̄ čechH1`. Option B rejected (genus frozen on Ext-based HModule). CRUX = Čech=derived comparison (affine quasi-coh H^{>0}-vanishing, Mathlib TBC). Chapter rewritten iter-015; `cech_H1_finiteDimensional` real (Riemann ineq). S2 GATE (blueprint-reviewer iter-016): `lem:grothendieck_vanishing_curve` has NO `\lean` (sole gap node, used-by 4) = SAME H²/Ext²-vanishing as the open `sorry` at RRFormula.lean:469 (`euler_char_shortExact_add`) — pin ONE shared project lemma, consume in both, before any S2 dispatch.
- HAZARD: `mathlib-build` lanes that CREATE new decls (no pre-existing sorry) get auto-dropped by plan-validate (zero open sorries ⇒ "prover quits immediately"). FIX: dispatch `lean-scaffolder` to land the target decls as `sorry` stubs FIRST, then dispatch plain `prove` mode. Cost iter-008/009 two wasted rounds on SmoothStalkDVR.
- Out-of-cone substrate — never assign as prover targets: `Genus0BaseObjects/*`, `Cohomology/*`, `RigidityLemma.lean`. (137 uncovered Lean decls all live here; no blueprint entries owed.)
- S3 cokernel iso `cokernel_carrierSheafHom_iso_skyscraper` (body sorry-free via `asIso`). Carrier-stalk chain CLOSED axiom-clean iter-016 (binding leaf `carrierSheaf_stalk_eq`=`orderAtPSubmodule` via germ-API seam A `carrierSheaf_stalk_iso_iSup` + finite-poles seam B; method reused germ-API not raw `colimit.ι`; B0 took GLOBAL `principal` finiteness, needs `[IsProper]`). G2/G3 leaves (`cokernel_stalk_at_iso_kbar` residue=k̄ via π^{n+1} aut; `cokernel_skyscraper_hom`+`_isIso` via `isIso_of_stalkFunctor_map_iso`) = iter-017 lane, gate-cleared. LAST deep S3 piece = bridge `carrierSheaf_zero_iso_toModuleKSheaf` (Hartshorne II.6.3A algebraic Hartogs): DECOMPOSED iter-017 → Mathlib anchor `IsDedekindDomain.HeightOneSpectrum.mem_integers_of_valuation_le_one` [verified] + `carrierSheaf_zero_sections_eq_structureSheaf` (chart⇒Dedekind, valuation=order, gluing). Do NOT re-attempt whole cokernel iso blind (0→2→4).
- No curve Serre-duality in Mathlib — genus-0 vanishing route is flasque-skyscraper induction (reuse M2+M3); only `H¹(flasque)=0` acyclicity needed, not full injective-resolution Ext.
