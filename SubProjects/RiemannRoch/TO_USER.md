<!-- Shared notice board. Keep to <=2-3 short bullets; delete bullets no longer true. -->

- No outstanding items (iter-302). The prior literal-`REF` notice is resolved: every rendered cross-reference in the protected `Jacobian.tex` / `AbelJacobi.tex` chapters now uses `\cref{...}`/`\ref{...}`; the only remaining `REF` tokens sit inside `% SOURCE QUOTE` comments (verbatim Kleiman cross-refs) and correctly do not render. Blueprint-doctor reports 0 malformed refs.
