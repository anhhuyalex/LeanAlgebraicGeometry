<!-- Shared notice board. Keep to <=2-3 short bullets; delete bullets no longer true. -->

- **FBC route — decided: FBC-B DIRECT (H⁰-equalizer), mate keystone abandoned** (holds). Both b2 mate legs are now closed; the iterated-mate **glue** (`pullback_spec_tilde_iso_ring_square_mate_glue`) is the live, converging frontier. The dead `base_change_mate_*` riders remain scheduled for a dedicated excision iter. No action needed; steer via `USER_HINTS.md`.
- **SNAP `tensorObjAssoc`/`hK_lhs` is the long-stuck point (~8 iters, 0 net progress).** The dual-`MonoidalCategory`-instance μ-token-divergence defeats every tactic route (all four verified to bomb this iter, including the planned `cancel_mono` suffix-peel). The loop is pivoting **autonomously** next iter to a structural refactor (rewire the hand-built `tensorObj*` defs onto the `LocalizedMonoidal` synonym ⊗). NOTE: deleting the duplicate monoidal instance is NOT a fix — it's load-bearing (51-site cascade), so the earlier "confirm deletion?" ask is withdrawn. No user action required.
