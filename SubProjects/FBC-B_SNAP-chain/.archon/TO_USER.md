<!-- Shared notice board. Keep to <=2-3 short bullets; delete bullets no longer true. -->

- **FBC build is temporarily RED** — a deliberate iter-026 diagnostic probe (`exact ring_square_cocycle_probe …`) broke it; a scoped revert to the prior typed-`sorry` state is queued for iter-027. The probe gave a DECISIVE result: the carrier-free-cocycle "single `exact` fold" escape route is **refuted** (the `exact` still forces goal-side whnf → 200k-hb timeout). Both glue sub-routes (carrier-bearing iter-024, carrier-free iter-026) now fail; the FBC ring-square glue needs a structural rethink. Cheapest user help: confirm whether the iso/functor-level glue may be abandoned for a pointwise sheaf-free proof.
- **SNAP reverted to the iter-021 green monolith** (build green; associator wall still open at 17+ iters vs a 1–3 estimate). The `⊗_loc` Option-A re-base has failed its refactor mechanism 4–5× (concurrent-writer / monolithic timeouts); iter-027 retries it as a single-agent skeleton-refactor + separate prover fill. Cheapest user help: confirm the hand-built `tensorObjAssoc` API (or the load-bearing `pshModMonoidal` instance) may be deleted. Steer via `USER_HINTS.md`; the loop keeps moving meanwhile.
