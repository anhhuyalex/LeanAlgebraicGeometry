<!-- ARCHON_MEMORY.md — condensed project knowledge for all agents.
     Written by the plan agent and archon discuss. Read by all agents.

     HARD LIMITS: max 10 bullets · ~600 chars total.
     Prune before adding. Only keep what would surprise an agent reading
     the code fresh. Do NOT duplicate things obvious from the codebase.

     Good candidates: dead-end tactics, files not to touch, Mathlib gap
     coordinates, protected invariants, per-file hazards, standing routes
     to avoid, axioms that must not be accepted.

     Bad candidates: things already obvious from the code or PROGRESS.md,
     current sorry counts, task-specific details that change every iter.
-->

- FBC: SHEAF-LEVEL mate route DEAD; seeds via concrete-tilde chain. Apparatus `base_change_mate_*`/`pushforward_base_change_mate_*` COMPILE-DEAD (seeds=bare sorry) — excise in a DEDICATED iter (sync blueprint `\uses`), never with an FBC prover; KEEP `base_change_mate_regroupEquiv`+`base_change_map_affine_local`. CRUX (`pst` ring-square pseudofunctoriality) uses CONJUGATE-mate, route LIVE: the 3 lemmas `iterated_mateEquiv_conjugateEquiv`/`conjugateEquiv_mateEquiv_vcomp`/`mateEquiv_conjugateEquiv_vcomp` DO EXIST (`…Adjunction.Mates`, v4.30; bp009 loogle false-neg). HAZARD: `mateEquiv` is `TwoSquare`-valued (recover NatTrans via `.natTrans`/`equivNatTrans`; whiskerings are TwoSquare pasting, not `Functor.whiskerLeft`); the `homEquiv.injective` hand-peel is a DEAD END (strands a unit-prefixed eq). `conjugateEquiv_pullbackComp_inv` IS REAL (`Scheme.Modules`, `Mathlib/AlgebraicGeometry/Modules/Sheaf.lean:238`, `@[simp]`; the geometric analogue of `conjugateEquiv_extendScalarsComp`) — prior "comment-fiction" note was FALSE (iter-018 analogist `fbc-geom-mate`); only `pullbackComp_eq_leftAdjointCompIso` is fiction. GEOM LEG route: `conjugateEquiv_comp` split + `conjugateEquiv_pullbackComp_inv`×2 + 3-line `conjugateEquiv_pullbackCongr`; NEVER `natTrans_ext_of_unit`/`unit_conjugateEquiv` over the composite (forces composite-unit whnf → 200k bomb). ALGEBRAIC LEG engine = `natTrans_ext_of_unit`+`conjugateEquiv_extendScalarsComp` (DONE); residual = composite-unit blocked by `Eq.mpr` casts in `…ReassocNat` defs → split via `conjugateEquiv_comp`. FOUNDATION `gammaPushforwardNatIso_comp` CLOSED iter-016 via MORPHISM-LEVEL route — element/sheaf family was VERIFIED-EXHAUSTED (iter-014: kernel bomb = the RHS-composite REDUCTION across the value-`ModuleCat`/`X.Modules` junction, NOT the cast; ANY `ext`/element descent bombs); morphism route crosses the junction ONCE + closes cast by proof-irrelevance (`analogies/fbc-morphism-comp.md`). `fbc-pst-pseudofunctor.md` mis-records this as "pointwise rfl" (FALSE = the bomb). BOTH mate legs CLOSED iter-018 (engine: `← conjugateEquiv_comp` split ×2 + `simp only […eq_mpr_eq_cast,cast_eq]` which DISSOLVES the `letI:Algebra`-`Eq.mpr` casts — the iter-011..017 "cast blocker" was a MISDIAGNOSIS, not a residual cast to strip; +3 helpers `conjugateEquiv_pullbackCongr`/`pushforwardCongr_inv_eq`/`conjugateEquiv_extendScalars_eqToHom`). FRONTIER = glue `pullback_spec_tilde_iso_ring_square_mate_glue` (both legs live → iterated-mate assembly via `iterated_mateEquiv_conjugateEquiv`, TwoSquare-valued; naive use whnf-bombs).
- `X.Modules`/value-ModuleCat diamond: positional `rw`/`simp`/`erw`/`comp_apply`/`hom_comp` fail → term-mode + `change`-to-nested-application.
- SNAP: carrier `AddCommGrpCat`; coherence via `Localization.Monoidal` `LocalizedMonoidal L W ε` synonym (full `MonoidalCategory(X.Modules)` DEAD; Option A redefine rejected). HAZARD: `⊗_loc` NOT defeq `tensorObj` → bridges object-iso-CONJUGATED via `tensorObjLocalizedIso=μ⁻¹;counit`, NOT bare `α=α^loc` (type error). `tensorObjAssoc` is a 5-segment composite; its bridge is NOT a clone of the braiding bridge — DECOMPOSED into 4 seams. HAZARD: whiskered unit `(η_P▷Q)^#` relates to μ ONLY via the `μ_natural_left/right` square (it joins two sheafified presheaf tensors, NOT a `⊗_loc`); a literal "whiskered-unit = μ component" is FALSE. `sectionMul_assoc_core` BRIDGE-FREE (section-η). Stalkwise DEAD. ASSEMBLY μ-pair straddles a `LocalizedMonoidal`↔`X.Modules` comp-instance boundary (defeq-NOT-syntactic): `rw`/`simp`/`change`/`dsimp` DEAD (iter-013; SUPERSEDES iter-011 `show`-pin). Cancel (analogist-VALIDATED, `snap-localized-comp-cancel.md`): `erw [assoc]; refine congrArg (fun t=>_≫t) ?_; erw [assoc, Iso.hom_inv_id_assoc]` — leading-μ.inv `congrArg` peel MANDATORY (unshielded 2nd `erw [assoc]` whnf-unfolds μ→`Localization.fac`). `hK_rhs` CLOSED iter-014 (reusable post-cancel unblock: `show`-to-uniform-localized-comp + `simp [tensorHom_comp_tensorHom]` (NOT rw) + `congr 1` + `left_triangle_components_assoc`). `hK_lhs` (analogist-VALIDATED `snap-assoc-expose.md`): the "`c_{A⊗B}`=`μ_{A,B}`∘`(c_A⊗c_B)`" idea is DEAD + NOT well-typed — `c_{A⊗B}` stays WHOLE. Route: Step-0 `show`-uniform FIRST, then `← tensorHom_id`/`← tensor_comp` → `associator_naturality` (native-ise α) → `associator_hom_app` → cancel μ_{A♭,B♭} pair → `have hHead` via `μ_natural_left`+`left_triangle_components`. ROOT CAUSE of the 5-iter hK_lhs μ-cancel wall (iter-016 analogist `snap-mu-identity`): NOT a hidden `Localization.fac`/`IsIso` witness (`Localization.Monoidal.μ` has NONE → `Subsingleton.elim`/`convert` VACUOUS) and NOT a missing cancel lemma (`Iso.hom_inv_id_assoc` IS the right non-whnf idiom) — it is a project-local DUAL `MonoidalCategory` instance (`pshModMonoidal` bare L1402 vs `MonoidalPresheaf X` synonym used by the keystone) making the two μ object-args token-divergent. FIX: pin inner tensors to `(C := MonoidalPresheaf X)` at STATEMENT level (in-proof `show`/`rfl`/`erw`/`convert` pinning isDefEq-BOMBS). iter-018 (analogist `snap-reassoc-pin`): dual-instance DELETION is REFUTED (load-bearing — `LocalizedMonoidal` requires `[MonoidalCategory C]` on the bare form; deletion cascades into ALL closed lemmas, 51-site blast → ABANDON, do NOT queue). The durable fix is the OBJECT-PHRASING DISCIPLINE: folded `A.tensorObj B` must NEVER sit inside a μ-object when reassoc/`associator_*`/`Category.assoc` runs → state hK_lhs reassoc as a sibling lemma `tensorObjAssoc_hK_lhs_native` with EVERY μ-object in unfolded `(L').obj _` image-form, then `simp only [Category.assoc]`+`associator_naturality`+`associator_hom_app` fire syntactically; in-proof reassoc (`rw`/`congrArg`-peel even pinned) BOMBS (peel works ONLY when both eq-sides are exact goal subterms). iter-016 REFINEMENT: statement-pin NECESSARY-but-NOT-SUFFICIENT — μ stays token-divergent post-pin; every cancel/merge fires via `erw` (NOT `rw`) ONLY when ISOLATED in `slice`/`have` (full-goal motive re-typechecks surrounding μ → 200k-hb bomb). `hK_rhs`+head CLOSED. iter-018 `native` BROKE the 7-iter reassoc wall (chain runs bomb-free to head). NEW RESIDUAL = applying head over the full-`tail` goal whnf-bombs (heavy μ in `tail`). FIX (analogist `snap-suffix-cancel`, PROCEED): REMOVE the heavy suffix first — `@[reassoc]`/sibling `_head_assoc` + `exact _` after `conv_rhs => rw[assocCommonForm];simp` tail-alignment (generalize fallback); residual PREFIX may still need head's `show`-to-uniform recast. STUCK 4 iters (pc019): if suffix route bombs → REFACTOR PIVOT to glue Option A (rewire hand-built defs onto `LocalizedMonoidal` synonym ⊗, bridges definitional), NOT user escalation (standing directive).
- Build `lake build <module>` (LSP hides kernel timeouts); never `maxHeartbeats 1e6`. No LLM API key.
