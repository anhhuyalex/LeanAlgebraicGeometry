<!-- ARCHON_MEMORY.md — condensed project knowledge for all agents.
     Written by the plan agent and archon discuss. Read by all agents.

     HARD LIMITS: max 10 bullets · ~600 chars total.
     Prune before adding. Only keep what would surprise an agent reading
     the code fresh. Do NOT duplicate things obvious from the codebase.

     Good candidates: dead-end tactics, files not to touch, Mathlib gap
     coordinates, protected invariants, per-file hazards, standing routes
     to avoid, axioms that must not be accepted.

     Bad candidates: things already obvious from the code or PROGRESS.md,
     current sorry counts, task-specific details that change every iter.
-->

- FBC: SHEAF-LEVEL mate route DEAD; seeds via concrete-tilde chain. Apparatus `base_change_mate_*`/`pushforward_base_change_mate_*` COMPILE-DEAD (seeds=bare sorry) — excise in a DEDICATED iter (sync blueprint `\uses`), never with an FBC prover; KEEP `base_change_mate_regroupEquiv`+`base_change_map_affine_local`. CRUX (`pst` ring-square pseudofunctoriality) uses CONJUGATE-mate, route LIVE: the 3 lemmas `iterated_mateEquiv_conjugateEquiv`/`conjugateEquiv_mateEquiv_vcomp`/`mateEquiv_conjugateEquiv_vcomp` DO EXIST (`…Adjunction.Mates`, v4.30; bp009 loogle false-neg). HAZARD: `mateEquiv` is `TwoSquare`-valued (recover NatTrans via `.natTrans`/`equivNatTrans`; whiskerings are TwoSquare pasting, not `Functor.whiskerLeft`); the `homEquiv.injective` hand-peel is a DEAD END (strands a unit-prefixed eq). `conjugateEquiv_pullbackComp_inv` IS REAL (`Scheme.Modules`, `Mathlib/AlgebraicGeometry/Modules/Sheaf.lean:238`, `@[simp]`; the geometric analogue of `conjugateEquiv_extendScalarsComp`) — prior "comment-fiction" note was FALSE (iter-018 analogist `fbc-geom-mate`); only `pullbackComp_eq_leftAdjointCompIso` is fiction. GEOM LEG route: `conjugateEquiv_comp` split + `conjugateEquiv_pullbackComp_inv`×2 + 3-line `conjugateEquiv_pullbackCongr`; NEVER `natTrans_ext_of_unit`/`unit_conjugateEquiv` over the composite (forces composite-unit whnf → 200k bomb). ALGEBRAIC LEG engine = `natTrans_ext_of_unit`+`conjugateEquiv_extendScalarsComp` (DONE); residual = composite-unit blocked by `Eq.mpr` casts in `…ReassocNat` defs → split via `conjugateEquiv_comp`. FOUNDATION `gammaPushforwardNatIso_comp` CLOSED iter-016 via MORPHISM-LEVEL route — element/sheaf family was VERIFIED-EXHAUSTED (iter-014: kernel bomb = the RHS-composite REDUCTION across the value-`ModuleCat`/`X.Modules` junction, NOT the cast; ANY `ext`/element descent bombs); morphism route crosses the junction ONCE + closes cast by proof-irrelevance (`analogies/fbc-morphism-comp.md`). `fbc-pst-pseudofunctor.md` mis-records this as "pointwise rfl" (FALSE = the bomb). BOTH mate legs CLOSED iter-018 (engine: `← conjugateEquiv_comp` split ×2 + `simp only […eq_mpr_eq_cast,cast_eq]` which DISSOLVES the `letI:Algebra`-`Eq.mpr` casts — the iter-011..017 "cast blocker" was a MISDIAGNOSIS, not a residual cast to strip; +3 helpers `conjugateEquiv_pullbackCongr`/`pushforwardCongr_inv_eq`/`conjugateEquiv_extendScalars_eqToHom`). FRONTIER = glue `pullback_spec_tilde_iso_ring_square_mate_glue`: monolithic-churned 3 iters → DECOMPOSED iter-022 into 6 `ring_square_glue_*` sub-lemmas (whiskerRight/Left_lift via `whiskerRight/Left_app`; geom_leg_nat via `conjugateEquiv_mateEquiv_vcomp`; alg_leg_nat via `mateEquiv_conjugateEquiv_vcomp`; natTrans; linchpin `pst_iterated_mate` via `iterated_mateEquiv_conjugateEquiv`, TwoSquare-valued — recover NatTrans via `.natTrans`/`TwoSquare.equivNatTrans`, never composite-unit whnf). iter-024: 5/6 closed; geom leg made SYNTACTIC by redefining obj-def `chartBaseChangeGeometricComparison` body = `(chartBaseChangeGeometricComparisonNat).app (tilde M)` (defeq, kills the `pullback.obj(tilde M)` sheafification-whnf fold bomb). LAST piece = the glue. The concrete `ring_square_glue_natTrans` telescope is OVER-BUDGET (VERIFIED iter-024: elaborates @800k-hb, kernel-bombs @200k whnf'ing the tilde/extendScalars carrier at BOTH statement-seam AND `.app M` fold) — ABANDON it. iter-026 pivot (analogist `fbc-glue-carrier-whnf.md`, br025-gated): prove an ABSTRACT carrier-free `ring_square_cocycle` (NO tilde/pullback/extendScalars in statement OR proof → no whnf can fire) by pure mate calculus (`conjugateEquiv_symm_comp`+`mateEquiv_vcomp`/`_hcomp`+`iterated_mateEquiv_conjugateEquiv`+`conjugateIsoEquiv`), then close the heavy glue with ONE `exact` binding carriers to metavars. DELETE `ring_square_glue_natTrans`. The 4 leg facts (geom/alg leg-nat, unit-triangle, gammaPushforwardNatIso_comp) are already CLOSED.
- `X.Modules`/value-ModuleCat diamond: positional `rw`/`simp`/`erw`/`comp_apply`/`hom_comp` fail → term-mode + `change`-to-nested-application.
- SNAP: carrier `AddCommGrpCat`; coherence via Mathlib `Localization.Monoidal` `LocalizedMonoidal L W ε` synonym (`= modulesLocalizedMonoidal X`; full `MonoidalCategory(X.Modules)` DEAD, stalkwise DEAD). **ROOT CAUSE of the ~10-iter associator-bridge wall = parallel-API anti-pattern: a hand-built `tensorObj`/`tensorObjAssoc` AND Mathlib `⊗_loc` are two `MonoidalCategory` instances on defeq copies of one category; reconciling their μ across the `LocalizedMonoidal`↔`X.Modules` comp-instance boundary (defeq-NOT-syntactic) bombs EVERY tactic (rw/simp/erw/change/show/congrArg-peel/`@[reassoc]`-suffix; 200k-hb whnf of μ→`Localization.fac`). The Option-B bridge route (`tensorObjLocalizedIso=μ⁻¹;counit`, seams, hK_lhs/native/head, suffix-cancel) is ABANDONED.** PIVOT iter-022 = **Option A**: re-base `tensorObj := MonoidalCategory.tensorObj (C := modulesLocalizedMonoidal X)` (+unitor/braiding/associator); synonym defeq + unit IS `unitModule X` (via ε) ⇒ object spellings unchanged ⇒ `tensorObjAssoc_eq_localizedAssociator` etc. become `rfl` (stuck crux DELETED, ~900L removed). μ then lives only opaque in `sectionsMul`/`tensorObjLocalizedIso`(=`Iso.refl`). Downstream coherences re-prove via Mathlib pentagon/triangle/hexagon + `Functor.Monoidal L` lax-coherences. Design = `analogies/snap-instance-design.md` (ALIGN_WITH_MATHLIB). HAZARD: dual-instance DELETION (remove `pshModMonoidal`) REFUTED — load-bearing (`LocalizedMonoidal` requires `[MonoidalCategory C]` on bare form; 51-site cascade). RISK (sc022): section-level coherences (`sectionsMul_*`, `sectionMul_coherent`) cross to Γ-level — if one re-summons the boundary, wall moved one layer down (signal = stubborn post-refactor sorry). KEEP: W-localization precondition machinery, `tensorPow`/`moduleTensorPow`/`sectionsCast`, all lemma names+sigs.
- Build `lake build <module>` (LSP hides kernel timeouts); never `maxHeartbeats 1e6`. No LLM API key. PROCESS (iter-023 regression): a file-split/extraction lane MUST cold-build the downstream CONSUMER, not just the extracted module — and extracted decls the consumer references must be NON-`private` (iter-023 marked them `private` → 30+ unknown-id errors; fixed iter-024 by de-privating `MonoidalPresheaf`/`sheafificationCounitIso`/`pshModMonoidal`/`pshModBraided`/`pshModSymmetric`/`Wsheaf`). HAZARD (iter-025 root cause): NEVER dispatch >1 refactor/prover agent writing the SAME file concurrently — iter-025 ran 3 concurrent Option-A writers on `SectionGradedRing.lean`, all killed by timeout, leaving a broken half-migration (RED build). A big refactor (re-base + re-prove in one agent budget) is ALSO too big → gets killed; decompose into SKELETON refactor (re-base defs + `sorry` broken sites, single agent) + a separate PROVER fill. iter-026 REVERTED SNAP to the iter-021 green MONOLITH (`.presplit-backup`) + removed the `SectionGradedRingLocalized` import from root (the split is undone; Localized.lean unimported on disk; broken half-migration saved as `.optiona-partial-broken-iter025`).
